# 2026-09-09 작업 피드백 · 9/10 실행 후보

> **[2026-09-22 현행화] 이 문서는 2026-09-09 시점의 스냅샷이다. 현재 상태의 기준은 `TODO.md`와 `DEVLOG.md`다.**
>
> 아래 체크박스 중 상당수는 이후 처리됐으나 그 내역이 `DEVLOG.md`에만 남아 이 문서의 `- [ ]`가 낡은 상태였다.
> 실제로 이 문서만 보고 "미완료"로 판단해 재작업할 뻔한 사례가 있어, 현재 코드와 대조해 상태를 갱신했다.
> **체크된 항목은 재작업 대상이 아니다.** 남은 항목만 `TODO.md`에서 추적한다.

오늘은 구현하지 않고 리뷰와 내일 할 일만 기록했다. DEVLOG.md의 9/9 항목, TODO.md, project-notes.md, 현재 소스 변경을 검토했고 Zone D는 별도 Explore가 데이터·스크립트를 읽었다. 실제 서비스 화면, AdSense 계정 설정, 수익·트래픽 수치는 확인하지 않았다. 아래 성능 효과와 광고 배치는 측정 전 제안이다.

## 오늘 작업에 대한 평가

가장 좋은 변화는 **메랜과 플래닛의 수치가 같다는 가정을 깨고 서버별 스킬 오버라이드를 만든 것**이다. 정령의 축복 200→22 수정도 결과 정확도에 직접 영향을 주는 중요한 수정이다. 몬스터 이미지 실패 처리를 공통 함수로 연결한 것도 유지보수 방향이 좋다.

드롭 병합 로직을 바로 바꾸지 않고 드라이런으로 가설을 반증한 과정은 특히 좋았다. 몬스터 이름이 같다고 합치거나 삭제하지 않고 mobCode로 조사한 것도 데이터 손실을 막는 방향이다.

다만 오늘의 완료 기준에는 보완이 필요하다. 외부 DB에 몬스터가 있다는 사실은 특정 서버에 출시됐다는 증거와 다르고, 마스터레벨 값 반영은 전체 레벨 곡선 검증과 다르다. ‘완료’ 옆에 **검증된 범위와 아직 상속·추정하는 범위**를 짧게 남기는 편이 좋다.

문서의 확신 수준도 낮춰야 할 곳이 있다. 조사 기간의 공지에서 언급을 못 찾았다는 사실만으로 ‘미출시 확정’, 외부 DB에서 못 찾았다는 사실만으로 ‘진짜 더미 확인’, 중복 그룹의 드롭 개수가 자연스럽다는 이유만으로 ‘오배정 없음 확인’까지 결론 내리기는 어렵다. 각각 ‘조사 범위에서 출시 근거 미확보’, ‘미확인 ID’, ‘개수 비교상 이상 징후 없음’이 근거에 더 맞는다. 이번 리뷰에서 게임 출시 사실 자체를 새로 판정한 것은 아니다.

## 내일 먼저 볼 정확도·회귀 문제

### 1. 정령의 축복: 기존 저장값도 새 상한 적용 — P1

- 근거: `src/app/(routes)/calculators/onehit/onehit-calculator-client.tsx:470`은 프리셋의 `spiritBlessingLevel`을 그대로 복원한다. `:1157`은 그 상태값을 바로 공격력에 곱한다. 입력 UI의 max=22만으로 저장된 200을 보정하지 못한다.
- 영향: 과거 높은 값으로 저장한 프리셋을 불러오면 상한 수정 후에도 공격력이 과대 계산될 수 있다.
- [x] 프리셋 복원과 계산 경계에서 서버별 상한·하한 및 유효 숫자를 적용한다. — `normalizePresetSkillLevel`(`src/lib/calculators/onehit.ts:210`)로 처리.
- [x] 저장값 200→플래닛 22, 음수·잘못된 값, 정상 22를 검증한다. 메랜 상한 20도 확인한다. — `src/lib/calculators/__tests__/presetSkillLevel.test.ts`에 회귀 테스트 존재.
- [x] 공식 확인이 끝난 플래닛 정축의 ‘잠정치’ UI 문구도 정리한다. — `BlessingConfig.confirmed` 플래그로 분리(플래닛 `true`, 메랜 `false`).

### 2. 이미지 폴백: 선택 변경 시 실패 상태 초기화 — P1

- 근거: `src/components/MonsterPanel.tsx:41`의 Image는 몬스터별 key가 없고, 실패 시 DOM에 `data-fallback`, `data-maple-code`, `data-maple-retry`를 직접 기록한다. 선택 변경 시 이를 초기화하는 코드가 없다.
- 영향: A 몬스터가 폴백을 사용한 뒤 B로 바꾸고 B 이미지도 실패하면, 재사용된 DOM의 A 코드로 재시도할 수 있다. 브라우저 재현은 아직 하지 않았다.
- `MonsterSelect.tsx`와 DropTable의 선택 이미지도 상수 `data-maple-retry="0"`만으로 직접 바뀐 DOM 속성이 매번 초기화된다고 가정하지 말고 함께 확인한다.
- [x] mobCode를 key로 주거나 이미지 소스 변경 때 폴백 상태를 확실히 초기화한다. — `MonsterPanel.tsx:46`, `MonsterSelect.tsx:157·186` 모두 `key={mobCode}` 적용.
- [ ] A 실패→B 선택→B 실패 시 B 후보만 요청하는지 확인한다. 후보 소진 후 대체 표시도 정한다. — key 적용으로 원인은 해소됐으나 **브라우저 재현 확인은 아직 안 함**. 후보 소진 시 표시는 `EXHAUSTED_PLACEHOLDER`("?")로 구현됨.

### 3. 서버별 스킬 분리의 경계 보강 — P1

- 근거: `onehit-calculator-client.tsx:975`는 플래닛 오버라이드가 없으면 메랜 기준 damageMapping을 사용한다. Zone D 검토 결과 오늘 메랜 변경 12종 중 브랜디쉬·블래스트를 제외한 10종은 플래닛 오버라이드가 없다. 예: 돌진 130→140, 래피드 파이어 160→200이 플래닛에도 전파된다.
- 이것이 실제 게임 수치 오류인지는 별도 검증이 필요하다. 현재 구조가 서버 간 변경을 자동 전파한다는 점은 확인된다.
- 스나이핑은 플래닛 전체 레벨에서 고정 데미지 예외를 해제하지만 새 수치는 마스터만 있다. 하위 레벨을 기존 곡선으로 계산해도 되는지 우선 확인한다.
- [~] 서버×스킬×레벨별 출처와 적용값을 대조한다. 특히 Lv1, 마스터 직전, 마스터를 확인한다. — **부분 처리.** 2026-09-21 maplab 데이터로 메랜 76종을 Lv1~마스터 전 구간 대조해 6종의 하위 레벨을 정정(`fa175e2`). 플래닛 쪽은 미완 — TODO "메랜 스킬 버프가 플래닛으로 새고 있다" 참조.
- [ ] 공통으로 검증한 기본값과 서버별 변경값을 분리하고, 미확인 하위 레벨은 UI에서 근사치임을 알린다.
- [~] 스킬 값 해석을 작은 순수 함수로 추출하고 서버 교차 회귀 테스트를 추가한다. — **부분 처리.** `normalizePresetSkillLevel`·`calcDamagePerSkill` 등은 `src/lib/calculators/onehit.ts`로 추출됨. 다만 **서버 교차 회귀 테스트는 아직 없다** — 남은 작업.
- 메랜 아란 노출 유지라는 기존 사용자 결정은 존중한다. 플래닛 실측 기반이라는 안내와 추후 검증 항목만 유지한다.

### 4. 오늘 데이터 보정의 재빌드 재현성 — P1 / Zone D

- 근거: `scripts/normalize-monsters.mjs`는 raw 입력에서 monsters.json을 다시 만든다. 오늘 63종 보정·샤레니안 5종 추가는 별도 후처리 스크립트여서 기본 정규화만 다시 돌리면 보정이 사라질 수 있다.
- [~] 기본 생성→보정 적용→플래닛 생성 순서를 한 명령으로 연결한다. — **부분 처리.** 보정 소스를 `scripts/sources/mobcode-corrections.json`으로 승격해 정규 빌드가 항상 읽도록 함(2026-09-10). `build:drop-table-from-dropchance`가 생성→보정→플래닛을 한 명령으로 연결. 다만 **일회성 보스 스크립트(카오스 자쿰/혼테일/발록 등)는 여전히 산출물에만 존재해 재빌드 시 유실** — TODO 미해결 항목.
- [ ] 깨끗한 입력에서 오늘 보정이 재현되는지, 두 번 실행해 결과가 같은지 확인한다.
- [ ] 출시 필터는 서버별 출시 근거와 스탯 출처를 분리한다. 외부 DB 존재만 확인한 ID는 재검토 목록으로 남긴다.
- [ ] 출처에 없는 acc/watk/matk의 0과 실제 측정 0, 형제 몬스터에서 추정한 값을 구분한다. 샤레니안 플래닛 HP도 메랜 상속값임을 추적한다.
- [ ] data.md의 release-filters 직접 편집 안내와 AGENTS의 산출물 직접 편집 금지 규칙을 일치시킨다.

## 전체 최적화 후보

### 우선: 성능 수집 자체의 중복 줄이기

`src/components/web-vitals-reporter.tsx`는 useReportWebVitals와 자체 PerformanceObserver 양쪽에서 지표를 전송한다. debug=false여도 observer와 전송이 동작한다. pathname 변경 시 observer가 재등록되며 buffered entry를 다시 읽을 수도 있다. `/api/vitals` POST는 각 요청을 저장하고 로그를 남긴다.

- [ ] 운영 지표와 상세 디버그 관측을 분리한다. 운영은 표준 지표 중심, 상세 observer는 디버그 또는 샘플링으로 제한한다.
- [ ] 동일 metric ID의 후속 업데이트를 무조건 버리는 처리도 검토한다. 자체 누적 CLS·최대 이벤트 지연을 표준 지표와 섞어 광고 전후 비교하지 않는다.
- 완료 기준: 같은 탐색 시나리오에서 요청 수·로그 양이 줄고, 필요한 최종 지표는 유지된다.

### 다음: 드롭 상세 중복 요청 차단

`DropTable.tsx:252`, `:271`은 완료된 응답 캐시를 확인하지만 진행 중 요청은 공유하지 않는다. 느린 네트워크에서 같은 대상을 다시 누르면 중복 fetch가 가능하다.

- [x] 서버+종류+ID 단위로 진행 중 Promise를 공유한다. 실패하면 재시도가 가능하도록 해제한다. — `DropTable.tsx:247` `inFlightRequestsRef`로 처리(실패 시 `delete`로 해제).
- 완료 기준: 응답 전 같은 대상 반복 선택에도 요청 1회, 실패 후 재시도 정상.

### 측정 후: 검색·이미지·번들

이미 DropTable은 상세 데이터를 필요할 때 가져오고 검색 키를 메모이제이션하며, MonsterSelect는 결과를 60개로 제한한다. 따라서 ‘전부 가상화’, ‘모든 곳에 useMemo 추가’를 먼저 할 이유는 약하다.

- [ ] 모바일에서 검색 입력 지연, 초기 JS 전송량, 이미지 실패 요청 횟수부터 측정한다.
- [ ] 반복해서 실패하는 이미지가 많으면 확인된 성공 URL 매핑이나 허용된 정적 이미지 관리로 폴백 왕복을 줄인다. 모든 이미지를 무조건 프록시하면 운영 비용이 늘 수 있다.
- [ ] 긴 계산기 파일은 스킬 해석·프리셋 검증부터 분리한다. 파일을 나누는 것 자체가 속도를 높여주지는 않는다.
- [ ] TODO는 실행 항목+완료 기준+DEVLOG 링크로 줄인다. 조사 경위가 길게 중복되면 다음 작업자가 미해결 핵심을 찾기 어렵다.

## AdSense 배치 의견

**첫 실험은 페이지별 결과·설명 아래 수동 광고 1개가 좋다.** 이 사이트는 입력→결과 비교를 반복하는 도구여서 본문 중간 광고를 넣기 자연스러운 블로그와 다르다. 배치가 애매하게 느껴지는 이유도 핵심 영역 대부분이 조작 공간이기 때문이다.

현재 `layout.tsx` 및 src의 TS/TSX에서는 AdSense 스크립트나 광고 슬롯을 찾지 못했다. GA는 연결되어 있다. 운영 환경의 별도 삽입이나 AdSense 자동 광고 설정 여부는 확인하지 못했으므로 기존 광고가 없다고 단정하지 않는다.

| 화면 | 첫 배치 후보 | 피할 자리 / 이유 |
|---|---|---|
| 한방컷·피격 계산기 | 결과 요약과 사용 설명이 끝난 뒤 독립된 가로 영역 | 입력과 결과 사이: 비교 동선을 끊음 |
| 드롭 테이블 | 결과 목록 전체 아래. PC 여유가 있으면 별도 오른쪽 칸 검토 | 검색 자동완성·드롭 카드 사이: 연속 클릭 동선과 섞임 |
| 큐브 시뮬레이터 | 시뮬레이터·기댓값 패널이 모두 끝난 뒤 | 큐브 사용 버튼 옆·결과가 교체되는 영역 |
| 퀘스트 | 목록 밖의 하단 영역 | 체크박스 행 사이: 반복 체크와 광고가 섞임 |
| 로그인·관리자·피드백 입력 | 첫 도입 대상에서 제외 | 이용 목적상 광고 실험 효과가 낮고 폼을 방해함 |

PC 우측 광고 칸은 2순위다. 현재 sidebar-shell은 펼침 상태에서 왼쪽 256px를 차지하고, DropTable은 본문 안에서 다시 340px 검색 칸+결과 칸을 쓴다. 여기에 광고 300px를 추가하면 일반 노트북에서는 결과 폭이 크게 줄 수 있다. 화면 너비뿐 아니라 **사이드바를 뺀 실제 본문 가용 폭**으로 노출 여부를 판단해야 한다. 충분히 넓은 화면에서만 별도 칸을 두고, 모바일에서는 하단으로 배치한다. 이 수치는 설계 예시이지 Google 필수 규격이 아니다.

모바일은 하단 일반 슬롯부터 시작하고 앵커·전면 광고는 첫 실험에서는 끄는 편을 권한다. 키보드, 메뉴, 결과 확인 영역을 얼마나 가리는지 검증한 후 별도 실험할 수 있다. 이는 UX 권고이며 해당 형식이 금지됐다는 뜻은 아니다.

Google은 드롭다운·내비게이션·애플리케이션 등의 조작부 근처 배치로 우발적 클릭이 생기지 않도록 주의를 요구한다. 광고를 결과 카드처럼 보이게 하지 않고 별도 영역으로 구분해야 한다. [Google 광고 게재위치 정책](https://support.google.com/adsense/answer/1346295?hl=ko)

자동 광고를 사용한다면 검색·입력·결과 비교 영역을 제외 대상으로 검토한다. Google은 페이지 내 자동 광고의 영역 제외 기능과 광고 형식 설정을 제공한다. 계정 미리보기에서 실제 적용 가능 영역을 확인해야 한다. [영역 제외 안내](https://support.google.com/adsense/answer/12626543?hl=ko), [자동 광고 설정](https://support.google.com/adsense/answer/9305577?hl=ko)

### 내일 광고 작업의 완료 기준

- [ ] 광고 없는 현재 화면과 광고 자리만 있는 시안을 모바일·노트북·넓은 PC에서 비교한다.
- [ ] 슬롯 크기에 맞게 공간을 미리 확보해 광고 로딩 때 결과나 버튼이 밀리지 않게 한다. 미송출 시 공간 처리도 확인한다.
- [ ] 실제 적용은 페이지별 독립 슬롯으로 시작한다. 전역 main 앞에 한꺼번에 넣지 않는다.
- [ ] 전후 비교는 페이지·기기별 수익뿐 아니라 검색/계산 사용률, 반복 이용, LCP·INP·CLS를 함께 본다. 기존 측정 중복부터 정리한다.
- [ ] 트래픽이 적으면 며칠 수익으로 결론 내리지 않는다. CTR 상승만 성공 기준으로 삼지 않는다.

## 추천 진행 순서

1. 프리셋 상한과 이미지 선택 변경 회귀 확인·수정.
2. 서버별 스킬 상속 경계와 데이터 재빌드 보존 점검.
3. 성능 수집 중복 정리 후 광고 없는 기준 측정.
4. 하단 광고 1개 시안 검토. 우측 광고 칸은 여유 폭 확인 후 결정.

내일 하루에 전부 끝내려 하기보다 1~2를 먼저 처리하고 광고는 시안까지 진행하는 편이 현실적이다. 이번 리뷰는 코드·데이터를 수정하지 않았다.

검증: 이번 리뷰에서 `npx tsc --noEmit`, `npx eslint src/`를 실행해 모두 통과했다. 타입·린트 통과는 위 런타임 시나리오의 정상 동작을 보장하지 않는다. 테스트 스위트·빌드·브라우저 재현은 이번에 실행하지 않았다.
