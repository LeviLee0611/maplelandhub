# 메랜Hub — 개발 일지

> 최신 항목이 맨 위. 기능 구현 결정과 이유 위주로 기록.

---

## 2026-07-21

### 드롭테이블에 카오스 혼테일 추가 (메이플랜드)

카오스 자쿰에 이어 사용자가 카오스 혼테일도 추가 요청. 자쿰과 동일한 방법론(메이플노트 클래식 스크래핑)을 그대로 적용하려 했으나 두 가지 지점에서 달랐음.

**스탯 소스가 달라짐**: 메이플노트 사이트의 원정대 노트(`/pquestnote?pquest=chaos_horntail`)에서 부위별(왼쪽머리/오른쪽머리/머리A·B·C/왼손/오른손/날개/다리/꼬리) mobCode(8810100~8810109)와 통합 카드(8810118, 여기에 "카오스 혼테일2~5"라는 HP만 있고 드랍 없는 더미 복제본들도 8810119~8810122로 존재)를 확인. 그런데 이번엔 `maplestory.io` KMS API 수치가 기존 정규 혼테일(8810018) 실측치(HP 2,090,000,000)와 **정확히 일치**함을 발견 — 혼테일은 자쿰과 달리 KMS 레벨(160)과 메랜 레벨(160)이 동일해서 별도 밸런싱 없이 공식 수치를 그대로 씀. 그래서 부위별 스탯은 메이플노트 사이트 텍스트(억/만 단위 반올림 표기라 부정확)가 아니라 KMS API `meta` 원본값을 직접 사용.

**드랍 데이터가 아예 없음**: 메이플노트 사이트에서 카오스 혼테일 통합 카드(8810118)와 부위별 페이지(8810100~8810109) 전부 GET 섹션이 비어있었음(자쿰 때는 통합 카드 8888889에 48건이 몰려있었던 것과 다름). 아이템 검색으로도 "카오스 혼테일의 OO" 계열 전용 아이템이 안 나와서, 이 사이트엔 카오스 혼테일 드랍 정보 자체가 없다고 판단.

**해결**: 사용자에게 확인 요청 → "기본적으로 일반 혼테일과 같은 드랍 + 카오스 혼테일의 목걸이 추가"로 답변받음. 실행:
- 통합 카드(8810118)엔 정규 혼테일 본체(8810018)의 드랍 39건을 그대로 복사 + "카오스 혼테일의 목걸이"(itemId 1122076, `maplestory.io` API로 메타 확인) 추가.
- 카오스 혼테일 머리B(8810103)엔 정규 혼테일 머리B(8810003)의 드랍 20건을 그대로 복사.
- 목걸이는 확률 출처가 없어 `prob` 필드를 비움(자쿰의 나뭇가지 때와 동일하게 UI에서 "정보 없음" 표시).

**부수 확인**: 사용자가 "타임리스 vs 리버스" 장비 차이를 물어봐서 확인차 메이플노트에서 검색해보니, 실제 메랜 데이터에선 "타임리스" 계열 아이템이 아예 없고 "리버스" 계열은 존재하지만 전부 **핑크빈** 드랍(레벨120, 0.5%)으로 붙어있어 카오스 혼테일과 무관함을 확인 — 그래서 신규 무기 세트를 추가하지 않고 사용자 지시대로 정규 드랍 재사용 + 목걸이 추가만 반영.

**적용 방식**: `scripts/add-chaos-horntail.mjs` 일회성 스크립트로 `monsters.json`/`drop-index.json`에 프로그래매틱 반영(직접 편집 금지 규칙 준수).

**검증**: `npx tsc --noEmit`/`npx eslint src/` 통과, 로컬 서버에서 드롭테이블 검색 → "카오스 혼테일" 스탯(HP 20억/물공 5812/마공 5590/회피 169) 및 목걸이 "정보 없음" 표시까지 브라우저로 확인.

**후속: 통합 카드 아이콘 누락 수정**: 사용자가 "카오스 자쿰/카오스 혼테일 둘 다 사진이 안 보인다"고 제보. 원인 확인 결과 두 몬스터 다 부위별 데이터를 하나로 합쳐 보여주는 "집계 카드"라 maplestory.io에 전용 아이콘이 없음 — 카오스 자쿰(8888889)은 애초에 공식 mobId가 아니라 KMS/GMS/JMS 어느 버전에도 없고, 카오스 혼테일(8810118)은 실제 공식 mobId지만 렌더용 아이콘이 등록 안 돼있음(같은 이유로 기존 일반 "혼테일" 8810018도 원래부터 아이콘이 비어있었음 — 이번에 새로 생긴 문제가 아니라 기존 데이터 갭). `src/lib/maplestory-io.ts`에 `MOB_ICON_OVERRIDES` 맵을 추가해 이 세 mobCode(8810018→8810000 왼쪽머리, 8810118→8810100 카오스 왼쪽머리, 8888889→8800102 카오스 3페이즈)의 아이콘/렌더/애니메이션 요청을 대표 부위로 자동 치환하도록 수정. `getMobIconUrl`/`getMobAnimatedUrl`/`getMobAnimatedFallbackUrl`/`getMobRenderUrl` 전부 내부에서 치환하므로 DropTable 외 다른 화면에서도 동일하게 적용됨. 브라우저로 재확인 완료.

**후속: 메이플 플래닛에도 반영**: 사용자가 플래닛 쪽에도 같은 몬스터를 적용해달라고 요청. `scripts/build-planet-data.mjs`가 애초에 `data/monsters.json`/`data/drop-index.json`(메랜 원본)을 읽어 `scripts/sources/planet/divergence-overrides.json`의 배율만 얹어 `data/planet/*.json`을 생성하는 구조라, 이번에 메랜 쪽에 추가한 카오스 자쿰/혼테일이 이미 원본 데이터에 포함돼있어 `divergence-overrides.json`의 `newMonsters`를 손댈 필요 없이 스크립트만 재실행하면 자동 반영됨. `node scripts/build-planet-data.mjs` 실행 결과 두 보스 전체가 플래닛 `monsters.json`에 복사되고, 드랍률도 플래닛 고유 배율(x4)이 자동 적용됨(예: 혼테일의 목걸이 30%→100% 클램프, 10%→40%). 브라우저로 `/planet/drop-table`에서 아이콘/스탯/드랍률까지 확인 완료.

**후속: 통합 카드 이미지를 실제 전신 이미지로 교체**: 대표 부위 아이콘 치환(왼쪽머리/3페이즈)으로는 부족하다는 사용자 피드백 — 혼테일은 "얼굴 3개"(가운데/왼쪽/오른쪽 3개 머리가 다 보이는 것), 자쿰은 "팔이랑 본체 다 있는 것"을 원함. 후보 비교용 아이콘 피커 아티팩트를 만들었으나, 사용자가 "인터넷에 사진 많다"며 대신 실사 이미지 소스를 쓰라고 지시. 인벤 메이플스토리 몬스터 도감(`maple.inven.co.kr/dataninfo/monster/detail.php?code=8810018`, `code=8800002`)에서 각각 몬스터북용 전신 컷을 확보 — 혼테일은 좌/중/우 세 머리가 모두 보이는 전신 드래곤 이미지, 자쿰은 몸통+주먹(양팔)이 함께 보이는 토템 이미지. `public/images/monsters/horntail-full.gif`, `zakum-full.gif`로 저장하고 `src/lib/maplestory-io.ts`의 아이콘 치환 방식을 "다른 mobCode로 리다이렉트"에서 "로컬 정적 이미지로 직접 대체"(`MOB_ICON_STATIC_OVERRIDES`)로 변경 — maplestory.io API 자체에 존재하지 않는 이미지라 mobCode 치환으로는 애초에 한계가 있었음. 8810018(혼테일 본체)/8810118(카오스 혼테일)엔 혼테일 전신 이미지, 8888889(카오스 자쿰 통합 카드)엔 자쿰 전신 이미지 적용. 브라우저로 재확인 완료.

**부수 질문 — 타임리스 vs 리버스**: 실제 라이브 메이플스토리 기준 타임리스(Timeless)는 챠오스 혼테일 최초 등장 시점의 최상위 장비 티어(무기 공격력 +6~7, 강화 스킬 보유, 핑크빈이 주는 '시간의 돌'로 강화, 최대 +6강), 리버스(Reverse)는 그보다 나중에 추가된 더 접근성 좋은(시간의 신전 몬스터가 주는 '시간의 조각'으로 강화, 최대 +4강) 대신 스탯은 타임리스보다 낮은 하위 호환 티어. 다만 이번 메랜 데이터셋 조사에서 확인했듯 메이플랜드/메랜Hub 쪽엔 "타임리스" 자체가 없고 "리버스"는 핑크빈 드랍으로 재배치되어 있어, 라이브 게임의 실제 카오스 혼테일 전용 장비 체계와는 무관함(위 "카오스 혼테일의 목걸이" 조사 항목 참고).

**후속: 목걸이 중복 드랍 수정**: 사용자가 "공식적으로 (일반 목걸이/카오스 목걸이) 둘 다 나오냐"고 재질문. 웹 검색으로 확인한 결과 실제 게임에서 "혼테일의 목걸이"는 이지/노멀 전용, "카오스 혼테일의 목걸이"는 카오스 전용으로 서로 배타적임(동시에 안 나옴) — 기존에 일반 혼테일 드랍을 통째로 복사하면서 정규 목걸이(1122000)까지 같이 들어간 게 잘못이었음. `scripts/add-chaos-horntail.mjs`에서 정규 목걸이를 명시적으로 제외하도록 수정(+이전 실행으로 꼬인 `monstersByItemId` 역인덱스도 정리)하고 재실행, `build-planet-data.mjs`도 재실행해 플래닛 쪽까지 동기화. 카오스 혼테일 통합 카드(8810118) 드랍이 39건→38건(정규 목걸이 제거, 카오스 목걸이만 유지)으로 정정됨.

**후속: 이미지 배경 투명 처리 + 애니메이션 소스 재확인**: 인벤에서 가져온 이미지가 흰 배경이라 다른 아이콘(전부 투명 배경)과 이질감이 있다는 피드백. 추가로 maplestory.io에 움직이는 투명 버전이 있는지 재확인 요청받아 raw mob 메타(`framebooks`)까지 들여다봄 — 8810018(혼테일 본체)은 실제로 `stand` 프레임이 존재하긴 하나 렌더링 결과가 비어있어, 게임 내에서 시각적 실체가 없는 "히트박스 전용" 로직 개체로 확인됨(아이콘/렌더/애니메이션 엔드포인트 전부 빈 이미지인 이유). "얼굴 3개/본체+팔" 컷은 게임 스프라이트가 아니라 별도 제작된 몬스터북 일러스트라서 애초에 움직이는 버전이 존재하지 않음. 대신 `sharp`로 흰 배경(RGB 245 이상)을 알파 0으로 치환해 `horntail-full.png`/`zakum-full.png`로 재생성(원본 gif는 삭제), `MOB_ICON_STATIC_OVERRIDES` 경로도 png로 갱신. 마젠타 배경에 합성해 경계에 흰 테두리(halo) 없이 깨끗하게 잘렸는지 확인 후 브라우저로 최종 확인.

---

### 드롭테이블에 카오스 자쿰 추가 (메이플랜드)

사용자가 드롭테이블에 카오스 자쿰이 없다고 제보. 확인해보니 `data/monsters.json`엔 일반 자쿰(mobCode 8800000~8800009)만 있었고, `build-planet-data.mjs` 주석엔 오히려 "카오스 자쿰은 빅뱅 이후 콘텐츠라 메랜 데이터셋에 원래 없음"이라 적혀 있어 실존 여부부터 검증이 필요했음.

**존재 확인**: 공식 메이플랜드 패치노트(`public.maple.land/board/notices`)나 로드맵 개발일지에선 직접적인 언급을 못 찾았지만, 사용자가 "레고77이 최근에 했다"고 확인해줌 — 메이플랜드 2.0 로드맵에서 "해적 직업군 이후 콘텐츠도 업데이트될 수 있다"는 정책 변경이 있었던 것과 부합.

**mobCode**: `maplestory.io` KMS API(`/api/KMS/389/mob?searchFor=자쿰`)에서 원작 공식 구조 확인 — 본체 3페이즈 8800100~8800102, 팔 8개 8800103~8800110. 메랜 기존 일반 자쿰(8800000~8800009)과 동일한 넘버링 규칙이라 신뢰도 높음.

**스탯**: KMS 리테일 API 수치(레벨180, HP 21억 등)가 메랜의 기존 일반 자쿰 실측치(레벨140, HP 66M)와 전혀 안 맞아서(메랜은 원작을 그대로 안 쓰고 자체 밸런싱함을 재확인) 그대로 못 씀. 대신 `scripts/fetch-new-items-drop-data.mjs`가 이미 드롭 스크래핑 소스로 쓰고 있는 "메이플노트 클래식"(`xn--o80b01o9mlw3kdzc.com`) 사이트에서 mobCode별 실측치를 직접 대조 — 이 사이트의 일반 자쿰 수치가 기존 `monsters.json`과 정확히 일치하는 걸 확인해 신뢰 소스로 채택. 카오스 자쿰 전체가 일반 자쿰의 정확히 8배 HP(예: 1페이즈 66M→528M, 팔1 33M→264M)로 스케일링되어 있어 일관성도 확인됨. acc/eva는 이 사이트에 표시되지 않아 KMS 원작값(acc 550/eva 200)을 잠정치로 사용 — 실측 나오면 교체 필요.

**드롭 아이템(1차)**: 같은 사이트의 `item_detail/1003112`에서 "카오스 자쿰의 투구"가 100% 확정 드롭으로 명시된 것만 반영(mobCode 8800102). "카오스 자쿰의 나뭇가지", 150제 "분노한 자쿰" 세트(투구/망토/벨트)는 해당 사이트에 아이템 페이지 자체가 없어(404) 메랜에 미구현일 가능성이 높다고 판단해 제외.

**"카오스 자쿰" 통합 카드 + 전체 드롭테이블 (후속 요청)**: 사용자가 "1/2/3페이즈+팔로 쪼개진 것도 맞지만, 카오스 자쿰 검색 시 전체 환산과 드롭테이블을 같이 보여줬으면 좋겠다"고 요청. 처음엔 개별 페이즈 페이지(`monster_detail/8800100~102`)의 GET 섹션만 확인해서 "투구 1개밖에 못 구했다"고 잘못 판단했는데, 재조사하며 이 사이트가 **페이즈와 별개로 "카오스 자쿰" 통합 엔티티(mobId `8888889`)를 따로 두고 거기에 전체 드롭표(50건)를 몰아넣는다**는 걸 발견 — 개별 페이즈 페이지는 애초에 드롭 데이터가 비어있는 게 정상 설계였음. 이 mobId를 그대로 `monsters.json`에 추가(이름 "카오스 자쿰", HP 3.9B/EXP 31M 합산치, watk/matk/def/mDef는 원본 그대로 0 — 전투 개체가 아니라 순수 집계 카드라 N방컷/피격뎀 계산기에 쓰면 무의미한 값이 나옴을 사용자에게 안내함). 드롭 50건 중 0% 확률 2건은 제외하고 48건 반영, 아이템 메타 47건은 기존 일반 자쿰 드롭과 겹쳐 재사용, 신규 1건("[마스터리북] 메이플용사 30", itemId 2290125)만 `maplestory.io` API로 메타 보강.

**나뭇가지 추가 확인**: 사용자가 "카오스 자쿰의 나뭇가지도 실제로 있다"고 정정 — 처음엔 `item_detail/1372073`(공식 itemId는 KMS API로 맞게 찾았었음) 페이지가 404라 "메랜 미구현"으로 오판했던 것. itemId 자체는 맞았고 단지 그 스크래핑 사이트의 DB 커버리지 문제였음. 드롭 확률은 어떤 소스에서도 확인 못 해 `prob` 필드를 아예 비워 UI가 "정보 없음"으로 정직하게 표시하도록 함(가짜 %를 넣지 않음) — 자쿰의 투구와 마찬가지로 "난이도 공통 획득" 아이템이라는 커뮤니티 정보에 따라 3페이즈(8800102)와 통합 카드(8888889) 양쪽에 연결.

**교훈**: 아이템 단위(`item_detail`)로만 역추적하지 말고 몬스터 단위(`monster_detail`) 페이지의 GET 섹션을 직접 확인했어야 더 빨리, 더 정확히 찾았을 것. 또한 "그 사이트에 없다 = 메랜에 없다"로 성급히 결론 내리지 말 것 — 사용자의 실측 확인이 더 신뢰도 높은 소스였음.

**검증**: 매 단계마다 `npx tsc --noEmit`/`npx eslint src/` 통과 확인, 로컬 서버(`npm run dev`)에서 드롭테이블 검색 → 개별 페이즈/통합 카드/나뭇가지 "정보 없음" 표시까지 브라우저로 직접 확인.

**적용 방식**: `data/monsters.json`, `data/drop-index.json`은 빌드 산출물이라 직접 편집 금지 규칙이 있어, 일회성 스크립트(스크래치패드)로 프로그래매틱하게 추가 — `fetch-missing-monsters.mjs`가 신규 몬스터를 API 스크래핑으로 `monsters.json`에 직접 추가하는 기존 패턴을 그대로 따름. `data/raw/monsters.raw.ts`는 건드리지 않음(raw.ts를 기준으로 `normalize-monsters.mjs`를 재실행하면 raw.ts에 없는 기존 239개 몬스터가 삭제되는 걸 사전에 확인해서 회피).

**검증**: `npx tsc --noEmit` 통과.

---

## 2026-07-16

### 정령의 축복 / 여제의 축복 계산기 반영, 비교 페이지 문구 정정

사용자가 N방컷 계산기에 정령의 축복(정축)/여제의 축복(여축)이 반영돼 있는지 물어와서 확인 — 둘 다 전혀 구현돼 있지 않았음(메이플 용사만 있었음). 실제 서버 수치를 넣기 전에 정확도부터 검증하는 데 상당한 시간을 씀: 나무위키 일반 스킬 문서(최신 본편 기준, 정축 최대 20/여축 최대 30) → 메이플랜드 커뮤니티 블로그(10렙당+1, 레벨당 공1마2) → 메이플 플래닛 유저 글(정축 최대 200/여축 최대 120) 순으로 소스마다 수치가 전부 달라서, 최종적으로 두 서버의 **공식 홈페이지**(mapleplanet.co.kr 패치노트, maple.land 개발일지)를 직접 뒤져서 확인.

**공식 확인 결과**:
- 플래닛(2026-05-13 공식 패치노트): "시그너스 기사단 최대 레벨 120, 10레벨당 1포인트로 여제의 축복 활성화" → 여축 최대 레벨은 **12**(120÷10)임을 확인. 이전에 유저 글을 잘못 읽고 120으로 넣었던 걸 정정.
- 메이플랜드(2.0 업데이트 미리보기 개발일지): 밸런스 기준이 "모험가의 귀환(KMS 1.2.89)"이라고 명시 — 이 시점은 여제의 축복이 도입된 1.2.111보다 이르므로, **메이플랜드에는 여제의 축복이 없다**는 판단의 근거로 채택.
- 정축/여축 중첩 여부는 두 공홈 어디에도 명시돼 있지 않았고, 플래닛 커뮤니티(dcinside/arca) 내에서도 "중첩된다"/"안 된다" 의견이 갈림. 일반 넥슨 메이플스토리 본편은 "중첩 안 되고 높은 쪽만 적용"이 공식 룰이라, 근거 불충분한 상태에서 **중첩 안 됨으로 가정**하기로 사용자와 합의.

**구현** (`onehit-calculator-client.tsx`): 서버별 설정(`SPIRIT_BLESSING_BY_SERVER`, `EMPRESS_BLESSING_BY_SERVER`)을 분리 — 메이플랜드는 정축만(최대 20, 레벨당 공+1/마+2), 플래닛은 정축(최대 200)+여축(최대 12) 둘 다 노출하되 `Math.max()`로 더 높은 쪽만 적용. "총 공격력/총 마력" 직접 입력 모드에서는 이미 게임 화면에 반영된 수치이므로 중복 합산되지 않도록, 스탯 기반 자동계산(`derived.attack/magic`) 경로에만 보너스를 더함 — 큐브 빌더에서 겪었던 것과 같은 이중 합산 함정을 여기서도 미리 방지. UI에는 "(잠정치)" 라벨을 달아 커뮤니티/공식 소스 기반 추정치이며 실제와 다를 수 있음을 명시.

사용자가 함께 제보한 "플래닛 쿠키 공격력 버프 캐시템"은 `equips.js` 전수 검색 결과 실존하지 않는 걸로 확인(동명의 발렌타인 이벤트 아이템만 존재) — 이번 반영에서 제외.

**비교 페이지 정정**: `/mapleland-vs-planet`의 "메이플스토리 월드(유저 제작 콘텐츠 플랫폼)" 표현이 넥슨의 공식 운영을 과소평가한다는 지적에 따라, 플래닛 공홈 FAQ/푸터에 명시된 "넥슨(주)의 메이플스토리월드 플랫폼에서 공식 출시된" 문구를 근거로 "넥슨이 직접 운영하는 공식 타이틀"로 정정.

**검증**: tsc/eslint/vitest(54개) 통과, 로컬에서 메랜/플래닛 두 버전 모두 UI 확인.

### "메이플랜드 vs 메이플플래닛" 비교 콘텐츠 페이지 신설

세분화/특정화로 유입을 늘리자는 방향에서, 유저 규모 리서치 중 우연히 발견한 단서를 바로 실행 — "메이플랜드 메이플플래닛 차이"류 질문이 a-ha/디시/인벤/에펨코리아 등에 반복해서 올라오는데(검색 상위 노출 확인), 정작 두 서버를 전문적으로 다루는 사이트에서 이 질문에 제대로 답하는 콘텐츠가 없었음. 메랜Hub는 두 서버를 동시에 지원하는 몇 안 되는 사이트라 이 검색 수요를 가장 잘 받을 수 있는 위치라고 판단.

**신규 페이지**: `/mapleland-vs-planet` (`src/app/(routes)/mapleland-vs-planet/page.tsx`) — `/about`, `/guide`와 동일한 정적 콘텐츠 페이지 패턴(별도 layout.tsx 없이 page.tsx에 메타데이터 직접 export, `glass-panel` 카드 레이아웃). 비교 표(오픈베타/정식출시일, 배율, 큐브 시스템 유무, 직업 구성) + 배율 설명 + 큐브 시스템 설명 + 중립적인 "어느 쪽이 맞을까" 안내 + 양쪽 도구로의 CTA 링크로 구성.

**정확성 검증**: 배율(경험치 4배/드랍률 4배/메소 2배)은 이 프로젝트 자체의 `scripts/sources/planet/divergence-overrides.json`(빌드 파이프라인이 실제로 곱해서 쓰는 값)과 외부 검색 결과(mapleplanet.co.kr 공식 FAQ 기반 요약)가 정확히 일치하는 것을 교차 확인 후 반영 — 추측 없이 근거 확보. 출시일(메랜 OBT 2023-10-31/정식 2024-10-31, 플래닛 OBT 2026-04-24)도 웹 검색으로 별도 확인. 직업 구성 차이는 TODO.md에 이미 기록된 "사용자 확인" 근거를 그대로 인용(과신하지 않고 적당히 hedge한 표현 사용). 어느 쪽이 "더 낫다"는 식의 편향된 서술은 의도적으로 피함 — 두 커뮤니티 유저를 다 상대해야 하는 사이트라 중립성이 중요하다고 판단.

**노출**: 사이드바 guideLinks(양쪽 서버 모드 공통 노출), 메이플랜드 홈 푸터, sitemap.xml에 추가.

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(54개), `npm run build`(43개 라우트, `/mapleland-vs-planet` 정적 생성 확인) 전부 통과. 브라우저로 렌더링/콘솔 에러 없음 확인.

### 메랜/플래닛 유저 규모 리서치 + 전략 방향 전환, 큐브 빌더 노출 중단

큐브 빌더 UX가 "체계화가 안 됐다"는 피드백에서 시작해서, "애초에 이 기능을 유저가 원하긴 하나"로 논의가 번짐. 결정을 위해 메이플랜드 vs 메이플플래닛 실제 유저 규모를 웹 검색으로 조사(둘 다 메이플스토리 월드 UGC 모드라 넥슨 공식 API 기반 인구 통계 사이트(츄츄지지 등)엔 안 잡히는 것도 확인 — 전부 커뮤니티/기사 보도 스냅샷 기준).

**결과**: 메이플랜드 누적 240만 vs 플래닛 34.5만(누적은 메랜이 압도적, 오래 운영됐으니 당연) / 동접은 메랜 1.3만~5.5만인데 플래닛이 출시 2주 만에 6.5만~11만대까지 급성장 — 플래닛이 메랜을 추월하는 추세라는 보도도 있었음. **사용자가 이 근거로 "플래닛은 더 이상 서브가 아니라 메랜/플래닛 둘 다 메인"으로 전략 전환 결정** — 메모리(`project_context.md`)에 기록해서 다음 세션에도 이어지게 함. 다만 이 전환에 안 맞는 기존 구조(사이드바가 플래닛 모드에서만 플래닛 nav 노출, 사이트명 자체가 메랜 중심, `/quests`에 플래닛 미러 없음 등)는 발견만 해두고 이번엔 안 건드림 — 플래닛 퀘스트 미러는 사용자가 "딱히 필요 없다"고 확인.

**우선순위**: "지금 있는 서비스를 세분화/특정화해서 유입 늘리는 게 먼저"로 확정, 큐브 빌더 UI 재구성 작업은 보류.

**큐브 빌더 처리**: "검증된 수요 없이 어중간하게 방치하는 게 제일 나쁘다"는 사용자 지적에 동의 — 완전 삭제 대신 **노출만 중단**하기로 결정(사이드바/플래닛 랜딩 카드/큐브 시뮬레이터 상호링크/sitemap.xml에서 전부 제거, `/planet/cube-builder` 라우트 자체는 유지하되 `robots: {index:false, follow:false}`로 noindex 처리해서 검색엔진엔 안 잡히게 함). URL을 직접 아는 사람은 여전히 쓸 수 있고, 나중에 실사용 신호가 생기면 다시 노출 복원 가능. 코드/테스트(cubeBuilder.ts, cubeBuilder.test.ts, dataAudit 테스트)는 전부 그대로 둠 — 로직 자체가 틀린 게 아니라 "지금 홍보할 우선순위가 아니다"라는 판단이라 삭제하지 않음.

**검증**: tsc/eslint/vitest(54개)/build 전부 통과.

### 드롭테이블/피격뎀 계산기 UX 감사 + GA4 이벤트 트래킹

GA4 데이터로 드롭테이블(40 조회)/피격뎀 계산기(29 조회)의 이탈률(39.4%/36.4%)을 확인했는데, 이게 정말 UX 문제인지 "빠르게 답 찾고 나간 성공 사례"가 이탈로 잘못 잡힌 건지 구분이 안 된다는 문제의식에서 시작 — 실제 컴포넌트 코드를 정독해서 진단.

**드롭테이블 실제 버그 발견**: 자동완성 드롭다운이 `query && showSuggestions` 조건에 걸려있어서 검색어 없이는(포커스만 줘도, 월드맵 필터만 바꿔도) 아무 목록도 안 뜨는 상태였음 — `INITIAL_SUGGESTION_COUNT` 상수로 기본 목록을 준비해두고도 렌더 조건 때문에 죽어있었던 것으로 보임. 조건을 `showSuggestions`로 단순화해서 수정, 초기화 버튼과 아이템 선택 시 관련 계산기 링크(가장 확률 높은 드랍처 기준)도 추가.

**피격뎀 계산기**: 모바일에서 입력 10개+를 스크롤해야 결과가 보이던 문제 — 스티키 요약바 추가. 자동완성에 없는 몬스터명을 직접 타이핑하면 안내 없이 목록 첫 몬스터로 조용히 계산되던 것도 발견해서 경고 문구 추가. 결과 복사 버튼, 중복 소개 카드 제거.

`src/lib/analytics.ts` 신설(GA4 이벤트 래퍼, 자유 텍스트는 절대 전달 안 함) — 두 페이지에 검색/필터/결과조회/에러/복사 등 13개 이벤트 계측. 커밋 `ef381e7`.

### 문서 정합성 정리 (Codex 피드백 반영)

Codex가 짚어준 문서 불일치 6건(nav-bar.tsx 잔재, 파티 기능 상태 충돌, data.md의 RSC payload 오해 소지 표현, 날짜 오래됨, TODO.md가 DEVLOG처럼 비대해짐, NOTE.md 빈 파일)을 실제 코드/git 상태와 대조해서 전부 검증 후 반영. 검증 과정에서 project-notes.md가 DropTable/TakenDamageCalculator 구조를 잘못 설명하던 것(존재하지 않는 컴포넌트 조합 언급)과, 이미 구현/해결된 TODO 항목 2개(드롭테이블 역검색, oneshot 리다이렉트 확인)가 스테일 상태로 남아있던 것도 추가로 발견해 정리. TODO.md는 완료 항목 장문 서술을 전부 제거(DEVLOG.md에 이미 더 상세히 있는 걸 확인) — 131줄에서 남은 액션 위주로 대폭 압축. 커밋 `63c9dd0`.

### 큐브 빌더 신설 — 캐릭터 스펙 기준 큐브 의사결정 도구

기존 큐브 시뮬레이터(`/planet/cube-simulator`)에 유입이 거의 없다는 문제 제기에서 출발 — 원인 진단(사이드바가 플래닛 모드에서만 노출, 메이플랜드 쪽엔 노출 거의 없음, `/services/*` 패턴의 SEO 설명 페이지가 없음)과 별개로, "지금 시뮬레이터가 기댓값만 주고 실제 스펙업은 안 보여준다"는 사용자 문제의식을 검증하려고 경쟁 사이트 cubeplanner.kr을 실제로 열어봄.

**발견**: cubeplanner.kr의 핵심은 캐릭터 스탯+장비 잠재 입력 → 모든 옵션을 "마력/공격력 N%"로 환산 → 부위별 "좋아질 확률"과 "+0.5%당 평균 큐브 개수"로 어느 부위를 먼저 돌려야 할지 추천하는 것. 이 사이트 가이드 FAQ에 "데미지 공식은 빅뱅 직전 메이플 표준"이라고 명시돼 있어서, 이미 이 저장소의 한방컷 계산기(`src/lib/calculators/onehit.ts`)가 같은 공식 계열을 구현해뒀다는 걸 확인 — 처음부터 새로 만들 필요 없이 재사용 가능하다고 판단, EnterPlanMode로 설계 확정 후 구현.

**구현**:
- `cubeSimulator.ts`에 `resolveOptionValues`(옵션의 원본 숫자 노출, 기존 `resolveOptionText`는 문자열만 반환해서 부족했음)와 `EquipCategory`/`isOptionAllowedForEquip`(장비 종류별 옵션 필터) export 추가 — 기존 동작 변경 없음. `ResolvedCubeLine`에 `entryId` 필드 추가해서 롤 결과를 원본 옵션으로 역추적 가능하게 함.
- `GradeOptionPicker`(등급→옵션 2단계 선택)와 등급 표시/포맷 유틸(`gradeMeta`/`formatCount`/`formatProbability`)을 `CubeSimulator.tsx`에서 `src/components/GradeOptionPicker.tsx`/`src/lib/cube-ui.ts`로 추출 — 큐브 시뮬레이터와 빌더 양쪽이 같은 컴포넌트/포맷을 공유.
- `data/planet/cube-index.json`의 suspicious(185)+miracle(132) 전체 옵션 텍스트 템플릿을 직접 덤프해서(56개 유니크) 손으로 분류표 작성 — `optionType`+변수명만으론 부족했던 부분 확인(예: `incDAMr` 변수명이 "총 데미지"와 "보스 공격 시 데미지" 둘 다에 쓰여서 반드시 `text` 접두어로 구분해야 했음, "올스탯" 옵션은 `text`엔 스탯 하나만 나오지만 `level` 객체엔 4개 스탯이 다 들어있음).
- `src/lib/calculators/cubeBuilder.ts` 신규 — `classifyOption`(옵션→스펙 델타 분류, 스킬재사용/이동속도/방어력 등은 "잡옵" 취급), `computeCharacterPower`(`calcBaseDamageFromStats` 재사용 + 크리티컬 기댓값 + 보스뎀/총뎀 곱연산 + 방어력 감산), `simulateSlotOutcome`(몬테카를로 — 기존 `rollCubeResult`를 그대로 반복 호출, cubeplanner.kr도 "5,000번 시뮬" 방식이라 방법론 일치. 기존 폐쇄형 확률 함수들은 "특정 옵션 하나" 겨냥용이라 목적이 달라 재사용 안 함).
- `/planet/cube-builder` 신규 라우트(`src/components/CubeBuilder.tsx`) — 캐릭터 정보 패널, 15개 장비 슬롯 그리드(클릭해서 추가, `GradeOptionPicker`로 3줄 입력), 부위별 가성비 추천 결과. `QuickSlots`로 로컬 저장(Supabase 연동은 범위 제외). 사이드바/플래닛 랜딩/sitemap.xml에 노출 추가, 큐브 시뮬레이터와 상호 링크.
- **이름 확정**: 처음엔 "큐브 플래너"로 만들었는데, 사용자가 "큐브 시뮬레이터"와 이름이 겹치는 걸 지적 — 두 기능이 서로 다른 서비스인지(굴려보기 vs 스펙 기준 의사결정) 설명하고 합칠지 분리 유지할지 확인한 뒤, 분리 유지하되 이름은 "큐브 빌더"로 변경(사용자 제안). 파일/타입/컴포넌트명까지 전부 `cubePlanner`→`cubeBuilder`, `/planet/cube-planner`→`/planet/cube-builder`로 일괄 리네임.

**코드 리뷰(자체) 후속 발견 및 수정**: 브라우저 실측 중 "빈 슬롯(잠재 미입력)"이 실제로는 가장 효율적인 큐브 대상인데도(무기 슬롯 평균 2.0개/0.5% vs 반지 3.2개/0.5%) 추천 카드가 계속 반지를 가리키는 버그 발견 — `recommended` 계산 로직이 `potentialIds.some(id => id !== null)`로 빈 슬롯을 후보에서 아예 걸러내고 있었음(의도치 않은 필터). 필터 제거해서 수정.

**알려진 한계(TODO.md에 기록)**: 보스뎀%/방어율무시% 배율은 표준 관례(단순 곱연산) 적용이라 메랜/플래닛 공식 검증은 아님. 몬스터 방어력은 절대값 입력(프리빅뱅 공식 특성상 %가 아님) — cubeplanner.kr의 "방어율%" UX와 다르다는 점을 페이지 안내 문구로 명시.

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(신규 17개 포함 48 passed), `npm run build`(42개 라우트, `/planet/cube-builder` 정적 생성 확인 — 첫 시도는 Windows 빌드 워커가 access violation으로 죽어서 재시도로 통과, 코드 문제 아님) 전부 통과. 브라우저(claude-in-chrome)로 반지에 STR+12(레어) 입력 → 현재 가치 +2.23%, 좋아질 확률 31.67%/평균 3.3개 계산 확인, 무기 슬롯을 빈 채로 추가했을 때 오히려 더 효율적으로(평균 2.0개) 계산되고 추천 카드가 정확히 그쪽으로 바뀌는 것까지 재확인. 큐브 시뮬레이터 페이지도 리팩토링 후 회귀 없이 그대로 동작하는 것 확인(같은 세션의 하이드레이션 경고는 브라우저 확장 프로그램발 노이즈, 기존에도 반복 확인된 것과 동일 원인).

**외부 리뷰(Codex) 반영**: 이름 리네임 과정에서 AGENTS.md Zone B/project-notes.md에 새 파일들을 반영 안 한 걸 발견해 정리. `GradeOptionPicker`에도 진짜 버그 하나 발견 — QuickSlots 복원이나 슬롯 전환처럼 `value`가 프로그래밍적으로 바뀌는 경우 내부 `gradeFilter`(로컬 state)가 안 따라가서 두 번째(옵션) 드롭다운이 빈 목록으로 깨지는 문제였음. `value`가 있으면 그 옵션이 속한 등급을 역산해서 쓰도록 수정. 리뷰가 선택창 활성화(disabled 여부)는 고쳤는데 강조색(chip) 스타일 쪽엔 같은 로직이 안 들어가 있던 잔여 불일치가 있어서 마저 통일.

**직접 재검토하다 발견한 더 심각한 버그**: 사용자가 "한 번 더 체크해달라"고 해서 `computeSlotValue`/`simulateSlotOutcome`을 다시 들여다보다가, 캐릭터 스탯 입력(예: STR)이 "현재 총합"(이미 그 슬롯의 잠재 효과 포함, cubeplanner.kr과 동일 관례)이라고 안내해놓고도 정작 슬롯 가치 계산은 그 위에 슬롯 잠재를 또 얹어서(이중 반영) 비교하고 있었음 — 재굴림 시뮬레이션도 "새 옵션"과 "기존 옵션"을 똑같이 이중 반영된 기준선 위에서 비교해서 정확한 비교가 아니었음. 우연히 "이미 낀 옵션과 똑같은 게 나오면 0%" 테스트만 양쪽이 대칭으로 틀려서 통과했던 것 — 버그를 가려버린 케이스였음. `removeDeltas`(델타 역연산, 순서 반대로 되돌림) 추가해서 "이 슬롯의 현재 기여분을 뺀 기준선"과 비교하도록 수정, 안내 문구에도 "슬롯 기여분은 자동으로 뺀다"고 명시. 회귀 테스트 1개 추가(캐릭터 str=550, 슬롯이 STR+50 기여 → 손계산한 기댓값과 정확히 일치하는지 검증) — 총 49개 테스트 통과.

**브라우저 재검증 미완료**: 이 세션 마지막에 브라우저 확장의 특정 버튼 클릭(장비 슬롯 추가)이 여러 탭/ref를 바꿔가며 시도해도 반응이 없어서(다른 상호작용(숫자 입력, 네비게이션)은 정상 동작) 이중 반영 수정 자체는 새 유닛 테스트(독립적으로 `computeCharacterPower`를 손으로 조합해 기대값을 계산하고 대조)로만 검증하고 실제 화면 확인은 다음 세션으로 미뤄둠 — 코드/수식 검증은 확실하지만 실제 UI 조작 확인이 남아있다는 점 기록.

**한 번 더 재검토해서 찾은 것 2건**(사용자가 "왜 계속 나오지" 하며 재확인 요청):
1. `data/planet/cube-index.json`을 직접 스크립트로 조사해서 수상한(185)/미라클(132) 풀의 id 겹침을 확인 — 132개는 id·text·optionType이 완전히 동일(안전)하지만, 수상한 전용 53개는 겹치지 않음. 큐브 종류를 바꿔도 슬롯에 골라둔 잠재 id를 그대로 유지하고 있었어서, 수상한 전용 옵션을 고른 상태로 미라클로 전환하면 조용히(에러 없이) 그 줄이 계산에서 빠지는 문제가 있었음 — `CubeSimulator.tsx`의 "장비 종류 필터 전환 시 선택 초기화" 패턴을 그대로 가져와 큐브 종류 전환 시 모든 슬롯의 잠재 선택을 초기화하도록 수정, 안내 문구 추가.
2. QuickSlots로 다른 스냅샷을 불러올 때 `expandedSlot`(현재 펼쳐진 슬롯 패널)이 복원 안 돼서, 복원된 슬롯 목록에 없는 슬롯 패널이 계속 펼쳐진 채로 남아있을 수 있던 사소한 불일치 — 복원 시 패널을 닫도록 수정.

CubeBuilder.tsx/cubeBuilder.ts 전체를 라인 단위로 다시 훑었고, 이번엔 새로 찾은 이슈 없음(49개 테스트 유지, tsc/eslint/build 재통과).

**한 번 더(3번째) 재검토**: 사용자가 "안 나올 때까지 계속 확인해달라, 거짓말은 안 된다"고 명시적으로 요청 — 지금까지는 수동으로 뽑은 56개 유니크 텍스트 템플릿만 보고 분류표를 짰던 거라, 실제 317개 엔트리 전체(수상한 185 + 미라클 132)를 `classifyOption`에 직접 돌려서 자동 감사하는 테스트(`cubeBuilder.dataAudit.test.ts`) 신규 작성:
- 전체 엔트리 크래시 없이 분류되는지
- "데미지/공격력/마력/크리티컬/방어율" 키워드가 텍스트에 있는데 잡옵으로 빠진 게 없는지(분류표 누락 탐지) — 처음엔 "피격 시 X% 확률로 데미지 무시"류(받는 데미지 경감, 방어형 프록)가 걸렸는데 확인해보니 실제로는 스펙 무관이 맞는 잡옵이라 오탐이었음, 필터를 정밀화해서 해결
- 델타 값이 전부 유한한 숫자인지
- STR/DEX/INT/LUK 텍스트와 분류된 stat 필드가 실제로 일치하는지
- "보스 공격 시 데미지"/"총 데미지" 텍스트가 정확히 그 kind로만 분류되는지

결과: 317개 중 159개(50%) 잡옵, 나머지는 stat 112/attack 36/ignoreDefense 10/bossDamage 10/totalDamage 8/criticalRate 8/criticalDamage 4로 분포 — 손으로 짠 분류표가 실제 전체 데이터에 대해 정확함을 확인. 이 테스트는 나중에 cube-data.js가 갱신돼도(주간 패치 체크 에이전트가 신규 옵션을 추가하는 경우 등) 분류표 누락을 자동으로 잡아주는 회귀 테스트로 남겨둠 — 총 54개 테스트 통과.

**정직하게 남겨두는 한계**: 브라우저로 실제 버튼을 눌러보는 검증은 이 세션 내내 특정 상호작용(장비 슬롯 추가 버튼 클릭)에서 막혀서 끝내 못 함 — 코드 검토(3회 반복)와 자동화된 유닛/데이터 감사 테스트(54개)로는 확실히 검증했지만, "실제 화면에서 클릭해서 눈으로 확인"은 다음 세션 숙제로 남음.

**4번째 재검토** — 이번엔 정확성 버그는 안 나왔고(코드/데이터 자체는 이제 상당히 검증됐다고 판단), 대신 성능 이슈 하나 발견: `outcomes`(슬롯별 결과)가 `useMemo`로 캐릭터 스탯이 바뀔 때마다 즉시 재계산되는데, 슬롯 하나당 몬테카를로 1500회를 돌기 때문에 슬롯을 여러 개(예: 10개+) 채운 상태에서 스탯 입력칸에 한 글자만 쳐도 눈에 띄게 버벅일 수 있음 — 정확성 문제는 아니지만 실사용성 문제. `TakenDamageCalculator`의 GA 이벤트 디바운스와 같은 패턴(입력 멈춘 뒤 400ms 후에만 재계산)으로 전환. `CubeSimulator.tsx`도 리팩토링 후 전체를 다시 정독해서 로직 변화 없음(import만 공유 모듈로 교체) 재확인.

---

## 2026-07-14

### GA4 애널리틱스 연동

조회수가 조금씩 나오기 시작하는데 애드센스를 붙이기엔 이른지 판단하고 싶다는 요청에서 시작. 아직 방문자 추이를 잴 수단이 없었어서(GA도 다른 애널리틱스도 미설치), 먼저 GA4부터 붙이기로 결정.

**구현**: `src/components/google-analytics.tsx` 신설 — `next/script`(`afterInteractive`)로 gtag 스크립트 삽입, 측정 ID는 `NEXT_PUBLIC_GA_ID` 환경변수로 주입하고 프로덕션 빌드에서만 렌더링(로컬 개발 중 조회수 오염 방지). `layout.tsx`에 `<GoogleAnalytics />` 추가. Cloudflare Pages 환경변수에 `NEXT_PUBLIC_GA_ID` 등록 필요(로컬 `.env.local`은 배포에 포함 안 됨).

**Cloudflare Pages "Retry deployment" 함정 발견**: 환경변수를 대시보드에서 새로 추가/수정한 뒤 "Retry deployment"로 재배포해도 실제 라이브 HTML엔 반영이 안 됐음(`curl`로 직접 확인 — 캐싱 문제 아님, `Cf-Cache-Status: DYNAMIC` 확인 완료). Retry deployment가 그 배포가 처음 트리거됐을 때의 환경변수 스냅샷을 재사용하는 것으로 추정 — **환경변수를 바꾼 뒤엔 반드시 새 커밋으로 완전히 새 배포를 트리거해야 함**. 실제로 빈 커밋(`git commit --allow-empty`)으로 새 배포를 유도하니 정상 반영됨.

**GA4 동의 모드 설정**: 사이트에 쿠키 동의 배너가 없는 상태라 "동의 배너를 사용하지 않음"으로 설정. 이 과정에서 측정 ID가 `G-K91GWXF2S1` → `G-B3TGJPBEE7`로 재발급됨(동의 모드 설정 완료 시 태그가 갱신되는 것으로 보임) — 새 ID로 환경변수/재배포 다시 진행.

**"데이터 수집 안 됨" 경고 디버깅**: GA4 관리 화면의 이 경고는 정적 안내 배너라 실제 수집 여부와 별개로 한동안 남아있음 — 실시간(Realtime) 보고서로 즉시 확인하는 게 맞는 방법. 실시간에서도 0으로 안 잡혀서 살펴보니, 원인은 사용자의 평소 브라우저에 깔린 **광고 차단 확장 프로그램**이 `googletagmanager.com` 요청을 막고 있던 것 — 시크릿 모드(확장 꺼짐)로는 정상 수집 확인. 실제 방문자 대부분은 광고 차단 안 쓰므로 문제 없음, 이 자체는 코드 이슈가 아니라 모든 GA 설치에 공통되는 업계 한계.

**보류 결정**:
- **애드센스**: 지금 신청(심사)하면 트래픽/콘텐츠 부족으로 반려될 위험 있고, 반려 시 재신청까지 대기 기간이 있어 오히려 늦어질 수 있음 — GA4로 1~2주 추이 확인 후 신청하기로. 계정 생성/`ads.txt` 등 사전 준비도 나중으로 미룸(사용자 판단).
- **광고차단 우회(Cloudflare Zaraz 등 퍼스트파티 프록시)**: 지금 트래픽 규모에선 불필요, 애드센스 붙이고 수익 분석이 중요해질 때 재검토.

**커밋**: `5080bf3`(GA4 연동), `8547c47`(재배포 트리거용 빈 커밋).

---

## 2026-07-09

### 주간 패치 체크 자동화 에이전트(Scheduled Cloud Routine) 신설

매랜/플래닛 공식 사이트를 매번 사람이 확인 안 해도 되게, 주기적으로 패치노트를 확인해서 관련 데이터를 자동으로 갱신하는 클라우드 에이전트가 있으면 좋겠다는 요청. 배포 타이밍은 사람이 가장 적게 플레이하는 시간대(우리 시간 오후 3시 = 한국시간 아침 6시)로 지정.

자율성 수준을 세 가지로 제안(완전자동 / 알림만 / 조사+수정+PR, 사람이 검토 후 머지)했고, **PR 기반(3번)**으로 확정 — 데이터 정확성이 중요한 프로젝트라 게임 수치 반영을 에이전트가 완전 자동으로 main에 바로 반영하는 건 리스크가 크다고 판단.

`RemoteTrigger`로 루틴 생성(`trig_01Wf6e7JvS3biMsG2Me2q2Cn`, 이름 "메랜Hub 매랜/플래닛 패치 체크 & 데이터 업데이트 PR", 레포 `LeviLee0611/maplelandhub`, 모델 `claude-sonnet-5`). 매 실행마다:
1. `CLAUDE.md`/`AGENTS.md`/`data.md`/`TODO.md`/`DEVLOG.md`를 먼저 읽어 프로젝트 규칙 파악
2. maple.land / mapleplanet.co.kr 공지·패치노트를 `data/.last-patch-check.json`(마지막 처리 기록)과 비교 — **새 게시물이 하나도 없으면 브랜치/커밋/PR 없이 조용히 종료** (매주 도는 작업이라 대부분 이 경로일 것으로 예상)
3. 새 패치가 있으면 우리 데이터(몬스터/드랍/아이템/퀘스트/스킬/큐브 등)와 관련 있는지 판단, `data.md` 파이프라인 그대로 반영(패치노트에 없는 수치는 절대 추측 금지 — 애매하면 TODO.md에 "확인 필요"로만 남김)
4. 커밋 전 `tsc --noEmit`/`eslint`/`vitest`/`build` 전부 통과 확인, 실패하면 되돌리고 커밋 안 함
5. 새 브랜치(`auto/patch-check-YYYY-MM-DD`)에 커밋 → push → `gh pr create`로 **main 대상 PR만 생성** — PR 직접 머지·main 직접 푸시는 금지, 사람이 검토 후 머지

GitHub 연동은 `/web-setup`이 실패해서(CLI 미인증) GitHub App 설치 링크로 대체 연동. 루틴 생성 시 사용자의 Gmail/Google Drive MCP 커넥터가 기본으로 자동 첨부됐는데, 이 루틴엔 전혀 불필요한 접근 권한이라 최소권한 원칙에 따라 제거 — `Claude_Code_Remote`만 남김.

스케줄은 처음엔 매일 오후 3시(MDT)로 만들었다가("어느 요일에 패치할지 모르니 매일 도는 게 안전"), "매일이면 업데이트 없는 날도 사이트 확인+문서 읽기 비용이 매번 든다, 토큰 낭비 아니냐"는 지적을 받아 **주 1회, 금요일 오후 3시(MDT) = 한국시간 토요일 오전 6시**로 변경(`cron_expression: "0 21 * * 5"`). 확인은 `https://claude.ai/code/routines/trig_01Wf6e7JvS3biMsG2Me2q2Cn` 또는 GitHub의 `auto/patch-check-*` 브랜치 PR에서.

### 메랜/플래닛 운영 구조 — 통합 유지로 결정

큐브 시뮬레이터의 "원하는 옵션 기댓값" 계산기를 롤 시뮬레이터에서 분리할지 논의하다가, 더 근본적으로 메랜과 플래닛을 아예 별도 사이트로 운영하는 게 나을지도 논의. 결론: **지금처럼 한 사이트에서 같이 운영하는 게 맞다**고 확정 — 별도 분리 없음, 코드 변경 없는 결정만. 큐브 기댓값 계산기 분리 여부는 별도 주제로 미뤄둠(아직 결론 없음).

### 플래닛 드랍테이블 아이콘 재제보 — 메랜/플래닛 데이터 파이프라인 동기화 누락

사용자가 플래닛 드랍테이블에서 "웃는 가면", "녹색 옷자락" 등 아이콘 없는 아이템을 재제보. 둘 다 오늘 아까 메랜 쪽에서 이미 `item-name-overrides.json`으로 고쳤던 바로 그 아이템들이라 이상해서 확인해보니, **새 버그가 아니라 동기화 누락**이었음 — `scripts/build-planet-data.mjs`가 `data/drop-index.json`(메랜)을 입력으로 받아 플래닛 배율/오버라이드를 얹어 `data/planet/drop-index.json`을 만드는 구조인데, 아까 메랜 drop-index.json만 재생성(`npm run build:drop-index`)하고 플래닛 쪽(`npm run build:planet-data`)은 재생성을 안 해서 플래닛 데이터가 여전히 옛날(synthetic 110개짜리) 스냅샷을 쓰고 있었음.

`npm run build:planet-data` 재실행으로 해결 — 플래닛도 synthetic 56개로 줄어듦(메랜과 정확히 같은 목록, "그륜"/"녹견량포" 2개만 여전히 미해결). 무관한 아이템 2600여 개·몬스터 653종 전부 그대로인 것 diff로 재확인.

**후속 메모**: 메랜/플래닛 데이터가 완전히 독립된 두 파이프라인이 아니라 플래닛이 메랜을 입력으로 삼는 단방향 의존 관계라, 앞으로 메랜 drop-index/monsters 등을 재생성하는 작업을 하면 **플래닛도 반드시 같이 재생성**해야 함 — 안 그러면 이번처럼 한쪽만 최신화되고 다른 쪽은 조용히 뒤처짐. `data.md`에 이 의존 관계는 이미 문서화돼 있었는데(빌드 스크립트 섹션) 실제 작업 시 놓쳤던 케이스.

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run` 통과(데이터만 바뀐 변경이라 코드 검증은 원래 통과하던 것 재확인 수준).

**재발 방지(같은 날 후속 조치)**: 위 "후속 메모"가 문서만으로는 또 놓칠 수 있다는 코드 리뷰 피드백을 받아, `package.json`의 `build:drop-index`/`build:drop-table-from-dropchance`/`build:item-detail-by`/`fetch:item-detail-by` 스크립트 끝에 `&& npm run build:planet-data`를 체이닝 — 메랜 쪽 소스(drop-index/item-detail-by)를 재생성하는 어떤 경로로 들어와도 플래닛이 자동으로 같이 재생성되어 사람이 기억해야 하는 단계가 아예 사라짐. 부수적으로 `build-planet-data.mjs`의 `drop-index`/`item-detail-by`/`cube-index` 출력이 실제 내용 변경 없이 `generatedAt`만 바뀌는 경우 파일을 건드리지 않도록 `writeJsonIfChanged`를 추가(리뷰에서 지적된 timestamp-only diff 노이즈 제거).

---

### QuestBoard 대용량 JSON 전송 최적화 — 버그 하나 발견하고 고침

DropTable에 이어 QuestBoard(퀘스트 페이지)도 같은 문제(quests.json 외에 monsters/monster-spawns/drop-index/item-detail-by/npc-locations 등 6개 JSON을 `"use client"` 컴포넌트에 그대로 props로 전달)가 있어서 이어서 처리.

**DropTable과 접근이 다름**: DropTable은 "검색 → 하나 선택 → 그 하나의 상세만 필요"라 온디맨드 API fetch가 맞았는데, QuestBoard는 703개 퀘스트를 통째로 검색/필터링하는 리스트 UI라 그 방식이 안 맞음. 대신 "퀘스트 데이터가 실제로 참조하는 아이템/NPC/몬스터가 몇 개나 되는지" 먼저 세어봄 — 보상템 393종(전체 아이템 카탈로그 2700여 종 중), 퀘스트 NPC 233종(전체 826종 중), 퀘스트가 참조하는 몬스터 447종(전체 689종 중) 정도로 원래 전체 카탈로그 대비 훨씬 작은 부분집합이었음. 그래서 온디맨드 fetch 대신 "서버(page.tsx)에서 이 부분집합만 미리 골라 훨씬 작은 버전을 만들어 한 번에 전달"하는 방식을 택함 — QuestBoard.tsx 내부 로직은 전혀 안 건드리고 page.tsx의 데이터 조립 부분만 손봄.

**버그 하나 발견**: 처음엔 "아이템"이라고 하면 당연히 보상 아이템(`quest.rewards.items`)이겠거니 하고 그 393종만 기준으로 슬림화했는데, 실제로 화면에서 "드랍 몬스터" 정보를 보여주는 `itemDropMonstersByItemId`는 보상이 아니라 **퀘스트 완료 시 제출해야 하는 수집 아이템**(`quest.requirements.complete.items`, 546종 — 보상 목록과는 겹치지 않는 별도 집합)에 쓰이는 거였음. `QuestBoard.tsx`를 다시 읽다가 `itemDropMonstersByItemId.get(item.id)`가 "조건" 컬럼(`requiredItems = quest.requirements.complete?.items`) 안에서만 호출되는 걸 발견하고 알아챔.

**검증 과정**: 스크린샷으로는 판단하기 애매해서(브라우저 확장이 이 세션 내내 불안정했음), DOM에서 `<p>드랍 몬스터</p>` 라벨 개수를 세는 방식으로 확인. 고치기 전엔 첫 20개 퀘스트 어디서도 0개(수집 아이템 393종이 아니라 잘못된 조합을 보고 있었으니 당연), Node 스크립트로 "실제로는 몇 개가 나와야 정상인지" 미리 계산해보니 7개가 나와야 함 — 고친 뒤 브라우저에서 정확히 7개로 일치하는 것 확인. 이 김에 `.next` 캐시를 지웠다가 기존에 떠있던 dev 서버(포트 3000)가 죽는 걸 발견해서 깨끗한 서버(포트 3001)로 재기동해 재검증 — 스테일 캐시 때문일 가능성도 배제.

**결과**: `/quests` 3.38MB→1.0MB(raw), gzip 334KB→124KB (약 3.3배 감소). 버서크 재검증 때처럼, "직접 확인 안 하고 넘어가면 조용히 깨진 기능을 그대로 배포할 뻔했다"는 케이스 — 대용량 데이터 최적화처럼 여러 파일 데이터를 재조합하는 작업은 payload 크기만 보고 끝내지 말고 실제 화면 기능까지 항상 확인이 필요함.

**코드 리뷰 후속 반영**: `rewardItemIds`라는 변수명이 실제로는 보상템+수집템을 다 담고 있어서 이름과 역할이 안 맞는다는 지적 → `referencedItemIds`로 개명. page.tsx에 그대로 들어있던 슬림 데이터 조립 로직(50줄 이상)도 `src/lib/quest-board-data.ts`(`buildSlimQuestBoardData`)로 추출 — DropTable 때 만든 `drop-table-lookup.ts`와 동일하게 "데이터 없는 순수 함수 + page.tsx는 JSON import와 호출만" 패턴으로 통일. 프로덕션 빌드 정적 HTML(`.next/server/app/quests.html`, 944KB)에서 "드랍 몬스터" 라벨 8개 확인해서 리팩토링 후에도 동일하게 동작하는 것 재검증(dev 서버가 세션 내내 불안정해서 이번엔 빌드 산출물 직접 검사로 확인).

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`, `npm run build` 통과.

---

### SEO 점검 + 드랍테이블 대용량 JSON 전송 문제 실제 수정

사용자가 "플래닛을 같이 병행하면서 최적화가 필요할 수도 있고, SEO도 점검해달라"고 요청.

**SEO 점검**: 처음엔 `/planet/drop-table`에 메타데이터가 없다고 판단했는데, `page.tsx`만 grep하고 같은 폴더의 `layout.tsx`(이 프로젝트가 메타데이터를 두는 실제 위치)를 놓친 오판이었음 — 재확인 결과 플래닛 페이지 5개 전부 title/description/canonical/OG/키워드 정상. 실제로 발견한 문제: `/admin/*`·`/ui/demo`가 robots.txt에서 크롤링 허용 상태(disallow 추가), `/feedback` 페이지 메타데이터 누락(layout.tsx 추가). OG 공유 이미지가 파비콘 하나로 통일된 것도 확인했지만 이미지 제작이 필요해 TODO로만 남김.

**대용량 JSON 문제 — 이번엔 진짜로 고침**: `data.md`에 "대용량 JSON은 API Route로 필터링 후 전달"이라는 규칙이 있고 TODO엔 "DropTable/QuestBoard 서버 props로 이전 완료"라고 기록돼 있었는데, 실측해보니 `/drop-table`·`/planet/drop-table` 페이지가 여전히 2.4MB(raw)를 클라이언트로 보내고 있었음 — "서버 컴포넌트에서 import"는 했지만 그 데이터를 그대로 `"use client"` DropTable 컴포넌트에 props로 넘기고 있어서, RSC 직렬화 과정에서 결국 클라이언트로 그대로 전송되고 있었던 것. 예전 기록이 "import 위치"만 보고 "완료"로 잘못 표시했던 것으로 보임.

**구현**: `items` 배열은 검색/아이콘 판별에 실제로 쓰는 필드(id/name/typeInfo.overallCategory·category/meta.synthetic/meta.equip.reqLevel)만 남기고 서버(page.tsx)에서 슬림화(0.82MB→0.18MB) — 나머지 필드(장비 스탯, 상점가, 캐시 여부 등)는 애초에 컴포넌트가 읽지도 않던 것들. 제일 무거운 `dropsByMonsterId`/`monstersByItemId`(합쳐서 ~1MB)는 초기 props에서 완전히 빼고, 몬스터/아이템을 실제로 선택했을 때만 새 API route로 온디맨드 fetch하도록 변경. 기존에 있던 "로컬 데이터 없으면 몬스터북 API로 폴백"하는 클라이언트 로직(`ensureFallbackDrops`)도 그대로 서버사이드로 옮겨서 하나의 라우트가 로컬/외부 소스를 알아서 판단하도록 통합 — 클라이언트 코드가 오히려 단순해짐. 검색 자체는 여전히 클라이언트 즉시 반응(네트워크 왕복 없음), 선택 시에만 짧은 로딩 문구.

**결과**: `/drop-table` 2.37MB→648KB(gzip 228KB→65KB), `/planet/drop-table` 2.4MB→679KB(gzip 228KB→72KB) — 약 3.5배 감소. QuestBoard(퀘스트 페이지, JSON 6개 겹쳐 전달 중, 1517줄로 DropTable보다 더 복잡)는 이번 범위에서 미해결로 남김.

**코드 리뷰(Codex) 반영 4건**:
1. 데이터 출처 배지가 실제 소스 문자열(`"dropchance-html+..."`)과 안 맞는 조건(`startsWith("drops-parsed")`) 때문에 항상 "MonsterBook reward"로 잘못 표시되던 버그 — 조건을 뒤집어 실제로 몬스터북 전용일 때만 그 라벨이 뜨도록 수정.
2. 신설한 API route 2개(`/api/drop-table/monster`, `/item`)가 edge 런타임에서 메랜+플래닛 데이터를 한 함수에 전부 번들하고 있었음(클라이언트 페이로드는 줄였지만 API 함수 번들/콜드스타트 비용으로 옮겨간 셈) — 서버별로 라우트 파일을 분리(`monster/mapleland`, `monster/planet`, `item/mapleland`, `item/planet`)하고 공통 로직은 `src/lib/drop-table-lookup.ts`에 데이터 없는 순수 함수로 추출. `npm run build`로 4개 라우트가 독립 함수로 분리된 것 확인.
3. 온디맨드 fetch 실패 시 빈 배열로 캐싱해서 재시도를 막던 문제 — 실패는 캐시하지 않고 별도 에러 상태로 분리, "다시 시도" 버튼 추가.
4. 타입상 항상 존재가 보장된 `monster?.mobCode`의 불필요한 옵셔널 체이닝 제거.

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`, `npm run build` 전부 통과. API 라우트는 curl로 메랜/플래닛 양쪽 다 확률 수치까지 재확인(플래닛이 정확히 4배), 브라우저로 검색→몬스터 선택→아이템 역방향 조회까지 실측(리팩토링 전후 결과 완전히 동일).

---

### 디자인 리뉴얼(색상), 드랍테이블 아이콘 누락 수정, 한방컷 계산기 Codex 리뷰 반영

**디자인 리뉴얼**: 어제 TODO에 "내일 색깔 위주로 전체 레이아웃 손보자"고 적어둔 항목 처리. 처음엔 사용자가 "현재 마젠타/버건디 강조색 유지"라고 했는데, 실제 화면을 열어보니 강조색이 시안/블루(`#38bdf8`, 플래닛은 앰버)였음 — 화면 확인 결과를 보여주고 재확인하니 "이번 기회에 마젠타/버건디로 교체"로 방향 전환. `--brand-accent` 계열 CSS 변수를 `#be123c`(버건디)로 바꾸고, `.retro-panel-tone-blue`(플래닛 전용으로만 있던 브랜드색 오버라이드)를 기본 규칙 자체에 병합해서 플래닛 전용 중복 블록 제거, 로그인 버튼·홈페이지 뱃지·사이드바 활성 카드 등 하드코딩된 시안 값도 함께 교체. 플래닛(앰버)은 `[data-server="planet"]` 분리 구조 그대로 둬서 두 서버 시각 구분 유지.

**드랍테이블 아이콘 누락**: 사용자가 드랍 테이블에서 아이콘 없는 아이템을 제보. 원인 조사 결과 드랍표 스크래핑 시 이름이 실제 아이템 DB와 안 맞으면 "가상 아이템"(synthetic, 음수 ID)으로 처리되는데, 마스터리북/카드/주문서류는 대표 아이콘으로 얼버무려지지만 나머지(장비·재료·펫템)는 아예 아이콘이 없었음. `data/drop-index.json`의 synthetic 아이템 110개 중 56개가 진짜 미해결 — maplestory.io 검색 API(`KMS/389/item?searchFor=`)로 정확히 일치하는 이름을 찾아 54개 해결(오타 3개도 같이 발견: "촉진게"→"촉진제", "얼음조각"에 "의" 누락, "포숀"→"포션"). `scripts/build-drop-index.mjs`의 `resolveDropItemId`가 기존에 `mapledbItemIdsByName`(레거시 mapledb.io 장비 데이터)만 참고하던 걸 확장해서, 새로 만든 수기 소스 `scripts/sources/item-name-overrides.json`을 우선 참고하도록 연결. `npm run build:drop-index` 재실행 후 old/new 아이템 셋 diff로 무관한 기존 아이템 전부 그대로인 것 확인(음수 id 54개 제거, 신규 39개 추가 — 15개는 이미 다른 몬스터 드랍으로 존재하던 실아이템이라 중복 제거된 것). 남은 2개("그륜", "녹견량포", 둘 다 G.팬텀워치 드랍)는 스크래핑 원본 이름 자체가 깨져 있어 실제 아이템 특정 불가 — 오매칭보다 미표시가 낫다고 판단해 그대로 둠.

**한방컷 계산기 — Codex 코드 리뷰 반영**: 이 세션에서 새로 만든 플래닛 스킬 기능에 대해 별도 Codex 리뷰를 받아 2건 수정.
1. **스나이핑 고정 데미지가 배율을 여전히 타던 버그(High)**: `specialBaseDamage`로 min/max를 199999에 고정해뒀지만, 그 뒤 `finalDamageMultiplier`(크리티컬샷/샤프아이즈/골든이글 등)가 그대로 곱해지고 있어서 "스탯/레벨 무관 고정 데미지"라는 안내 문구와 달리 199999를 넘길 수 있었음. 스나이핑 선택 시 `finalDamageMultiplier` 전체를 1로 고정하도록 수정 — 크리티컬샷 30 설정해도 199,999 그대로인 것 재확인.
2. **돌진이 다른 스킬에 잘못 곱해지던 문제(Medium)**: 앞선 구현에서 돌진을 "히어로 돌진 레벨" 보너스 슬라이더 방식(브랜디쉬 등 실제 사용 스킬에 곱연산으로 얹는 방식)으로 넣었는데, 리뷰에서 "돌진은 패시브 버프가 아니라 독립된 공격 스킬이라 다른 스킬에 최대 1.3배 곱해지는 게 게임 메커니즘과 안 맞다"는 지적을 받아 재설계. `damageMappingActive`에서 빼서 `damageMapping`으로 옮기고(count=1) 전사 3직업 `mainSkillMapping`에 독립 선택형 스킬로 추가, 기존 "돌진 레벨" 보너스 슬라이더(히어로 원래 있던 것 포함)와 `rushBonus` 상태·스냅샷 코드 전부 제거. 레벨1→30(72%→130%) 스케일링 정확히 확인(2,534→4,576, 비율 1.806배 = 130/72 일치).
3. **문서화(Low)**: `data.md`에 `scripts/sources/item-name-overrides.json` 설명 추가 — 손수 관리 소스, `build-drop-index.mjs`가 읽음, 새 미매칭 아이템 발견 시 추가하는 방법까지 기록.

**검증**: `npx tsc --noEmit`, `npx eslint src/ scripts/`, `npx vitest run`(30개 통과) 전부 통과.

---

### 한방컷 계산기 — 플래닛 누락 스킬 6종 구현 (돌진/생츄어리/닌자 스톰/드래곤 펄스/스나이핑) + 버서크 오판 정정

어제(2026-07-08) TODO에 "누락"으로 기록해둔 11개 스킬 중, 소환수 6종(엘퀴네스/이프리트/바하뮤트/피닉스/프리져/서포트 옥토퍼스)과 닌자 앰부쉬는 이 계산기의 "1회 사용=N타" 모델과 구조적으로 안 맞아서(지속시간 동안 알아서 도는 소환수·도트뎀 스킬) 사용자 확인 후 이번 범위에서 제외. 나머지 6종만 구현.

**리서치**: 타격 횟수(count) 등 부족한 데이터를 채우려고 서브에이전트로 namu.wiki "RED 이전 스킬" 문서를 재조사(claude-in-chrome, WebFetch는 namu.wiki Cloudflare 403으로 막힘). 결과: 돌진은 히어로/다크나이트 문서와 팔라딘 문서 수치가 서로 달라서(Lv15/30: 100%/130% vs 110%/140%) 사용자에게 확인받아 히어로/다크나이트 값으로 확정. 드래곤 펄스/스나이핑/피닉스/프리져/호밍/서포트 옥토퍼스는 애초에 1~30 레벨 테이블 자체가 존재하지 않는 마스터 전용 고정 효과 스킬로 확인(조사 실패가 아니라 정상).

**중요 발견 — 버서크는 원래 안 빠져 있었음**: 구현 착수 전 코드를 다시 훑어보니 `damageMappingActive["버서크"]`에 레벨 0~30 실데이터가 이미 있고, 다크나이트 패널에 슬라이더까지 이미 정상 동작 중이었음. 어제 조사가 `mainSkillMapping.json`(선택형 주력기 목록)만 grep해서 "버서크"가 안 보이니 누락으로 판단했는데, 실제로는 패시브 보너스 전용의 별도 메커니즘(`damageMappingActive` + 잡별 전용 슬라이더)으로 이미 구현되어 있었던 것 — 이 계산기엔 "선택형 주력 스킬"과 "레벨 입력하면 자동 % 환산되는 패시브 슬라이더" 두 가지 트랙이 있다는 걸 이번에 명확히 파악. 같은 이유로 히어로의 "돌진 레벨" 슬라이더(`rushBonus`)도 이미 존재했었는데, 데이터가 없어서 `1+레벨/100`이라는 조잡한 자리표시자 근사식만 쓰고 있었음.

**구현**:
1. **돌진**: 기존 히어로 전용 `rushBonus` 슬라이더의 계산식을 조잡한 근사치에서 `damageMappingActive["돌진"]` 실데이터 조회로 교체(레벨1 72%~레벨30 130%, 버서크와 동일한 패턴). 팔라딘·다크나이트 패널에도 동일 슬라이더 추가해 3개 전사 계열 공용화(`rushEffect` 훅, `isHeroJob || isPagePaladinJob || isSpearmanJob` 게이트).
2. **생츄어리/닌자 스톰/드래곤 펄스/스나이핑**: `mainSkillMapping`에 선택 가능한 스킬로 추가. 앵커값(Lv1/15/30)만 있는 생츄어리·닌자 스톰은 기존 브랜디쉬 방식(1~10/11~30 구간보간, `v10 = v15 - 5*step2`로 경계값 역산)으로 전체 1~30 테이블 생성. 드래곤 펄스는 마스터 전용 고정 100%라 전 레벨 동일값으로 등록.
3. **스나이핑 특수 메커니즘**: `specialBaseDamage`에 신규 분기 추가 — 스탯 계산을 아예 건너뛰고 min/max를 199999로 고정, `ignoreDefense: true`로 방어력 감산도 스킵. 기존 힐/베놈 등 특수 스킬과 동일한 패턴이라 엔진 자체는 변경 없음.
4. **생츄어리/스나이핑 안내 문구**: 생츄어리는 "대상 HP 1 고정, 즉사 아님"을, 스나이핑은 "스탯/레벨 무관 고정 데미지"를 스킬 패널에 문구로 표시(스나이핑 HP1 고정 룰과 즉사 판정을 실제 계산 로직에 반영하진 않음 — 이 계산기의 "몇 방컷" 개념 자체와 안 맞아서 정보 제공용 안내로만 처리).

**부수 발견 — 데이터 파이프라인 드리프트**: 스킬 데이터를 추가하려고 `data/raw/skills.raw.ts`를 열어보니, 커밋된 `data/skills/{mainSkillMapping,range20,range30,damageMapping}.json` 4개 파일이 시그너스 나이츠 5개 직업(107개 스킬, 소울마스터/플레임위자드/윈드브레이커/나이트워커/스트라이커) 데이터를 담고 있는데 정작 소스 `skills.raw.ts`엔 그 내용이 전혀 없었음 — `data.md`의 "산출물 JSON 직접 편집 금지, 소스 고치고 재빌드" 골든룰이 예전에 깨져 있었던 상태(시그너스 데이터를 산출물에 직접 넣고 소스는 안 고친 것으로 추정). 그대로 `node scripts/normalize-skills.mjs`를 돌렸으면 시그너스 데이터가 통째로 날아갈 뻔했음 — 재생성 전에 커밋된 JSON과 소스를 diff해서 시그너스분만 추출해 `skills.raw.ts`에 역이식한 뒤 재생성. 재생성 후 diff로 데이터 유실 없음 재확인.

**작업 중 실수 하나**: 새 스킬 데이터를 삽입할 때 `damageMapping`의 실제 닫는 위치를 착각해서(중간에 `sharpEyesMapping`/`criticalThrowMapping` 블록이 끼어있는 걸 놓침) 처음엔 `criticalThrowMapping` 안에 잘못 삽입했었음 — brace-depth를 직접 카운트하는 스크립트로 진짜 닫는 위치를 찾아서 발견 및 수정. 이후 `require()`로 실제 파싱 결과를 매번 검증하는 방식으로 전환.

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(기존 30개 그대로 통과) 전부 통과. 개발 서버 띄워서 claude-in-chrome으로 직접 확인 — 팔라딘 생츄어리 선택 시 안내 문구 노출, 신궁 스나이핑 선택 시 한방컷 공격력이 스탯 무관 199,999 고정으로 나오는 것, 나이트로드 닌자 스톰이 레벨 1→30에서 717→1,365로 42%/80% 비율 그대로 스케일링되는 것, 히어로 브랜디쉬 기준으로 돌진 레벨 0→30 설정 시 9,504→12,355(정확히 1.3배)로 오르는 것까지 전부 실측 확인.

---

### 큐브 시뮬레이터 — 장비 종류 필터를 실제 롤 시뮬레이터에도 적용

코드 리뷰로 발견: "장비 종류" 선택 필터가 "원하는 옵션 기댓값" 계산기(`estimateOptionOdds`/`estimateCombinedOptionOdds`용 옵션 목록)에만 적용되고, 정작 큐브를 클릭해서 굴리는 실제 롤(`rollCubeResult`)과 그 확률 계산의 등급 내 가중치 합산(`eligiblePoolAtGrade`)은 필터 없이 전체 풀을 그대로 썼음 — 다른 부위 전용 스킬 옵션(예: 신발 선택 중인데 "쓸만한 미스틱 도어"가 뽑힘)이 실제로 결과에 나올 수 있었고, 가중치 총합 계산에도 그 옵션들이 끼어들어 다른 옵션들의 확률이 미세하게 부정확했음.

- "장비 종류" 선택 UI를 롤/기댓값 계산기 양쪽에서 공유하도록 상단 공용 위치로 이동.
- `suspiciousPoolForEquip`/`miraclePoolForEquip`(`isOptionAllowedForEquip`로 미리 필터링한 풀)을 롤(`rollCubeResult`)과 확률 계산 양쪽에 동일하게 전달하도록 수정 — 필터링 기준이 한 곳(컴포넌트)에만 있고 엔진 함수들은 이미 필터링된 풀을 받는 구조 유지.
- 검증: Node로 2만 회 가중치 추출 시뮬레이션을 돌려 신발 선택 시 "쓸만한 컴뱃 오더스"만 나오고 다른 부위 스킬(미스틱 도어/하이퍼 바디/샤프 아이즈/어드밴스드 블레스/윈드 부스터)은 전혀 안 나오는 것 확인. `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run` 통과.

### 큐브 시뮬레이터 — 같은 옵션 중복 선택 시 잘못 계산되던 버그 수정

코드 리뷰로 발견: `estimateCombinedOptionOdds`가 선택된 옵션 id들을 `new Set(targetEntryIds)`로 그냥 중복 제거해서 넘기고 있었음 — 사용자가 같은 옵션을 3슬롯 중 2개에 고르면(예: STR을 두 줄에) "그 옵션이 최소 1줄 나올 확률"로 잘못 계산되고 있었고, 실제로는 "최소 2줄 나올 확률"을 계산해야 정확함.

- `requiredCountById`(`Map<id, 선택된 횟수>`)로 같은 id가 몇 번 선택됐는지 추적하도록 전면 재작성. 줄이 최대 3개·목표도 최대 3개라 계산량이 작으므로, "N줄 이상 동시 만족" 확률을 조합을 직접 열거(brute-force)하는 방식으로 정확하게 계산.
- UI: 선택된 옵션 배지에 "(N줄)" 라벨 추가, 같은 id가 여러 슬롯에 선택돼도 React key 중복이 안 나도록 distinct id 기준으로 렌더링.
- 부수 개선: 캐시 비용 표시에 "(추정 환율 기준)"과 메포 병기 추가 — 원화 단독 표시가 실제 결제 금액처럼 보인다는 지적 반영.
- 신규 vitest 1개 추가(총 30개). `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run` 통과.

### 큐브 시뮬레이터 — 비상식적으로 큰 기댓값 수정, 등급업/옵션재설정 분리, 장비 종류 필터, UI 정리

사용자가 "약 118,311,108,362,038개" 같은 비상식적인 숫자를 지적하며, 실제 다른 사이트(maplanet.net)의 큐브 계산기가
쓰는 방식을 참고해도 좋다고 함. maplanet.net/cube-calc를 직접 열어 UI 구조를 확인:

- **목표 타입: "등급 업" / "옵션 띄우기"** 라디오 버튼으로 분리되어 있음 — "등급 업"은 옵션과 무관하게 순수 등급 상승
  기댓값만, "옵션 띄우기"는 "잠재능력 등급"(현재 등급, 드롭다운엔 레어/에픽/유니크만 있고 일반·레전드리 없음)을
  고정해두고 그 등급 안에서의 옵션 재설정 비용만 계산함. 즉 "등급까지 도달하는 비용"과 "그 등급에서 옵션 맞추는
  비용"을 아예 분리해서 보여줌 — 우리가 기존에 "옵션 기댓값" 하나로 등급 상승 확률까지 다 섞어서 계산하다 보니
  레전드리 등급 다중 옵션 조합에서 극단적으로 큰 숫자(1억 단위 이상)가 나온 이유였음.
- **"장비 종류" 드롭다운**(무기/한벌옷/하의/상의/얼굴장식/눈장식/귀고리/펜던트/벨트/반지/모자/장갑/신발/방패/망토)이
  있고, 안내 문구에 "옵션 목록은 고른 장비에서 실제로 나올 수 있는 옵션만 보여요. 예: 장갑→쓸만한 샤프
  아이즈(쓸샾), 상의·하의·전신 갑옷→쓸만한 하이퍼 바디(쓸바디), 모자→쓸만한 미스틱 도어, 신발→쓸만한 헤이스트"
  라고 명시 — 스킬 재사용("쓸만한 OO 스킬 사용 가능") 옵션이 장비 부위별로 다르다는 걸 확인.

이를 토대로 반영:

1. **`estimateGradeUpExpectedUses(fromGrade, toGrade=4)`** (`cubeSimulator.ts`) 신규 추가 — 옵션과 무관하게
   순수 등급 상승 확률(`GRADE_UP_CHANCE`)만으로 "이미 fromGrade에 도달한 아이템을 레전드리까지 올리는 데 평균
   몇 개 필요한지" 계산(각 단계 확률의 역수 합). "등급업 기댓값" 전용 패널로 분리해서 항상 노출.
2. **`estimateOptionOdds`/`estimateCombinedOptionOdds`에 `fixedOverallGrade?` 파라미터 추가** — 넘기면 등급
   상승 카스케이드 전체를 적분하지 않고 그 등급 하나로 고정해서 계산(`gradeIterations` 헬퍼로 통합). "이미 그
   등급에 도달했다고 가정" 체크박스로 노출 — 껐을 때는 기존처럼 등급업 확률까지 섞인 "처음부터" 비용(레전드리
   3옵션 조합이면 실제로 수천만 개 단위가 나올 수 있고, 이건 버그가 아니라 정직한 계산 결과임을 토글로 명시),
   켰을 때는 옵션 재설정만 남아 훨씬 현실적인 숫자(검증: 모자용 레전드리 스킬 옵션 하나 기준 23,162,081개 →
   토글 On 시 75.0개).
3. **장비 종류 필터** — `cube-data.js`에서 7개 스킬 옵션("쓸만한 헤이스트/미스틱 도어/샤프 아이즈/하이퍼
   바디/컴뱃 오더스/어드밴스드 블레스/윈드 부스터")의 `optionType`을 직접 확인한 결과, 등급만 다르고
   optionType이 같은 짝이 있음을 발견(51=미스틱 도어·어드밴스드 블레스, 53=하이퍼 바디, 54=샤프
   아이즈·윈드 부스터, 55=헤이스트·컴뱃 오더스) — 즉 "같은 부위의, 등급별로 다른 이름의 스킬 옵션"이었음.
   웹 검색으로 51=모자, 53=상의/하의/전신 갑옷, 54=장갑, 55=신발임을 교차 확인(더쿠 검색 결과: 어드밴스드
   블레스=모자, 윈드 부스터=장갑, 컴뱃 오더스=신발, 레전드리 등급·120레벨 이상 한정 — maplanet.net의 4개
   예시와 optionType 매칭 결과가 서로 일치). 이 4개 optionType만 선택한 장비 종류에 맞게 필터링하고, 나머지
   스탯/공격력류 옵션은 부위 무관하게 항상 노출(스탯 옵션이 부위별로 다르다는 근거는 못 찾아서 확대 적용 안 함).
4. **UI 정리** — 등급→옵션 드롭다운 쌍을 슬롯별로 테두리 박스에 묶어서 시각적으로 구분되게 정리, "등급업
   기댓값"과 "원하는 옵션 기댓값"을 별도 카드로 분리.

검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(신규 5개 포함 29 passed) 통과. 브라우저에서 장비
종류를 "모자"로 선택 시 스킬 옵션 목록이 실제로 필터링되는 것, 레벨 120 이상에서만 "쓸만한 어드밴스드 블레스"가
노출되는 것(reqLevel 120), 토글 on/off에 따라 23,162,081개 ↔ 75.0개로 바뀌는 것 확인.

---

## 2026-07-08

### 플래닛 데이터 정확도 재검증

**556종 카탈로그 매칭 몬스터 스탯 차이 로컬 분석**: `scripts/sources/planet/monster-catalog-data.js`(KMS pre-BigBang 데이터마이닝)와 `data/monsters.json`을 직접 diff하는 임시 스크립트로 분석. 556종 중 346종은 EXP가 정확히 4배 배율과 일치(문제없음). 나머지 중 50종은 **레벨 자체가 다름** — 몇 배씩 벌어지는 HP/레벨 차이 발견.

**중요 발견 — 예티/킹슬라임 계열이 포스트 빅뱅 스탯을 쓰고 있었을 가능성**: 예티(mobCode 6300001) 기존 데이터는 Lv117·HP406890인데, namu.wiki "메이플스토리/빅뱅 이전" 문서 검색 결과 실제 빅뱅 이전 예티는 **Lv65·HP11000**(엘나스) — catalog 값과 정확히 일치. 킹슬라임도 동일 패턴(기존 Lv23 vs 빅뱅 이전 정설 Lv40, catalog와 일치). 즉 `data/monsters.json`(메랜 원본, Zone A) 자체가 빅뱅 이후 리메이크된 스탯을 잘못 가져다 쓴 것으로 보이는 버그 — 플래닛뿐 아니라 메랜 본가에도 영향 있을 수 있어 별도 확인 필요(오늘은 플래닛 데이터에만 오버라이드 적용, 메랜 원본은 손대지 않음).

레벨 불일치 50종 전체를 `divergence-overrides.json`의 `monsterOverrides`에 catalog 값으로 일괄 반영(`scripts/build-planet-data.mjs` 재실행 후 `data/planet/monsters.json` 재생성). 예티·킹슬라임 2건만 namu.wiki로 개별 재검증했고 나머지는 동일 소스 신뢰도에 근거한 잠정 적용 — 주석에 명시.

**핑크빈 EXP는 미해결로 남김**: 웹 검색으로 확인한 표준 빅뱅 이전 값(EXP 6,290,000 — 메랜 원본과 정확히 일치)에 4배를 적용하면 25,160,000인데, 기존 오버라이드(조아요 소스) 값은 50,000,000(~7.95배), catalog 값은 200,000,000(~31.8배)로 세 값이 전부 불일치. 추가 검증 없이는 판단 불가라 조아요 값을 잠정 유지하고 주석에 세 후보값을 모두 기록.

**버그 발견 및 수정**: `applyMonsterOverrides`가 오버라이드 객체를 그대로 스프레드(`{...monster, ...override}`)해서 `_source`/`_comment` 같은 내부 주석 필드가 `data/planet/monsters.json` 출력에 그대로 leak되고 있었음(`appendNewMonsters`는 이미 `_`-prefix 필드를 걸러내고 있었는데 이 함수만 누락). `_`-prefix 필드 필터링 추가로 수정.

**시그너스 여제의 축복 스택 여부**: 웹 검색 결과 플래닛 고유 차이라는 근거를 찾지 못함 — 정령의 축복과 중첩 안 되고 더 높은 효과만 적용되는 표준 메이플스토리 규칙과 동일하게 작동하는 것으로 보임(디시 마이너 갤러리 질문글 1건 외 참고자료 없어 확정은 아님).

### 드랍 확률 샘플 검증 — 조아요(chowayo.com) 직접 대조

달팽이(100100)/파란 달팽이(100101) 두 몬스터의 드랍 아이템을 `chowayo.com/monsters/{mobCode}`와 전수 대조.

- **일반 아이템/소비품/광물**: 빨간 포션·초록사과·활전용 화살·석궁전용 화살·청동/가넷/강철/자수정의 원석 등 7~8개 항목 전부 정확히 일치 — 4배 배율 로직 자체는 정확하게 동작 확인.
- **문제 발견 — 주문서(장비 강화 스크롤) 드랍률 불일치**: 망토 마법방어력 주문서 60%·투구 방어력 주문서 10%(달팽이), 망토 체력 주문서 100%(파란 달팽이) 전부 chowayo에서 **동일하게 0.012%**로 표시되는데, 우리 데이터는 몬스터마다 다른 값(메랜 원본 base × 4 — 0.032%/0.024%/0.024%). 주문서는 몬스터별 개별 확률이 아니라 스크롤 자체의 고정/등급별 확률 공식을 따르는 것으로 추정 — 메랜 원본 스크래핑 단계에서부터 부정확했을 가능성. `data/drop-index.json`(메랜 원본, Zone D) 이슈라 플래닛 전용 오버라이드로는 해결 안 됨, 원본 스크래핑 로직 재검토 필요.
- **커버리지 갭 — 몬스터 카드 드랍 자체가 데이터에 없음**: chowayo엔 각 몬스터별 "OO 카드"(0.4%) 드랍이 있는데 `data/drop-index.json` 전체에 몬스터 카드 아이템이 하나도 없음(카드키/메모리카드 등 이름만 비슷한 무관 아이템 2개뿐). 드랍표 스크래핑 소스가 애초에 몬스터 카드 시스템을 다루지 않는 것으로 보임 — 정확도 버그라기보다 기능 커버리지 공백.

### 큐브 시뮬레이터 재정비

**옵션 수치 로직 수정**: `cube-data.js`의 모든 옵션이 정확히 `level["1"]~["20"]` 20개 브라켓을 갖는 걸 재확인. 기존 구현은 이 20단계를 "무작위 균등분포"로 뽑고 있었는데(페이지에도 그렇게 명시), 실제로는 옵션 값이 아이템 착용 레벨에 따라 결정되는 고정 구간(1~10 → 브라켓1, 11~20 → 브라켓2, ... 191~200 → 브라켓20)이라는 게 데이터 구조 자체(레벨 제한 필드 `reqLevel`과의 관계, 값이 브라켓 11 이후 고점(plateau)에서 멈추는 패턴)로 봤을 때 훨씬 타당함. `src/lib/calculators/cubeSimulator.ts`에 `levelBracket(itemLevel)` 함수 추가해 착용 레벨로 결정론적으로 값을 계산하도록 변경. 실제 서버의 정확한 구간 경계값은 확인 못 해서(공식 자료 없음) 페이지 안내 문구에 "약간 다를 수 있음" 명시는 유지.

**부수적으로 발견한 버그**: `scripts/build-planet-data.mjs`의 `applyMonsterOverrides`가 오버라이드 객체를 그대로 스프레드해서 `_source` 등 내부 주석 필드가 `data/planet/monsters.json`에 그대로 leak되고 있었음(위 "플래닛 데이터 정확도 재검증" 항목 참고) — `appendNewMonsters`처럼 `_`-prefix 필터링 추가.

**UI**: 결과 히스토리(`최근 결과` 패널)가 지금까지는 등급 구분 없는 회색 텍스트였는데, 메인 "결과" 패널과 동일한 등급별 색상(`gradeClassName`)을 적용하고 각 항목에 어떤 큐브(수상한/미라클)를 사용했는지 라벨 추가.

**단위 테스트 도입**: 프로젝트에 테스트 러너가 전혀 없었음(`src/lib/calculators/takenDamage/__tests__/sample.ts`는 assertion 없이 console.log만 하는 수동 확인용 스크립트) — `vitest` 설치(`vitest.config.ts`, `npm run test`), `src/lib/calculators/__tests__/cubeSimulator.test.ts`에 `levelBracket`/`rollCubeLine`/`rollCubeLines` 대상 12개 테스트 작성(브라켓 경계값, reqLevel 필터링, 결정론적 값 재현 등). `build-planet-data.mjs`는 내부 함수가 export되어 있지 않아 이번엔 테스트 대상에서 제외.

**검증**: `npx vitest run`(12 passed), `npx tsc --noEmit`, `npx eslint src/` 전부 통과. 개발 서버 띄워서 브라우저로 큐브 사용 → 결과/히스토리 렌더링, 착용 레벨 변경 시 수치 변화까지 직접 확인.

### 큐브 시뮬레이터 UI 전면 재설계 — 실제 아이템 이미지 + 이펙트/사운드

사용자 요청: 최대한 간단한 UI + 실제 큐브 이미지를 클릭하는 방식 + 사용 시 이펙트 + 사운드(가능하면 메이플 UI 느낌).

- **레이아웃 단순화**: 기존 좌(설정)/우(결과) 2단 그리드 → 착용 레벨 입력 → 큐브 2개(수상한/미라클) 아이콘 → 결과 영역이 위에서 아래로 흐르는 1단 레이아웃으로 교체. 큐브 종류 선택용 탭과 "사용하기" 버튼을 없애고, **큐브 아이콘 자체를 클릭하면 그 큐브를 바로 사용**하도록 통합(선택+실행이 한 동작).
- **실제 아이템 아이콘**: maplestory.io 공개 API에서 아이템 검색(`/api/KMS/389/item?searchFor=`)으로 "수상한 큐브"(itemId 2710000)와 "미라클 큐브"(itemId 5062000) 실제 아이콘을 찾아 적용. 기존 `getItemIconCandidateUrls`(여러 리전/버전 폴백) 패턴 재사용, `DropTable.tsx`의 아이콘 폴백 컴포넌트 패턴을 그대로 차용.
- **이펙트**: 클릭 시 큐브가 흔들리는 `cube-shake`(0.45s) → 골드/시안 방사형 플래시(`cube-flash`) → 결과가 살짝 떠오르며 나타나는 `cube-reveal` 순서로 연출. 키프레임은 `globals.css` 말미에 기존 `fadeUp`/`slimeFloat` 패턴과 동일한 방식으로 추가, `prefers-reduced-motion`에서는 전부 비활성화.
- **사운드**: 외부 음원 없이 Web Audio API로 직접 합성(`src/lib/audio/cubeSfx.ts`) — 클릭 즉시 낮은 "퉁" 타격음, 결과 공개 시 상승 아르페지오(도-미-솔-도) 차임. 저작권 있는 실제 게임 음원을 가져오는 대신 자체 합성으로 대체. 헤더에 🔊/🔇 음소거 토글 추가.
- **메이플 UI 느낌**: 기존 사이트의 레트로/글래스 테마(`--retro-border`, `--brand-accent` 등)를 그대로 따르되, 큐브를 장비창 슬롯처럼 보더 박스로 감싸 아이템 사용 UI에 가깝게 표현. 전체 테마를 픽셀아트로 새로 짜지는 않음(기존 디자인 시스템과의 일관성 우선).
- **검증**: 브라우저에서 직접 클릭 → 흔들림 → 플래시 → 결과 노출 확인, 콘솔에 코드 관련 에러 없음(브라우저 확장 프로그램발 hydration 경고 1건은 무관 — React 자체가 "브라우저 확장 프로그램이 DOM을 건드렸을 때 발생"이라고 명시).

### 큐브 시뮬레이터 — 등급 상승 연출 추가 + 가독성 개선

사용자 피드백: 미라클 큐브는 실제로 에픽 → 유니크 → 레전드리처럼 등급이 단계적으로 올라가는 연출이 있는데 그게 없었고, 클릭해도 임팩트가 부족했고, 결과 옵션 텍스트가 잘 안 보임.

- **등급 상승(climb) 연출**: 최종 결과(3줄 중 최고 등급)를 먼저 계산해두고, 큐브별 시작 등급(수상한 큐브=레어, 미라클 큐브=에픽 — 실제 서버 기준)부터 최종 등급까지 한 단계씩(550ms 간격) 큰 배지로 올라가며 보여줌. 각 단계마다 배지가 팝(스케일 바운스, `cube-grade-bump` 키프레임)하고 등급이 높아질수록 음높이가 올라가는 효과음(`playCubeGradeUpSound`) 재생 — 최종 결과의 최고 등급이 시작 등급보다 낮으면(가중치 데이터상 드물게 가능) 억지 연출 없이 바로 그 등급만 보여줌(거짓 서스펜스 방지).
- **가독성 개선**: 기존 3단계 색상 구분(파랑/초록/회색 계열)을 실제 게임 등급 색상 5단계로 교체(일반=회색, 레어=파랑, 에픽=보라, 유니크=골드, 레전드리=에메랄드), 등급 표시를 작은 회색 텍스트 → 진하게 채워진 pill 배지로 변경, 옵션 텍스트를 `text-sm` → `text-base font-bold`로 키움.
- **버그 수정**: `Date.now()`를 비동기 핸들러 안에서 직접 호출하다 `react-hooks/purity` eslint 규칙(impure function 경고)에 걸려 `useRef` 카운터로 교체.
- **검증**: `npx tsc --noEmit`, `npx eslint src/`(0 errors) 통과, 브라우저에서 미라클 큐브 클릭 → 등급 상승 배지 → 결과 노출까지 직접 확인.

### 큐브 시뮬레이터 — 등급 고정 모드 추가

사용자 피드백: 실제 서버는 반복 사용으로 등급이 오르는 "등급업" 과정이 있는데, 그거 말고 원하는 등급을 바로 골라서 그 등급의 옵션만 재설정(리롤)해보고 싶다는 요청. 다른 메이플 큐브 시뮬레이터(츄츄지지 등)를 참고해보니 "시작 등급 고정" 형태로 이미 흔한 기능임을 확인.

- `src/lib/calculators/cubeSimulator.ts`에 `filterPoolByGrade(pool, grade)` 추가 — 등급 고정 모드에서 해당 등급 옵션만 남겨서 뽑음. 유닛 테스트 2개 추가(총 14개).
- 컴포넌트에 "등급 고정" 체크박스 + 등급 5개(일반~레전드리) 선택 칩 추가. 켜져 있으면 등급 상승(climb) 연출 없이 흔들림 → 바로 결과 공개(고른 등급이 이미 확정이라 서스펜스 연출이 의미 없음).
- 안내 문구에 "등급 고정 모드는 실제 서버의 등급업 확률을 재현하지 않는다"는 점 명시 — 확률 시뮬레이션이 아니라 옵션 구성 미리보기 용도임을 분명히 함.
- 검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(14 passed) 전부 통과. 브라우저에서 레전드리 고정 → 미라클 큐브 사용 → 3줄 전부 레전드리로 즉시 공개되는 것 확인.

### 큐브 시뮬레이터 — 실제 등급 배치 로직 적용 (1번째=표기등급, 2·3번째=한단계 낮은 등급)

사용자 피드백: "레전드리는 유니크랑 레전드리가 나오고, 유니크는 에픽이랑 유니크가 나온다" — 실제 서버는 3줄이 전부 독립적으로 같은 등급 풀에서 뽑히는 게 아니라, 1번째 줄은 큐브의 "표기 등급" 그대로, 2·3번째 줄은 기본적으로 한 단계 낮은 등급이되 낮은 확률로 표기 등급 그대로 나오는("이탈") 구조. 기존 구현(등급 고정 모드 포함)은 3줄을 전부 같은 등급 풀에서 독립적으로 뽑고 있어서 이 부분이 틀려 있었음.

**리서치**: 넥슨 공식 공지("큐브의 잠재능력 재설정 로직과 세부 확률 공개", maplestory.nexon.com/News/Notice/Notice/133316)에서 이탈확률 확인 — 레드 큐브 2번째 10%/3번째 1%, 블랙 큐브 2번째 20%/3번째 5%. 메이플플래닛의 수상한 큐브/미라클 큐브에 대한 공식 수치는 못 찾아서, 수상한 큐브=레드 큐브급, 미라클 큐브=블랙 큐브급으로 대응해 근사 적용(잠정치, 코드에 출처 주석 명시).

**구현**: `src/lib/calculators/cubeSimulator.ts`를 3줄 독립 샘플링(`rollCubeLine`/`rollCubeLines`) 방식에서 `rollCubeResult(pool, itemLevel, cubeType, fixedOverallGrade?)` 단일 함수로 교체 —
1. `pickOverallGrade`: 등급별 가중치 합계로 큐브의 "표기 등급"을 먼저 결정(고정 등급 모드에서는 이 단계를 건너뛰고 사용자가 고른 등급을 그대로 사용)
2. 1번째 줄은 표기 등급에서, 2·3번째 줄은 이탈 확률에 따라 표기 등급 또는 한 단계 낮은 등급에서 각각 뽑음
3. `filterPoolByGrade`(직전 세션에서 추가했던 등급 고정용 헬퍼)는 이 변경으로 불필요해져 제거, 등급 고정 모드도 `rollCubeResult`에 `fixedOverallGrade`를 넘기는 방식으로 통합

기존 `rollCubeLine`/`rollCubeLines`/`filterPoolByGrade`와 그 테스트는 전부 제거하고 `rollCubeResult`/`pickOverallGrade` 기준으로 테스트 재작성(14개).

검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(14 passed) 통과. 브라우저에서 레전드리 고정 + 미라클 큐브 사용 → 레전드리/유니크/유니크로 나오는 것 확인.

### 큐브 시뮬레이터 — 등급 확률을 mapleplanet.co.kr 공식 페이지에서 직접 검증

사용자가 "레드/블랙 큐브 근사치 말고 실제 수치를 다른 사이트에서 찾아 적용해달라"고 요청. `mapleplanet.co.kr/Cube`를 WebFetch로 열어봤지만 403(봇 차단)이라, claude-in-chrome으로 실제 브라우저에서 직접 열어 JS로 상태를 조작하며 확인함.

**사이트 자체 버그 발견**: 페이지의 "수상한 큐브"/"미라클 큐브" 탭 버튼을 클릭해도 확률표 텍스트가 항상 "(수상한 큐브)"로 고정 표시되는 버그가 있음(`document.getElementById('miracleCubeLink').click()`으로 내부 상태 `window.activeCube`는 실제로 "miracle"로 바뀌는데 화면 텍스트만 안 바뀜). 수상한 큐브 → 미라클 큐브로 순서대로 두 번 클릭해야 정상적으로 갱신됨(진짜 상태 변경 이벤트가 필요했던 것으로 추정).

**확인된 사실(두 큐브 완전히 동일)**:
- 등급 상승: 레어→에픽 6.0000%, 에픽→유니크 1.8000% (수상한/미라클 동일 — 이전에 미라클을 블랙 큐브급 20%/5%로 다르게 넣었던 게 틀렸음, 삭제)
- 옵션 등급: 1번째 옵션은 표기 등급 100%, 2번째 옵션은 표기 등급 유지 10%(아니면 한 단계 하락), 3번째 옵션은 유지 1%(아니면 하락) — 기존에 넣었던 수상한 큐브 수치(10%/1%)와 정확히 일치, 미라클도 동일하게 통일
- **공식 확률표엔 레전드리 단계가 없음**(유니크까지만 표기). 사이트 전역 JS `window.gradeLabel`도 `{0:노멀,1:레어,2:에픽,3:유니크}` 4개뿐이고 페이지 전체에 "레전드리" 문자열 자체가 없음 — 그런데 `cube-data.js` 원본 풀엔 grade:4 옵션이 실제로 존재(수상한 56개/미라클 44개)

사용자에게 이 모순을 보고하고 처리 방법을 물어봄 → 레전드리 유지하기로 결정(실제 게임에서 레전드리를 봤다는 사용자 경험 신뢰). 처음엔 유니크→레전드리 확률을 앞 두 단계 감소 비율(1.8%/6%=0.3배)로 추정한 0.54%를 임시로 넣었으나, 사용자가 "다른 곳은 확인했냐"고 재차 확인함 — 실제로 다른 사이트/커뮤니티 정리글을 더 찾아봐도 메이플플래닛의 유니크→레전드리 수치는 어디에도 없었음. 다만 넥슨 공식 큐브 확률 가이드를 순서대로 대조하다가 **레어→에픽 6.0%/에픽→유니크 1.8%가 "레드 큐브"의 공식 확률표와 소수점까지 정확히 일치**하는 걸 발견 — 메이플플래닛이 레드 큐브 표를 그대로 가져다 쓴 것으로 보여, 추정치 대신 레드 큐브의 공식 유니크→레전드리 확률(0.3%)로 교체함. `cubeSimulator.ts` 주석에 근거 명시.

**구현**: `pickOverallGrade`(풀 가중치 합산 방식)를 완전히 폐기하고 `rollOverallGrade()`로 교체 — 항상 레어(1)에서 시작해 순차적으로 에픽→유니크→레전드리 상승을 시도하는 카스케이드 방식(공식 확률표 그대로 재현). `DEVIATION_CHANCE`도 두 큐브 동일값(10%/1%)으로 통일. 컴포넌트의 `CLIMB_BASELINE`(큐브별로 다른 시작 등급)도 두 큐브 모두 레어 시작으로 확인되어 `CLIMB_BASELINE_GRADE` 단일 상수로 단순화.

테스트 15개로 갱신(`rollOverallGrade`의 에스컬레이션 단계별 검증 3개 추가, 두 큐브 이탈확률 동일함 검증으로 기존 "미라클이 더 높다" 테스트 대체). `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(15 passed) 통과. 브라우저에서 수상한 큐브 사용 → 레어/일반/일반(가장 흔한 케이스, 이탈 실패)으로 나오는 것 확인.

### 큐브 시뮬레이터 — 연출 속도 개선 (결과를 빨리 보고 싶어함)

사용자 피드백: 등급이 실제로 오르지 않는 대부분의 경우(레어 유지, 확률상 가장 흔함)에도 매번 상승 연출을 보여주는 게 느리게 느껴짐 — 사람들은 상승 연출보다 옵션 결과를 빨리 보고 싶어함. 그리고 큐브 사용 중에 이전 결과 옵션 리스트가 사라지는 것도 원치 않음.

- 실제로 등급 상승이 없었던 경우(표기 등급이 시작 등급인 레어에 그대로 머무름 — 확률상 94%로 가장 흔함) 상승 연출을 완전히 생략하고 흔들림 직후 바로 옵션 공개. 등급 고정 모드도 동일하게 즉시 공개.
- 이전 결과를 `null`로 지우지 않고 그대로 화면에 둔 채로 다음 큐브 사용을 준비 — 흔들리는 동안 화면이 비지 않고 이전 옵션이 계속 보이다가, 새 결과가 준비되면 그 자리에서 바로 교체됨(상승 연출이 필요한 경우에만 그 사이에 등급 배지가 잠깐 뜸).
- 흔들림/상승 단계별 시간도 단축(450ms→350ms, 550ms→400ms), 마지막 홀드 딜레이 제거.
- 검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(15 passed) 통과. 브라우저에서 연속으로 큐브 사용 → 등급 유지 시 즉시 옵션 교체, 다른 큐브를 눌러도 이전 옵션이 화면에 남아있다가 새 결과로 바로 바뀌는 것 확인.

### 큐브 시뮬레이터 — 유니크→레전드리 확률 근거 보강 + "원하는 옵션 기댓값" 계산기 추가

사용자가 등급 고정으로 레전드리를 계속 돌렸는데 2·3번째 줄 이탈(레전드리 유지)이 거의 안 나온다고 문의 — 실제로 1만 회 시뮬레이션 결과 10.3%/1.0%로 공식 확률(10%/1%)과 정확히 일치함을 확인, 의도한 정상 동작임을 답변. 이어서 "공식으로 적용된 게 맞냐"는 재질문에 등급상승/이탈 확률은 공식 확인, 유니크→레전드리(0.54%, 앞 단계 감소비율 추정치)만 미확인이라고 정정.

"다른 곳은 확인 안 했냐"는 지적에 재검색 — 메이플플래닛 전용 정보글은 여전히 유니크에서 끊김. 대신 넥슨 공식 큐브 가이드를 큐브 종류별로 대조하다가 **레어→에픽 6.0%/에픽→유니크 1.8%가 "레드 큐브" 공식 확률표와 소수점까지 정확히 일치**하는 걸 발견 — 메이플플래닛이 레드 큐브의 등급 상승표를 통째로 가져다 쓴 것으로 보여, 추정치(0.54%) 대신 레드 큐브의 공식 유니크→레전드리 확률(0.3%)로 교체(`GRADE_UP_CHANCE.uniqueToLegendary`). 다만 메이플플래닛 자체가 100% 레드 큐브와 동일한지는 확인된 게 아니라 TODO에 잠정치로 기록.

**"원하는 옵션 기댓값" 계산기 추가**: 사용자가 특정 옵션을 고르면 큐브 1회당 확률과 평균 기댓값을 계산해서 보여주는 기능 요청 — UI로 추가하기로 결정. 실제 반복 시뮬레이션 없이 닫힌 형태(closed-form) 수식으로 계산:
- `overallGradeDistribution()`: 등급 상승 카스케이드(레어 94%/에픽 5.892%/유니크 0.1077%/레전드리 0.000324%)를 정확한 확률 분포로 계산
- `estimateOptionOdds(pool, itemLevel, cubeType, targetEntryId)`: 표기 등급별로 1~3번째 줄이 해당 등급·해당 옵션일 확률을 조합해 "큐브 1회당 확률"을 계산, 기댓값은 역수(1/p)
- 컴포넌트에 큐브 종류 탭 + 등급별로 그룹화된 옵션 선택 `<select>` + 확률/기댓값 표시 UI 추가. `resolveOptionText`를 `cubeSimulator.ts`에서 export해서 선택 목록에 착용 레벨 기준 실제 수치 텍스트 표시
- 테스트 4개 추가(레어/레전드리 등급 각각 손계산 검증값과 대조, 가중치 비율에 따른 확률 스케일링 검증, 존재하지 않는 id는 null) — 총 19개
- 검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(19 passed) 통과. 브라우저에서 레전드리 등급 옵션(기댓값 약 2200만 회) vs 일반 등급 옵션(확률 11.47%, 기댓값 8.7회)으로 등급별 스케일 차이 확인.

### 큐브 시뮬레이터 — 코드 리뷰 반영 + 기댓값 계산기 확장(메소 비용/3옵션 동시 선택)

**코드 리뷰(Codex)로 발견된 버그 수정**:
- `estimateOptionOdds`의 `withinGradeProb` 분자가 착용 레벨 미달 옵션에도 `target.weight`를 그대로 써서, 실제로는 절대 안 나오는 옵션(예: 레벨 70에 reqLevel 100 옵션)에도 0이 아닌 확률이 표시되던 버그. `lineHitProbability` 헬퍼로 통합하면서 target이 `eligiblePoolAtGrade` 후보에 실제로 포함되는지 확인하도록 수정, 회귀 테스트 추가.
- 컴포넌트의 옵션 선택 목록도 레벨 미달 옵션을 그대로 보여주고 있던 문제 — `eligiblePoolAtGrade`를 export해서 UI 목록 자체를 착용 레벨 기준으로 필터링(레벨 미달 옵션은 목록에서 아예 제외).
- `sitemap.ts`에서 플래닛 라우트 5개(큐브 시뮬레이터 포함)가 이번 세션 이전부터 이미 빠져 있던 것 발견 — 복원.
- `<img>` eslint 경고 — `DropTable.tsx`와 동일하게 파일 상단에 `/* eslint-disable @next/next/no-img-element */` 추가(외부 동적 아이콘이라 next/image 부적합, 기존 컨벤션과 통일).

**기댓값에 메소 비용 포함**: 큐브 가격을 고정값으로 넣으려 했으나 조사 결과 미라클 큐브는 메소가 아니라 **캐시샵(현금) 아이템**이고, 수상한 큐브는 **거래소 시세가 유동적**(고정 메소가 없음)이라는 걸 발견 — 사용자에게 확인 후 실제로 다른 사이트(maplanet.net, 메이플플래닛 비공식 거래소 시세 사이트)에서 직접 조사: 미라클 큐브는 "캐시샵 110개 묶음=45,000메포" 환율 계산으로 645,386메소(2026-07-08 기준) 확인, 수상한 큐브는 시세 추적 자체가 없어(몬스터 드랍으로도 획득 가능해 거래 가치가 낮은 것으로 추정) 신뢰할 기본값을 못 찾음. 큐브 가격을 직접 입력하는 필드로 추가(미라클 큐브만 기본값 645,386 채움), 입력 시 "예상 총 비용(기댓값 × 가격)" 표시.

**3개 옵션 동시 선택**: 잠재능력 줄이 3개라는 지적에 따라 단일 선택 → 3슬롯 선택으로 확장. `estimateCombinedOptionOdds` 추가 — 등급 상승 카스케이드의 각 분기(표기 등급 x 줄2/3의 유지·하락)마다 포함-배제 원리로 "선택한 옵션들이 전부 동시에 나올 확률"을 계산(줄마다 최대 하나의 옵션만 나올 수 있다는 제약 반영). 각 옵션의 개별 확률도 함께 표시.

테스트 5개 추가(reqLevel 회귀 테스트, 단일/2개/3개 선택 조합 확률 손계산 대조) — 총 24개. 검증: `npx tsc --noEmit`, `npx eslint src/`(0 errors, 0 warnings), `npx vitest run`(24 passed) 통과. 브라우저에서 2옵션(DEX+INT, 결합확률 0.74%)/1옵션+미라클 큐브 가격(예상 비용 4,900,214메소) 확인.

### 큐브 시뮬레이터 — 메소 비용 대신 "몇 개 필요한지" + 미라클 큐브 캐시 비용으로 변경

사용자 피드백: 메소 비용은 필요 없고, 대략 몇 개의 큐브를 써야 하는지(이미 "기댓값"으로 표시 중)와 — 미라클 큐브는 캐시 아이템이니 **캐시로 얼마나 써야 하는지**를 보여주면 좋겠다는 요청.

- 큐브 가격 직접 입력 필드/메소 비용 계산 전부 제거(`cubePriceByType` 상태, `CUBE_PRICE_DEFAULT` 등).
- 미라클 큐브 전용으로 "예상 캐시 비용" 추가 — 공식 공지(mapleplanet.co.kr/board/notice/511, "미라클 큐브 가격 변경 관련 안내")에서 확인한 캐시샵 묶음 판매가(110개=45,000메포) 기준으로 `기댓값 × (45000/110)` 계산, 메포 단위로 표시. 수상한 큐브는 캐시 아이템이 아니라서 캐시 비용 표시 안 함.
- 검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(24 passed, 로직 변경 없어 테스트 그대로 통과) 통과. 브라우저에서 미라클 큐브 레전드리 옵션 선택 → 기댓값 17,823,419개·예상 캐시 비용 약 72억 메포로 정상 계산되는 것 확인(극희귀 옵션이라 나온 큰 숫자, 계산식 자체는 검증됨).

### 큐브 시뮬레이터 — 원화 환산 + 검색 가능한 옵션 선택 UI + 자동 기댓값 + 확률 프레이밍 개선

이어진 사용자 피드백 4가지를 한 번에 반영:

1. **캐시는 현금(원화)으로**: 메포 표시로는 부족하다는 지적 — 메포->원화 공식 고정가를 찾지 못해(플랫폼 결제 페이지 접근 불가, maplanet.net도 메소↔메포 환율만 취급하고 메포↔원화는 다루지 않음), 커뮤니티에 통용되는 메소마켓 환율(1,150~1,250메포 ≈ 1,000원)의 중간값(1,200메포=1,000원)을 근사치로 채택 — `KRW_PER_MEPO_APPROX` 상수로 명시, 잠정치임을 주석에 남김.
2. **옵션 선택 UI 개선**: 기존 3개의 큰 `<select>`(등급별 optgroup만 있고 텍스트 검색 불가)를 커스텀 `OptionPicker` 컴포넌트로 교체 — 검색창 + 등급별 색상 배지 목록의 드롭다운(바깥 클릭/Esc로 닫힘). 선택된 옵션은 버튼 자체가 해당 등급 색으로 물듦.
3. **"원하는 옵션 기댓값" 패널 강조**: 일반 텍스트 헤더 → 🎯 아이콘 + 브랜드 강조색 테두리를 두른 카드로 승격. "평균 몇 개 필요한지"를 가장 크고 굵은 숫자로 표시(`text-xl font-extrabold`), 확률/캐시 비용은 보조 정보로 아래에.
4. **옵션 미선택 시 자동 기댓값**: 옵션을 하나도 고르지 않은 상태에서 큐브를 사용하면, 그 결과(방금 뽑힌 3줄) 자체의 확률/기댓값을 자동으로 계산해서 보여줌("옵션을 고르지 않아서, 방금 사용한 OO 큐브 결과 기준으로 보여드려요"). 이를 위해 `ResolvedCubeLine`에 `id` 필드 추가(기존엔 text/grade/optionType만 있어 결과 줄을 다시 특정 옵션으로 역참조할 수 없었음), `pickLineAtGrade`가 채워주도록 수정.
5. **확률 표기 방식 변경**: "STR : +9%"처럼 희귀한 옵션이 "0.0000%"로 표시돼 체감이 안 된다는 피드백 — `formatProbability`를 새로 만들어 0.001% 미만이면 "1/N" 분수 형태로 표시(예: "1/36,731,195"), 그 이상은 기존처럼 %. 기댓값("평균 몇 개")을 확률보다 먼저·크게 보여주는 방향으로 순서도 변경.

검증: `npx tsc --noEmit`, `npx eslint src/`(React Compiler가 처음에 `useMemo` 안의 `for...of` + 조기 `return` 패턴을 memoization 보존 불가로 에러 처리 — `flatMap`/`find` 조합으로 바꿔서 해결), `npx vitest run`(24 passed) 통과. 브라우저에서 옵션 검색·선택, 등급 색상 배지, 옵션 미선택 상태로 큐브 사용 후 자동 기댓값(수상한 큐브 결과 마법방어력+70/STR+4/DEX+4 조합 → 결합확률 0.0604%·기댓값 1656.6개) 전부 확인.

### 큐브 시뮬레이터 — 옵션 드롭다운을 등급→옵션 2단계로, 자동 기댓값 제거

바로 다음 세션에서 사용자가 방향을 일부 되돌림:

1. **옵션 드롭다운이 너무 길다** — 검색 가능한 커스텀 콤보박스(`OptionPicker`, 등급 그룹 전체를 한 목록에 표시)를 버리고, "등급 선택" 드롭다운 → 그 등급 안의 옵션만 보여주는 두 번째 드롭다운으로 교체(`GradeOptionPicker`). 등급을 안 고르면 옵션 드롭다운은 비활성화(`등급을 먼저 선택` 플레이스홀더). 등급 변경 시 이전 선택 옵션은 초기화. 검색 UI(입력창, 바깥 클릭 감지 등)가 통째로 필요 없어져 컴포넌트가 더 단순해짐.
2. **"옵션을 골랐을 때만" 기댓값 표시** — 직전에 추가했던 "옵션 미선택 시 방금 뽑은 결과 기준 자동 표시" 기능을 제거. 이를 위해 추가했던 `ResolvedCubeLine.id` 필드와 `pickLineAtGrade`의 관련 코드도 함께 되돌림(다른 곳에서 쓰이지 않는 필드라 정리).
3. **"등급 고정" 옵션은 평소에도 보이게** — 큐브 사용 시뮬레이터 쪽의 "등급 고정" 등급 배지 버튼들이 체크박스를 켜야만 보이던 것을, 체크 여부와 무관하게 항상 보이도록 변경(체크박스는 여전히 실제 롤에 고정 등급을 적용할지만 결정).

검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(24 passed, 엔진 로직은 안 건드려서 그대로 통과) 통과. 브라우저에서 등급 선택 → 옵션 목록이 그 등급으로 좁혀지는 것, 옵션 미선택 시 안내 문구만 뜨고 선택해야 확률/기댓값이 뜨는 것, 등급 고정 배지가 체크 여부와 무관하게 항상 보이는 것 확인.

---

## 2026-07-07

### 메이플 플래닛(Planet) 서버 지원 추가

**배경**: 메이플랜드 외에 최근 흥행 중인 신규 사설서버 "메이플 플래닛"(넥슨 MapleStory Worlds 플랫폼, 빅뱅 직전 KMS 1.2.95~98 기반 — 메이플랜드와 동일 버전)의 유저층을 흡수하기 위해 드랍테이블/계산기부터 플래닛 버전 제공. 이미 플래닛 전용 커뮤니티 사이트가 여럿(planet-helper.com, 플래닛고, 조아요, mapleplanet.gg) 있어 정면 대결보다 "메랜Hub가 잘하는 기능부터" 전략으로 진행.

**라우트 구조**: 기존 메랜 URL(`/`, `/drop-table`, `/calculator/damage`, `/calculators/onehit`)은 그대로 두고 `/planet` 이하에 미러링 — SEO 자산 보존 + 서버별 URL 분리.
- `/planet` — 랜딩 페이지 (기능 카드 그리드)
- `/planet/drop-table`, `/planet/calculator/damage`, `/planet/calculators/onehit`
- `/planet/cube-simulator` — 플래닛 전용 신규 기능(아래 참고, 메랜엔 큐브/잠재능력 시스템 자체가 없음)

**컴포넌트 재사용 전략**: `DropTable`/`TakenDamageCalculator`/`OneHitCalculatorClient`(구 onehit page.tsx에서 분리)는 메랜과 플래닛이 공유. 몬스터 데이터/서버 구분은 전부 **prop으로 주입**(`monsters`, `server`, `calculatorBasePath`, `itemLinkBase`) — 기존에 있던 `setMonsterProvider`(전역 mutable 싱글턴) 패턴은 동시 요청 환경에서 안전하지 않아 사용하지 않음.

**사이드바/홈페이지**: 서버 전환 pill을 큰 카드형 버튼(🌲메이플랜드/🪐메이플 플래닛)으로 확장, 홈페이지에도 "서버를 선택하세요" 2카드 섹션 추가. 메랜Hub 로고 옆에 항상 MAPLELAND/PLANET 배지 표시 (기존엔 플래닛일 때만 표시했었음 — 대칭으로 수정).

---

### 플래닛 색상 테마 분리 (cyan ↔ amber)

`--brand-accent`, `--brand-accent-2` 등 CSS 커스텀 프로퍼티를 도입, `[data-server="planet"]` 선택자로 앰버/오렌지 톤으로 전환되게 함(메랜은 기존 cyan/emerald 유지). `DropTable`/`TakenDamageCalculator`/`onehit-calculator-client`의 하드코딩된 cyan/sky/emerald 클래스를 CSS 변수 참조로 교체. `PanelHeader.tsx`(드랍테이블류)와 `Panel.tsx`(계산기류)가 서로 다른 톤 시스템을 쓰고 있어서 둘 다 따로 `[data-server="planet"]` 오버라이드 필요했음 — 처음에 `PanelHeader`만 고치고 `Panel`을 놓쳐서 계산기 페이지 창 헤더가 여전히 파란색이던 버그를 나중에 발견해 수정.

**Turbopack 트러블슈팅**: `globals.css` 증분 수정이 dev 서버에 반영 안 되는 현상이 세션 내내 반복됨 — `.next` 캐시 삭제 후 서버 재시작으로만 해결됨. CSS만 고쳤는데 브라우저에 반영이 안 되면 우선 의심할 것.

---

### 플래닛 데이터 파이프라인

Planet은 메랜과 동일 KMS 버전이라 **메랜 원본 데이터를 베이스로 재사용 + 배율/보강 데이터만 얹는 방식**으로 구축(`scripts/build-planet-data.mjs`, 상세는 `data.md` 참고).

- **드랍률 4배** 배율을 `drop-index.json`/`item-detail-by.json`의 `prob`에 직접 반영 (커뮤니티 사이트 패치노트로 배율 로직 검증함: 듀얼 버크의 일비 표창 0.008%→0.032% 정확히 일치)
- **몬스터 속성(불/얼음/전기/독/성 약점·반감·면역)**: 외부 영문 데이터셋을 mobCode로 매칭해 자동 보강 (464종 매칭, 기존 데이터와 충돌 0건 — 항상 상위집합이라 안전하게 덮어씀)
- **신규 몬스터 124종 추가**: 메랜 원본에 없는 몬스터(파풀라투스, 피아누스, 도도, 오베론 등 포함)를 외부 카탈로그 데이터에서 자동 추가. 기존에 있는 mobCode는 스탯 차이가 있어도 검증 없이 덮어쓰지 않음(556종 보류 중)
- **출현 맵(`map` 필드) 보강**: maplestory.io 공개 API 기반 데이터로 451종 몬스터의 구체적 스폰 맵 채움 (기존엔 전 몬스터 공백이었음)
- **데이터 정확도 검증**: 조아요(chowayo.com) 등과 스팟체크 — 자쿰 3페이즈/기본 달팽이는 완전 일치, 빨간 달팽이(레벨/EXP 뒤바뀜)·핑크빈(EXP 큰 차이) 등 불일치 발견해 `divergence-overrides.json`에 수정 반영(단일 출처만 확인한 잠정치)

**소스 파일 위치 정리**: 손수 관리/외부 출처 데이터(`divergence-overrides.json`, `monster-attribute-data.js`, `monster-catalog-data.js`, `map-catalog-data.js`, `cube-data.js`)는 `data/*.json`이 "빌드 산출물 전용, 직접 편집 금지"라는 프로젝트 규칙과 충돌해서 `data/planet/` → **`scripts/sources/planet/`**로 이동. `data/planet/`엔 이제 산출물만 남음.

**중요 발견**: 세션 도중 받은 데이터 파일 중 `app.js`가 실제로는 경쟁 사이트(플래닛고)의 **웹앱 소스코드 + 라이브 Firebase 자격증명**이었음을 발견 — 삭제 처리. 나머지 데이터 파일들은 출처가 명시돼 있어(공식 mapleplanet.co.kr, maplestory.io 공개 API, KMS 데이터마이닝 팩) 사용 가능하다고 판단.

---

### 큐브 시뮬레이터 (신규 기능, `/planet/cube-simulator`)

공식 mapleplanet.co.kr/Cube에 공개된 수상한 큐브(185개)/미라클 큐브(132개) 옵션 확률 풀 기반. 옵션 종류는 공식 가중치(weight)로 뽑고, 옵션 내 수치 등급(1~20단계)은 균등분포로 단순화 — 페이지에 이 한계를 명시. 메랜엔 큐브/잠재능력 시스템 자체가 없어 사이드바에 플래닛 모드에서만 노출.

---

### 계산기 프리셋 서버 분리 + 로컬 상태 버그 수정

`character_presets` 테이블에 `server` 컬럼 추가 마이그레이션(`20260707_add_server_to_character_presets.sql`, unique 제약을 `(user_id, calculator, server, name)`으로 확장) — 운영 Supabase에 적용 완료 확인함(익명 키로 스키마 조회해 컬럼 존재 검증).

외부 코드 리뷰(codex) 피드백으로 발견된 문제 수정:
- 한방컷 계산기의 로컬 프로필 저장 키, 피격뎀 계산기의 QuickSlots 저장 키가 서버 구분 없이 공통이라 메랜↔플래닛 전환 시 몬스터/스탯 상태가 서로 덮이는 문제 — `server`별로 키 분리(단, **기존 메랜 유저 저장분 유실처럼 보이지 않게 메랜은 기존 키 유지, 플래닛만 새 키** 사용)

---

### SEO — 플래닛 검색 노출

`sitemap.ts`에 플래닛 라우트 5개 전부 추가(기존엔 메랜만 있었음), 루트 레이아웃과 플래닛 각 페이지에 "메이플 플래닛/메이플플래닛/플래닛" 등 keywords 메타 추가, 한방컷·큐브 시뮬레이터 페이지엔 JSON-LD 구조화 데이터도 추가(기존 메랜 한방컷 계산기 SEO 커밋 패턴 재사용).

---

## 2026-06-30

### 드롭테이블 신규 아이템 추가 (시그너스 업데이트)

**원인**: xn--o80b01o9mlw3kdzc.com 사이트가 `drop-rate-box` div에 `style` 속성을 추가했으나
기존 정규식 `<div class="drop-rate-box">` 가 속성 있는 경우를 매칭 못 하던 버그 수정.
→ `scripts/fetch-item-detail-by.mjs` 정규식 수정 (`"[^>]*>` 패턴 적용)

**신규 추가 스크립트**: `scripts/fetch-new-items-drop-data.mjs`
- xn--o80b01o9mlw3kdzc.com/itemnote 페이지 전체 스크래핑 (레벨 70+)
- 기존 drop-index.json에 없는 신규 아이템 필터링 후 item_detail 드롭 데이터 수집
- 카테고리: 전사/마법사/궁수/도적/해적/무기(Lv70+) + 소비(Weapon/Armor/Mastery Scroll 전체)

**결과** (`data/drop-index.json` 재빌드):
- 아이템 수: 2515 → 2687 (+172)
- 타임리스/리버스 세트 전 직업 완전 추가 (핑크빈 드롭: 0.125%/0.5%)
- 신규 주문서 203종, 마스터리북 포함

**재빌드 순서**:
1. `node scripts/fetch-new-items-drop-data.mjs` — 신규 item_detail 드롭 데이터 수집
2. `node scripts/build-drop-index.mjs` — drop-index.json 재빌드

---

### 시그너스 기사단 N방컷 계산기 추가

**스킬 데이터 (data/skills/)**
- `mainSkillMapping.json`: 5개 직업 항목 추가 (소울마스터, 플레임위자드, 윈드브레이커, 나이트워커, 스트라이커)
- `damageMapping.json`: 시그너스 공격 스킬 25종 전 레벨 데이터 추가 (총 107개 스킬)
  - 기존 동명 스킬과 충돌 방지: 어벤져(나워), 트리플 스로우(나워), 에너지 버스터(스커), 쇼크웨이브(스커) 등 명칭 구분
- `range20.json`: 20레벨 만렙 시그너스 스킬 20종 추가
- `range30.json`: 30레벨 만렙 시그너스 스킬 6종 추가 (소울 블레이드, 브랜디쉬(소마), 소울 드라이버, 플레임 기어, 파이어 스트라이크, 샤크 웨이브)

**계산기 페이지 (calculators/onehit/page.tsx)**
- `jobGroups`에 "시그너스" 추가 → 드롭다운에서 선택 가능
- `jobOptionsByGroup.시그너스`: 5개 직업 정의
- `jobProfile`: 5개 직업 스탯 프로필 (플레임위자드 = INT/LUK/마법직, 나머지 = 물리직)
- 직업 플래그 추가: isCygnusJob, isSoulMasterJob, isFlameWizardJob, isWindBreakerJob, isNightWalkerJob, isStrikerJob, isArcherOrWindBreaker
- 무기 타입: 소울마스터(검), 플레임위자드(없음), 윈드브레이커(활, 화살선택), 나이트워커/스트라이커(잠금)
- 정확도 공식: 시그너스 직업별 분기 (전사계/마법계/궁수계/도적계/해적계 공식 각각 적용)
- 나이트워커: 도적계 부스탯(STR) 합산, 크리티컬 스로우, 표창 선택 UI 활성화
- 플레임위자드: 마법사와 동일하게 패시브 마스터리 비적용

---

### 파일 구조 정리
- `party/page.tsx`: Coming Soon 스텁 → `redirect('/parties')`로 교체 (실제 기능 페이지로 연결)
- `nav-bar.tsx` 삭제: 정의만 있고 아무 데서도 import되지 않던 미사용 컴포넌트
- 루트 잡파일 삭제: `This`, `You` (빈 파일), `FETCH_HEAD` (git 아티팩트 잘못 위치)
- TypeScript 에러 0개 확인

---


### 문서화 체계 수립
- CLAUDE.md, AGENTS.md, database.md, data.md, project-notes.md, TODO.md, DEVLOG.md 작성
- 프로젝트 문서화 체계 수립 (Pet/pawprint 방식 접목)
- 데이터 파이프라인 현황 정리 (`data.md`)
- DB 스키마 전체 문서화 (`database.md`)
- 레거시/중복 경로 목록 확인 필요 항목 TODO에 추가

### 전체 최적화 (번들 + 버그)

**클라이언트 번들 경량화 (~9.5MB 절감)**
- `DropTable.tsx`: `drop-index.json` (3.8MB) + `item-detail-by.json` (318KB) import 제거
  - 두 파일을 서버 컴포넌트(`drop-table/page.tsx`)에서 import → props로 전달
  - `"use client"` 번들에서 ~4.1MB 제거
- `QuestBoard.tsx`: 6개 JSON import 모두 제거 (quests, monsters, monster-spawns, drop-index, item-detail-by, npc-locations = ~5.4MB)
  - `quests/page.tsx` 서버 컴포넌트에서 import → props로 전달
- `tsconfig.json`: 중첩된 `maplelandhub/` 디렉토리 exclude 추가 (TS 컴파일 충돌 방지)

**코드 품질 수정**
- `sidebar-shell.tsx`: 파티 매칭 링크 `/party`(Coming Soon 스텁) → `/parties`(실제 기능) 수정
- `calculators/onehit/page.tsx`: 프로덕션 `console.log` 제거 (useEffect 전체 삭제)
- `StatTable.tsx`: `highlight` prop 버그 수정 — 두 분기가 동일한 클래스를 사용하던 문제, highlight 시 `--retro-header-yellow` 색상 적용
- `TakenDamageCalculator.tsx`: 538번째 줄 중복 `<p>` 제거

**코드 구조 개선**
- `DropTable.tsx`: `getItemGroup`, `getItemLevel` 함수를 컴포넌트 바디에서 모듈 레벨로 이동
- `QuestBoard.tsx`: 로컬 `formatNumber` 제거 → `src/lib/utils.ts` 공유 함수 사용
- `DropIndexData`, `ItemDetailByData` 타입 export (page에서 재사용)
- `QuestBoardProps` 타입 export

---

### 드랍 테이블 검색 성능 최적화 + 자동완성 선택 버그 수정

**문제**: `/drop-table` 페이지에서 아이템/몬스터 검색 시 타이핑할 때 체감 딜레이 발생.

**원인** (`src/components/DropTable.tsx`):
- `filteredItems`/`filteredMonsters`가 키 입력마다 아이템 2,687개·몬스터 653개 전체를 `sort()`로 정렬
- 정렬 비교 함수(comparator) 안에서 `getMatchScore` → `getSearchKeys`(정규식 다수 + 한글 초성 변환)를 이름마다 매 비교 시점에 재계산
- 정렬 특성상 같은 항목의 검색 키가 비교당 여러 번 중복 계산되어, 키 입력 1번에 검색 키 계산이 수만 번 발생

**수정**:
- `itemSearchKeysById`, `monsterSearchKeysByMobCode`: 아이템/몬스터별 검색 키를 `useMemo`로 1회만 계산해 `Map`에 캐싱 (의존성은 `dropData.items` / 빈 배열 — 마운트 후 재계산 안 됨)
- `getMatchScore(name, keyword)` → `getMatchScoreFromKeys(keys, keyword)`로 변경, 캐싱된 키 배열을 직접 사용
- 정렬 비교 함수에서 점수를 매번 재계산하던 방식 → 키워드가 있을 때 점수를 단일 패스(O(n))로 먼저 계산한 뒤, 가벼운 숫자 비교로만 정렬

**검증**:
- 브라우저 실측(`next dev`, 비압축 빌드 기준): "주문서" 입력 시 키 입력당 처리 시간 4.7ms → 41.4ms(첫 글자, JIT 워밍업) → 12.5ms → 9.9ms로 프레임 예산(16ms) 안쪽 안정화
- `npx tsc --noEmit`, `npx eslint src/` 통과

**추가로 발견한 버그 수정**:
- `suggestionItems` 배열 순서가 `[...items, ...monsters]`(아이템 우선)였는데, 실제 렌더링 순서는 몬스터 → 아이템이라 키보드 `ArrowDown`/`Enter` 선택 시 화면 하이라이트와 실제 선택 항목이 어긋나는 버그 존재
- `[...monsters, ...items]`로 순서를 렌더링과 일치시켜 수정
- 브라우저에서 ArrowDown → 하이라이트된 몬스터 → Enter → 동일 몬스터 선택까지 직접 확인

---

<!-- 새 작업은 여기 위에 날짜 역순으로 추가 -->
