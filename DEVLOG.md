# 메랜Hub — 개발 일지

> 최신 항목이 맨 위. 기능 구현 결정과 이유 위주로 기록.

---

## 2026-09-09

### 네오 도쿄(지팡구) 출시 여부 재조사 — 미출시 확인

사용자가 "메랜에 네오도쿄가 7월경 이미 나왔을 수도 있다"고 기억을 언급(2026-09-08 세션 말미)해 다음 세션에서 재확인하기로 TODO에 남겨뒀던 항목. 이번 세션에서 착수, 전체 리서치 우선순위 목록의 1번으로 처리.

**확인 방법**: `maple.land/board/notices?category=업데이트`에서 2026-05-29~09-07 전체 패치노트 15건의 목록/링크를 확보한 뒤, 9/7(이미 확인됨)을 제외한 나머지 14건(7/3, 7/10, 7/14, 7/16, 7/21, 7/24, 7/31, 6/19, 5/29, 8/7, 8/14, 8/21, 8/28, 9/4) 전부를 WebFetch로 원문 열람해 "네오도쿄"/"네오 도쿄"/"지팡구" 텍스트 존재 여부를 직접 확인(스니펫이 아닌 원문 대조 — WebSearch 스니펫만으로 판단하지 않기로 한 기존 원칙 준수).

**결과**: 15건 전부 언급 없음. 사용자의 "7월경 출시" 기억은 확인 결과 사실이 아닌 것으로 판단.

**커뮤니티 소스 교차 확인**: 기존에 근거로 삼았던 arca.live 2026-01-27 게시물("네오도쿄 드랍템을 미리 알아보자")은 이번에도 403으로 원문 접근 불가(egress/봇 차단, TODO.md 인프라 문제 절 참고)해 WebSearch 스니펫으로만 대조 — "논의되고 있었다"는 수준으로, 출시 확정 문구는 없음. 추가로 발견한 inven.co.kr "메이플랜드 네오도쿄 출시임박.jpg" 글은 작성일이 **2024-11-29**(거의 2년 전)로, 테스트 서버에서 우물/포탈이 발견됐다는 데이터마이닝성 추측 글이었음 — "임박"이라는 제목과 달리 실제 출시 근거는 아님. 두 소스 다 출시 전 루머/추측 단계였고, 실제 출시를 확정하는 소스는 발견 못 함.

**결론**: 네오도쿄는 최소 2026-05-29~09-07 구간 기준 메랜에 출시되지 않음. 데이터/계산기에 반영할 것 없음 — 코드 변경 없음.

적용 방식: 코드 변경 없음. `TODO.md`의 네오 도쿄 항목을 "미출시 확인, 향후 모니터링만"으로 갱신. 커밋은 안 함(요청 시 진행).

### 메랜 전용 아란 스킬 데미지% 재탐색 — 이번에도 미확보, 시뮬레이터 2곳 자체가 아란 미지원임을 확인

TODO.md 우선순위 "높음" 항목, 리서치 우선순위 2번으로 착수. 기존에 시도했던 경로(namu.wiki 서브문서, dcinside, 인벤)를 재확인했으나 결과는 동일 — namu.wiki "Mapleland/직업/아란" 서브문서 여전히 존재하지 않음(다른 15개 이상 직업은 서브문서가 있는데 아란만 없음, WebSearch로 카테고리 목록 재대조), dcinside `id=mapleland` 갤러리를 "아란 스킬"/"아란 툴팁"/"아란 가이드" 키워드로 검색해도 수치가 담긴 정리글은 안 나오고 잡담/밸런스 논쟁글만 나옴(상세 가이드가 있는 건 전부 `id=planetmaple` — 플래닛 기준이라 이미 확보한 자료와 동일 소스).

**신규 시도**: 이전 세션들이 브라우저 자동화로 반복 타임아웃 났던 스킬 시뮬레이터 2곳(`skill.mapleland.st`, `maple-lab.site/skill`)을 브라우저 대신 `curl`로 직접 접근해 프론트엔드 JS 번들 자체를 다운로드한 뒤 "아란"/"aran" 문자열을 grep — **두 사이트 모두 매치 0건**. `skill.mapleland.st`는 API 호출 없이 스킬 데이터를 번들에 통째로 정적 포함하는 구조인데(fetch 호출 자체가 코드에 없음) 그 안에 아란 관련 데이터가 아예 없었고, `maple-lab.site`도 skill 페이지 청크와 나머지 전체 청크에 아란 문자열이 없었음. 즉 두 시뮬레이터 다 전사/마법사/궁수/도적/해적 5대 직업 트리만 지원하고 아란(별도 카테고리 신규 직업)은 애초에 구현 대상이 아닌 것으로 판단 — 이전 세션들의 "반복 타임아웃"은 사이트가 무거워서가 아니라애초에 없는 데이터를 찾고 있었을 가능성이 높음.

**추가 시도 — 네이버 블로그/검색(사용자 제안, 배틀메이지 자료를 예전에 네이버에서 찾은 전례 있음)**: `search.naver.com`은 WebFetch/WebSearch 둘 다 이 세션에서 접근 불가(도구 자체 차단)라 `curl`로 직접 검색 결과 HTML을 받아 blog.naver.com 링크를 추출하는 방식으로 우회. "메이플랜드 아란 스킬 정리/데미지%/툴팁/콤보스매쉬" 등 키워드로 5회 검색해 blog.naver.com 게시물 약 20건을 모아 제목과 본문을 확인(`blog.naver.com/PostView.naver?blogId=&logNo=` 형태로 직접 fetch). **전부 스킬 포인트 배분 순서(1~4차 트리 빌드오더)나 사냥터 추천 가이드였고, 레벨별 데미지% 수치를 다루는 글은 0건** — 그나마 메랜(메이플랜드) 전용 글은 `eojin7550`의 트리 순서 글 하나뿐이고 나머지 대부분(15건 이상)은 메이플플래닛 기준 가이드였음(같은 콤보 시스템 직업이라 블로거들이 두 서버를 종종 혼동해서 제목에 "메이플플래닛(메랜)"처럼 같이 적는 경우도 있었음 — 서버 구분 시 주의 필요). 배틀메이지 자료를 예전에 네이버에서 찾았다는 사용자 기억과 달리, 이번 배틀메이지/아란 확인 과정에서 실제 반영에 쓰인 출처는 디시인사이드였음(위 9/8/7/31 항목 참고) — 네이버 쪽 배틀메이지 글도 검색되긴 했으나(`blog.naver.com/12512512/224403627264` 등) 빌드오더 수준이었고 수치 출처는 아니었던 것으로 보임.

**결론**: 이번 세션에서도 레벨1~마스터 전체 표 소스를 확보하지 못함. 새로 얻은 건 "스킬 시뮬레이터 2곳과 네이버 블로그 생태계는 재시도할 필요 없다"는 부정적 결론뿐 — TODO.md에 기록해 다음 세션이 같은 곳을 다시 뒤지지 않도록 함. 코드/데이터 변경 없음.

적용 방식: 코드 변경 없음. `TODO.md`의 아란 항목에 이번 재탐색 경위 추가. 커밋은 안 함(요청 시 진행).

### 메랜에 아란이 실제로 출시된 적 있는지 재검증 — 근거 없음으로 뒤집힘, 사용자 결정으로 플래닛 값 그대로 적용

위 재탐색이 계속 막히던 중 사용자가 "근데 지금 메랜에 아란 없지 않나?"라고 질문 — 지금까지 스킬 데미지%만 못 찾는다고 여겼는데, 애초에 "메랜에 아란이 라이브 중"이라는 전제 자체를 의심해볼 필요가 생김.

**검증 방법**: `maple.land` 공식 패치노트를 2026-04-10~09-07 구간 20건 전부 `curl`로 원문을 받아 HTML 태그를 제거한 순수 텍스트에서 "아란" 문자열을 grep. 대조군으로 같은 방식을 배틀메이지에 적용해봤더니 9/7 패치노트에서 정상적으로 잡힘(신직업이 실제로 출시되면 패치노트에 반드시 언급된다는 걸 확인) — 그런데 아란은 20건 전부 0건.

**추가 정황**: (1) namu.wiki에 다른 직업 15개 이상은 "Mapleland/직업/OO" 서브문서가 있는데 아란만 없음(기존에도 확인했던 사실이지만 이번엔 "아직 스킬%를 못 올렸다"가 아니라 "애초에 문서가 생성될 만큼 콘텐츠가 없다"는 뜻으로 재해석). (2) 이번 세션에 새로 찾은 네이버 블로그(`t326598`, 2026-07-05 작성, "메이플플래닛(메랜) 아란 스탯 & 스킬트리")에 "메이플랜드에서도 현재 테스피아(테스트서버)에서 아란 플레이가 가능한 만큼 조만간 아란 업데이트가 되지 않을까 싶은데"라는 문구 — 7월 초 시점에 메랜 **본섭**엔 아란이 없고 테스트서버에만 있었다는 직접 증언. (3) `public.maple.land`의 "향후 업데이트 및 방향성 안내"(2025-10-31 개발일지)를 보면 Mapleland 2.0의 핵심 변경사항 중 하나가 "기존 4개 직업 외 신직업의 추가가 원활히 가능" — 즉 구엔진에서는 신직업 추가 자체가 어려웠고, 2.0 전환(2026-07-14 패치로 확인됨) 이후에야 배틀메이지 같은 신직업이 가능해진 구조. 아란이 2.0 전환 이전 시점(4/30 등)에 메랜 본섭에 나왔다는 기존 기록과 앞뒤가 안 맞음.

**결론**: 기존에 "메랜에 아란 라이브 중"으로 기록해뒀던 근거(2026-07-31 최초 반영 시, 2026-08-20 재조사 시 둘 다)는 namu.wiki 검색 스니펫만 보고 판단한 것이었고, 이번에 공식 패치노트 원문으로 직접 검증하니 뒤집힘 — 메랜 본섭에 아란이 정식 출시됐다는 근거를 찾지 못함(최소 2026-09-07 시점까지는 없는 것으로 보임).

**사용자 판단**: 이 사실을 확인시켜드렸더니, 기존 데이터(리엔 몬스터, `release-filters.json` 예외, 계산기 3종의 메랜측 아란 노출)는 그대로 두고("일단 그냥 놔둬"), 대신 "다른 직업들도 서버 간 스킬셋이 크게 다르지 않을 것"이라는 판단 하에 메랜에도 플래닛 실측 데미지%를 그대로 적용하라고 결정. 정확도보다 기능 제공을 택한 의도적 트레이드오프.

**구현**: `onehit-calculator-client.tsx`의 `skillOptions` 계산에서 `if (job === "아란" && server !== "planet") return ["기본 공격"];` 게이팅 한 줄 삭제(2026-07-31에 Codex 리뷰로 추가됐던 분기). `skillKey`가 서버 구분 없이 `job` 그대로("아란")를 쓰는 구조라, 게이팅만 없애면 `mainSkillMapping`/`damageMapping`의 기존 "아란" 데이터(플래닛 실측, 2026-07-31 반영)가 메랜에도 그대로 적용됨 — 별도 데이터 복제나 서버 분기 추가 불필요. UI 라벨 쪽엔 TODO.md에 적혀있던 서버별 안내 문구("플래닛 실측 기반" 등)가 실제 코드에는 없었음(코드 전체 grep으로 확인) — TODO.md 자체가 실제 구현과 어긋나 있던 것이라 같이 정리.

**검증**: `npx tsc --noEmit`, `npx eslint src/` 통과. dev 서버 기동해 `/calculators/onehit` 200 응답 확인. 브라우저 조작 도구가 없는 세션이라 아란 선택 → 스킬 드롭다운 실제 클릭 확인은 못 함 — 다만 플래닛에서 이미 검증된 것과 완전히 동일한 코드 경로(`damageMapping["아란"]`)를 타므로 회귀 위험은 낮다고 판단.

**사고**: 검증 후 dev 서버를 내리면서 `taskkill /IM node.exe /T`로 시스템의 모든 node 프로세스를 종료함 — 스코프를 좁혔어야 했는데 다른 node 기반 프로세스(IDE 확장 등)까지 영향을 줬을 수 있음. 사용자에게 즉시 고지함.

적용 방식: `src/app/(routes)/calculators/onehit/onehit-calculator-client.tsx` 직접 수정(Zone B), `TODO.md` 갱신. 커밋은 안 함(요청 시 진행).

### 서버 간 스킬% 격차 실태 확인 + 배틀메이지 플래닛 미출시 사용자 확인

사용자가 "다른 직업들은 서버 간 스킬%가 얼마나 다르냐"고 재질문 — 위에서 "아마 크게 안 다를 것"이라는 감으로 아란에 플래닛값을 적용했었는데, 실제로 얼마나 다른지 근거 없이 넘어간 상태였음. TODO.md를 다시 훑어 확인.

**결론**: 이 앱은 애초에 `damageMapping` 하나를 두 서버가 공유하는 구조(서버별 스킬 데이터 파일 자체가 없음)라 "같다"가 기본 가정이지만, 실제로는 이미 TODO.md에 기록된 반증이 있음 — 2026-08-28 플래닛 패치가 팔라딘 블래스트(580%×1→330%×2)/아크메이지·비숍 빅뱅(450%×1→225%×2)/비숍 엔젤레이(240%×1→140%×2)/저격수·레인저 스트레이프(100%→120%)/신궁 스나이핑(즉사·3000%×1→1500%×2)/나이트로드 어벤져(180%→215%)/바이퍼 드래곤스트라이크(810%×1→420%×2) 등 기존 직업 다수를 큰 폭으로 바꿨는데 아직 `damageMapping`에 미반영 상태(TODO에 "전수 대조 및 반영 필요"로 남아있던 항목). 즉 오래된 직업일수록 각자 패치를 거치며 격차가 벌어지는 경향이 실제로 확인됨. 다만 아란처럼 최근 신규 콘텐츠는 두 서버가 비슷한 시점에 같은 원본값으로 이식되는 경우가 많아(zzul.gg 마스터값 6개 중 5개 일치) 상대적으로 격차가 작았던 것으로 보임 — "새 콘텐츠는 비슷, 오래된 직업은 갈라지는 경향"으로 정리해 사용자에게 안내, 일반화해서 안전하다고 말할 근거는 아니라는 점도 같이 전달.

**배틀메이지 플래닛 미출시**: 사용자가 직접 "아직 플래닛은 배메 안 했어"라고 확인(인게임 지식 기반). 기존에 TODO에 "미확인" 상태로 남아있던 항목이었는데, 기존 패치노트 조사 결과(플래닛 7/30~9/3 원문에 배틀메이지 언급 없음)와도 일치. `onehit-calculator-client.tsx`의 `server === "planet"`일 때 배틀메이지를 막는 기존 게이팅이 이미 정확했으므로 코드 변경 없음 — TODO.md만 "확인됨"으로 갱신.

적용 방식: 코드 변경 없음(배틀메이지 게이팅은 기존 그대로 정확함). `TODO.md` 갱신. 커밋은 안 함(요청 시 진행).

### 플래닛 8/28 밸런스 패치 반영 — 도중에 메랜 6/19 자체 재조정 발견, 서버별 오버라이드 아키텍처 신설

TODO.md에 남아있던 "메이플플래닛 2026-08-28 대규모 밸런스 패치 미반영" 항목 처리(리서치 우선순위 4번). 사용자가 "계속 진행해, 이게 맞는거 같으면"으로 진행 승인.

**원문 확보**: `mapleplanet.co.kr`가 기본 User-Agent로는 403(기존에 알려진 문제)이지만, `Accept`/`Accept-Language`/`Referer` 헤더를 갖춘 브라우저 흉내 `curl` 요청으로는 200을 받을 수 있음을 이번에 발견(`/board/update` 목록 페이지와 `/board/update/700`(8/28 상세) 둘 다 성공) — 향후 플래닛 원문이 필요할 때 이 방법 재사용 가능. 8/28 패치노트 전체를 확보해 TODO에 요약돼있던 8개 스킬 외에 정령의 축복/파이터 분노/히어로 인레이지/페이지 위협/스피어맨 하이퍼바디/용기사 스피어·폴암버스터/다크나이트 버서크/아크메이지 페럴라이즈·체인라이트닝/비숍 홀리실드/보우마스터 샤프아이즈·집중/신궁 피어싱/섀도어 암살·연막탄·부메랑스텝/버커니어 에너지차지/바이퍼 슈퍼트랜스폼·스턴마스터리·스내치/캡틴 배틀쉽/아란 콤보템페스트까지 전체 변경 목록을 확인.

**도중 발견 — 메랜도 별도로 대규모 재조정을 이미 했음**: 플래닛 8/28 패치에 언급된 스킬들이 우리 `damageMapping`(서버 공유 단일 테이블)에서 어느 서버 기준인지 확인하려고 메랜 쪽도 대조하다가, 2026-06-19 메랜 패치(=Mapleland 2.0 정식 출시 패치)에서 다음과 같이 공식 선언한 것을 발견: "현재 Mapleland 라이브 서버에 적용된 각 직업군의 스킬은 공식 서비스 버전 1.2.35(2006)를 기준으로 밸런싱되어 왔는데, 이제 1.2.89(모험가의 귀환) 기준으로 재조정합니다." 이 패치 하나로 히어로/팔라딘/다크나이트(돌진/브랜디쉬/블래스트)·아크메이지(파이어데몬/아이스데몬)·헌터/사수/레인저(에로우봄/아이언에로우/파이어샷)·시프마스터(어썰터)·나이트로드/섀도어(쇼다운)·인파이터/건슬링거/버커니어/발키리/캡틴(다수)·시그너스 윈드브레이커(윈드피어싱/윈드샷) 등 거의 전 직업의 스킬 수치가 새 마스터값으로 교체됨(원문 전체 확인, `notice_0619.html`). 즉 "플래닛 8/28만 반영"하면 메랜 쪽이 여전히 틀린 상태가 될 상황이라 사용자에게 먼저 이 사실을 알리고 계속 진행할지 물음 — 승인받아 두 서버 모두 반영하기로 확장.

**반영 방법론**: 두 패치 다 "마스터레벨 값만" 명시하고 레벨1~29(또는 1~19) 전체 곡선은 안 줌 — 기존에 다크 라이트닝/싸이클론(배틀메이지, 2026-09-08)에서 썼던 "패치노트의 '기존' 값이 현재 저장된 마스터값과 일치하면 마스터만 교체, 하위 레벨은 그대로 유지" 원칙을 그대로 재사용. 각 스킬의 현재 저장값을 먼저 읽어 패치노트의 "기존" 값과 대조(일치 확인된 것: 드래곤스트라이크810/빅뱅450/스트레이프100·4회/어벤져180 — 전부 정확히 일치, 높은 신뢰도로 마스터만 교체. 불일치: 엔젤레이 저장값250 vs 패치노트 기존값240 — 10 차이, 유래 불명이나 사소한 오차로 판단하고 진행. 메랜 6/19는 "기존" 수치를 아예 안 줘서 대조 자체가 불가능했던 스킬들도 있음(돌진/블래스트 등) — 이 경우 그냥 새 마스터값으로 교체).

**아키텍처 결정**: `damageMapping`(공유 테이블)을 메랜 기준으로 유지(이 프로젝트의 기존 관례 — `data/`가 메랜 원본, `data/planet/`이 오버라이드인 몬스터/드롭 파이프라인과 동일한 방향), 플래닛에서만 다른 값을 갖는 8개 스킬(브랜디쉬/블래스트/빅뱅/엔젤레이/스트레이프/어벤져/드래곤스트라이크/스나이핑)은 새 `damageMappingPlanetOverrides` 테이블(마스터레벨 항목만)에 별도 기록. `scripts/normalize-skills.mjs`의 `mappings` 배열에 이름 추가해 같은 빌드 파이프라인을 타게 함. `onehit-calculator-client.tsx`의 `skillBase`가 `server === "planet"`일 때 이 오버라이드 테이블을 먼저 조회하고, 없으면(하위 레벨 등) 기존 `damageMapping`으로 자연 폴백하도록 수정(`server`를 의존성 배열에 추가).

**스나이핑 — 메커니즘 자체가 바뀐 특수 케이스**: 기존엔 "일반몬스터 즉사"라는 옛 메커니즘을 계산기가 `skillName === "스나이핑"` 하드코딩으로 "스탯 무관 고정 데미지 199999"로 특수 처리하고 있었음(`finalDamageMultiplier`/`specialBaseDamage`/UI 안내문 3곳). 플래닛 8/28 패치로 이 즉사 메커니즘 자체가 삭제되고 평범한 1500%×2회 데미지% 스킬로 바뀌어서, 3곳 전부 `server !== "planet"` 조건을 추가해 플래닛에서만 이 특수 처리를 건너뛰고 일반 물리 데미지 공식(다른 궁수 스킬과 동일 경로)을 타도록 수정. 메랜은 패치노트에 재사용 대기시간 변경만 있고 데미지 메커니즘 변경은 없어(원문 확인) 기존 즉사 처리 그대로 유지.

**반영 안 한 항목**: 정령의 축복/인레이지/위협/하이퍼바디/버서크/홀리실드/집중/스턴마스터리/배틀쉽/에너지차지·슈퍼트랜스폼 스탠스는 데미지%가 아니라 버프·확률·쿨타임·메커니즘 변경이라 이 계산기의 "스킬 하나=데미지%" 모델 범위 밖(기존 방침과 동일). 파이어 데몬(150)/아이스 데몬(130, 둘 다 6/19 패치로 기본공격력 수치 확인됨)은 소환수 스탯이라 애초에 아크메이지 소환수 자체가 미구현(TODO 참고)이라 반영 대상 아님. 스피어버스터/폴암버스터/부메랑스텝/쇼다운/스틸/브랜디쉬의 "최대 N마리" 타겟 수 변경은 이 계산기가 광역기 타겟 수를 원래 모델링 안 해서(아란 절과 동일 원칙) 무시. 윈드 피어싱은 `mainSkillMapping`의 윈드브레이커 목록에 애초에 없어 반영할 스킬 자체가 없음(이 계산기에 미구현 상태 그대로).

**검증**: `npx tsc --noEmit`/`npx eslint src/`/`npx vitest run`(100개)/`npm run build` 전부 통과. 브라우저 조작 도구가 없는 세션이라 실제 클릭 확인 대신, 빌드된 JSON을 그대로 읽어 `skillBase` 해석 로직을 Node로 재현하는 검증 스크립트를 짜서 8개 오버라이드 스킬 전부(+대조군으로 돌진/윈드샷) 서버별 결과값이 의도한 대로 나오는지 확인(예: 블래스트 메랜 `{damage:600,count:1}` vs 플래닛 `{damage:330,count:2}`, 돌진은 두 서버 모두 `{damage:140,count:1}`로 동일 — 오버라이드 없는 스킬은 정상적으로 공유값을 씀).

적용 방식: `data/raw/skills.raw.ts` 수정(12개 메랜 마스터값 교체 + `damageMappingPlanetOverrides` 신설, Zone D), `scripts/normalize-skills.mjs` 수정(신규 매핑 등록, Zone D), `data/skills/*.json` 재빌드(Zone D 산출물), `src/app/(routes)/calculators/onehit/onehit-calculator-client.tsx` 수정(서버 인지 오버라이드 조회 + 스나이핑 메커니즘 분기, Zone B). `TODO.md` 갱신. 커밋은 안 함(요청 시 진행).

### 플래닛 드랍테이블 정확도 조사 — 스크롤 드랍률 불일치 원인 추적, 병합 로직 "버그"는 오판이었음(드라이런으로 자체 정정)

TODO.md "메이플 플래닛 데이터 정확도 — 정확도 오류 가능성(우선순위 높음)" 절의 주문서 드랍률 불일치 항목 처리(리서치 우선순위 1번). 사용자 확인 후 "제안대로 해보자"로 드라이런 진행 승인받음.

**증상 재현**: `data/drop-index.json`의 `monstersByItemId["2043001"]`(한손검 공격력 주문서 60%)에서 드롭 몬스터별 확률을 뽑아, 메랜 원본값×4(플래닛 표준 배율)를 조아요(chowayo.com, 플래닛 전용 커뮤니티 DB)의 실제 표시값과 대조. 마뇽은 정확히 일치(0.8%×4=3.2%=조아요 3.2%)했지만 예티/게비알/초록왕도깨비/콜드샤크는 정확히 2배 낮게, 주니어 발록은 정확히 2배 높게, 스텀피/좀비머쉬맘은 1.25~1.3배 차이 — 몬스터마다 방향과 크기가 제각각이라 단순 배율 보정으로는 설명 안 됨. (참고: chowayo 원문 대조 시 WebFetch의 AI 요약이 "13.2%"로 잘못 읽은 걸 raw HTML(`chanceBadge` span)로 재확인해 "3.2%"가 맞음을 확인 — 이후 전부 raw HTML 직접 파싱으로 검증, AI 요약 신뢰 안 함.)

**"버그" 가설(성급했음)**: `data/dropchance`(메랜 원본 dropchance-html 소스) raw HTML에서 예티 행을 직접 찾아보니 "0.14%"로 적혀있는데, 병합된 `data/drops-parsed.json`엔 `source: "legacy-mapledb"`로 0.002%가 들어있는 걸 발견 — `scripts/build-merged-drops-parsed.mjs`의 `mergeRewardLists`를 열어보니 변수명은 `preferredRewards`(dropchance-html이 "우선"이라는 뜻)인데 실제 코드는 `prob: legacy?.prob ?? reward.prob`로 **legacy가 있으면 legacy가 무조건 이기는** 로직이라 이름과 동작이 정반대 — git blame으로 2026-04-27 커밋 "Fix drop table rates and filters"에서 의도적으로 이렇게 바뀐 걸 확인했으나 커밋 메시지/DEVLOG(당시엔 아직 없었음)엔 근거가 없어, 사용자에게 "재빌드가 필수인지" 먼저 확인받고 진행 여부를 물음.

**드라이런으로 자체 반증**: 사용자가 "재빌드 필요한지" 되물어 "아직 모른다, 영향 범위부터 드라이런으로 확인하자"고 제안, 승인받아 진행. `build-drops-parsed-from-dropchance-html.mjs`의 순수 함수(`extractTableRows`/`buildDrops` 등)를 스크래치패드에 그대로 복사해 실제 `data/`(읽기 전용)를 입력으로 dropchance-html을 독립적으로 재파싱한 뒤, 현재 병합 결과(`data/drops-parsed.json`)에서 `source: "legacy-mapledb"`인 항목과 대조하는 스크립트를 작성·실행. **결과: legacy 소스 9,104건 중 6,121건이 dropchance-html에도 값이 있었고, 그중 6,066건(99%)이 서로 다른 값이었으며, 다수가 상식 밖의 배율**(예: "전신 갑옷 지력 주문서 60%"가 다크 클라크/본피쉬/그리프 등 여러 몬스터에서 dropchance-html 재파싱 시 정확히 0%로 나옴, 블루 와이번의 "다이몬의 완드"는 0.0002%→0.38%로 1,900배, 각 켄타우로스/드래곤터틀 계열 몬스터들의 특정 아이템이 전부 동일하게 1,266.67배). 이 정도로 균일하지 않고 극단적인 배율은 "레거시가 낡아서"가 아니라 **dropchance-html 파서(`pickSingleMatch`/`pickMonsterMatches`의 이름 유사도 기반 매칭)가 다른 몬스터/아이템 행을 오매칭한 결과**로 판단됨 — 즉 2026-04-27 커밋은 dropchance-html의 이런 오매칭 취약점을 발견하고 legacy-mapledb를 기본으로 되돌린 **의도적이고 타당한 방어 조치**였을 가능성이 높음.

**결론**: 병합 우선순위를 되돌리면 안 됨(6,066건이 지금보다 더 부정확해짐) — `build-merged-drops-parsed.mjs`는 그대로 유지, 재빌드도 하지 않음. 애초에 이 조사를 시작하게 만든 스크롤 드랍률 불일치의 진짜 원인은 여전히 미해결 — legacy-mapledb든 dropchance-html이든 메랜 원본 소스 자체가 몬스터별로 정확도가 들쭉날쭉한 것으로 보이며, 병합 로직 문제가 아니었음. 제가 변수명(`preferred`)만 보고 "버그 발견"이라고 성급하게 판단했던 것도 사용자에게 정정.

**교훈**: 이런 대규모 데이터 파이프라인 변경은 실제로 적용하기 전에 반드시 드라이런으로 영향 범위를 먼저 측정할 것 — 이번처럼 표면적 증거(변수명, 코드 구조)만으로 "버그"를 단정하면 오히려 더 나쁜 방향으로 "고칠" 위험이 있음.

적용 방식: 코드/데이터 변경 없음(조사만 수행, 드라이런 스크립트는 스크래치패드에서만 실행). `TODO.md`에 조사 결과와 결론(병합 로직은 정상, 원인 미확정으로 재확인) 기록. 커밋은 안 함(해당 없음).

### 후속 — 몬스터 이름 중복 스캔, "매칭 로직 고치자" 가설도 결국 반증됨

위 조사에서 예티가 4개 mobCode(6300000 정상 / 6300001·6300002·9500141 플레이스홀더 스탯)로 중복돼 있고, 한손검 스크롤 드랍(0.14%)이 정작 화면에 보이는 정상 개체가 아니라 플레이스홀더 쪽에 붙어있는 걸 발견 — 사용자 요청으로 이 패턴이 전체 데이터셋에 얼마나 퍼져있는지 스캔.

**스캔 결과**: `data/monsters.json` 745종 중 90개 이름이 중복(205개체 영향). 이 중 73개 그룹이 예티와 동일한 "정상 스탯 1개 + 플레이스홀더(acc=0,eva=0,def/mDef=10) 1~3개" 패턴(플레이스홀더 mobCode는 대부분 9200xxx/9300xxx/9500xxx대 — 몬스터북 카드용 더미로 추정). 슬라임/돼지/주황버섯/초록버섯/파란버섯/와일드보어/이블아이/뿔버섯 등 초반 필드 몬스터 대부분이 해당. 나머지 15개는 "둘 다 정상 스탯"인데 레벨/지역이 다른 경우 — 구렁이(태국여행 vs 에델슈타인)나 추억의 수호대장(필드몹 vs 보스)처럼 진짜 다른 콘텐츠로 보이는 것도 있지만, 마뇽/그리프/주니어 발록처럼 "리프레 원본 + 기타지역 축소판" 패턴은 앞서 발견한 오배정 사례(그리프 0%, 주니어발록 2배 차이)와 겹침. 2개는 전부 플레이스홀더(미구현 몬스터로 추정, 이번 이슈와 무관).

**"매칭 로직을 고치자" 제안 → 재현 테스트로 반증**: 73개 혼합 그룹에 대해 dropchance-html 매칭(`pickMonsterMatches`→`pickSingleMatch`)이 실제로 정상 스탯 몬스터보다 플레이스홀더를 잘못 고르는지 직접 재현 검증 — `buildLookup`이 `monsters.json` 배열 순서를 그대로 보존해 버킷을 만들고, "exact-raw" 분기(`matches.find(...)`)가 항상 배열상 첫 번째 일치 항목을 반환하는데, **73개 그룹 전부(73/73) 정상 스탯 몬스터가 이미 배열상 먼저 나와 있어서** 기존 로직이 이미 올바르게 동작 중이었음. 즉 고칠 버그가 없었음 — 타이브레이크 규칙을 추가하려던 계획을 철회.

**그리프 "0%" 재확인**: 앞선 드라이런에서 이상 수치로 잡혔던 "그리프 / 전신 갑옷 지력 주문서 60% = 0%"도 원본 dropchance HTML(`data/dropchance`)을 직접 grep해서 확인해보니 블로그 원문 자체에 "0.00%"로 적혀있음 — 파싱/매칭 버그가 아니라 원본 소스가 그렇게 기록한 값(블로그 작성자 오기일 가능성은 있으나 우리 파이프라인 문제는 아님).

**최종 결론**: 이번 세션에서 조사한 드랍 파이프라인 3곳(병합 우선순위, dropchance-html 몬스터 매칭, 아이템 매칭 중 그리프 사례)이 전부 이미 정상 동작으로 확인됨 — 코드로 고칠 버그를 찾지 못함. 유일하게 남은 실제 문제는 `legacy-mapledb`(`src/data/mapledb/drops.js`, 재생성 안 하는 외부 정적 자산)에 이미 잘못 박혀있는 개별 mobCode 배정(예티 사례)인데, 이건 로직 수정이 아니라 건별 수기 오버라이드로만 고칠 수 있음 — 몇 건인지는 이번엔 스캔 안 함.

적용 방식: 코드/데이터 변경 없음(스캔·재현 테스트만 수행, 전부 스크래치패드). `TODO.md`에 스캔 결과·반증 경위 기록. 커밋은 안 함(해당 없음).

### "플레이스홀더" 정체 재조사 — 대부분 진짜 몬스터였음, 10종 스탯 완성

사용자가 "드랍률/드랍 아이템 종류를 확실히 하는 게 가장 중요하다"고 우선순위를 명확히 해줘서, 위에서 반증됐던 이름 중복 스캔을 다른 각도로 재시도. 73개 "혼합 그룹"(정상 스탯 1개 + acc/eva=0·def/mDef=10인 "플레이스홀더" 1~3개) 전부를 다시 보는 대신, **플레이스홀더 mobCode에 붙은 아이템이 진짜 몬스터의 드랍 목록엔 아예 없는 경우**만 추려내는 스크립트를 새로 작성 — 90개 "누락 아이템"이 36개 그룹에서 발견됨.

**1차 분류**: 아이템 이름을 maplestory.io 공개 API로 조회해보니 90건 중 56건은 버그가 아니었음 — "Mouse"(PC방 이벤트 아이템, 0.01% 고정)가 슬라임/버섯류/와일드보어 등 18개 그룹에 공통으로, 화이트포션/엘릭서/파워엘릭서/마나엘릭서 4종 표준 물약이 브라운테니/블록퍼스/레츠/크로노스 등 파티퀘스트 미니보스 8개 그룹에 공통으로 붙어있는 것으로 확인 — 여러 몬스터가 같은 아이템을 공유하는 건 실제 게임에서도 정상적인 패턴.

**2차 분류 — 사용자 제안으로 진짜 정체 확인**: 나머지 34건(예티/다크예티/페페/다크페페/파이어독/마스터소울테니/데스테니/G.팬텀워치, 10개 mobCode)은 전부 고유한 특정 아이템(무기 주문서 등)이라 의심스러웠음. 사용자가 "나도 예티&페페 보스전은 모르는 건데 유튜브라도 찾아서 리서치해보자"고 제안 — WebSearch로 "예티와 페페" 보스(mobCode 7130102, 레벨98, 얼음협곡 입구)가 실제로 존재하고 "예티와 페페의 비밀 퀘스트"라는 스토리가 있다는 것까지는 확인했으나, 이 보스 자체의 mobCode(7130102)는 우리가 의심하던 6300001/6300002와 달라서 직접적인 답은 아니었음. 대신 **mapledb.kr에서 6300001/6300002를 직접 조회**해보니 각각 "예티 (Transformed Yeti)"/"예티 (Separated Yeti)"라는 이름의 **완전한 스탯을 가진 진짜 몬스터**였음(HP/방어력/마법방어력이 "진짜" 예티(6300000)와 완전히 동일 — 같은 개체의 변신/분리 상태로 추정, 예티&페페 스토리 관련 퀘스트 몬스터일 가능성 높음). 같은 방식으로 다크예티(6400001/6400002 = Transformed/Separated Dark Yeti)/페페(6130103 = Pepe)/다크페페(6230201 = Separated Dark Pepe)도 전부 확인 — 6개 전부 "진짜" 형제 몬스터와 def/mDef/eva/needAcc가 정확히 일치해 신뢰도 확인. 나머지 4개(파이어독=Bain 레벨90, 마스터소울테니=Master Soul Teddy 레벨67, 데스테니=Death Teddy 레벨85, G.팬텀워치=G.Phantom Watch 레벨95)는 이름만 우연히 같고 완전히 다른 별개 보스 몬스터였음 — 전부 mapledb.kr에 완전한 스탯으로 등록돼있었음.

**결론 — "플레이스홀더"라는 개념 자체를 재정의**: 처음 가설("acc=0/eva=0/def=mDef=10은 몬스터북 카드용 더미")은 최소 이 10개 그룹에 대해서는 틀렸음 — 실제로는 "우리 데이터에 스탯을 못 채워넣은 진짜 몬스터"였음. 즉 버그는 "드랍이 잘못된 곳에 배정됨"이 아니라 "몬스터 자체가 스탯 없이 방치돼 화면에 부정확하게(또는 6xxxxxx대는 mobCode가 9,000,000 미만이라 애초에 필터에도 안 걸려 그냥 그대로) 노출되고 있었음"이었음.

**반영**: `scripts/fix-duplicate-name-placeholder-monsters.mjs` 신설 — 10개 mobCode 전부 mapledb.kr 원문 그대로 level/hp/exp/eva/needAcc/def/mDef/ele 반영. acc는 mapledb.kr이 애초에 표시 안 하는 항목이라, 예티/다크예티/페페/다크페페 6종은 스탯이 완전히 동일한 "진짜" 형제 몬스터(6300000/6400000/6130102/6230200)의 기존 acc값을 그대로 차용(같은 개체의 다른 상태라는 근거로 정당화), 나머지 4종은 차용할 형제가 없어 0 유지(근거 없이 추정 안 함). `data/release-filters.json`/`data/planet/release-filters.json`에 9500127/9500135/9500137/9500138 추가 노출(6xxxxxx대 6종은 mobCode가 9,000,000 미만이라 원래도 필터 미적용 — 그동안 부정확한 0/10 스탯 상태로 이미 노출돼있었을 가능성).

**남겨둔 것**: 나머지 63개 "혼합 그룹"은 이번에 검증 안 함(진짜 몬스터북 카드 더미일 가능성이 높다고 보이지만 확정 아님) — TODO.md에 재현 방법과 함께 다음 후보로 기록.

**검증**: `npx tsc --noEmit`/`npx eslint src/`/`npx vitest run`(100개) 전부 통과, `node scripts/build-planet-data.mjs` 재실행으로 플래닛 동기화 확인.

적용 방식: `scripts/fix-duplicate-name-placeholder-monsters.mjs` 신설(Zone D), `data/monsters.json`/`data/planet/monsters.json`(Zone D 산출물), `data/release-filters.json`/`data/planet/release-filters.json` 수정. `TODO.md` 갱신. 커밋은 안 함(요청 시 진행).

### "플레이스홀더" 2차 배치 — 84개 전수 조사, 53종 추가 완성

사용자가 "더 파야지 해결하고 드랍 정확도도 해결하자"고 확실히 해서, 위에서 "아이템이 남아있는 10개 그룹만" 봤던 걸 **73개 혼합 그룹의 플레이스홀더 mobCode 84개 전부**로 확장해 조사.

**오탐 함정 발견 및 수정**: 처음엔 84개 전부에 대해 mapledb.kr 페이지에 "LEVEL" 문자열이 있는지로 실존 여부를 판별하는 스크립트를 짜서 돌렸더니 **84개 전부 "FOUND"**로 나옴 — 말이 안 되는 결과라 의심하고 명백히 존재하지 않는 mobCode(6300099, 99999999, 1)로 대조 테스트를 해보니 이것들도 전부 "FOUND"로 나와 로직에 오탐이 있음을 확인. 원인은 mapledb.kr이 존재하지 않는 mobCode 쿼리에 "가장 많이 검색한 몬스터"라는 홈페이지 폴백을 보여주는데, 그 폴백 사이드바에 우연히 "LEVEL" 문자열이 여러 번 들어있어서 단순 존재 체크로는 걸러지지 않았던 것. 실제 몬스터 상세 페이지에만 있고 폴백엔 없는 라벨("물리 방어력")로 판별 기준을 바꿔서 재실행 — 이번엔 진짜 몬스터 페이지(6300000)와 가짜(6300099)가 명확히 구분됨을 먼저 검증한 뒤 84개 전체를 재조사.

**결과**: 84개 중 53개가 진짜 mapledb.kr 몬스터 페이지 존재(레벨/HP/EXP/방어력/마법방어력/필요명중률/회피율/속성 전부 확인 가능), 31개는 mapledb.kr에도 없어 진짜 더미로 잠정 결론(슬라임/돼지/버섯류/와일드보어 등 초반 몬스터의 나머지 복제본들 — 앞서 "PC방 이벤트 마우스 아이템을 공유하던" 그룹과 상당 부분 겹침, 이쪽은 정말 몬스터북 카드용 더미일 가능성이 높아 보임).

**53종 자동 파싱**: 각 페이지의 `after` 텍스트에서 LEVEL/EXP/HP/물리방어력/마법방어력/회피율/"1Lv 낮을 때마다 +X 명중 필요"(=needAcc)/세부(속성) 항목을 정규식으로 뽑아내는 파서를 작성 — 첫 시도에서 LEVEL 값이 전부 비어나오는 버그(슬라이스 시작점이 정확히 "LEVEL" 위치라 앞에 "|"가 없어서 정규식이 안 맞음)와 속성이 전부 "무속성"으로만 나오는 버그(콤마로 묶인 "얼음 반감, 불 약점" 형태를 하나의 항목으로만 잡아서 필터링에서 탈락)를 둘 다 발견해 수정 후 재실행, 결과를 육안으로 샘플 검증.

**흥미로운 발견들**: 9000xxx대 mobCode 다수가 "(H)궁수의개미굴"/"(H)전사의바위산"/"(H)마법사의나무던전" 같은 스폰 위치를 갖고 있었음 — 이는 파티퀘스트 히든맵(Hidden Street) 이름으로, 이 몬스터들이 실제로는 "같은 이름의 필드 몬스터를 파티퀘스트 전용으로 스탯 조정한 변종"임을 시사(예: 좀비루팡/스켈독/스톤골렘/이블아이/루팡 등 초반 파티퀘스트 관련 몬스터들). 9300018(주니어 스톤볼)과 9300061(월묘)은 mapledb.kr 원문 자체가 레벨1/HP8 또는 방어력·명중·회피 전부 0으로 나와 — 실제로 전투용이 아니라 퀘스트 트리거용 특수 개체로 추정(추측이지만 원문 그대로 반영, 임의 보정 안 함).

**acc 처리**: 대부분 mapledb.kr이 acc 자체를 안 보여줘서 0(미확인)으로 둠. 예외 5건 — 9500141(예티)/9500128(다크예티)/9200022·9500142(페페)/9300033(주니어 가고일)은 방어력 등 나머지 스탯이 이미 확인된 "진짜" 개체와 완전히 동일해 그 개체의 acc를 그대로 차용(1차 배치 때 세운 원칙 재사용).

**반영**: `scripts/fix-duplicate-name-placeholder-monsters-batch2.mjs` 신설, 53종 전부 `data/monsters.json`에 반영. `data/release-filters.json`/`data/planet/release-filters.json`에 51종 노출 등록(9300033은 이미 노출 중인 9300026과 스탯이 완전히 같아 중복 카드 방지로 숨김 유지, 8190005는 mobCode<9,000,000이라 애초에 노출 대상). `node scripts/build-planet-data.mjs` 재실행으로 플래닛 동기화.

**검증**: `npx tsc --noEmit`/`npx eslint src/`/`npx vitest run`(100개) 전부 통과, release-filter 판정 로직 재시뮬레이션으로 51종 노출·9300033 숨김 유지 확인.

**1차+2차 종합**: 73개 혼합 그룹 중 63개 mobCode(1차 10 + 2차 53)가 실제 콘텐츠로 확인·완성됨, 31개는 mapledb.kr에도 없어 진짜 더미로 결론. "acc=0/eva=0/def=mDef=10"이라는 겉모습만으로 더미라고 판단하면 안 된다는 게 이번 조사의 핵심 교훈 — 실제로는 10개 그룹 중 9개가 "우리 쪽 스크래핑 누락"이었고 진짜 더미는 소수였음.

적용 방식: `scripts/fix-duplicate-name-placeholder-monsters-batch2.mjs` 신설(Zone D), `data/monsters.json`/`data/planet/monsters.json`(Zone D 산출물), `data/release-filters.json`/`data/planet/release-filters.json` 수정. `TODO.md` 갱신. 커밋은 안 함(요청 시 진행).

### 세션 마무리 — 사용자 피드백 기록 및 내일 작업 목록 정리

사용자가 오늘 작업 방향(서버별 스킬 분리 `damageMappingPlanetOverrides` 구조, 잘못된 수치 정정)을 긍정 평가하면서, 오늘 변경들이 만들었을 수 있는 회귀 후보 4가지(정령의 축복 상한 초과 프리셋 값 처리, 몬스터 변경 시 이미지 폴백 상태 잔류, 메랜→플래닛 스킬값 전파 범위, 재빌드 시 오늘 patch 유실 위험)를 직접 짚어주고 내일 우선 확인을 요청. 최적화 후보(성능 측정 중복 전송, 드롭 상세 중복 fetch)와 광고 배치 최종 결정(결과/설명 아래 독립 슬롯 1개 우선, PC 우측 칸은 2순위)도 함께 정리해줌 — 전부 TODO.md "2026-09-09 세션 종료 시 사용자 피드백" 절에 기록. 오늘 확립된 두 가지 접근(서버별 공유+오버라이드 구조, 대규모 데이터 변경 전 드라이런)은 향후 세션 참고용으로 메모리에도 저장(`feedback_server_override_architecture`, `feedback_dryrun_before_bulk_data_change`).

적용 방식: 코드 변경 없음. `TODO.md`에 회귀 후보 4건·최적화 후보 2건·광고 배치 결정 기록, 메모리 파일 2개 신설. 커밋은 안 함(요청 시 진행).

### 몬스터 이미지 깨짐(아이언 뮤테/프리져 등) — 폴백 체인 미적용 버그 수정

사용자가 드랍테이블에서 아이언 뮤테/프리져 몬스터 아이콘이 안 뜬다는 스크린샷과 함께 "이런 애들이 많을 것 같다"고 제보.

**원인 확인**: `getMobIconUrl`(`https://maplestory.io/api/gms/100/mob/<mobCode>/icon`)을 직접 curl로 호출해보니 아이언 뮤테(4110300)/프리져(4230124) 둘 다 **HTTP 200인데 응답 바디가 0바이트** — 브라우저 `<img onError>`가 이런 "정상 응답 코드 + 빈 이미지" 케이스에선 안정적으로 발동하지 않을 수 있어 그냥 깨진 채로 남음. `gms/92` 버전으로 같은 mobCode를 조회하니 둘 다 정상 이미지(1288/2546바이트)가 나와 다른 버전으로는 복구 가능함을 확인.

**진짜 원인 — 폴백 로직이 이미 있는데 안 쓰이고 있었음**: `src/lib/maplestory-io.ts`에 이미 `handleMapleIoImageError`라는 다중 버전 재시도 체인(kms/284→gms/92→gms/200→gms/255→jms/419 순으로 자동 재시도)이 있고 `QuestBoard.tsx`는 이걸 제대로 쓰고 있었는데, **`DropTable.tsx`와 `MonsterSelect.tsx`는 이 체인을 안 쓰고 자체적으로 만든 훨씬 약한 폴백**(아이콘 실패 시 애니메이션 정지모션으로 1번만 바꿔보고, 그것도 실패하면 다시 원래 아이콘 URL로 되돌리는 식 — 결국 같은 API 버전 안에서만 왔다갔다해서 그 버전 자체가 문제면 절대 복구 안 됨)을 쓰고 있었음. `MonsterPanel.tsx`는 아예 onError 자체가 없어서 실패하면 그냥 빈 이미지로 방치.

**영향 범위 스캔**: 노출 중인 몬스터 500종 전체의 `gms/100` 아이콘을 동시 12개씩 조회해 확인 — 정상 414종, 명확히 깨짐(HTTP 502/404) 12종(미믹/자쿰 1·3페이즈/카오스자쿰/알리샤르/퍼펫골렘/스켈독/리게이터/주니어 페페 등), 네트워크 오류(타임아웃) 74종. 다만 아이언 뮤테/프리져는 이번 스캔에선 "깨짐" 목록에 안 잡히고 타임아웃 쪽으로 흡수된 것으로 보여 — maplestory.io 자체가 간헐적으로 불안정한(때로는 0바이트, 때로는 타임아웃, 때로는 정상) 무료 외부 API라는 정황이 강해짐(TODO.md에 이미 있던 "maplestory.io API 다운 시 fallback 처리" 항목과 같은 맥락).

**반영**: `DropTable.tsx`(검색 자동완성 아이콘/선택된 몬스터 아이콘/결과 카드의 render 이미지 3곳)와 `MonsterSelect.tsx`(선택된 몬스터 아이콘/드롭다운 목록 아이콘 2곳)의 자체 폴백 로직을 전부 제거하고 `handleMapleIoImageError`로 교체(`data-maple-code`/`data-maple-retry` 속성 추가 필요). `MonsterPanel.tsx`는 원래 폴백이 아예 없었어서 "애니메이션 실패 시 정적 아이콘으로, 그것도 실패하면 다중 버전 체인"으로 새로 추가. 어디에도 안 쓰여서 죽은 코드가 된 `getMobAnimatedFallbackUrl`(내용이 `getMobAnimatedUrl`과 완전히 동일했음)은 삭제.

**검증**: `npx tsc --noEmit`/`npx eslint src/`/`npx vitest run`(100개)/`npm run build` 전부 통과. 브라우저 도구가 없는 세션이라 실제 화면에서 깨진 아이콘이 복구되는 것까지는 확인 못 함 — 다만 이미 QuestBoard.tsx에서 검증된 것과 동일한 함수를 재사용하는 거라 리스크는 낮다고 판단.

**남겨둔 것**: 502/404로 명확히 깨지는 12종은 다중 버전 체인으로도 복구 안 될 수 있음(서버 자체 렌더링 실패로 보임) — 이런 애들은 `MOB_ICON_STATIC_OVERRIDES`처럼 정적 이미지로 수동 교체하는 게 유일한 해법인데, 이번엔 범위가 커서 착수 안 함(TODO.md에 12종 목록 기록).

적용 방식: `src/components/DropTable.tsx`, `src/components/MonsterSelect.tsx`, `src/components/MonsterPanel.tsx`, `src/lib/maplestory-io.ts` 수정(Zone A — DropTable/MonsterPanel/MonsterSelect은 원래 Zone A 소유 파일). `TODO.md` 갱신 예정. 커밋은 안 함(요청 시 진행).

### "둘 다 정상 스탯"인 47개 중복 그룹 — 오배정 없음 확인

앞선 두 배치가 "acc=0 플레이스홀더"였다면, 이번엔 애초에 양쪽 다 완전한 스탯을 가진 47개 이름 중복 그룹(마뇽/그리프/예티(6300000 vs 6300001/6300002)/다크예티/페페/주니어발록/크림슨발록/킹슬라임/머쉬맘 등)을 대상으로 드랍이 엉뚱한 쪽에 쏠려있는지 확인. 각 그룹의 두 mobCode가 가진 드랍 개수를 전부 뽑아봄 — 예를 들어 마뇽(8180000, 리프레)은 70개, 마뇽의 "기타 지역" 사본(9500174)은 5개; 페페(6130102, 리프레)는 60개, 그 사본들(6130103/9200022/9500142)은 각각 25/10/10개. 이런 식으로 **항상 "본체/필드 버전"이 더 많고 "PQ·기타지역 버전"이 더 적은 일관된 패턴**이라 오배정 징후 없음 — 서로 다른 진짜 콘텐츠가 각자 자기 드랍표를 갖고 있는 정상적인 모습으로 결론.

(item 이름 조회 시 `dropIndex.items`가 itemId가 아니라 배열 인덱스로 키가 잡혀있고 필드명도 `itemId`(소문자d)라 처음 짠 스크립트에서 이름이 전부 공백으로 나왔음 — 드랍 "개수" 비교만으로 결론 내는 데는 지장 없어 이름 조회 버그는 고치지 않고 넘어감.)

**결론**: legacy-mapledb 개별 오배정은 1차 배치에서 발견한 예티(6300001) 사례가 이례적인 케이스였을 가능성이 높고, 이름 중복으로 인한 광범위한 드랍 오배정 문제는 추가로 발견되지 않음 — 이 방향의 조사는 여기서 마무리.

적용 방식: 코드/데이터 변경 없음(조사만 수행). `TODO.md`에 결론 기록. 커밋은 안 함(해당 없음).

### 길드대항전(샤레니안) 몬스터 스탯 완성 — 5종 신규 추가, 14종 전체 노출

TODO.md "메이플랜드 2026-08-21 패치 길드대항전" 항목 처리(리서치 우선순위 1번 재선정). 8/21 패치노트 원문을 다시 열어보니 몬스터가 14종인데(악마 슬라임/머슬스톤/다크 머슬스톤/마스터 머슬스톤/퍼펫골렘/미스트 나이트/블랙 나이트/**주니어 가고일**/가고일/사자석상 A·B/기사석상 A·B/에레고스) 기존 TODO 목록엔 "주니어 가고일"이 빠져있었음 — 재확인 필요성 재확인.

**의외의 발견**: `data/monsters.json`을 직접 뒤져보니 14종 중 9종(주니어 가고일 포함)이 이미 완전한 스탯(acc/eva/needAcc/def/mDef 전부)으로 들어있었음 — TODO엔 "Monster 타입 필수 필드 확보 못 함"이라고 적혀있었는데 실제로는 상당 부분 확보돼있던 것. 출처는 불명(과거 세션에서 mapledb.kr로 가져다놓고 TODO 갱신을 놓친 것으로 추정) — 이 프로젝트에서 여러 번 반복된 "TODO.md가 실제 코드/데이터 상태와 어긋나 있다" 패턴이 여기서도 재현됨.

**mobCode 추리**: 이미 있는 9종의 mobCode가 9300019~9300028 구간에 몰려있는데 9300021 하나만 비어있음 — 패치노트의 몬스터 나열 순서(머슬스톤→다크 머슬스톤→마스터 머슬스톤)와 맞춰보면 "다크 머슬스톤=9300021"일 가능성이 높다고 추론. mapledb.kr에서 9300021을 직접 조회해 실제로 "다크 머슬스톤" 데이터가 나오는 것으로 추론이 맞았음을 확인 — 같은 방식으로 사자석상 A/B(9300029/9300030), 기사석상 A/B(9300031/9300032)도 순차 조회해 전부 찾아냄.

**신뢰도 확인**: 사자석상 A/B, 기사석상 A/B는 각각 서로 HP만 다르고 def/mDef/eva/needAcc가 완전히 동일하게 나왔는데, 이게 namu.wiki 검색 스니펫에서 확인했던 "사자 석상 A/B는 스킬과 체력만 다르고 나머지는 동일하다"는 설명과 정확히 일치 — 서로 다른 두 독립 소스가 같은 결론을 내서 신뢰도가 높다고 판단. maplelandzzul.gg에서도 레벨/HP/EXP가 mapledb.kr과 일치함을 교차검증.

**다크 머슬스톤 특이사항**: 레벨200/HP9,999,999/방어1,999/mDef1,999/회피999처럼 극단적으로 둥근 수치가 mapledb.kr 원문 그대로 확인됨. 배치 위치가 "신념의 방"(길드대항전 던전 특정 구간)인 걸 보면 실제로 잡으라고 만든 파밍 대상이 아니라 진행을 막는 장애물/벽 몬스터일 가능성이 높다고 판단(추정, 확정 아님) — 다만 수치 자체는 mapledb.kr 표시값을 그대로 반영(임의 보정 안 함).

**acc 공백**: mapledb.kr 페이지들에 명중률(acc) 항목 자체가 없어(회피율/필요명중률만 있음) 5종 전부 acc=0으로 처리 — 이 데이터셋 전반에 이미 존재하는 "acc 원본 자체가 비어있는 경우 다수" 패턴과 동일하게 취급, 근거 없이 추정하지 않음.

**반영**: `scripts/add-sharenian-statue-monsters.mjs` 신설(Zone D, 에델슈타인 때의 `import-edelstein-monsters.mjs`와 같은 "일회성 스크립트로 직접 patch" 패턴) — 다크 머슬스톤/사자석상 A·B/기사석상 A·B 5종을 `data/monsters.json`에 추가. `data/release-filters.json`/`data/planet/release-filters.json`의 `allowedMobCodes`에 14종 mobCode(9300019~9300032) 전부 등록해 노출(둘 다 편집 — 플래닛 쪽은 `build-planet-data.mjs`가 최초 1회만 복사하고 이후 자동 동기화 안 하는 파일이라 별도 수정 필요했음, `data.md` 참고). `node scripts/build-planet-data.mjs` 재실행으로 플래닛 `monsters.json`에도 5종 반영 확인. 8/28 플래닛 패치노트에 "길드대항전 추가, 에레고스/사자석상/기사석상 HP가 원작 대비 조정됨"이라는 문구가 있어 플래닛에도 실제 라이브 콘텐츠임을 재확인했으나 정확한 조정값은 원문에 없어 메랜값을 그대로 상속(플래닛 전용 오버라이드는 추후 정확한 수치 확보 시 추가).

**검증**: `npx tsc --noEmit`/`npx eslint src/`/`npx vitest run`(100개) 전부 통과. release-filter 판정 로직을 Node로 직접 재현해 14종 전부 노출되고, 이름 중복 플레이스홀더(주니어 가고일의 duplicate, mobCode 9300033)는 의도대로 계속 차단되는 것을 확인.

**반영 안 함**: 드롭 데이터 — mapledb.kr 페이지들에 드롭 정보 자체가 없었고, 공식 패치노트도 "마스터리북 5종은 길드대항전 콘텐츠 클리어 보상"이라고만 명시(몬스터 드롭 아님). 착수한다면 에델슈타인 때처럼 `monster_detail/<mobCode>` 페이지 직접 스크래핑이 다음 시도 후보 — TODO.md에 별도 항목으로 남겨둠.

적용 방식: `scripts/add-sharenian-statue-monsters.mjs` 신설(Zone D), `data/monsters.json`/`data/planet/monsters.json`(Zone D 산출물), `data/release-filters.json`/`data/planet/release-filters.json` 수정. `TODO.md` 갱신. 커밋은 안 함(요청 시 진행).

### 정령의 축복(플래닛) 슬라이더 상한 오류 정정 — "잠정치 200" → 공식 확인된 22

사용자가 "플래닛 맥스레벨 220 아냐?"라고 질문 — 확인해보니 실제로 8/28 패치로 캐릭터 레벨 상한이 200→220으로 늘어난 건 맞지만(이미 이번 세션 초반 8/28 패치노트 조사 때 확보해뒀던 내용), 계산기 코드엔 캐릭터 레벨 상한을 하드코딩한 곳이 아예 없어서(레벨 입력은 자유 입력) 이것 때문에 고칠 코드는 없었음.

대신 `onehit-calculator-client.tsx`의 `SPIRIT_BLESSING_BY_SERVER.planet.maxLevel = 200`이 눈에 띄었는데, 이건 2026-07-16 세션에 "플래닛 유저 글에서 정축 최대 200을 봤다"는 미검증 커뮤니티 정보로 넣어둔 값(당시 DEVLOG에 "정축 최대 200/여축 최대 120"으로 기록됨) — 지금 와서 보니 스킬 레벨이 아니라 **캐릭터 레벨(옛 만렙 200)을 착각해서 그대로 넣은 값**이었던 것으로 보임. 마침 이번 세션 초반에 확보해둔 8/28 패치노트 원문에 "정령의 축복 스킬 마스터 레벨이 22레벨로 확장(물리+22/마법+44/명중+22/회피+22)"이라고 공식적으로 명시돼있어, 코드의 `attPerLevel:1/mattPerLevel:2` 공식과 정확히 일치함을 확인 — 200을 22로 정정(공식 패치노트 근거 확보로 "잠정치" 딱지도 뗄 근거 마련, UI 라벨 문구는 이번엔 안 건드림).

검증: `npx tsc --noEmit`/`npx eslint src/` 통과(한 줄짜리 상수 변경이라 vitest 별도 재실행 안 함, 기존 100개 스위트에 이 값 직접 검증하는 테스트 없음).

적용 방식: `src/app/(routes)/calculators/onehit/onehit-calculator-client.tsx` 수정(Zone B). 커밋은 안 함(요청 시 진행).

---

## 2026-09-08

### 배틀메이지 신규 직업 추가 (한방컷 계산기), 자동 패치체크 루틴 소멸 발견

오랜만에 복귀한 사용자가 "메랜에 업데이트 있는데 확인해봤어?"라고 물어 패치 상태를 점검. 8/20에 만들어둔 자동 패치체크 클라우드 루틴(`trig_01Wf6e7JvS3biMsG2Me2q2Cn`)이 `RemoteTrigger` API에서 아예 404로 나와 — 이번엔 인프라 문제가 아니라 루틴 자체가 사라진 것으로 확인(원인 불명, PR도 0건). 사용자에게 상태 재확인 필요 사실만 보고, 대신 이 세션에서 수동으로 메랜(maple.land, WebFetch 가능)·플래닛(mapleplanet.co.kr, 이번에도 WebFetch 403) 8/14~9/8 패치노트를 확인.

**메랜 9/7 패치 핵심**: 레지스탕스 2차 전직 "배틀메이지" 신규 직업 출시("빅뱅 이전 2010년 5월 25일 KMST 데이터 기반"), 트리플/쿼드/데스/피니쉬 블로우 기본공격력 20/28/42/60 공개, 다크 라이트닝 250→300, 싸이클론 770→850. 플래닛은 WebFetch가 막혀 있었는데, 사용자가 직접 7/30~9/3 플래닛 패치노트 원문 전체를 대화에 붙여넣어줘서 그 범위는 확보(스크립트 상태 파일에 반영).

**배틀메이지 스킬 데이터 확보 과정**: namu.wiki는 이 세션 내내 403, 스킬 시뮬레이터(skill.mapleland.st)는 브라우저 자동화로 반복 타임아웃. 최종적으로 디시인사이드 메랜 갤러리 "스압) 빅뱅전 테섭 [T12135] 배틀메이지,와일드헌터 스킬 데이터"(낭냥2, 2026-09-04) 글을 브라우저로 직접 열람해 1~4차 스킬 전체를 끝까지 스크롤하며 확보. 첫 시도에서 "다크 라이트닝"을 4차로 착각해 못 찾았는데, 실제로는 3차였음 — 사용자가 로컬에 저장해둔 스크린샷(`~/Downloads/스크린샷 2026-09-08 110508.png`)으로 직접 확인해줘서 정정. 이후 재차 꼼꼼히 스크롤해 스태프부스터/블러드드레인/리바이브/컨버전/슈퍼바디 등 놓쳤던 버프 스킬들도 확인(계산기엔 미반영 대상).

**반영 방식 결정**: 다크 라이트닝/싸이클론은 마스터값만 상향된 걸로 판단(구버전 마스터값이 패치노트의 "이전" 수치와 정확히 일치) — 레벨 1~29는 구버전 표 그대로, 마스터(30)만 신규값으로 교체. 트리플/쿼드/데스/피니쉬 블로우는 "연타 방식→1회 입력시 자동 전타"로 메커니즘 자체가 개편돼 구버전 레벨별 표를 버리고, 패치노트 값을 레벨 무관 고정 데미지로 반영(`damageMapping.json`의 "드래곤 펄스"/"스나이핑"처럼 이미 있던 레벨-무관 고정값 패턴 재사용). 다크 제네시스는 패치노트 언급 없어 구버전 표 그대로.

**계산기 등록**: `data/raw/skills.raw.ts`에 "배틀메이지" 직업군 추가(스킬 7종: 트리플/쿼드/데스/피니쉬 블로우, 다크 라이트닝, 싸이클론, 다크 제네시스) 후 `node scripts/normalize-skills.mjs`로 재빌드. 한방컷 계산기(`onehit-calculator-client.tsx`)와 피격뎀 계산기(`TakenDamageCalculator.tsx`)에 아란과 같은 패턴으로 독립 직업군 등록 — INT/LUK 주부스탯, 배율 1.0(마법 계열), jobClass "magician". 코드 리딩 결과 `jobProfile.primary === "int"` 하나로 무기 공격력/무기 상수/스탯 계산 분기가 이미 전부 처리되고 있어(마법사 관련 하드코딩된 `jobGroup==="마법사"` 체크는 대부분 우회 가능), 실제 필요한 추가 분기는 `passiveMasteryBonus` 리셋 2곳뿐이었음. 배틀메이지가 같은 기간 플래닛 패치노트엔 없어 플래닛 서버에서는 아란의 반대 패턴(플래닛 전용 아님 → 메랜 전용)으로 스킬 목록을 "기본 공격"만 노출하도록 게이팅.

**검증**: `npx tsc --noEmit`/`npx eslint`/`npx vitest run`(100개) 전부 통과. 로컬 dev 서버 띄워 브라우저로 실제 확인 — 직업군에 배틀메이지 선택 시 무기 타입 선택 사라지고 INT/LUK+마력 입력으로 전환, 스킬 드롭다운에 7종 정상 노출, 다크 라이트닝 스킬레벨 29(245%)↔30(300%) 전환 시 데미지 값이 실제로 달라지는 것까지 확인.

**남은 일**: 8/21 길드대항전 신규 몬스터(레벨/HP/EXP는 mapledb.kr로 확보했으나 Monster 타입 필수 필드 acc/eva/needAcc/def/mDef 미확보, 사자석상·기사석상 mobCode도 미확인) — 사용자가 이번엔 반영 보류 결정. 배틀메이지 플래닛 출시 여부 미확인. 플래닛 8/28 전직업 대규모 밸런스 패치(팔라딘 블래스트, 아크메이지/비숍 빅뱅, 비숍 엔젤레이, 저격수 스트레이프, 신궁 스나이핑, 나이트로드 어벤져, 바이퍼 드래곤스트라이크 등)가 기존 damageMapping의 여러 스킬 수치와 충돌할 가능성 높은데 이번 세션에선 미착수 — 전부 TODO.md에 기록.

적용 방식: `data/raw/skills.raw.ts`/`data/skills/*.json`(재빌드) 수정, `src/app/(routes)/calculators/onehit/onehit-calculator-client.tsx`·`src/components/TakenDamageCalculator.tsx` 수정, `TODO.md`/`scripts/state/last-patch-check.json`/`DEVLOG.md` 갱신. 커밋은 안 함(요청 시 진행).

### 에델슈타인(9/7) 신규 몬스터 25종 반영

위 배틀메이지 작업 직후, "목록 자체를 못 구함"으로 남겨뒀던 에델슈타인 몬스터를 Maple Note(`mapnote`) 몬스터맵 스냅샷(`mapClass=에델슈타인`)에서 재확보. 이름/레벨/HP/EXP/속성/등장 맵(25종, mobCode 150000~8105005) 전체를 표에서 직접 옮겨적어 일회성 스크립트 `scripts/import-edelstein-monsters.mjs` 신설 — 스냅샷 HTML을 파싱해 `data/monsters.json`(신규 mobCode는 push, 기존 mobCode는 교체)/`data/monster-spawns.json`(스폰맵 연결)에 반영. 몬스터 상세페이지 스냅샷은 이번엔 확보 못 해 acc/eva/needAcc/def/mDef/watk/matk는 기존값(대부분 0, `existing` fallback) 유지 — 스크립트가 `detail-html-dir` 인자를 받아 나중에 상세 스냅샷이 생기면 재실행만으로 채워 넣을 수 있도록 설계해둠. 드롭 데이터는 이번 범위 밖(미반영).

검증: `node scripts/import-edelstein-monsters.mjs`로 파싱 개수(25종)가 하드코딩된 스폰맵 목록 개수와 일치하는지 스크립트 자체가 assert(불일치 시 throw). `npx tsc --noEmit` 통과(스크립트는 타입 체크 대상 아님, 순수 Node).

적용 방식: `scripts/import-edelstein-monsters.mjs` 신설(Zone D), `data/monsters.json`/`data/monster-spawns.json` 갱신(Zone D 산출물), `TODO.md`/`scripts/state/last-patch-check.json` 갱신. 커밋은 안 함(요청 시 진행).

### 에델슈타인 아이템 드랍 반영 + 몬스터 레벨 정정 + 공지 관리 기능(수정/고정 토글) 추가

에델슈타인 몬스터를 반영한 직후 사용자가 "아이템 드랍 정보 미반영" 상태를 지적, 실제로 처리 가능한 범위라 바로 착수.

**드랍 데이터**: 기존 `fetch-new-items-drop-data.mjs`는 "이미 알려진 아이템"은 재검사하지 않는 구조라 신규 몬스터가 기존 아이템을 드랍해도 못 잡는 구조적 한계 확인. 대신 몬스터 쪽에서 접근하는 `scripts/fetch-edelstein-drops.mjs` 신설 — `monster_detail/<mobCode>` 페이지의 GET 섹션(`item-box`/`drop-rate-box`)을 직접 파싱해 25종 전부 드랍 연결(39건). 정규식 첫 시도에서 non-greedy 옵셔널 그룹이 매칭을 건너뛰는 문제(드랍률 캡처 실패)를 겪어 `<a>` 블록을 통째로 잘라낸 뒤 2단계로 파싱하도록 수정. AF형/고장난 DF형 안드로이드/광석 이터가 배틀메이지 마스터리북(피니쉬 블로우/다크 제네시스/와일드 발칸)을 드랍하는 것으로 나와 오늘 추가한 배틀메이지 콘텐츠와 자연스럽게 맞아떨어짐 — 데이터 신뢰도의 방증. HEAD 대비 diff로 기존 드랍 663→691종, 유실 0건 재확인.

**레벨 정정**: 사용자가 "네오 도쿄" 유무를 묻는 과정에서 마플랜드 공식 9/7 패치노트 원문을 직접 열람하게 됐는데, 거기 명시된 에델슈타인 몬스터 레벨이 기존에 Maple Note 스냅샷으로 가져온 값과 7종에서 ±1~3레벨 차이가 남을 발견(순찰로봇 14→15, 이상한 이정표 16→17, 구렁이 18→19, 안전제일 31→30, 경비로봇L 79→78, 라칸 82→83, 고장난 DF형 안드로이드 96→95). 공식 패치노트가 가장 신뢰도 높은 소스라 즉시 `data/monsters.json`/`data/planet/monsters.json` 정정. hp/exp는 패치노트에 안 나와 있어 기존값 유지.

**공지 관리 기능**: `/admin/announcements`에 수정 기능이 없어 삭제 후 재등록만 가능했던 것을 지적받아 개선 — 폼에 `editingId` 상태를 추가해 "수정" 버튼 클릭 시 기존 값으로 폼을 채우고 저장 시 insert 대신 update, 취소 버튼으로 편집 모드 이탈 가능. 별도로 목록 전체 편집 없이 바로 켜고 끌 수 있는 "상단 고정/고정 해제" 토글 버튼도 행마다 추가(`is_pinned` 단독 업데이트). 이 자동화 브라우저 세션은 사용자 로그인 세션과 프로필이 달라 실제 클릭 테스트는 못 함 — `npx tsc --noEmit`/`npx eslint`로만 검증.

**네오 도쿄 조사(반영 안 함, 리드만)**: 사용자가 "네오 도쿄 있나?"라고 물어 조사 — KMS엔 원래 없던 해외서비스(JMS 등) 전용 콘텐츠임을 확인. 다만 arca.live "메이플랜드 채널"에서 2026-01-27 게시물이 "배틀메이지 | 메이플랜드 2.0" 태그로 네오 도쿄 관련 글을 묶어뒀던 것과, 커뮤니티가 "메랜엔 언제 나올라나" 식으로 계속 기대하던 정황을 확인 — 배틀메이지와 같은 로드맵으로 예고돼있었다는 뜻. 방금 확인한 9/7 공식 패치노트엔 네오 도쿄 언급이 없어 이번 패치엔 미포함 확정, TODO.md에 다음 패치체크 확인 항목으로 등록만 하고 반영은 안 함(출시 확정 전까지 데이터 만들 근거 없음).

검증: `npx tsc --noEmit`/`npx eslint`/`npx vitest run`(100개) 전부 통과.

적용 방식: `scripts/fetch-edelstein-drops.mjs` 신설(Zone D), `data/drop-index.json`/`data/item-detail-by.json`/`data/monsters.json`/`data/planet/monsters.json` 갱신, `src/app/(routes)/admin/announcements/page.tsx` 수정(Zone E), `TODO.md` 갱신. 커밋은 안 함(요청 시 진행).

---

## 2026-08-20

### Codex 리뷰 반영 — 퀘스트 로컬스토리지 이전 실패 처리 + 테스트 보강 + 문서 오타

같은 날 세션에서 만든 퀘스트 비로그인 로컬스토리지 기능(위 절 참고)에 대해 사용자가 Codex 리뷰를 받아 3건 지적됨.

1. **(중간) 로컬 퀘스트 이전 실패가 화면에 안 보임** — `loadTrackers`에서 로그인 시 로컬→서버 이전(`upsert`)이 실패해도 에러를 무시하고 서버 데이터만 그대로 렌더링해서, 로컬 데이터는 `localStorage`에 남아있는데도(이전 실패 시 `clearLocalTrackers` 미호출) 화면에서는 사라지고 사용자에게 안내도 없었음. 서버 목록에 없는 로컬 항목을 병합해 화면에서 계속 보이게 하고, `syncError`로 "이 기기에는 남아있으니 다시 로그인하면 재시도됩니다" 안내 추가.
2. **(낮음) 로컬스토리지 핵심 로직 테스트 부재** — `loadLocalTrackers`/`saveLocalTrackers`/`clearLocalTrackers`에 `export` 추가 후 `src/components/__tests__/questBoardLocalTrackers.test.ts` 신설(6개 테스트: 저장→복원 왕복, 담기 해제 후 사라짐, 손상된 JSON 폴백, SSR(window 없음) 환경에서 예외 안 던짐, 로그인 시 clear 확인). jsdom/testing-library 없이 vitest 기본 node 환경에서 `globalThis.window`에 최소 in-memory localStorage만 꽂아 검증(새 의존성 추가 안 함). React 컴포넌트 렌더링/클릭 시뮬레이션까지는 아니라 완전한 회귀 방지는 아니지만, 이번에 지적된 핵심 저장/복원 계약은 커버함.
3. **(문서) DEVLOG 오타** — "build-planet-data.mjs 순수 함수 8개"라고 적었는데 실제 export/테스트 대상은 7개 — 정정.

검증: `npx tsc --noEmit`/`npx eslint`/`npx vitest run` 전부 통과(기존 94개 + 신규 6개 = 100개).

적용 방식: `src/components/QuestBoard.tsx` 수정(이전 실패 처리 + export 3개), `src/components/__tests__/questBoardLocalTrackers.test.ts` 신설, DEVLOG.md 오타 수정(Zone C). 커밋은 안 함(요청 시 진행).

### 기술부채 — 테스트 커버리지 확충 (build-planet-data.mjs, takenDamage 계산기)

TODO.md "기술 부채" 항목 처리. 5개 항목 중 범위가 명확하고 리스크가 낮은 2개(테스트 부재)만 이번에 처리 — 나머지 3개(컴포넌트 재구성, API 에러 표준화, Web Vitals 활용계획)는 각각 별도 설계/우선순위 판단이 필요한 큰 작업이라 이번 세션 범위 밖으로 남김.

**`build-planet-data.mjs`**: 순수 함수 7개(`decodeAttributeCode`/`clampProbability`/`applyDropRateMultiplierToRewardMap`/`applyDropRateMultiplierToItemDetailBy`/`applyMonsterOverrides`/`appendNewMonsters`/`applyItemOverrides`)에 `export` 추가(동작 변화 없음, 파일 I/O를 하는 `main()`은 그대로 미export) 후 `scripts/__tests__/build-planet-data.test.mjs` 신설, 16개 테스트. 특히 TODO에 명시된 "`_`-prefix 필드 leak" 회귀 테스트(`applyMonsterOverrides`/`appendNewMonsters` 양쪽)를 핵심으로 작성 — `_source` 같은 주석용 메타 필드가 병합 결과에 새지 않는지 확인.

**`src/lib/calculators/takenDamage/`**: 빈 폴더로만 존재하던 `__tests__/`에 실제 테스트 3개 파일 신설(`standardPdd.test.ts`/`physical.test.ts`/`magical.test.ts`, 24개 테스트). `getStandardPDD`는 테이블 조회 로직이라 정확한 값으로 직접 검증(정확 일치/브라켓 보간/범위 밖 처리), `calcPhysicalTakenDamage`/`calcMagicalTakenDamage`는 공식 자체를 손으로 재검증하는 대신(부동소수 반올림 오차로 깨지기 쉬움) 방향성 속성으로 검증 — PDD/MDD가 높을수록 데미지 감소, 몬스터 공격력이 높을수록 데미지 증가, InvinciblePercent 100%면 최소치(1)로 clamp, PowerUpPercent 200이면 대략 2배 스케일, MaxPad/MaxMad/MaxDamage 등 limits가 실제로 clamp하는지, 마법사가 동일 스탯에서 다른 직업보다 보정계수가 커 데미지가 더 낮다는(코드상 0.3 vs 0.25 배율) 사실 등.

검증: `npx tsc --noEmit`/`npx eslint`/`npx vitest run` — 기존 54개 + 신규 40개(build-planet-data 16 + takenDamage 24) = 94개 전부 통과. `node scripts/build-planet-data.mjs` 재실행해 export 추가가 실제 산출물에 영향 없음도 재확인(diff 없음).

적용 방식: `scripts/build-planet-data.mjs`(export 추가만), `scripts/__tests__/build-planet-data.test.mjs` 신설(Zone D), `src/lib/calculators/takenDamage/__tests__/*.test.ts` 3개 신설(Zone B). 커밋은 안 함(요청 시 진행).

### Panel 컴포넌트 중복 정리 — `ui/Panel.tsx` → `PanelSurface`로 이름 분리

TODO.md "레거시/중복 정리" 항목 처리. `src/components/Panel.tsx`(title/tone/actions 받는 실제 앱 전역 사용 컴포넌트, 8개 파일에서 사용 중)와 `src/components/ui/Panel.tsx`(className만 받는 얇은 wrapper, `/ui/demo`라는 noindex 스타일가이드 페이지 1곳에서만 사용)가 동명이라 혼란 유발. 실제로는 서로 다른 API를 가진 별개 컴포넌트라 "중복 제거"보다는 "이름 충돌 해소"가 맞는 처리 — `ui/Panel.tsx`를 `ui/PanelSurface.tsx`로 이름 변경(export도 `PanelSurface`로), 유일한 사용처인 `src/app/(routes)/ui/demo/page.tsx`의 import/JSX 태그 갱신. `PanelHeader`/`TableGrid`/`Cells`와 조합해 쓰는 atomic 패턴 자체는 그대로 보존(스타일가이드 페이지가 실제 앱에서 이 패턴을 아직 안 쓰고 있다는 사실은 안 건드림 — 별도 결정 사항).

검증: `npx tsc --noEmit`/`npx eslint` 통과. 순수 rename이라 시각적 변경 없음(브라우저 확장 미연결로 직접 스크린샷 확인은 못 했으나 클래스/구조 변경 없어 리스크 낮음).

적용 방식: `src/components/ui/Panel.tsx` 삭제 → `PanelSurface.tsx` 신설, `ui/demo/page.tsx` 수정(Zone 무관 공용 컴포넌트, 사용처가 단일 데모 페이지라 Main 직접 처리). 커밋은 안 함(요청 시 진행).

### 메랜 전용 아란 스킬 데미지% 재조사 — 소스 부족으로 반영 보류

TODO.md 우선순위 높음 항목. 메랜에 아란이 실제로 라이브 중인지부터 재확인 필요(사용자 질문) — namu.wiki 검색 스니펫으로 "2026-04-30 릴리즈 확정, 5/11 공개 테스트" 확인, 이미 우리 데이터(리엔 몬스터/드롭)에도 반영돼 있어 라이브 콘텐츠임엔 의문 없음.

메랜 전용 스킬 데미지% 소스를 찾기 위해 `maplelandzzul.gg/skill-tree/aran`(메랜 전용 스킬트리 사이트, 신뢰도 검증된 이력 있음)을 직접 열어봄 — 마스터레벨 값만 제공: 더블스윙140%·트리플스윙250%·콤보스매쉬700%(최대10)·바디프레셔100%·콤보펜릴800%(2회공격,최대10)·롤링스핀120%(최대12)는 플래닛 저장값과 마스터값이 정확히 일치, 파이널차지는 다름(메랜90%·최대12 vs 플래닛70%·최대9 — TODO에 이미 있던 "서버 간 수치 차이 가능성"이 실측으로 재확인됨), 파이널토스는 페이지에 노출 안 됨.

레벨1~마스터 전체 표를 주는 소스는 못 찾음 — dcinside 메이플랜드 갤러리(스킬트리 순서 글은 있으나 %표 없음), namu.wiki(다른 직업들은 "Mapleland/직업/OO/스킬" 서브문서가 있는데 아란만 아직 없는 것으로 확인), 인벤(일반 KMS 정보라 메랜 자체 값과 다를 수 있음) 전부 실패. 마스터값이 6/8 일치한다고 중간 레벨까지 플래닛 커브를 그대로 재사용하는 건 사용자가 명시한 "실제값 확인 전 적용 금지" 원칙 위반이라 보류.

적용 방식: 코드 변경 없음, TODO.md에 재조사 경위와 부분 확인된 마스터값 기록.

### 핑크빈 EXP 3자 불일치 해결 — 원인은 메랜 원본 데이터 자체의 노후화

TODO.md에 "핑크빈 EXP 미해결"로 남아있던 데이터 정확도 항목 처리. 기존엔 조아요(50,000,000)/catalog(200,000,000)/메랜 원본×4배율(25,160,000) 3개 값이 서로 안 맞아 조아요 값을 낮은 확신도로 잠정 적용 중이었음(2026-07-08 세션).

**재조사 결과**: 메랜 원본(`data/monsters.json`) 자체가 낡은 값이었던 게 원인으로 확인됨. 핑크빈(mobCode 8820001)의 HP(2,100,000,000)는 모든 소스에서 정확히 일치해 같은 몬스터를 가리키는 게 확실한데, 메랜 전용 사이트 3곳(maplelandzzul.gg, 메이플노트 클래식, 웹 검색 종합)이 전부 EXP 50,000,000·물리방어 1,700·마법방어 1,930으로 일치했음(우리 저장값은 EXP 6,290,000·방어 70/70). 자체 드롭테이블에도 핑크빈 드롭 78종(리버스 등급 장비 포함)이 이미 존재해 라이브 콘텐츠임도 재확인(namu.wiki 검색 스니펫 하나가 "출시 예정"이라고 했으나 오래된 리비전 캐시로 판단, 자체 드롭 데이터가 이를 반증).

**결정적 교차검증**: 정정된 메랜 원본 EXP(50,000,000)에 플래닛 표준 4배율(`rateMultipliers.exp: 4`)을 적용하면 50,000,000×4=200,000,000 — TODO에 있던 catalog 값과 정확히 일치. 낡은 원본을 기준으로 계산했을 때만 3자가 어긋났던 것이고, 원본을 고치니 catalog가 처음부터 정답이었음이 드러남.

**반영**: `scripts/update-pinkbean-stats.mjs` 일회성 스크립트로 `data/monsters.json`의 핑크빈 exp(629만→5,000만)/def(70→1,700)/mDef(70→1,930) 정정 → `node scripts/build-planet-data.mjs` 재실행으로 플래닛 동기화 → `scripts/sources/planet/divergence-overrides.json`의 8820001 오버라이드를 조아요값(5,000만, 사실상 메랜 원본을 그대로 옮긴 것)에서 표준 4배율 적용값(2억)으로 교체, `_source` 주석에 재해결 경위 기록. acc/eva는 이번에도 확실한 소스를 못 찾아 기존값(0/0) 유지.

**영향 범위**: 메랜 피격뎀 계산기에서 핑크빈 상대 방어력이 70→1,700(24배)으로 바뀌어 계산 결과가 크게 달라짐 — 부정확했던 이전 값보다 정확해지는 방향. 사용자에게 방어력 변경의 파급력을 미리 확인받고 진행함.

**검증**: `npx tsc --noEmit`/`npx eslint`/`npx vitest run`(54개) 전부 통과. `git diff --stat`으로 `data/monsters.json`/`data/planet/monsters.json` 변경분이 핑크빈 항목 3필드만(6줄/파일) 반영된 것 확인, 나머지 플래닛 산출물(drop-index/item-detail-by/cube-index/release-filters)은 내용 변경 없어 스킵됨을 빌드 로그로 확인.

**적용 방식**: `scripts/update-pinkbean-stats.mjs` 신규(Zone D 일회성 패턴), `scripts/sources/planet/divergence-overrides.json` 직접 수정(Zone D 손수 관리 소스), `data/monsters.json`/`data/planet/monsters.json`은 스크립트 산출물로만 갱신(직접 편집 안 함). 커밋은 안 함(요청 시 진행).

---

### 퀘스트 추적기 — 비로그인 로컬스토리지 지원

TODO.md 우선순위 높음 항목 처리. 기존엔 "내 퀘스트 담기"/"완료하기"/"핵심 퀘스트 담기"/"내 퀘스트만 보기" 전부 비로그인 시 `/login`으로 강제 리다이렉트했음(`QuestBoard.tsx`) — 로그인 없이도 퀘스트 진행 상황을 추적하고 싶은 유저는 계정을 만들어야만 했음.

**구현**: `localStorage`(`mrhub:quest-tracker:v1`, `{questId: {isCompleted}}` 형태)에 직접 저장하도록 변경. 기존에 서버 트래커를 `Map<number, TrackerMapValue>`(`id`/`quest_id`/`is_completed`)로 관리하던 구조를 그대로 재사용 — 로컬 항목은 `id: "local:{questId}"`로 합성해서 렌더링/필터링 로직(담기 여부, 완료 여부, 정렬 등)을 손대지 않고 저장 위치만 분기(`userId` 유무로 판단). 4개 핸들러(`handleToggleTracked`/`handleToggleCompleted`/`handleToggleTrackedOnly`/`handleAddVisiblePriorityToTracked`)에서 로그인 강제 리다이렉트 제거.

**로그인 시 자동 이전**: 로그인하면 `loadTrackers`가 로컬스토리지에 남은 항목을 계정으로 upsert — 단 `ignoreDuplicates: true`를 줘서 서버에 이미 존재하는 퀘스트는 덮어쓰지 않음(로컬 상태로 서버의 완료 여부가 실수로 되돌아가는 걸 방지). 성공하면 로컬스토리지 비움, 이후엔 기존처럼 서버 동기화로 완전히 전환.

**안내 문구**: 하단 상태 배너를 "로그인 후 담을 수 있음"에서 "내 퀘스트 N개(이 기기에만 저장) / 로그인하면 여러 기기에서 동기화됩니다"로 변경 — 비로그인도 정식 기능이라는 걸 명확히 함.

**검증**: `npx tsc --noEmit`/`npx eslint src/`/`npx vitest run`(54개) 전부 통과. 개발 서버로 `/quests` 200 렌더 확인. **브라우저 확장 미연결로 실제 클릭(담기→새로고침→유지 확인)은 못 했음** — 다음 세션에서 확장 연결되면 마저 확인 필요.

**적용 방식**: `src/components/QuestBoard.tsx` 직접 수정(Zone C). 커밋은 안 함(요청 시 진행).

---

### 자동 패치체크 루틴 무력화 발견 + 수동 패치 점검 (반영 없이 확인만)

오랜만에 복귀한 사용자가 "업데이트된 내용 적용됐으면 좋겠다"고 요청. `gh`/GitHub API로 PR 목록을 확인해보니 2026-07-09부터 매주 도는 자동 패치체크 클라우드 루틴(`trig_01Wf6e7JvS3biMsG2Me2q2Cn`)이 실제로는 PR을 단 한 번도 만들지 못했음을 발견 — `RemoteTrigger`의 `list_runs`/`get_run_log`로 지난 6회 실행 로그를 확인.

**원인 1 — 네트워크 egress 차단**: 그 클라우드 환경에서 `WebFetch`/`curl`로 `maple.land`/`mapleplanet.co.kr`/`arca.live`/`namu.wiki`에 접근하면 전부 403 `EGRESS_BLOCKED` — 패치노트 원문을 못 읽고 `WebSearch` 스니펫에만 의존해야 하는 상태였음.

**원인 2 — GitHub 쓰기 권한 없음**: 2026-08-07 실행은 그래도 우회해서(WebSearch로 간접 확인) 로컬 커밋까지 만들었지만, `git push`와 GitHub MCP 쓰기 도구(`create_branch`/`push_files`/`create_or_update_file`) 전부 403 "Resource not accessible by integration"로 실패 — 로컬 커밋은 컨테이너 안에 갇힌 채 유실됨. 2026-08-14 실행은 원인 1에서 아예 막혀 시도조차 안 함.

두 문제 다 이 세션(대화형)에는 해당 안 됨 — 이 세션에서 직접 `maple.land`는 WebFetch로 정상 접근됨(`mapleplanet.co.kr`는 403이지만 사이트 자체의 봇 차단으로 보이고 WebSearch로는 스니펫 확인 가능). git push도 정상.

**수동 패치 점검 (2026-07-24 이후)**: 이 세션에서 직접 원문 확인.
- 메랜 8/7: "무릉도장"(무릉도원 38층 도전 던전, 최상층 보스 "소공") 신규 콘텐츠. 8/14: "웨딩 빌리지"(혼인 시스템, 계산기/드롭테이블 무관) + 마법사 계열 스킬 밸런스(매직 컴포지션/파이어 데몬/아이스 데몬 "기본 공격력 향상" — 정확한 %가 원문에 없음, 패럴라이즈 타겟 1→2/체인 라이트닝 전이 공식 변경은 수치는 있으나 계산기가 다타겟 메커니즘을 애초에 모델링 안 함).
- 플래닛 7/24: 아란 바디 프레셔 크리티컬 미적용 버그 수정(계산기에 크리티컬 스킬별 예외 로직이 없어 우리 쪽엔 원래도 영향 없었음), "석탄 가루" 드롭처 변경(폐광→엘나스, 그런데 우리 데이터엔 석탄 가루 자체가 없음). 8/18: 카오스 자쿰/혼테일 입장 제한 매일→주2회(파밍 매니저가 아직 placeholder라 트래킹 기능 자체 없음), 아란 밀착 몹 버그 수정 등.

**결론 — 반영한 데이터 변경 없음**: 확인된 항목 전부 (a) 정확한 수치가 원문에 없거나, (b) 계산기/드롭테이블이 애초에 추적 안 하는 메커니즘이거나, (c) 우리 데이터셋에 해당 아이템/기능이 없어서 반영할 대상 자체가 없었음. "수치 명시 안 되면 추측 금지" 원칙에 따라 전부 TODO.md에 확인 필요 항목으로만 기록(위 "신규 콘텐츠/패치 확인 필요" 절 참고).

**후속 조치**: `scripts/state/last-patch-check.json` 신규 생성(이전엔 8/7 실행이 `data/.last-patch-check.json`으로 만들려다 push 실패로 유실됐던 파일 — 이 세션에서 커밋 예정). TODO.md에 자동 루틴의 두 인프라 문제를 "사용자 조치 필요" 항목으로 기록 — claude.ai 쪽에서 네트워크 도메인 허용 설정과 GitHub App 쓰기 권한을 확인해야 다음 실행부터 정상화됨.

**Codex 리뷰로 발견·수정한 문제 3건(같은 세션 내)**:
1. 체크포인트 날짜만 전진시키면 자동 루틴이 "이미 처리한 패치"로 오인해 위 보류 항목들(무릉도장, 마법사 스킬 정확한 수치, 석탄 가루 등)을 영구히 재검토 안 할 위험 — 체크포인트에 `pendingReview` 배열을 추가해 보류 사유를 남기고, 나중에 정보가 확보되면 이 목록에서 제거하는 방식으로 변경.
2. `data/.last-patch-check.json`으로 만들었던 최초 버전이 AGENTS.md의 "`data/*.json` 직접 편집 금지" 규칙과 정면 충돌(스크립트 산출물이 아닌 파일을 `data/`에 손으로 만듦) — `scripts/state/last-patch-check.json`으로 위치 이동, `data.md`에 이 파일의 성격(에이전트 실행 상태, 게임 데이터 아님)을 명문화.
3. 이번 절 초안에 "이번엔 정상 커밋"이라고 과거형으로 적었는데 실제로는 아직 커밋 전이었고(작업 트리에만 존재), 플래닛 원문 접근 여부도 TODO.md 초안엔 "직접 접근"으로, 여기엔 "403이라 WebSearch로만"이라고 서로 다르게 적혀있던 모순 발견 — TODO.md 쪽 문구를 사실대로 정정(플래닛은 WebSearch 스니펫 근거, 원문 미대조).

**적용 방식**: TODO.md/DEVLOG.md/data.md 직접 수정, `scripts/state/last-patch-check.json` 신규 생성(빌드 스크립트 산출물이 아닌 에이전트 상태 파일이라 `data/*.json` 규칙 적용 대상 아님). 검증: 데이터/코드 변경이 없어 `tsc`/`eslint`/`vitest` 재실행 불필요.

---

## 2026-08-03

### 한방컷 계산기 — 공격 모션 제거, 해적 직업 개별 분리

사용자 요청 2건.

**공격 모션(베기/찌르기/자동) 제거**: 도끼/둔기/폴암/창 무기에서 선택하던 공격 모션 UI와 관련 로직(`weaponMotionConstants`/`weaponMultiplierRange`/`motionWeightedAvgDamage`/`motionDamageRanges`)을 전부 삭제. 기존 "자동" 모드가 계산하던 평균값은 표준 무기 min~max 범위(`weaponConstants`) 계산으로 자연스럽게 대체됨 — 다른 무기 타입(한손검/너클 등)이 원래 쓰던 방식과 동일해진 것. `ResultPanel.tsx`의 베기/찌르기 상세 breakdown 표시도 함께 제거.

**해적 직업 "인파이터/버커니어/바이퍼" 묶음 표기를 개별 6종으로 분리**: 사용자가 "직업칸에 이상한 거 들어있다"고 지적 — 확인 질문 결과 해적 직업군 표기 방식을 말한 것으로 확인(처음엔 아란 관련 지적인 줄 알았으나 사용자가 직접 정정: 아란은 원래대로 단일 유지, 해적이 분리 대상). `jobOptionsByGroup.해적`을 `["인파이터/버커니어/바이퍼", "건슬링거/발키리/캡틴"]`(묶음 2개) → `["인파이터","버커니어","바이퍼","건슬링거","발키리","캡틴"]`(개별 6개)로 변경. 스킬 데이터(`data/skills/mainSkillMapping.json`)는 여전히 3단계 묶음 키로만 존재해서(빌드 산출물, 직접 편집 금지 대상) 개별 직업명 선택 시에도 `skillKey`/`jobProfile` 조회를 묶음 키로 alias하도록 처리 — 스피어맨 계열의 기존 alias 패턴(`job.includes("스피어맨") → "스피어맨/드래곤나이트/다크나이트"`)과 동일한 방식. 레거시 저장 프리셋(예전 묶음값)도 같은 alias 로직으로 커버됨.

**작업 중 발견한 버그(수정 완료)**: 검증 중 직업군을 "해적"으로 바꾸면 50ms 안에 "아란"으로 되돌아가는 버그 발견 — 지난 세션 아란 재분류 작업 때 레거시 프리셋 보정용으로 추가했던 `useEffect`(`if (job === "아란" && jobGroup !== "아란") setJobGroup("아란")`)를 `TakenDamageCalculator.tsx`에서는 `react-hooks/set-state-in-effect` 린트 에러 때문에 제거하고 복원 지점(`applyQuickSnapshot`/localStorage restore)으로 옮겼었는데, 이 파일(`onehit-calculator-client.tsx`)에서는 같은 작업을 깜빡하고 effect를 안 지운 채 복원 지점 patch만 추가로 얹어놨던 것 — 두 로직이 공존하면서 "nextJob 리셋 effect"와 매 렌더마다 경합해 사용자가 수동으로 다른 직업군을 선택해도 즉시 "아란"으로 튕기는 상태가 됐었음. 남아있던 `useEffect`를 제거해 해결(복원 지점 patch만으로 충분).

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(54개) 전부 통과. 로컬 서버에서 브라우저 콘솔 스크립트로 직업군 해적→개별 직업(캡틴) 선택, 아란 그룹 진입/이탈 왕복까지 확인 — 정상 동작.

**적용 방식**: `src/app/(routes)/calculators/onehit/onehit-calculator-client.tsx`, `src/components/ResultPanel.tsx` 직접 수정(Zone B). 커밋은 안 함(요청 시 진행).

---

### Codex 리뷰 5개 항목 — 우선순위 결정

사용자가 Codex에 이 세션의 변경분 리뷰를 맡겨 5개 항목이 나옴. 항목별 처리 방침:

1. **미커밋 변경 검증** — 완료. drop-index.json 유실 사고 발견·복구 포함(아래 항목들 참고).
2. **운영 알림(Resend/Cloudflare 환경변수) 문제** — 환경변수 등록 자체는 사용자 권한 필요(Cloudflare 대시보드)라 이 세션에서 처리 불가. 코드 쪽(어드민 화면에 "알림 미설정/전송 실패" 표시)은 비용 대비 가치 있다고 판단해 다음 작업으로 보류 — `TODO.md`의 "배포 / 인프라" 절에 이미 추적 중.
3. **데이터 정확도(핑크빈 EXP, 주문서 드롭률, 큐브 확률)** — 이미 `TODO.md`에 추적 중, 이번 세션 범위 아님.
4. **퀘스트 비로그인 로컬 저장** — `TODO.md` 우선순위 높음 항목, 다음 기능 후보로 유지.
5. **문서 정합성(TODO.md 자기설명 위반/CLAUDE.md schema.sql 설명/project-notes.md RSC 설명/Panel.tsx 중복)** — 전부 텍스트 정합성 문제고 실제 버그·막힌 작업은 아니라 이번엔 보류. 사용자 확인: "필요 없음, 나중에".

### 계산기 직업 분류 수정 — 아란을 "전사" 하위가 아닌 독립 직업군으로 재분류

사용자 지적: 아란은 시그너스 기사단처럼 그 자체로 독립된 직업군이지 전사(파이터/페이지/스피어맨 계열)의 하위 분화가 아님 — 혼(魂) 시스템 자체가 1~4차 전직 구조가 없는 단일 직업이라 "전사" 안에 끼워넣는 게 구조적으로 틀렸음(2026-07-31 아란 최초 추가 당시 "전사 그룹으로 등록"한 걸 그대로 따른 것이 원인).

**한방컷 계산기(`onehit-calculator-client.tsx`)**: `jobGroups`에 "아란" 추가(전사/마법사/궁수/도적/해적/시그너스와 동급), `jobOptionsByGroup.전사`에서 "아란" 제거하고 `아란: ["아란"]` 단일 그룹 신설. `weaponTypesByGroup`도 이전엔 jobGroup이 "전사"라 무기 타입 드롭다운에 전사 8종 무기가 다 뜨고(실제로는 이펙트로 강제 "폴암" 고정) 있었는데, `jobGroup === "아란"` 분기를 추가해 애초에 "폴암" 하나만 노출하도록 정리.

**피격뎀 계산기(`TakenDamageCalculator.tsx`)**: 같은 구조로 `jobGroups`/`jobOptionsByGroup` 수정. 여기서 더 중요했던 건 `jobClass: JobClass` 매핑(전사→"warrior", 마법사→"magician" 등, 나머지는 전부 "beginner"로 폴백)에 "아란"이 빠지면 데미지 계산 자체가 초보자 프로필로 폴백되는 심각한 회귀가 날 뻔했던 부분 — `jobGroup === "전사" || jobGroup === "아란"` 조건으로 수정해 방지.

**레거시 프리셋 마이그레이션**: 기존에 저장된 퀵 프리셋/프로필(localStorage, 캐릭터 프리셋 DB)엔 `jobGroup: "전사", job: "아란"` 조합이 남아있어, 그대로면 복원 시 "직업" 드롭다운에 없는 값이 선택되는 불일치 상태가 됨. 처음엔 `useEffect`로 사후 보정하려 했으나 `react-hooks/set-state-in-effect` 린트 에러 발생(계단식 렌더 유발 패턴) — effect 대신 각 복원 지점(퀵 프리셋 `applyQuickSnapshot`, 한방컷의 `localStorage` 프로필 복원) 안에서 직접 `job === "아란"`이면 `jobGroup`도 같이 "아란"으로 맞춰주는 방식으로 수정. 실제 브라우저에서 이 정확한 시나리오(과거에 저장된 전사+아란 조합)가 있던 프로필로 재현·자동 보정 확인.

**손대지 않은 부분**: 큐브 빌더는 애초에 전사/마법사/궁수/도적 4개 굵은 단위로만 프로필을 관리하고 아란 전용 옵션 자체가 없이 "전사" 프로필을 그대로 재사용하는 구조(2026-07-31 결정 유지)라 이번 변경 대상 아님.

**검증**: `npx tsc --noEmit`, `npx eslint`, `npx vitest run`(54개) 전부 통과. 로컬 서버에서 한방컷 계산기 실측 확인 — 실제 브라우저에 저장돼있던 구버전 프로필(jobGroup=전사/job=아란)이 페이지 로드 시 자동으로 "아란" 직업군으로 보정되고 무기 타입도 "폴암" 하나만 노출되는 것까지 확인.

**적용 방식**: `src/app/(routes)/calculators/onehit/onehit-calculator-client.tsx`, `src/components/TakenDamageCalculator.tsx` 직접 수정(Zone B). 커밋은 안 함(요청 시 진행).

---

### 카에데성(일본 여행) 몬스터 전체가 드랍테이블에서 숨겨져 있던 문제 수정

유저 피드백 2건("드랍테이블 카에데성 추가해주세요", "하급닌자/쿠노이치 눈장식 종류 주문서가 검색되지 않아요") 처리 중 하나의 원인으로 수렴됨. 조사 결과 카에데성 데이터 자체는 `data/monsters.json`/`data/drop-index.json`에 이미 정상적으로 존재했음(까마귀/불너구리/구름여우/하급닌자/중급닌자/상급닌자/닌자두목/쿠노이치/아시가루/갑주무사 등 mobCode 9400xxx 17종, region: "해외여행: 일본") — 다만 전부 `blockedMobCodeMin: 9,000,000` 필터에 걸려 화면에서 통째로 숨겨져 있었음(아란 튜토리얼 무루 9300383과 동일한 오탐 케이스, `data.md` 참고). 사이드바 지역 카운트로 실측 확인(해외여행: 일본 (1) → (18), 유일하게 안 걸려있던 대나무 무사 1종만 노출 중이었음).

`data/release-filters.json`/`data/planet/release-filters.json`의 `allowedMobCodes`에 17종 mobCode 전부 추가해 해제. Planet 쪽엔 몬스터 카탈로그(680종) 병합으로 같은 mobCode 대역에 "Extra A/B", "Slot Machine", "Emperor Toad" 등 미검증 placeholder 영문 몬스터도 섞여 있었으나 이번엔 손대지 않음(스코프 밖 — 별도 데이터 정확도 이슈).

**눈장식 명중률 주문서 검색 안 되는 문제(피드백 2번째 항목)**: itemId 2040200/2040201/2040202("눈장식 명중률 주문서" 10%/60%/100%)는 드랍 인덱스에 이미 연결돼 있었지만, `build-drop-index.mjs`의 이름 조회가 KMS API에서 이 아이템을 못 찾아(해당 itemId가 KMS 389/284 어느 버전에도 없음, GMS엔 존재) GMS 영문 폴백 이름("Scroll for Eye Accessory for Accuracy 60%" 등)이 그대로 들어가 있었음 — 한글 검색으로 안 잡히던 원인. `src/data/mapledb/equips.js`(이름 조회 1순위 소스)에 3종 한글명을 수기 추가함 — 이 소스 수정 자체는 유지, 다음에 정말로 전체 재빌드를 해야 할 일이 생기면 이번 같은 문제가 재발하지 않도록.

**주의(사고 후 수습): `npm run build:drop-index` 재실행이 기존 일회성 보스 데이터를 지워버림** — 처음엔 data.md 문서대로 `npm run build:drop-index`를 재실행해서 반영했는데, 이 명령이 `data/drop-index.json`을 스크래핑 소스(`drops-parsed.json`/`item-detail-by.json`)만 가지고 처음부터 다시 생성하는 방식이라, 과거 세션에서 일회성 스크립트(`add-chaos-horntail.mjs`, `update-balrog-drops.mjs`, `add-aran-rien-monsters.mjs` 등)로 **산출물 파일에 직접 patch**해뒀던 카오스 자쿰(8888889, 49→0건)/카오스 혼테일(8810118, 38→0건)/발록 추가 무기 21종(64→38건)/아란 리엔 몬스터 드롭(무루 등, 1→0건)이 전부 통째로 사라짐 — `git diff --stat`으로 drop-index.json 두 파일에서 약 2,800줄이 순감소한 걸 보고서야 발견. `git checkout -- data/drop-index.json data/planet/drop-index.json`으로 되돌린 뒤, 전체 재빌드 대신 3개 itemId의 `name` 필드만 직접 patch하는 스크립트로 최소 반영(diff 6줄/파일)으로 수정. **교훈**: 이 프로젝트의 일회성 보스/이벤트 데이터 스크립트들은 스크래핑 소스가 아니라 산출물(`data/drop-index.json`)에 직접 기록하는 구조라, `build:drop-index`(또는 그 체인인 `build:planet-data`)를 재실행하면 이 데이터들이 소스 어디에도 없어서 전부 유실됨 — data.md의 "일회성 보스 데이터 보강 스크립트" 절엔 "스크립트 재실행 시 위험"만 적혀있고 "기반 파이프라인(build:drop-index) 재실행 시에도 유실된다"는 경고가 없었음, TODO.md에 추가함.

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(54개) 전부 통과. `git diff --stat`으로 drop-index.json 변경분이 3개 아이템 이름만 바뀐 6줄/파일로 축소된 것 확인, 카오스 자쿰/카오스 혼테일/발록/무루 드롭 건수를 정정 전(HEAD) 수치와 재대조(49/38/64/1건 모두 일치). 로컬 서버에서 드랍테이블 사이드바 지역 카운트 변화(1→18)로 카에데성 언블록 확인, 검색 드롭다운에서 "눈장식 명중률 주문서" 3종이 아이콘과 함께 뜨는 것 확인. 이후 브라우저 확장 세션이 불안정해져(타이핑 입력이 아예 반영 안 됨, 페이지와 무관한 도구 자체 문제로 판단) 아이템→몬스터 역방향 목록 클릭 확인은 코드 리뷰로 대체(`drop-table-lookup.ts`의 `resolveItemMonsters`가 `itemDetailBy.itemsByItemId`를 우선 사용하는데 이 소스에 이미 mobId 9400400 등이 들어있었고, `DropTable.tsx`의 `isReleasedMobCode` 필터도 이번 allowedMobCodes 추가로 통과하게 됨을 코드로 확인).

**적용 방식**: `src/data/mapledb/equips.js`(Zone D 입력 소스) 수정 + `data/drop-index.json`/`data/planet/drop-index.json`은 전체 재빌드 대신 3개 itemId 이름만 직접 patch, `data/release-filters.json`/`data/planet/release-filters.json` 수동 수정. 커밋은 안 함(요청 시 진행).

---

## 2026-07-31

### 아란(Aran) 직업 추가 — 1차 반영(리엔 몬스터/드롭 + 계산기 등록, 스킬 데미지%는 이후 세션에서 반영 — 아래 후속 절 참고)

사용자가 "플래닛이랑 메랜에 아란 추가하긴해야해"로 시작한 작업. 코드/문서 전체 grep 결과 아란 관련 문자열이 0건이라 완전한 사각지대였음 — 착수 전 서버 존재 여부부터 확인이 필요했음.

**서버 실존 확인**: 웹 검색으로 메이플플래닛은 전용 공지("신규 직업군 [아란] 관련 안내", `mapleplanet.co.kr/board/notice/649`)가 있고, 메이플랜드는 2.0 업데이트(2026-07-14경 정식 출시)에 아란이 포함되어 있음을 확인 — 가상의 미래 콘텐츠가 아니라 이미 라이브 서비스 중인데 이 프로젝트 데이터만 못 따라간 상태였음. 두 서버 다 프리빅뱅(KMS 1.2.9x) 기반이지만 아란/리엔 원작 콘텐츠 자체가 KMS 빅뱅 이전(2009-11) 출시라 시기적으로도 부합.

**범위 결정**: 사용자에게 "계산기만" vs "계산기+리엔 지역 데이터 전체" 중 선택받아 후자로 진행. 병렬 리서치(fork 3개: 스킬 데미지%/리엔 몬스터·드롭/구인모루 퀘스트라인)로 착수.

**리엔 몬스터 6종 반영 (완료)**: 무루(100130)/무루파(100131)/무루피아(100132)/무루무루(100133)/무루쿤(100134)/튜토리얼 무루(9300383). 흥미롭게도 `data/planet/monsters.json`엔 이미 이 6종이 부산물로 존재했음(`scripts/sources/planet/monster-catalog-data.js` 680종 카탈로그 경유, 의도적 큐레이션이 아니라 우연히 포함돼있었던 것) — 메랜 원본(`data/monsters.json`)에만 없었음.

스탯 소스 검증 중 충돌 발견: 카탈로그 값(=플래닛 기존 배포값)과 `maplestory.io` KMS/389 라이브 API 값이 HP/EXP/acc/watk/matk 전부 다름. 라이브 API는 명중(acc) 레벨 스케일링이 사라지고 순수 물리 몹인데도 마공(matk)이 붙는 등 포스트빅뱅 재조정 특징을 보여, 프리빅뱅 스타일(레벨별 acc 스케일링, 물리몹 matk=0)과 부합하는 카탈로그 값을 채택 — 이미 플래닛에 배포된 값과의 일관성도 근거. `scripts/add-aran-rien-monsters.mjs`(발록/카오스혼테일 패턴 그대로) 신규 작성해 반영, 드롭은 몬스터당 "털뭉치" 1종씩(확률 출처 없어 `prob` 비움, "정보 없음" 표시) 추가. `node scripts/build-planet-data.mjs` 재실행으로 플래닛도 동기화 — 이제 플래닛 쪽도 카탈로그 대신 메랜 원본을 그대로 상속하는 정식 경로로 전환됨. 브라우저로 메랜/플래닛 양쪽 드롭테이블에서 "무루" 검색 확인 완료.

**계산기 3종에 아란 등록 (완료, 스킬 데미지%는 미포함)**: 아란은 일반 전사처럼 1~4차 전직명 체계가 아니라 혼(魂) 시스템(레벨 10/30/60/100/200 해금, 전직 UI 없음)이라, `mainSkillMapping` 등의 "X/Y/Z" 슬래시 연결 키 컨벤션이 안 맞음 — 2026-07-16에 추가된 시그너스 기사단 5종(단일 클래스 구조)이 이미 같은 문제를 겪었을 것으로 보고 그 키 포맷을 확인해 동일하게 `"아란"` 단일 키로 통일. 한방컷/피격뎀 계산기에 전사 그룹으로 등록, 폴암 무기 고정, STR 프로필 재사용 — 스킬 데이터가 없는 상태에서도 기존 fallback 로직으로 "기본 공격" 계산은 정상 동작. 큐브 빌더는 직업군 단위(전사/마법사/궁수/도적)로만 프로필을 관리해 코드 변경 불필요.

**스킬 데미지% — 반영 보류**: 나무위키(구스킬 문서)/StrategyWiki(403)/AyumiLove/넥슨 공식 아카이브/matip.kr(경쟁 계산기, 아란 미구현 확인)/maplelandzzul.gg/인벤 총 7개 출처를 조사했지만 레벨 1~마스터 전체 표를 제공하는 곳이 하나도 없었고(대부분 마스터값 또는 시작/끝 값만), 있는 값들끼리도 서로 상충(더블 스윙 나무위키 300%×2타 vs 넥슨 아카이브 102%→140% vs zzul.gg 140%). 중간 레벨 값을 보간해서 채우는 건 "근거 없는 수치 절대 금지" 원칙 위반이라 이번 반영에서 스킬 10종(더블스윙/트리플스윙/콤보스매쉬/파이널차지/파이널토스/오버스윙/파이널블로우/콤보템페스트/롤링스핀/바디프레셔) 전부 제외 — UI엔 "(잠정치, 기본 공격만 지원)" 라벨로 명시. 상세는 TODO.md "아란(신규 직업) 콘텐츠" 절 참고.

**구인모루 퀘스트라인 — 반영 안 함**: 순서/이름(13단계, 나무위키)은 확보했지만 NPC/조건/보상 수치를 확인 못했고, `scripts/import-quests-from-turbopack.mjs`가 게임 클라이언트 덤프 전용 파서라 위키 텍스트를 그대로 못 넣는 구조적 문제도 있음 — TODO.md에 남기고 이번엔 손대지 않음.

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(54개) 전부 통과. 리엔 몬스터는 브라우저로 메랜/플래닛 양쪽 드롭테이블 확인, 계산기는 브라우저로 아란 선택→기본 공격 계산 확인.

**적용 방식**: `scripts/add-aran-rien-monsters.mjs` 일회성 스크립트(Zone D), `data/raw/skills.raw.ts`의 `weaponMapping`에 구조만 추가(Zone D), 계산기 3종 직접 수정(Zone B). 커밋은 안 함(요청 시 진행).

### 후속 — 스킬 데미지% 실측 반영 + 컴퓨터 재시작 이후 발견된 버그 3건 수정

같은 날 세션이 컴퓨터 재시작으로 중단됐다가 재개됨. 중단 시점에 이미 스킬 데미지% 출처(디시인사이드 페리온 갤러리 실측 캡처, `no=65932`)를 확보하고 반영 에이전트를 돌리던 중이었으나 API 세션 한도로 실패한 상태였음 — 재개 후 동일 작업을 재실행해 9개 스킬(더블스윙/트리플스윙/콤보스매쉬/파이널차지/롤링스핀/콤보펜릴/파이널토스/파이널블로우/바디프레셔) 반영 완료. 상세 수치·출처·제외 사유는 TODO.md "한방컷 계산기 — 스킬 미구현 항목"의 아란 항목 참고.

이후 사용자가 Codex에 리뷰를 맡겨 버그 3건이 확인됨:
1. **피격뎀 계산기가 아란에게 아킬레스/파워가드를 잘못 적용** — 두 스킬 다 일반 전사 전용인데 `jobGroup === "전사"` 조건만으로 판별해 아란도 걸림. `job !== "아란"` 조건 추가해 수정(`TakenDamageCalculator.tsx`).
2. **튜토리얼 무루(mobCode 9300383)가 드롭테이블에서 안 보임** — `blockedMobCodeMin: 9000000` 필터에 걸림(9백만 이상 mobCode 일괄 차단). `release-filter.ts`에 `allowedMobCodes` 명시적 예외 목록을 신규 지원하도록 추가하고 `data/release-filters.json`/`data/planet/release-filters.json` 양쪽에 9300383 등록.
3. **플래닛 실측 스킬 데미지%가 메이플랜드 계산기에도 그대로 노출** — 스킬 데이터 출처가 플래닛 인게임 캡처 하나뿐인데 한방컷 계산기가 서버 구분 없이 같은 `damageMapping`을 공유해 메랜에도 적용됨(파이널 차지는 이미 서버 간 수치 차이 가능성이 TODO에 기록돼 있던 상태). `onehit-calculator-client.tsx`의 `skillOptions`를 `server` prop 기준으로 분기해 `server !== "planet"`일 땐 아란이 "기본 공격"만 노출하도록 수정, 직업 선택 드롭다운 라벨도 서버별로 문구 분리("플래닛 실측 기반" vs "플래닛 실측치만 확보됨 — 메랜은 기본 공격만 지원").

검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(54개) 전부 통과.

### 후속 2 — Codex 재검토로 추가 버그 3건 발견·수정 (아란 범위 밖, 기존 전사 3계열 전체에 영향)

Codex에 2차 리뷰를 요청해 다음을 발견:

1. **퀵 프리셋으로 메랜 아란의 스킬 제한 우회 가능** — `onehit-calculator-client.tsx`의 프리셋 로드 로직이 저장된 `skillName`을 `skillOptions` 검증 없이 그대로 복원해서, 플래닛에서 저장한 프리셋(예: 더블 스윙)을 메랜에서 불러오면 서버 분리 로직을 우회해 계산됨. `skillName`이 `skillOptions`에 없을 때 자동 보정하는 범용 검증 effect로 수정(아란 전용 특수 처리가 아니라 프리셋 복원 전반에 적용되는 일반 보정이라 다른 legacy 프리셋 이슈도 같이 방지됨).

2. **아킬레스/파워가드 계열 오적용** — 지난 수정에서 `job !== "아란"`만 걸었는데, Codex가 "파워가드가 스피어맨 계열에도 적용된다"고 재지적. 이 시점에 직접 재검증(웹서치)한 결과 Codex의 제안("아킬레스는 전사 공통, 파워가드만 파이터·페이지 계열")도 부정확함을 확인 — 아킬레스가 전사 3계열 공통 4차 스킬이 된 건 **빅뱅 이후** 리워크(나무위키 팔라딘 RED-이전 문서: "전직 후 자동 습득... 빅뱅 이후에 추가된 것으로 보임")이고, 이 프로젝트가 타겟하는 프리빅뱅 원작에서는 아킬레스=파이터/크루세이더/히어로 전용, 파워가드=페이지/나이트/팔라딘 전용(선행스킬 "위협"이 페이지 2차 스킬)이었음 — 스피어맨/드래곤나이트/다크나이트 계열은 원래 둘 다 없음. `TakenDamageCalculator.tsx`를 `job === "파이터/크루세이더/히어로"`(아킬레스)/`job === "페이지/나이트/팔라딘"`(파워가드)로 각각 정확히 제한 — 이건 아란 이전부터 있던 버그라 스피어맨/드래곤나이트/다크나이트, 페이지 계열, 파이터 계열 기존 유저 계산 결과도 함께 바뀜(전부 정확해지는 방향).

3. **`allowedMobCodes` 문서 누락** — `data.md`의 필터 절에 신규 옵션과 판정 순서(`blockedMobCodes` → `allowedMobCodes` → `blockedMobCodeMin`) 추가.

검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(54개) 전부 통과.

---

## 2026-07-24

### 메이플 플래닛 드랍테이블 노출 부족 — 내부 링크 보강

사용자가 "플래닛 드랍테이블 퍼포먼스/노출이 안 좋다"는 분석 결과를 전달해서 원인 조사. `/planet/drop-table` 자체는 콘텐츠(플래닛 전용 안내 문단)와 sitemap 등록까지 돼 있었지만, **내부 링크가 `/planet` 랜딩 페이지 하나뿐**이었음 — 메랜 홈에서 2클릭 거리(홈→`/planet`→드랍테이블, 메랜 자체 `/drop-table`은 1클릭), 메랜 `/drop-table`/`/services/drop-table`/`/mapleland-vs-planet` 어디서도 플래닛판으로의 링크가 없었음. 사이드바 네비도 이미 `/planet/*` 경로에 들어와 있을 때만 플래닛 링크가 뜨는 구조라(`sidebar-shell.tsx`), 메랜 쪽에서 유입된 크롤러/유저는 애초에 노출 자체가 안 됐음. 2026-07-16 기록에 이미 "사이드바가 플래닛 모드에서만 플래닛 nav 노출"로 발견은 됐지만 그때는 안 건드리기로 했던 이슈와 같은 뿌리.

**적용**: 코드 동작 변경 없이 순수 내부 링크만 3곳 추가 —
- `drop-table/page.tsx`(메랜): 하단에 "메이플 플래닛 드랍 테이블 보기" 링크 추가
- `services/drop-table/page.tsx`: 버튼 1개(메랜만) → 메랜/플래닛 버튼 2개로 분리
- `mapleland-vs-planet/page.tsx`: CTA 문단에 드랍 테이블 인라인 링크(메랜/플래닛판) 추가

사이드바 자체를 양쪽 모드에서 항상 플래닛 링크를 노출하도록 바꾸는 건 더 큰 UX 변경이라 이번엔 손대지 않음 — 필요하면 후속 검토.

**검증**: `npx tsc --noEmit`/`npx eslint` 통과.

### 문서 정리 (Codex 교차 리뷰 반영)

사용자가 Codex에게 md 문서 전체 리뷰를 받아와서 항목별로 사실관계 확인 후 반영.

- **`src/data/mapledb/` 용도 확정**: TODO.md에 "레거시인지 별도 용도인지 미판단"으로 남아있던 항목을 실제 조사로 해소 — `src/` 런타임 코드에서 import하는 곳은 0곳(QuestBoard.tsx의 "mapledb" 문자열은 외부 사이트 `mapledb.kr` 링크일 뿐 무관), 대신 `scripts/build-drop-index.mjs` 등 6개 빌드 스크립트(Zone D)의 입력 소스로 확인됨. data.md의 "Next.js 내부에서 직접 import" 설명이 틀렸었던 것 — 정정하고 TODO.md에서 항목 제거.
- **큐브 빌더 노출 상태 문서 불일치**: DEVLOG(2026-07-16, 노출 중단/noindex)와 project-notes.md(일반 제공 기능처럼 서술)가 어긋나 있어서 project-notes.md에 현재 상태(노출만 중단, 코드/라우트 유지) 명시.
- **브라우저 검증 숙제 누락**: 큐브 빌더 장비 슬롯 추가 버튼의 실제 클릭 검증이 안 됐다는 기록(DEVLOG:187/204, 2026-07-09)이 TODO.md에 반영 안 돼 있던 걸 "재노출 전 확인 필요" 항목으로 TODO.md 데이터 정확도 절에 추가.
- **데이터 정확도 TODO 재분류**: "메이플 플래닛 데이터 정확도" 절을 정확도 오류 가능성(우선순위 높음)/데이터 누락/UX·향후 검토 3그룹으로 재구성.
- **project-notes.md 오타**: `app/announcements/page.tsx` → `src/app/announcements/page.tsx`.
- **data.md 보강**: 마지막 업데이트일 갱신, 일회성 보스 데이터 스크립트(발록/카오스 자쿰/카오스 혼테일) 보존·재실행 주의사항 절 추가, 확률 미상 시 `prob` 비워두는 정책 명문화.
- **보류**: 피드백 알림(RESEND_API_KEY 미설정 시 조용히 스킵) 관련 어드민 상태 표시는 실제 기능 추가라 이번엔 TODO.md에 위험 설명만 보강하고 구현은 안 함.

---

## 2026-07-22

### 발록(마왕 발록) 데이터 정합성 보강

사용자가 메이플랜드 7/16 패치에서 "마왕 발록" 원정대(신전의 밑바닥 NPC 무영, 레벨50~200, 6~15인)가 소개된 걸 보고 데이터 업데이트 요청. 확인해보니 발록 자체(mobCode 8830000)는 이미 존재했지만 여러 군데 부실했음.

**용어 정리**: "마왕 발록"은 이 콘텐츠(원정대) 전체의 이름이고, 실제 전투 개체는 "발록"(본체)+"발록(왼손)"+"발록(오른손)" 3파츠 — 자쿰/혼테일처럼 "카오스" 같은 별도 버전이 있는 게 아니라 원래부터 하나의 보스라서, `name` 필드는 그대로 "발록"으로 유지하기로 결정. 검색은 `DropTable.tsx`의 양방향 부분일치 매칭(`key.includes(keyword) || keyword.includes(key)`) 덕분에 "마왕 발록"으로 검색해도 이미 걸림.

**스탯**: 본체가 `acc:0/eva:0`이던 placeholder였고 watk/matk 필드 자체가 없었음(피격뎀 계산기에서 데미지 0으로 나오는 버그) — maplestory.io KMS API(mob meta)로 확인한 공식값(acc49/eva36, watk580/matk583)으로 교체. 커뮤니티 스크랩 사이트(메이플노트 클래식)가 보여준 레벨/스탯은 이례적으로 KMS와 어긋나서(본체 레벨105 vs 실제70) 신뢰 안 함 — HP는 KMS가 기존 로컬 데이터와 정확히 일치해서 KMS 쪽을 채택. 왼손(8830001)/오른손(8830002) 파츠는 아예 없어서 KMS 기준(레벨70, HP264만/306만)으로 신규 추가.

**드롭**: 무기류가 KMS 공식 검색 기준 58종 존재하는데 기존 로컬엔 37종만 있었음 — 누락 21종(마법사 완드/스태프 8종, 폴암/케인/너클 등 13종) 추가. 사용자가 "발록한테는 주문서 안 나오나?"라고 재질문한 덕에 백의 주문서 1%(0.9%)/혼돈의 주문서 60%(0.05%)가 아예 안 걸려있던 것도 발견해서 추가, 발록의 가죽조각(0.06%)도 추가. 패치노트가 언급한 "발록의 가죽/털가죽 신발"(1072375/1072376, GMS 메타로 요구레벨/스탯 확보, TUC는 패치노트대로 6→5 반영)도 추가했으나 확률은 스크랩 사이트 내에서 같은 항목이 "0.09%"/"0%"로 자기모순되게 표시되고 개별 아이템 페이지도 "???"라 미상 처리(자쿰 나뭇가지 때와 동일 원칙 — 불확실하면 확률을 비워서 "정보 없음" 표시).

**후속 버그**: 무기 21종을 드롭테이블에 연결만 하고 아이템 카탈로그(`drop-index.json`의 `items` 배열)에 이름 메타를 안 넣어서 "미확인 아이템"으로 뜨는 문제 발생 — 사용자가 "비확인 아이템이 왜 이렇게 많아?" 지적해서 발견, KMS 이름으로 21종 카탈로그 등록해서 해결. `dropsByMonsterId["8830000"]`의 모든 itemId가 카탈로그에 있는지 전수 검증하는 걸로 마무리.

**적용 방식**: `scripts/update-balrog.mjs`(본체/팔 스탯), `scripts/update-balrog-drops.mjs`(드롭 보강) 일회성 스크립트로 반영 후 `node scripts/build-planet-data.mjs` 재실행해 플래닛 쪽 동기화.

**검증**: `npx tsc --noEmit`/`npx eslint src/` 통과. 로컬 서버(`npm run dev`)에서 사용자가 직접 드롭테이블 확인.

---

## 2026-07-21 (2)

### Google Search Console 색인 이슈 3종 진단 및 수정

사용자가 GSC에서 본 색인 문제 3가지(중복 페이지/발견됨-미색인/크롤링됨-미색인)를 붙여넣고 원인을 물어봐서 코드 기반으로 진단.

**1) `/feedback` — "사용자가 선택한 표준이 없는 중복 페이지"**: `/contact`("문의하기")와 `/feedback`("문의/요청")이 사실상 같은 목적(문의 채널 안내)의 페이지 — `/contact`가 아예 `/feedback`으로 링크까지 걸어주는 게이트웨이 관계인데 문구도 겹쳐서 구글이 중복으로 판단, 사용자가 지정한 canonical을 무시하고 자체적으로 하나만 색인. 사용자 확인 후 `/feedback`의 canonical을 `/contact`로 명시 지정(`feedback/layout.tsx`) — 폼 자체보다 채널 안내 페이지가 더 완결된 콘텐츠라 `/contact`를 표준으로 선택.

**2) "발견됨 - 현재 색인이 생성되지 않음" (about/guide/login/planet/services 3종)**: `/services/drop-table`·`/services/onehit`·`/services/quests`는 `data.md`/DEVLOG 과거 기록상 원래 "검색 유입용 설명 페이지"로 의도적으로 만든 건데, 실제로는 사이트 내 링크가 `/services/drop-table`(그것도 숨겨진 `/probability-secret`에서만) 하나뿐이라 사실상 고아 페이지였음 — 내부 링크 신호 부재로 크롤 우선순위가 낮아진 것으로 판단. `/login`은 로그인 페이지라 원래 미색인이 정상(문제 아님). `/about`·`/guide`·`/planet`은 홈페이지에서 정상 링크되고 있어 고아는 아니고, 신생/니치 사이트라 도메인 권위 문제로 시간이 필요한 것으로 판단(코드 수정 대상 아님).

**3) "크롤링됨 - 현재 색인이 생성되지 않음" (`/planet/drop-table` + favicon 2건)**: `/planet/drop-table`이 `/drop-table`과 같은 `DropTable.tsx` 컴포넌트를 데이터만 바꿔 재사용하다 보니 UI 뼈대/설명 문구가 거의 동일 — 근접 중복 콘텐츠로 판단해 플래닛 전용 안내 문단(프리빅뱅 기반 + 드랍률 4배 등 실제 차이점)을 페이지 본문에 추가해 콘텐츠 차별화. favicon.ico 2건은 GSC의 흔한 노이즈(파비콘이 페이지로 잘못 집계)라 실질적 문제 아님, 조치 안 함.

**적용**: `drop-table/page.tsx`, `calculators/onehit/page.tsx`, `calculator/damage/page.tsx`, `quests/page.tsx` 각각에 대응하는 `/services/*` 설명 페이지로 가는 작은 링크 추가(고아 페이지 해소) + `planet/drop-table/page.tsx`에 플래닛 차별화 안내 문단 추가 + `feedback/layout.tsx` canonical을 `/contact`로 변경.

**검증**: `npx tsc --noEmit`/`npx eslint src/` 통과, 로컬 서버에서 4개 페이지 전부 새 링크 렌더링 확인.

---

## 2026-07-21

### 드롭테이블에 카오스 혼테일 추가 (메이플랜드)

카오스 자쿰에 이어 사용자가 카오스 혼테일도 추가 요청. 자쿰과 동일한 방법론(메이플노트 클래식 스크래핑)을 그대로 적용하려 했으나 두 가지 지점에서 달랐음.

**스탯 소스가 달라짐**: 메이플노트 사이트의 원정대 노트(`/pquestnote?pquest=chaos_horntail`)에서 부위별(왼쪽머리/오른쪽머리/머리A·B·C/왼손/오른손/날개/다리/꼬리) mobCode(8810100~8810109)와 통합 카드(8810118, 여기에 "카오스 혼테일2~5"라는 HP만 있고 드랍 없는 더미 복제본들도 8810119~8810122로 존재)를 확인. 그런데 이번엔 `maplestory.io` KMS API 수치가 기존 정규 혼테일(8810018) 실측치(HP 2,090,000,000)와 **정확히 일치**함을 발견 — 혼테일은 자쿰과 달리 KMS 레벨(160)과 메랜 레벨(160)이 동일해서 별도 밸런싱 없이 공식 수치를 그대로 씀. 그래서 부위별 스탯은 메이플노트 사이트 텍스트(억/만 단위 반올림 표기라 부정확)가 아니라 KMS API `meta` 원본값을 직접 사용.

**드랍 데이터가 아예 없음**: 메이플노트 사이트에서 카오스 혼테일 통합 카드(8810118)와 부위별 페이지(8810100~8810109) 전부 GET 섹션이 비어있었음(자쿰 때는 통합 카드 8888889에 48건이 몰려있었던 것과 다름). 아이템 검색으로도 "카오스 혼테일의 OO" 계열 전용 아이템이 안 나와서, 이 사이트엔 카오스 혼테일 드랍 정보 자체가 없다고 판단.

**해결**: 사용자에게 확인 요청 → "기본적으로 일반 혼테일과 같은 드랍 + 카오스 혼테일의 목걸이 추가"로 답변받음. 실행:
- 통합 카드(8810118)엔 정규 혼테일 본체(8810018)의 드랍 39건을 그대로 복사 + "카오스 혼테일의 목걸이"(itemId 1122076, `maplestory.io` API로 메타 확인) 추가.
- 카오스 혼테일 머리B(8810103)엔 정규 혼테일 머리B(8810003)의 드랍 20건을 그대로 복사.
- 목걸이는 확률 출처가 없어 `prob` 필드를 비움(자쿰의 나뭇가지 때와 동일하게 UI에서 "정보 없음" 표시).

**부수 확인**: 사용자가 "타임리스 vs 리버스" 장비 차이를 물어봐서 확인차 메이플노트에서 검색해보니, 실제 메랜 데이터에선 "타임리스" 계열 아이템이 아예 없고 "리버스" 계열은 존재하지만 전부 **핑크빈** 드랍(레벨120, 0.5%)으로 붙어있어 카오스 혼테일과 무관함을 확인 — 그래서 신규 무기 세트를 추가하지 않고 사용자 지시대로 정규 드랍 재사용 + 목걸이 추가만 반영.

**적용 방식**: `scripts/add-chaos-horntail.mjs` 일회성 스크립트로 `monsters.json`/`drop-index.json`에 프로그래매틱 반영(직접 편집 금지 규칙 준수).

**검증**: `npx tsc --noEmit`/`npx eslint src/` 통과, 로컬 서버에서 드롭테이블 검색 → "카오스 혼테일" 스탯(HP 20억/물공 5812/마공 5590/회피 169) 및 목걸이 "정보 없음" 표시까지 브라우저로 확인.

**후속: 통합 카드 아이콘 누락 수정**: 사용자가 "카오스 자쿰/카오스 혼테일 둘 다 사진이 안 보인다"고 제보. 원인 확인 결과 두 몬스터 다 부위별 데이터를 하나로 합쳐 보여주는 "집계 카드"라 maplestory.io에 전용 아이콘이 없음 — 카오스 자쿰(8888889)은 애초에 공식 mobId가 아니라 KMS/GMS/JMS 어느 버전에도 없고, 카오스 혼테일(8810118)은 실제 공식 mobId지만 렌더용 아이콘이 등록 안 돼있음(같은 이유로 기존 일반 "혼테일" 8810018도 원래부터 아이콘이 비어있었음 — 이번에 새로 생긴 문제가 아니라 기존 데이터 갭). `src/lib/maplestory-io.ts`에 `MOB_ICON_OVERRIDES` 맵을 추가해 이 세 mobCode(8810018→8810000 왼쪽머리, 8810118→8810100 카오스 왼쪽머리, 8888889→8800102 카오스 3페이즈)의 아이콘/렌더/애니메이션 요청을 대표 부위로 자동 치환하도록 수정. `getMobIconUrl`/`getMobAnimatedUrl`/`getMobAnimatedFallbackUrl`/`getMobRenderUrl` 전부 내부에서 치환하므로 DropTable 외 다른 화면에서도 동일하게 적용됨. 브라우저로 재확인 완료.

**후속: 메이플 플래닛에도 반영**: 사용자가 플래닛 쪽에도 같은 몬스터를 적용해달라고 요청. `scripts/build-planet-data.mjs`가 애초에 `data/monsters.json`/`data/drop-index.json`(메랜 원본)을 읽어 `scripts/sources/planet/divergence-overrides.json`의 배율만 얹어 `data/planet/*.json`을 생성하는 구조라, 이번에 메랜 쪽에 추가한 카오스 자쿰/혼테일이 이미 원본 데이터에 포함돼있어 `divergence-overrides.json`의 `newMonsters`를 손댈 필요 없이 스크립트만 재실행하면 자동 반영됨. `node scripts/build-planet-data.mjs` 실행 결과 두 보스 전체가 플래닛 `monsters.json`에 복사되고, 드랍률도 플래닛 고유 배율(x4)이 자동 적용됨(예: 혼테일의 목걸이 30%→100% 클램프, 10%→40%). 브라우저로 `/planet/drop-table`에서 아이콘/스탯/드랍률까지 확인 완료.

**후속: 통합 카드 이미지를 실제 전신 이미지로 교체**: 대표 부위 아이콘 치환(왼쪽머리/3페이즈)으로는 부족하다는 사용자 피드백 — 혼테일은 "얼굴 3개"(가운데/왼쪽/오른쪽 3개 머리가 다 보이는 것), 자쿰은 "팔이랑 본체 다 있는 것"을 원함. 후보 비교용 아이콘 피커 아티팩트를 만들었으나, 사용자가 "인터넷에 사진 많다"며 대신 실사 이미지 소스를 쓰라고 지시. 인벤 메이플스토리 몬스터 도감(`maple.inven.co.kr/dataninfo/monster/detail.php?code=8810018`, `code=8800002`)에서 각각 몬스터북용 전신 컷을 확보 — 혼테일은 좌/중/우 세 머리가 모두 보이는 전신 드래곤 이미지, 자쿰은 몸통+주먹(양팔)이 함께 보이는 토템 이미지. `public/images/monsters/horntail-full.gif`, `zakum-full.gif`로 저장하고 `src/lib/maplestory-io.ts`의 아이콘 치환 방식을 "다른 mobCode로 리다이렉트"에서 "로컬 정적 이미지로 직접 대체"(`MOB_ICON_STATIC_OVERRIDES`)로 변경 — maplestory.io API 자체에 존재하지 않는 이미지라 mobCode 치환으로는 애초에 한계가 있었음. 8810018(혼테일 본체)/8810118(카오스 혼테일)엔 혼테일 전신 이미지, 8888889(카오스 자쿰 통합 카드)엔 자쿰 전신 이미지 적용. 브라우저로 재확인 완료.

**부수 질문 — 타임리스 vs 리버스**: 실제 라이브 메이플스토리 기준 타임리스(Timeless)는 챠오스 혼테일 최초 등장 시점의 최상위 장비 티어(무기 공격력 +6~7, 강화 스킬 보유, 핑크빈이 주는 '시간의 돌'로 강화, 최대 +6강), 리버스(Reverse)는 그보다 나중에 추가된 더 접근성 좋은(시간의 신전 몬스터가 주는 '시간의 조각'으로 강화, 최대 +4강) 대신 스탯은 타임리스보다 낮은 하위 호환 티어. 다만 이번 메랜 데이터셋 조사에서 확인했듯 메이플랜드/메랜Hub 쪽엔 "타임리스" 자체가 없고 "리버스"는 핑크빈 드랍으로 재배치되어 있어, 라이브 게임의 실제 카오스 혼테일 전용 장비 체계와는 무관함(위 "카오스 혼테일의 목걸이" 조사 항목 참고).

**후속: 목걸이 중복 드랍 수정**: 사용자가 "공식적으로 (일반 목걸이/카오스 목걸이) 둘 다 나오냐"고 재질문. 웹 검색으로 확인한 결과 실제 게임에서 "혼테일의 목걸이"는 이지/노멀 전용, "카오스 혼테일의 목걸이"는 카오스 전용으로 서로 배타적임(동시에 안 나옴) — 기존에 일반 혼테일 드랍을 통째로 복사하면서 정규 목걸이(1122000)까지 같이 들어간 게 잘못이었음. `scripts/add-chaos-horntail.mjs`에서 정규 목걸이를 명시적으로 제외하도록 수정(+이전 실행으로 꼬인 `monstersByItemId` 역인덱스도 정리)하고 재실행, `build-planet-data.mjs`도 재실행해 플래닛 쪽까지 동기화. 카오스 혼테일 통합 카드(8810118) 드랍이 39건→38건(정규 목걸이 제거, 카오스 목걸이만 유지)으로 정정됨.

**후속: 이미지 배경 투명 처리 + 애니메이션 소스 재확인**: 인벤에서 가져온 이미지가 흰 배경이라 다른 아이콘(전부 투명 배경)과 이질감이 있다는 피드백. 추가로 maplestory.io에 움직이는 투명 버전이 있는지 재확인 요청받아 raw mob 메타(`framebooks`)까지 들여다봄 — 8810018(혼테일 본체)은 실제로 `stand` 프레임이 존재하긴 하나 렌더링 결과가 비어있어, 게임 내에서 시각적 실체가 없는 "히트박스 전용" 로직 개체로 확인됨(아이콘/렌더/애니메이션 엔드포인트 전부 빈 이미지인 이유). "얼굴 3개/본체+팔" 컷은 게임 스프라이트가 아니라 별도 제작된 몬스터북 일러스트라서 애초에 움직이는 버전이 존재하지 않음. 대신 `sharp`로 흰 배경(RGB 245 이상)을 알파 0으로 치환해 `horntail-full.png`/`zakum-full.png`로 재생성(원본 gif는 삭제), `MOB_ICON_STATIC_OVERRIDES` 경로도 png로 갱신. 마젠타 배경에 합성해 경계에 흰 테두리(halo) 없이 깨끗하게 잘렸는지 확인 후 브라우저로 최종 확인.

---

### 드롭테이블에 카오스 자쿰 추가 (메이플랜드)

사용자가 드롭테이블에 카오스 자쿰이 없다고 제보. 확인해보니 `data/monsters.json`엔 일반 자쿰(mobCode 8800000~8800009)만 있었고, `build-planet-data.mjs` 주석엔 오히려 "카오스 자쿰은 빅뱅 이후 콘텐츠라 메랜 데이터셋에 원래 없음"이라 적혀 있어 실존 여부부터 검증이 필요했음.

**존재 확인**: 공식 메이플랜드 패치노트(`public.maple.land/board/notices`)나 로드맵 개발일지에선 직접적인 언급을 못 찾았지만, 사용자가 "레고77이 최근에 했다"고 확인해줌 — 메이플랜드 2.0 로드맵에서 "해적 직업군 이후 콘텐츠도 업데이트될 수 있다"는 정책 변경이 있었던 것과 부합.

**mobCode**: `maplestory.io` KMS API(`/api/KMS/389/mob?searchFor=자쿰`)에서 원작 공식 구조 확인 — 본체 3페이즈 8800100~8800102, 팔 8개 8800103~8800110. 메랜 기존 일반 자쿰(8800000~8800009)과 동일한 넘버링 규칙이라 신뢰도 높음.

**스탯**: KMS 리테일 API 수치(레벨180, HP 21억 등)가 메랜의 기존 일반 자쿰 실측치(레벨140, HP 66M)와 전혀 안 맞아서(메랜은 원작을 그대로 안 쓰고 자체 밸런싱함을 재확인) 그대로 못 씀. 대신 `scripts/fetch-new-items-drop-data.mjs`가 이미 드롭 스크래핑 소스로 쓰고 있는 "메이플노트 클래식"(`xn--o80b01o9mlw3kdzc.com`) 사이트에서 mobCode별 실측치를 직접 대조 — 이 사이트의 일반 자쿰 수치가 기존 `monsters.json`과 정확히 일치하는 걸 확인해 신뢰 소스로 채택. 카오스 자쿰 전체가 일반 자쿰의 정확히 8배 HP(예: 1페이즈 66M→528M, 팔1 33M→264M)로 스케일링되어 있어 일관성도 확인됨. acc/eva는 이 사이트에 표시되지 않아 KMS 원작값(acc 550/eva 200)을 잠정치로 사용 — 실측 나오면 교체 필요.

**드롭 아이템(1차)**: 같은 사이트의 `item_detail/1003112`에서 "카오스 자쿰의 투구"가 100% 확정 드롭으로 명시된 것만 반영(mobCode 8800102). "카오스 자쿰의 나뭇가지", 150제 "분노한 자쿰" 세트(투구/망토/벨트)는 해당 사이트에 아이템 페이지 자체가 없어(404) 메랜에 미구현일 가능성이 높다고 판단해 제외.

**"카오스 자쿰" 통합 카드 + 전체 드롭테이블 (후속 요청)**: 사용자가 "1/2/3페이즈+팔로 쪼개진 것도 맞지만, 카오스 자쿰 검색 시 전체 환산과 드롭테이블을 같이 보여줬으면 좋겠다"고 요청. 처음엔 개별 페이즈 페이지(`monster_detail/8800100~102`)의 GET 섹션만 확인해서 "투구 1개밖에 못 구했다"고 잘못 판단했는데, 재조사하며 이 사이트가 **페이즈와 별개로 "카오스 자쿰" 통합 엔티티(mobId `8888889`)를 따로 두고 거기에 전체 드롭표(50건)를 몰아넣는다**는 걸 발견 — 개별 페이즈 페이지는 애초에 드롭 데이터가 비어있는 게 정상 설계였음. 이 mobId를 그대로 `monsters.json`에 추가(이름 "카오스 자쿰", HP 3.9B/EXP 31M 합산치, watk/matk/def/mDef는 원본 그대로 0 — 전투 개체가 아니라 순수 집계 카드라 N방컷/피격뎀 계산기에 쓰면 무의미한 값이 나옴을 사용자에게 안내함). 드롭 50건 중 0% 확률 2건은 제외하고 48건 반영, 아이템 메타 47건은 기존 일반 자쿰 드롭과 겹쳐 재사용, 신규 1건("[마스터리북] 메이플용사 30", itemId 2290125)만 `maplestory.io` API로 메타 보강.

**나뭇가지 추가 확인**: 사용자가 "카오스 자쿰의 나뭇가지도 실제로 있다"고 정정 — 처음엔 `item_detail/1372073`(공식 itemId는 KMS API로 맞게 찾았었음) 페이지가 404라 "메랜 미구현"으로 오판했던 것. itemId 자체는 맞았고 단지 그 스크래핑 사이트의 DB 커버리지 문제였음. 드롭 확률은 어떤 소스에서도 확인 못 해 `prob` 필드를 아예 비워 UI가 "정보 없음"으로 정직하게 표시하도록 함(가짜 %를 넣지 않음) — 자쿰의 투구와 마찬가지로 "난이도 공통 획득" 아이템이라는 커뮤니티 정보에 따라 3페이즈(8800102)와 통합 카드(8888889) 양쪽에 연결.

**교훈**: 아이템 단위(`item_detail`)로만 역추적하지 말고 몬스터 단위(`monster_detail`) 페이지의 GET 섹션을 직접 확인했어야 더 빨리, 더 정확히 찾았을 것. 또한 "그 사이트에 없다 = 메랜에 없다"로 성급히 결론 내리지 말 것 — 사용자의 실측 확인이 더 신뢰도 높은 소스였음.

**검증**: 매 단계마다 `npx tsc --noEmit`/`npx eslint src/` 통과 확인, 로컬 서버(`npm run dev`)에서 드롭테이블 검색 → 개별 페이즈/통합 카드/나뭇가지 "정보 없음" 표시까지 브라우저로 직접 확인.

**적용 방식**: `data/monsters.json`, `data/drop-index.json`은 빌드 산출물이라 직접 편집 금지 규칙이 있어, 일회성 스크립트(스크래치패드)로 프로그래매틱하게 추가 — `fetch-missing-monsters.mjs`가 신규 몬스터를 API 스크래핑으로 `monsters.json`에 직접 추가하는 기존 패턴을 그대로 따름. `data/raw/monsters.raw.ts`는 건드리지 않음(raw.ts를 기준으로 `normalize-monsters.mjs`를 재실행하면 raw.ts에 없는 기존 239개 몬스터가 삭제되는 걸 사전에 확인해서 회피).

**검증**: `npx tsc --noEmit` 통과.

---

## 2026-07-16

### 정령의 축복 / 여제의 축복 계산기 반영, 비교 페이지 문구 정정

사용자가 N방컷 계산기에 정령의 축복(정축)/여제의 축복(여축)이 반영돼 있는지 물어와서 확인 — 둘 다 전혀 구현돼 있지 않았음(메이플 용사만 있었음). 실제 서버 수치를 넣기 전에 정확도부터 검증하는 데 상당한 시간을 씀: 나무위키 일반 스킬 문서(최신 본편 기준, 정축 최대 20/여축 최대 30) → 메이플랜드 커뮤니티 블로그(10렙당+1, 레벨당 공1마2) → 메이플 플래닛 유저 글(정축 최대 200/여축 최대 120) 순으로 소스마다 수치가 전부 달라서, 최종적으로 두 서버의 **공식 홈페이지**(mapleplanet.co.kr 패치노트, maple.land 개발일지)를 직접 뒤져서 확인.

**공식 확인 결과**:
- 플래닛(2026-05-13 공식 패치노트): "시그너스 기사단 최대 레벨 120, 10레벨당 1포인트로 여제의 축복 활성화" → 여축 최대 레벨은 **12**(120÷10)임을 확인. 이전에 유저 글을 잘못 읽고 120으로 넣었던 걸 정정.
- 메이플랜드(2.0 업데이트 미리보기 개발일지): 밸런스 기준이 "모험가의 귀환(KMS 1.2.89)"이라고 명시 — 이 시점은 여제의 축복이 도입된 1.2.111보다 이르므로, **메이플랜드에는 여제의 축복이 없다**는 판단의 근거로 채택.
- 정축/여축 중첩 여부는 두 공홈 어디에도 명시돼 있지 않았고, 플래닛 커뮤니티(dcinside/arca) 내에서도 "중첩된다"/"안 된다" 의견이 갈림. 일반 넥슨 메이플스토리 본편은 "중첩 안 되고 높은 쪽만 적용"이 공식 룰이라, 근거 불충분한 상태에서 **중첩 안 됨으로 가정**하기로 사용자와 합의.

**구현** (`onehit-calculator-client.tsx`): 서버별 설정(`SPIRIT_BLESSING_BY_SERVER`, `EMPRESS_BLESSING_BY_SERVER`)을 분리 — 메이플랜드는 정축만(최대 20, 레벨당 공+1/마+2), 플래닛은 정축(최대 200)+여축(최대 12) 둘 다 노출하되 `Math.max()`로 더 높은 쪽만 적용. "총 공격력/총 마력" 직접 입력 모드에서는 이미 게임 화면에 반영된 수치이므로 중복 합산되지 않도록, 스탯 기반 자동계산(`derived.attack/magic`) 경로에만 보너스를 더함 — 큐브 빌더에서 겪었던 것과 같은 이중 합산 함정을 여기서도 미리 방지. UI에는 "(잠정치)" 라벨을 달아 커뮤니티/공식 소스 기반 추정치이며 실제와 다를 수 있음을 명시.

사용자가 함께 제보한 "플래닛 쿠키 공격력 버프 캐시템"은 `equips.js` 전수 검색 결과 실존하지 않는 걸로 확인(동명의 발렌타인 이벤트 아이템만 존재) — 이번 반영에서 제외.

**비교 페이지 정정**: `/mapleland-vs-planet`의 "메이플스토리 월드(유저 제작 콘텐츠 플랫폼)" 표현이 넥슨의 공식 운영을 과소평가한다는 지적에 따라, 플래닛 공홈 FAQ/푸터에 명시된 "넥슨(주)의 메이플스토리월드 플랫폼에서 공식 출시된" 문구를 근거로 "넥슨이 직접 운영하는 공식 타이틀"로 정정.

**검증**: tsc/eslint/vitest(54개) 통과, 로컬에서 메랜/플래닛 두 버전 모두 UI 확인.

### "메이플랜드 vs 메이플플래닛" 비교 콘텐츠 페이지 신설

세분화/특정화로 유입을 늘리자는 방향에서, 유저 규모 리서치 중 우연히 발견한 단서를 바로 실행 — "메이플랜드 메이플플래닛 차이"류 질문이 a-ha/디시/인벤/에펨코리아 등에 반복해서 올라오는데(검색 상위 노출 확인), 정작 두 서버를 전문적으로 다루는 사이트에서 이 질문에 제대로 답하는 콘텐츠가 없었음. 메랜Hub는 두 서버를 동시에 지원하는 몇 안 되는 사이트라 이 검색 수요를 가장 잘 받을 수 있는 위치라고 판단.

**신규 페이지**: `/mapleland-vs-planet` (`src/app/(routes)/mapleland-vs-planet/page.tsx`) — `/about`, `/guide`와 동일한 정적 콘텐츠 페이지 패턴(별도 layout.tsx 없이 page.tsx에 메타데이터 직접 export, `glass-panel` 카드 레이아웃). 비교 표(오픈베타/정식출시일, 배율, 큐브 시스템 유무, 직업 구성) + 배율 설명 + 큐브 시스템 설명 + 중립적인 "어느 쪽이 맞을까" 안내 + 양쪽 도구로의 CTA 링크로 구성.

**정확성 검증**: 배율(경험치 4배/드랍률 4배/메소 2배)은 이 프로젝트 자체의 `scripts/sources/planet/divergence-overrides.json`(빌드 파이프라인이 실제로 곱해서 쓰는 값)과 외부 검색 결과(mapleplanet.co.kr 공식 FAQ 기반 요약)가 정확히 일치하는 것을 교차 확인 후 반영 — 추측 없이 근거 확보. 출시일(메랜 OBT 2023-10-31/정식 2024-10-31, 플래닛 OBT 2026-04-24)도 웹 검색으로 별도 확인. 직업 구성 차이는 TODO.md에 이미 기록된 "사용자 확인" 근거를 그대로 인용(과신하지 않고 적당히 hedge한 표현 사용). 어느 쪽이 "더 낫다"는 식의 편향된 서술은 의도적으로 피함 — 두 커뮤니티 유저를 다 상대해야 하는 사이트라 중립성이 중요하다고 판단.

**노출**: 사이드바 guideLinks(양쪽 서버 모드 공통 노출), 메이플랜드 홈 푸터, sitemap.xml에 추가.

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(54개), `npm run build`(43개 라우트, `/mapleland-vs-planet` 정적 생성 확인) 전부 통과. 브라우저로 렌더링/콘솔 에러 없음 확인.

### 메랜/플래닛 유저 규모 리서치 + 전략 방향 전환, 큐브 빌더 노출 중단

큐브 빌더 UX가 "체계화가 안 됐다"는 피드백에서 시작해서, "애초에 이 기능을 유저가 원하긴 하나"로 논의가 번짐. 결정을 위해 메이플랜드 vs 메이플플래닛 실제 유저 규모를 웹 검색으로 조사(둘 다 메이플스토리 월드 UGC 모드라 넥슨 공식 API 기반 인구 통계 사이트(츄츄지지 등)엔 안 잡히는 것도 확인 — 전부 커뮤니티/기사 보도 스냅샷 기준).

**결과**: 메이플랜드 누적 240만 vs 플래닛 34.5만(누적은 메랜이 압도적, 오래 운영됐으니 당연) / 동접은 메랜 1.3만~5.5만인데 플래닛이 출시 2주 만에 6.5만~11만대까지 급성장 — 플래닛이 메랜을 추월하는 추세라는 보도도 있었음. **사용자가 이 근거로 "플래닛은 더 이상 서브가 아니라 메랜/플래닛 둘 다 메인"으로 전략 전환 결정** — 메모리(`project_context.md`)에 기록해서 다음 세션에도 이어지게 함. 다만 이 전환에 안 맞는 기존 구조(사이드바가 플래닛 모드에서만 플래닛 nav 노출, 사이트명 자체가 메랜 중심, `/quests`에 플래닛 미러 없음 등)는 발견만 해두고 이번엔 안 건드림 — 플래닛 퀘스트 미러는 사용자가 "딱히 필요 없다"고 확인.

**우선순위**: "지금 있는 서비스를 세분화/특정화해서 유입 늘리는 게 먼저"로 확정, 큐브 빌더 UI 재구성 작업은 보류.

**큐브 빌더 처리**: "검증된 수요 없이 어중간하게 방치하는 게 제일 나쁘다"는 사용자 지적에 동의 — 완전 삭제 대신 **노출만 중단**하기로 결정(사이드바/플래닛 랜딩 카드/큐브 시뮬레이터 상호링크/sitemap.xml에서 전부 제거, `/planet/cube-builder` 라우트 자체는 유지하되 `robots: {index:false, follow:false}`로 noindex 처리해서 검색엔진엔 안 잡히게 함). URL을 직접 아는 사람은 여전히 쓸 수 있고, 나중에 실사용 신호가 생기면 다시 노출 복원 가능. 코드/테스트(cubeBuilder.ts, cubeBuilder.test.ts, dataAudit 테스트)는 전부 그대로 둠 — 로직 자체가 틀린 게 아니라 "지금 홍보할 우선순위가 아니다"라는 판단이라 삭제하지 않음.

**검증**: tsc/eslint/vitest(54개)/build 전부 통과.

### 드롭테이블/피격뎀 계산기 UX 감사 + GA4 이벤트 트래킹

GA4 데이터로 드롭테이블(40 조회)/피격뎀 계산기(29 조회)의 이탈률(39.4%/36.4%)을 확인했는데, 이게 정말 UX 문제인지 "빠르게 답 찾고 나간 성공 사례"가 이탈로 잘못 잡힌 건지 구분이 안 된다는 문제의식에서 시작 — 실제 컴포넌트 코드를 정독해서 진단.

**드롭테이블 실제 버그 발견**: 자동완성 드롭다운이 `query && showSuggestions` 조건에 걸려있어서 검색어 없이는(포커스만 줘도, 월드맵 필터만 바꿔도) 아무 목록도 안 뜨는 상태였음 — `INITIAL_SUGGESTION_COUNT` 상수로 기본 목록을 준비해두고도 렌더 조건 때문에 죽어있었던 것으로 보임. 조건을 `showSuggestions`로 단순화해서 수정, 초기화 버튼과 아이템 선택 시 관련 계산기 링크(가장 확률 높은 드랍처 기준)도 추가.

**피격뎀 계산기**: 모바일에서 입력 10개+를 스크롤해야 결과가 보이던 문제 — 스티키 요약바 추가. 자동완성에 없는 몬스터명을 직접 타이핑하면 안내 없이 목록 첫 몬스터로 조용히 계산되던 것도 발견해서 경고 문구 추가. 결과 복사 버튼, 중복 소개 카드 제거.

`src/lib/analytics.ts` 신설(GA4 이벤트 래퍼, 자유 텍스트는 절대 전달 안 함) — 두 페이지에 검색/필터/결과조회/에러/복사 등 13개 이벤트 계측. 커밋 `ef381e7`.

### 문서 정합성 정리 (Codex 피드백 반영)

Codex가 짚어준 문서 불일치 6건(nav-bar.tsx 잔재, 파티 기능 상태 충돌, data.md의 RSC payload 오해 소지 표현, 날짜 오래됨, TODO.md가 DEVLOG처럼 비대해짐, NOTE.md 빈 파일)을 실제 코드/git 상태와 대조해서 전부 검증 후 반영. 검증 과정에서 project-notes.md가 DropTable/TakenDamageCalculator 구조를 잘못 설명하던 것(존재하지 않는 컴포넌트 조합 언급)과, 이미 구현/해결된 TODO 항목 2개(드롭테이블 역검색, oneshot 리다이렉트 확인)가 스테일 상태로 남아있던 것도 추가로 발견해 정리. TODO.md는 완료 항목 장문 서술을 전부 제거(DEVLOG.md에 이미 더 상세히 있는 걸 확인) — 131줄에서 남은 액션 위주로 대폭 압축. 커밋 `63c9dd0`.

### 큐브 빌더 신설 — 캐릭터 스펙 기준 큐브 의사결정 도구

기존 큐브 시뮬레이터(`/planet/cube-simulator`)에 유입이 거의 없다는 문제 제기에서 출발 — 원인 진단(사이드바가 플래닛 모드에서만 노출, 메이플랜드 쪽엔 노출 거의 없음, `/services/*` 패턴의 SEO 설명 페이지가 없음)과 별개로, "지금 시뮬레이터가 기댓값만 주고 실제 스펙업은 안 보여준다"는 사용자 문제의식을 검증하려고 경쟁 사이트 cubeplanner.kr을 실제로 열어봄.

**발견**: cubeplanner.kr의 핵심은 캐릭터 스탯+장비 잠재 입력 → 모든 옵션을 "마력/공격력 N%"로 환산 → 부위별 "좋아질 확률"과 "+0.5%당 평균 큐브 개수"로 어느 부위를 먼저 돌려야 할지 추천하는 것. 이 사이트 가이드 FAQ에 "데미지 공식은 빅뱅 직전 메이플 표준"이라고 명시돼 있어서, 이미 이 저장소의 한방컷 계산기(`src/lib/calculators/onehit.ts`)가 같은 공식 계열을 구현해뒀다는 걸 확인 — 처음부터 새로 만들 필요 없이 재사용 가능하다고 판단, EnterPlanMode로 설계 확정 후 구현.

**구현**:
- `cubeSimulator.ts`에 `resolveOptionValues`(옵션의 원본 숫자 노출, 기존 `resolveOptionText`는 문자열만 반환해서 부족했음)와 `EquipCategory`/`isOptionAllowedForEquip`(장비 종류별 옵션 필터) export 추가 — 기존 동작 변경 없음. `ResolvedCubeLine`에 `entryId` 필드 추가해서 롤 결과를 원본 옵션으로 역추적 가능하게 함.
- `GradeOptionPicker`(등급→옵션 2단계 선택)와 등급 표시/포맷 유틸(`gradeMeta`/`formatCount`/`formatProbability`)을 `CubeSimulator.tsx`에서 `src/components/GradeOptionPicker.tsx`/`src/lib/cube-ui.ts`로 추출 — 큐브 시뮬레이터와 빌더 양쪽이 같은 컴포넌트/포맷을 공유.
- `data/planet/cube-index.json`의 suspicious(185)+miracle(132) 전체 옵션 텍스트 템플릿을 직접 덤프해서(56개 유니크) 손으로 분류표 작성 — `optionType`+변수명만으론 부족했던 부분 확인(예: `incDAMr` 변수명이 "총 데미지"와 "보스 공격 시 데미지" 둘 다에 쓰여서 반드시 `text` 접두어로 구분해야 했음, "올스탯" 옵션은 `text`엔 스탯 하나만 나오지만 `level` 객체엔 4개 스탯이 다 들어있음).
- `src/lib/calculators/cubeBuilder.ts` 신규 — `classifyOption`(옵션→스펙 델타 분류, 스킬재사용/이동속도/방어력 등은 "잡옵" 취급), `computeCharacterPower`(`calcBaseDamageFromStats` 재사용 + 크리티컬 기댓값 + 보스뎀/총뎀 곱연산 + 방어력 감산), `simulateSlotOutcome`(몬테카를로 — 기존 `rollCubeResult`를 그대로 반복 호출, cubeplanner.kr도 "5,000번 시뮬" 방식이라 방법론 일치. 기존 폐쇄형 확률 함수들은 "특정 옵션 하나" 겨냥용이라 목적이 달라 재사용 안 함).
- `/planet/cube-builder` 신규 라우트(`src/components/CubeBuilder.tsx`) — 캐릭터 정보 패널, 15개 장비 슬롯 그리드(클릭해서 추가, `GradeOptionPicker`로 3줄 입력), 부위별 가성비 추천 결과. `QuickSlots`로 로컬 저장(Supabase 연동은 범위 제외). 사이드바/플래닛 랜딩/sitemap.xml에 노출 추가, 큐브 시뮬레이터와 상호 링크.
- **이름 확정**: 처음엔 "큐브 플래너"로 만들었는데, 사용자가 "큐브 시뮬레이터"와 이름이 겹치는 걸 지적 — 두 기능이 서로 다른 서비스인지(굴려보기 vs 스펙 기준 의사결정) 설명하고 합칠지 분리 유지할지 확인한 뒤, 분리 유지하되 이름은 "큐브 빌더"로 변경(사용자 제안). 파일/타입/컴포넌트명까지 전부 `cubePlanner`→`cubeBuilder`, `/planet/cube-planner`→`/planet/cube-builder`로 일괄 리네임.

**코드 리뷰(자체) 후속 발견 및 수정**: 브라우저 실측 중 "빈 슬롯(잠재 미입력)"이 실제로는 가장 효율적인 큐브 대상인데도(무기 슬롯 평균 2.0개/0.5% vs 반지 3.2개/0.5%) 추천 카드가 계속 반지를 가리키는 버그 발견 — `recommended` 계산 로직이 `potentialIds.some(id => id !== null)`로 빈 슬롯을 후보에서 아예 걸러내고 있었음(의도치 않은 필터). 필터 제거해서 수정.

**알려진 한계(TODO.md에 기록)**: 보스뎀%/방어율무시% 배율은 표준 관례(단순 곱연산) 적용이라 메랜/플래닛 공식 검증은 아님. 몬스터 방어력은 절대값 입력(프리빅뱅 공식 특성상 %가 아님) — cubeplanner.kr의 "방어율%" UX와 다르다는 점을 페이지 안내 문구로 명시.

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(신규 17개 포함 48 passed), `npm run build`(42개 라우트, `/planet/cube-builder` 정적 생성 확인 — 첫 시도는 Windows 빌드 워커가 access violation으로 죽어서 재시도로 통과, 코드 문제 아님) 전부 통과. 브라우저(claude-in-chrome)로 반지에 STR+12(레어) 입력 → 현재 가치 +2.23%, 좋아질 확률 31.67%/평균 3.3개 계산 확인, 무기 슬롯을 빈 채로 추가했을 때 오히려 더 효율적으로(평균 2.0개) 계산되고 추천 카드가 정확히 그쪽으로 바뀌는 것까지 재확인. 큐브 시뮬레이터 페이지도 리팩토링 후 회귀 없이 그대로 동작하는 것 확인(같은 세션의 하이드레이션 경고는 브라우저 확장 프로그램발 노이즈, 기존에도 반복 확인된 것과 동일 원인).

**외부 리뷰(Codex) 반영**: 이름 리네임 과정에서 AGENTS.md Zone B/project-notes.md에 새 파일들을 반영 안 한 걸 발견해 정리. `GradeOptionPicker`에도 진짜 버그 하나 발견 — QuickSlots 복원이나 슬롯 전환처럼 `value`가 프로그래밍적으로 바뀌는 경우 내부 `gradeFilter`(로컬 state)가 안 따라가서 두 번째(옵션) 드롭다운이 빈 목록으로 깨지는 문제였음. `value`가 있으면 그 옵션이 속한 등급을 역산해서 쓰도록 수정. 리뷰가 선택창 활성화(disabled 여부)는 고쳤는데 강조색(chip) 스타일 쪽엔 같은 로직이 안 들어가 있던 잔여 불일치가 있어서 마저 통일.

**직접 재검토하다 발견한 더 심각한 버그**: 사용자가 "한 번 더 체크해달라"고 해서 `computeSlotValue`/`simulateSlotOutcome`을 다시 들여다보다가, 캐릭터 스탯 입력(예: STR)이 "현재 총합"(이미 그 슬롯의 잠재 효과 포함, cubeplanner.kr과 동일 관례)이라고 안내해놓고도 정작 슬롯 가치 계산은 그 위에 슬롯 잠재를 또 얹어서(이중 반영) 비교하고 있었음 — 재굴림 시뮬레이션도 "새 옵션"과 "기존 옵션"을 똑같이 이중 반영된 기준선 위에서 비교해서 정확한 비교가 아니었음. 우연히 "이미 낀 옵션과 똑같은 게 나오면 0%" 테스트만 양쪽이 대칭으로 틀려서 통과했던 것 — 버그를 가려버린 케이스였음. `removeDeltas`(델타 역연산, 순서 반대로 되돌림) 추가해서 "이 슬롯의 현재 기여분을 뺀 기준선"과 비교하도록 수정, 안내 문구에도 "슬롯 기여분은 자동으로 뺀다"고 명시. 회귀 테스트 1개 추가(캐릭터 str=550, 슬롯이 STR+50 기여 → 손계산한 기댓값과 정확히 일치하는지 검증) — 총 49개 테스트 통과.

**브라우저 재검증 미완료**: 이 세션 마지막에 브라우저 확장의 특정 버튼 클릭(장비 슬롯 추가)이 여러 탭/ref를 바꿔가며 시도해도 반응이 없어서(다른 상호작용(숫자 입력, 네비게이션)은 정상 동작) 이중 반영 수정 자체는 새 유닛 테스트(독립적으로 `computeCharacterPower`를 손으로 조합해 기대값을 계산하고 대조)로만 검증하고 실제 화면 확인은 다음 세션으로 미뤄둠 — 코드/수식 검증은 확실하지만 실제 UI 조작 확인이 남아있다는 점 기록.

**한 번 더 재검토해서 찾은 것 2건**(사용자가 "왜 계속 나오지" 하며 재확인 요청):
1. `data/planet/cube-index.json`을 직접 스크립트로 조사해서 수상한(185)/미라클(132) 풀의 id 겹침을 확인 — 132개는 id·text·optionType이 완전히 동일(안전)하지만, 수상한 전용 53개는 겹치지 않음. 큐브 종류를 바꿔도 슬롯에 골라둔 잠재 id를 그대로 유지하고 있었어서, 수상한 전용 옵션을 고른 상태로 미라클로 전환하면 조용히(에러 없이) 그 줄이 계산에서 빠지는 문제가 있었음 — `CubeSimulator.tsx`의 "장비 종류 필터 전환 시 선택 초기화" 패턴을 그대로 가져와 큐브 종류 전환 시 모든 슬롯의 잠재 선택을 초기화하도록 수정, 안내 문구 추가.
2. QuickSlots로 다른 스냅샷을 불러올 때 `expandedSlot`(현재 펼쳐진 슬롯 패널)이 복원 안 돼서, 복원된 슬롯 목록에 없는 슬롯 패널이 계속 펼쳐진 채로 남아있을 수 있던 사소한 불일치 — 복원 시 패널을 닫도록 수정.

CubeBuilder.tsx/cubeBuilder.ts 전체를 라인 단위로 다시 훑었고, 이번엔 새로 찾은 이슈 없음(49개 테스트 유지, tsc/eslint/build 재통과).

**한 번 더(3번째) 재검토**: 사용자가 "안 나올 때까지 계속 확인해달라, 거짓말은 안 된다"고 명시적으로 요청 — 지금까지는 수동으로 뽑은 56개 유니크 텍스트 템플릿만 보고 분류표를 짰던 거라, 실제 317개 엔트리 전체(수상한 185 + 미라클 132)를 `classifyOption`에 직접 돌려서 자동 감사하는 테스트(`cubeBuilder.dataAudit.test.ts`) 신규 작성:
- 전체 엔트리 크래시 없이 분류되는지
- "데미지/공격력/마력/크리티컬/방어율" 키워드가 텍스트에 있는데 잡옵으로 빠진 게 없는지(분류표 누락 탐지) — 처음엔 "피격 시 X% 확률로 데미지 무시"류(받는 데미지 경감, 방어형 프록)가 걸렸는데 확인해보니 실제로는 스펙 무관이 맞는 잡옵이라 오탐이었음, 필터를 정밀화해서 해결
- 델타 값이 전부 유한한 숫자인지
- STR/DEX/INT/LUK 텍스트와 분류된 stat 필드가 실제로 일치하는지
- "보스 공격 시 데미지"/"총 데미지" 텍스트가 정확히 그 kind로만 분류되는지

결과: 317개 중 159개(50%) 잡옵, 나머지는 stat 112/attack 36/ignoreDefense 10/bossDamage 10/totalDamage 8/criticalRate 8/criticalDamage 4로 분포 — 손으로 짠 분류표가 실제 전체 데이터에 대해 정확함을 확인. 이 테스트는 나중에 cube-data.js가 갱신돼도(주간 패치 체크 에이전트가 신규 옵션을 추가하는 경우 등) 분류표 누락을 자동으로 잡아주는 회귀 테스트로 남겨둠 — 총 54개 테스트 통과.

**정직하게 남겨두는 한계**: 브라우저로 실제 버튼을 눌러보는 검증은 이 세션 내내 특정 상호작용(장비 슬롯 추가 버튼 클릭)에서 막혀서 끝내 못 함 — 코드 검토(3회 반복)와 자동화된 유닛/데이터 감사 테스트(54개)로는 확실히 검증했지만, "실제 화면에서 클릭해서 눈으로 확인"은 다음 세션 숙제로 남음.

**4번째 재검토** — 이번엔 정확성 버그는 안 나왔고(코드/데이터 자체는 이제 상당히 검증됐다고 판단), 대신 성능 이슈 하나 발견: `outcomes`(슬롯별 결과)가 `useMemo`로 캐릭터 스탯이 바뀔 때마다 즉시 재계산되는데, 슬롯 하나당 몬테카를로 1500회를 돌기 때문에 슬롯을 여러 개(예: 10개+) 채운 상태에서 스탯 입력칸에 한 글자만 쳐도 눈에 띄게 버벅일 수 있음 — 정확성 문제는 아니지만 실사용성 문제. `TakenDamageCalculator`의 GA 이벤트 디바운스와 같은 패턴(입력 멈춘 뒤 400ms 후에만 재계산)으로 전환. `CubeSimulator.tsx`도 리팩토링 후 전체를 다시 정독해서 로직 변화 없음(import만 공유 모듈로 교체) 재확인.

---

## 2026-07-14

### GA4 애널리틱스 연동

조회수가 조금씩 나오기 시작하는데 애드센스를 붙이기엔 이른지 판단하고 싶다는 요청에서 시작. 아직 방문자 추이를 잴 수단이 없었어서(GA도 다른 애널리틱스도 미설치), 먼저 GA4부터 붙이기로 결정.

**구현**: `src/components/google-analytics.tsx` 신설 — `next/script`(`afterInteractive`)로 gtag 스크립트 삽입, 측정 ID는 `NEXT_PUBLIC_GA_ID` 환경변수로 주입하고 프로덕션 빌드에서만 렌더링(로컬 개발 중 조회수 오염 방지). `layout.tsx`에 `<GoogleAnalytics />` 추가. Cloudflare Pages 환경변수에 `NEXT_PUBLIC_GA_ID` 등록 필요(로컬 `.env.local`은 배포에 포함 안 됨).

**Cloudflare Pages "Retry deployment" 함정 발견**: 환경변수를 대시보드에서 새로 추가/수정한 뒤 "Retry deployment"로 재배포해도 실제 라이브 HTML엔 반영이 안 됐음(`curl`로 직접 확인 — 캐싱 문제 아님, `Cf-Cache-Status: DYNAMIC` 확인 완료). Retry deployment가 그 배포가 처음 트리거됐을 때의 환경변수 스냅샷을 재사용하는 것으로 추정 — **환경변수를 바꾼 뒤엔 반드시 새 커밋으로 완전히 새 배포를 트리거해야 함**. 실제로 빈 커밋(`git commit --allow-empty`)으로 새 배포를 유도하니 정상 반영됨.

**GA4 동의 모드 설정**: 사이트에 쿠키 동의 배너가 없는 상태라 "동의 배너를 사용하지 않음"으로 설정. 이 과정에서 측정 ID가 `G-K91GWXF2S1` → `G-B3TGJPBEE7`로 재발급됨(동의 모드 설정 완료 시 태그가 갱신되는 것으로 보임) — 새 ID로 환경변수/재배포 다시 진행.

**"데이터 수집 안 됨" 경고 디버깅**: GA4 관리 화면의 이 경고는 정적 안내 배너라 실제 수집 여부와 별개로 한동안 남아있음 — 실시간(Realtime) 보고서로 즉시 확인하는 게 맞는 방법. 실시간에서도 0으로 안 잡혀서 살펴보니, 원인은 사용자의 평소 브라우저에 깔린 **광고 차단 확장 프로그램**이 `googletagmanager.com` 요청을 막고 있던 것 — 시크릿 모드(확장 꺼짐)로는 정상 수집 확인. 실제 방문자 대부분은 광고 차단 안 쓰므로 문제 없음, 이 자체는 코드 이슈가 아니라 모든 GA 설치에 공통되는 업계 한계.

**보류 결정**:
- **애드센스**: 지금 신청(심사)하면 트래픽/콘텐츠 부족으로 반려될 위험 있고, 반려 시 재신청까지 대기 기간이 있어 오히려 늦어질 수 있음 — GA4로 1~2주 추이 확인 후 신청하기로. 계정 생성/`ads.txt` 등 사전 준비도 나중으로 미룸(사용자 판단).
- **광고차단 우회(Cloudflare Zaraz 등 퍼스트파티 프록시)**: 지금 트래픽 규모에선 불필요, 애드센스 붙이고 수익 분석이 중요해질 때 재검토.

**커밋**: `5080bf3`(GA4 연동), `8547c47`(재배포 트리거용 빈 커밋).

---

## 2026-07-09

### 주간 패치 체크 자동화 에이전트(Scheduled Cloud Routine) 신설

매랜/플래닛 공식 사이트를 매번 사람이 확인 안 해도 되게, 주기적으로 패치노트를 확인해서 관련 데이터를 자동으로 갱신하는 클라우드 에이전트가 있으면 좋겠다는 요청. 배포 타이밍은 사람이 가장 적게 플레이하는 시간대(우리 시간 오후 3시 = 한국시간 아침 6시)로 지정.

자율성 수준을 세 가지로 제안(완전자동 / 알림만 / 조사+수정+PR, 사람이 검토 후 머지)했고, **PR 기반(3번)**으로 확정 — 데이터 정확성이 중요한 프로젝트라 게임 수치 반영을 에이전트가 완전 자동으로 main에 바로 반영하는 건 리스크가 크다고 판단.

`RemoteTrigger`로 루틴 생성(`trig_01Wf6e7JvS3biMsG2Me2q2Cn`, 이름 "메랜Hub 매랜/플래닛 패치 체크 & 데이터 업데이트 PR", 레포 `LeviLee0611/maplelandhub`, 모델 `claude-sonnet-5`). 매 실행마다:
1. `CLAUDE.md`/`AGENTS.md`/`data.md`/`TODO.md`/`DEVLOG.md`를 먼저 읽어 프로젝트 규칙 파악
2. maple.land / mapleplanet.co.kr 공지·패치노트를 `data/.last-patch-check.json`(마지막 처리 기록)과 비교 — **새 게시물이 하나도 없으면 브랜치/커밋/PR 없이 조용히 종료** (매주 도는 작업이라 대부분 이 경로일 것으로 예상)
3. 새 패치가 있으면 우리 데이터(몬스터/드랍/아이템/퀘스트/스킬/큐브 등)와 관련 있는지 판단, `data.md` 파이프라인 그대로 반영(패치노트에 없는 수치는 절대 추측 금지 — 애매하면 TODO.md에 "확인 필요"로만 남김)
4. 커밋 전 `tsc --noEmit`/`eslint`/`vitest`/`build` 전부 통과 확인, 실패하면 되돌리고 커밋 안 함
5. 새 브랜치(`auto/patch-check-YYYY-MM-DD`)에 커밋 → push → `gh pr create`로 **main 대상 PR만 생성** — PR 직접 머지·main 직접 푸시는 금지, 사람이 검토 후 머지

GitHub 연동은 `/web-setup`이 실패해서(CLI 미인증) GitHub App 설치 링크로 대체 연동. 루틴 생성 시 사용자의 Gmail/Google Drive MCP 커넥터가 기본으로 자동 첨부됐는데, 이 루틴엔 전혀 불필요한 접근 권한이라 최소권한 원칙에 따라 제거 — `Claude_Code_Remote`만 남김.

스케줄은 처음엔 매일 오후 3시(MDT)로 만들었다가("어느 요일에 패치할지 모르니 매일 도는 게 안전"), "매일이면 업데이트 없는 날도 사이트 확인+문서 읽기 비용이 매번 든다, 토큰 낭비 아니냐"는 지적을 받아 **주 1회, 금요일 오후 3시(MDT) = 한국시간 토요일 오전 6시**로 변경(`cron_expression: "0 21 * * 5"`). 확인은 `https://claude.ai/code/routines/trig_01Wf6e7JvS3biMsG2Me2q2Cn` 또는 GitHub의 `auto/patch-check-*` 브랜치 PR에서.

### 메랜/플래닛 운영 구조 — 통합 유지로 결정

큐브 시뮬레이터의 "원하는 옵션 기댓값" 계산기를 롤 시뮬레이터에서 분리할지 논의하다가, 더 근본적으로 메랜과 플래닛을 아예 별도 사이트로 운영하는 게 나을지도 논의. 결론: **지금처럼 한 사이트에서 같이 운영하는 게 맞다**고 확정 — 별도 분리 없음, 코드 변경 없는 결정만. 큐브 기댓값 계산기 분리 여부는 별도 주제로 미뤄둠(아직 결론 없음).

### 플래닛 드랍테이블 아이콘 재제보 — 메랜/플래닛 데이터 파이프라인 동기화 누락

사용자가 플래닛 드랍테이블에서 "웃는 가면", "녹색 옷자락" 등 아이콘 없는 아이템을 재제보. 둘 다 오늘 아까 메랜 쪽에서 이미 `item-name-overrides.json`으로 고쳤던 바로 그 아이템들이라 이상해서 확인해보니, **새 버그가 아니라 동기화 누락**이었음 — `scripts/build-planet-data.mjs`가 `data/drop-index.json`(메랜)을 입력으로 받아 플래닛 배율/오버라이드를 얹어 `data/planet/drop-index.json`을 만드는 구조인데, 아까 메랜 drop-index.json만 재생성(`npm run build:drop-index`)하고 플래닛 쪽(`npm run build:planet-data`)은 재생성을 안 해서 플래닛 데이터가 여전히 옛날(synthetic 110개짜리) 스냅샷을 쓰고 있었음.

`npm run build:planet-data` 재실행으로 해결 — 플래닛도 synthetic 56개로 줄어듦(메랜과 정확히 같은 목록, "그륜"/"녹견량포" 2개만 여전히 미해결). 무관한 아이템 2600여 개·몬스터 653종 전부 그대로인 것 diff로 재확인.

**후속 메모**: 메랜/플래닛 데이터가 완전히 독립된 두 파이프라인이 아니라 플래닛이 메랜을 입력으로 삼는 단방향 의존 관계라, 앞으로 메랜 drop-index/monsters 등을 재생성하는 작업을 하면 **플래닛도 반드시 같이 재생성**해야 함 — 안 그러면 이번처럼 한쪽만 최신화되고 다른 쪽은 조용히 뒤처짐. `data.md`에 이 의존 관계는 이미 문서화돼 있었는데(빌드 스크립트 섹션) 실제 작업 시 놓쳤던 케이스.

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run` 통과(데이터만 바뀐 변경이라 코드 검증은 원래 통과하던 것 재확인 수준).

**재발 방지(같은 날 후속 조치)**: 위 "후속 메모"가 문서만으로는 또 놓칠 수 있다는 코드 리뷰 피드백을 받아, `package.json`의 `build:drop-index`/`build:drop-table-from-dropchance`/`build:item-detail-by`/`fetch:item-detail-by` 스크립트 끝에 `&& npm run build:planet-data`를 체이닝 — 메랜 쪽 소스(drop-index/item-detail-by)를 재생성하는 어떤 경로로 들어와도 플래닛이 자동으로 같이 재생성되어 사람이 기억해야 하는 단계가 아예 사라짐. 부수적으로 `build-planet-data.mjs`의 `drop-index`/`item-detail-by`/`cube-index` 출력이 실제 내용 변경 없이 `generatedAt`만 바뀌는 경우 파일을 건드리지 않도록 `writeJsonIfChanged`를 추가(리뷰에서 지적된 timestamp-only diff 노이즈 제거).

---

### QuestBoard 대용량 JSON 전송 최적화 — 버그 하나 발견하고 고침

DropTable에 이어 QuestBoard(퀘스트 페이지)도 같은 문제(quests.json 외에 monsters/monster-spawns/drop-index/item-detail-by/npc-locations 등 6개 JSON을 `"use client"` 컴포넌트에 그대로 props로 전달)가 있어서 이어서 처리.

**DropTable과 접근이 다름**: DropTable은 "검색 → 하나 선택 → 그 하나의 상세만 필요"라 온디맨드 API fetch가 맞았는데, QuestBoard는 703개 퀘스트를 통째로 검색/필터링하는 리스트 UI라 그 방식이 안 맞음. 대신 "퀘스트 데이터가 실제로 참조하는 아이템/NPC/몬스터가 몇 개나 되는지" 먼저 세어봄 — 보상템 393종(전체 아이템 카탈로그 2700여 종 중), 퀘스트 NPC 233종(전체 826종 중), 퀘스트가 참조하는 몬스터 447종(전체 689종 중) 정도로 원래 전체 카탈로그 대비 훨씬 작은 부분집합이었음. 그래서 온디맨드 fetch 대신 "서버(page.tsx)에서 이 부분집합만 미리 골라 훨씬 작은 버전을 만들어 한 번에 전달"하는 방식을 택함 — QuestBoard.tsx 내부 로직은 전혀 안 건드리고 page.tsx의 데이터 조립 부분만 손봄.

**버그 하나 발견**: 처음엔 "아이템"이라고 하면 당연히 보상 아이템(`quest.rewards.items`)이겠거니 하고 그 393종만 기준으로 슬림화했는데, 실제로 화면에서 "드랍 몬스터" 정보를 보여주는 `itemDropMonstersByItemId`는 보상이 아니라 **퀘스트 완료 시 제출해야 하는 수집 아이템**(`quest.requirements.complete.items`, 546종 — 보상 목록과는 겹치지 않는 별도 집합)에 쓰이는 거였음. `QuestBoard.tsx`를 다시 읽다가 `itemDropMonstersByItemId.get(item.id)`가 "조건" 컬럼(`requiredItems = quest.requirements.complete?.items`) 안에서만 호출되는 걸 발견하고 알아챔.

**검증 과정**: 스크린샷으로는 판단하기 애매해서(브라우저 확장이 이 세션 내내 불안정했음), DOM에서 `<p>드랍 몬스터</p>` 라벨 개수를 세는 방식으로 확인. 고치기 전엔 첫 20개 퀘스트 어디서도 0개(수집 아이템 393종이 아니라 잘못된 조합을 보고 있었으니 당연), Node 스크립트로 "실제로는 몇 개가 나와야 정상인지" 미리 계산해보니 7개가 나와야 함 — 고친 뒤 브라우저에서 정확히 7개로 일치하는 것 확인. 이 김에 `.next` 캐시를 지웠다가 기존에 떠있던 dev 서버(포트 3000)가 죽는 걸 발견해서 깨끗한 서버(포트 3001)로 재기동해 재검증 — 스테일 캐시 때문일 가능성도 배제.

**결과**: `/quests` 3.38MB→1.0MB(raw), gzip 334KB→124KB (약 3.3배 감소). 버서크 재검증 때처럼, "직접 확인 안 하고 넘어가면 조용히 깨진 기능을 그대로 배포할 뻔했다"는 케이스 — 대용량 데이터 최적화처럼 여러 파일 데이터를 재조합하는 작업은 payload 크기만 보고 끝내지 말고 실제 화면 기능까지 항상 확인이 필요함.

**코드 리뷰 후속 반영**: `rewardItemIds`라는 변수명이 실제로는 보상템+수집템을 다 담고 있어서 이름과 역할이 안 맞는다는 지적 → `referencedItemIds`로 개명. page.tsx에 그대로 들어있던 슬림 데이터 조립 로직(50줄 이상)도 `src/lib/quest-board-data.ts`(`buildSlimQuestBoardData`)로 추출 — DropTable 때 만든 `drop-table-lookup.ts`와 동일하게 "데이터 없는 순수 함수 + page.tsx는 JSON import와 호출만" 패턴으로 통일. 프로덕션 빌드 정적 HTML(`.next/server/app/quests.html`, 944KB)에서 "드랍 몬스터" 라벨 8개 확인해서 리팩토링 후에도 동일하게 동작하는 것 재검증(dev 서버가 세션 내내 불안정해서 이번엔 빌드 산출물 직접 검사로 확인).

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`, `npm run build` 통과.

---

### SEO 점검 + 드랍테이블 대용량 JSON 전송 문제 실제 수정

사용자가 "플래닛을 같이 병행하면서 최적화가 필요할 수도 있고, SEO도 점검해달라"고 요청.

**SEO 점검**: 처음엔 `/planet/drop-table`에 메타데이터가 없다고 판단했는데, `page.tsx`만 grep하고 같은 폴더의 `layout.tsx`(이 프로젝트가 메타데이터를 두는 실제 위치)를 놓친 오판이었음 — 재확인 결과 플래닛 페이지 5개 전부 title/description/canonical/OG/키워드 정상. 실제로 발견한 문제: `/admin/*`·`/ui/demo`가 robots.txt에서 크롤링 허용 상태(disallow 추가), `/feedback` 페이지 메타데이터 누락(layout.tsx 추가). OG 공유 이미지가 파비콘 하나로 통일된 것도 확인했지만 이미지 제작이 필요해 TODO로만 남김.

**대용량 JSON 문제 — 이번엔 진짜로 고침**: `data.md`에 "대용량 JSON은 API Route로 필터링 후 전달"이라는 규칙이 있고 TODO엔 "DropTable/QuestBoard 서버 props로 이전 완료"라고 기록돼 있었는데, 실측해보니 `/drop-table`·`/planet/drop-table` 페이지가 여전히 2.4MB(raw)를 클라이언트로 보내고 있었음 — "서버 컴포넌트에서 import"는 했지만 그 데이터를 그대로 `"use client"` DropTable 컴포넌트에 props로 넘기고 있어서, RSC 직렬화 과정에서 결국 클라이언트로 그대로 전송되고 있었던 것. 예전 기록이 "import 위치"만 보고 "완료"로 잘못 표시했던 것으로 보임.

**구현**: `items` 배열은 검색/아이콘 판별에 실제로 쓰는 필드(id/name/typeInfo.overallCategory·category/meta.synthetic/meta.equip.reqLevel)만 남기고 서버(page.tsx)에서 슬림화(0.82MB→0.18MB) — 나머지 필드(장비 스탯, 상점가, 캐시 여부 등)는 애초에 컴포넌트가 읽지도 않던 것들. 제일 무거운 `dropsByMonsterId`/`monstersByItemId`(합쳐서 ~1MB)는 초기 props에서 완전히 빼고, 몬스터/아이템을 실제로 선택했을 때만 새 API route로 온디맨드 fetch하도록 변경. 기존에 있던 "로컬 데이터 없으면 몬스터북 API로 폴백"하는 클라이언트 로직(`ensureFallbackDrops`)도 그대로 서버사이드로 옮겨서 하나의 라우트가 로컬/외부 소스를 알아서 판단하도록 통합 — 클라이언트 코드가 오히려 단순해짐. 검색 자체는 여전히 클라이언트 즉시 반응(네트워크 왕복 없음), 선택 시에만 짧은 로딩 문구.

**결과**: `/drop-table` 2.37MB→648KB(gzip 228KB→65KB), `/planet/drop-table` 2.4MB→679KB(gzip 228KB→72KB) — 약 3.5배 감소. QuestBoard(퀘스트 페이지, JSON 6개 겹쳐 전달 중, 1517줄로 DropTable보다 더 복잡)는 이번 범위에서 미해결로 남김.

**코드 리뷰(Codex) 반영 4건**:
1. 데이터 출처 배지가 실제 소스 문자열(`"dropchance-html+..."`)과 안 맞는 조건(`startsWith("drops-parsed")`) 때문에 항상 "MonsterBook reward"로 잘못 표시되던 버그 — 조건을 뒤집어 실제로 몬스터북 전용일 때만 그 라벨이 뜨도록 수정.
2. 신설한 API route 2개(`/api/drop-table/monster`, `/item`)가 edge 런타임에서 메랜+플래닛 데이터를 한 함수에 전부 번들하고 있었음(클라이언트 페이로드는 줄였지만 API 함수 번들/콜드스타트 비용으로 옮겨간 셈) — 서버별로 라우트 파일을 분리(`monster/mapleland`, `monster/planet`, `item/mapleland`, `item/planet`)하고 공통 로직은 `src/lib/drop-table-lookup.ts`에 데이터 없는 순수 함수로 추출. `npm run build`로 4개 라우트가 독립 함수로 분리된 것 확인.
3. 온디맨드 fetch 실패 시 빈 배열로 캐싱해서 재시도를 막던 문제 — 실패는 캐시하지 않고 별도 에러 상태로 분리, "다시 시도" 버튼 추가.
4. 타입상 항상 존재가 보장된 `monster?.mobCode`의 불필요한 옵셔널 체이닝 제거.

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`, `npm run build` 전부 통과. API 라우트는 curl로 메랜/플래닛 양쪽 다 확률 수치까지 재확인(플래닛이 정확히 4배), 브라우저로 검색→몬스터 선택→아이템 역방향 조회까지 실측(리팩토링 전후 결과 완전히 동일).

---

### 디자인 리뉴얼(색상), 드랍테이블 아이콘 누락 수정, 한방컷 계산기 Codex 리뷰 반영

**디자인 리뉴얼**: 어제 TODO에 "내일 색깔 위주로 전체 레이아웃 손보자"고 적어둔 항목 처리. 처음엔 사용자가 "현재 마젠타/버건디 강조색 유지"라고 했는데, 실제 화면을 열어보니 강조색이 시안/블루(`#38bdf8`, 플래닛은 앰버)였음 — 화면 확인 결과를 보여주고 재확인하니 "이번 기회에 마젠타/버건디로 교체"로 방향 전환. `--brand-accent` 계열 CSS 변수를 `#be123c`(버건디)로 바꾸고, `.retro-panel-tone-blue`(플래닛 전용으로만 있던 브랜드색 오버라이드)를 기본 규칙 자체에 병합해서 플래닛 전용 중복 블록 제거, 로그인 버튼·홈페이지 뱃지·사이드바 활성 카드 등 하드코딩된 시안 값도 함께 교체. 플래닛(앰버)은 `[data-server="planet"]` 분리 구조 그대로 둬서 두 서버 시각 구분 유지.

**드랍테이블 아이콘 누락**: 사용자가 드랍 테이블에서 아이콘 없는 아이템을 제보. 원인 조사 결과 드랍표 스크래핑 시 이름이 실제 아이템 DB와 안 맞으면 "가상 아이템"(synthetic, 음수 ID)으로 처리되는데, 마스터리북/카드/주문서류는 대표 아이콘으로 얼버무려지지만 나머지(장비·재료·펫템)는 아예 아이콘이 없었음. `data/drop-index.json`의 synthetic 아이템 110개 중 56개가 진짜 미해결 — maplestory.io 검색 API(`KMS/389/item?searchFor=`)로 정확히 일치하는 이름을 찾아 54개 해결(오타 3개도 같이 발견: "촉진게"→"촉진제", "얼음조각"에 "의" 누락, "포숀"→"포션"). `scripts/build-drop-index.mjs`의 `resolveDropItemId`가 기존에 `mapledbItemIdsByName`(레거시 mapledb.io 장비 데이터)만 참고하던 걸 확장해서, 새로 만든 수기 소스 `scripts/sources/item-name-overrides.json`을 우선 참고하도록 연결. `npm run build:drop-index` 재실행 후 old/new 아이템 셋 diff로 무관한 기존 아이템 전부 그대로인 것 확인(음수 id 54개 제거, 신규 39개 추가 — 15개는 이미 다른 몬스터 드랍으로 존재하던 실아이템이라 중복 제거된 것). 남은 2개("그륜", "녹견량포", 둘 다 G.팬텀워치 드랍)는 스크래핑 원본 이름 자체가 깨져 있어 실제 아이템 특정 불가 — 오매칭보다 미표시가 낫다고 판단해 그대로 둠.

**한방컷 계산기 — Codex 코드 리뷰 반영**: 이 세션에서 새로 만든 플래닛 스킬 기능에 대해 별도 Codex 리뷰를 받아 2건 수정.
1. **스나이핑 고정 데미지가 배율을 여전히 타던 버그(High)**: `specialBaseDamage`로 min/max를 199999에 고정해뒀지만, 그 뒤 `finalDamageMultiplier`(크리티컬샷/샤프아이즈/골든이글 등)가 그대로 곱해지고 있어서 "스탯/레벨 무관 고정 데미지"라는 안내 문구와 달리 199999를 넘길 수 있었음. 스나이핑 선택 시 `finalDamageMultiplier` 전체를 1로 고정하도록 수정 — 크리티컬샷 30 설정해도 199,999 그대로인 것 재확인.
2. **돌진이 다른 스킬에 잘못 곱해지던 문제(Medium)**: 앞선 구현에서 돌진을 "히어로 돌진 레벨" 보너스 슬라이더 방식(브랜디쉬 등 실제 사용 스킬에 곱연산으로 얹는 방식)으로 넣었는데, 리뷰에서 "돌진은 패시브 버프가 아니라 독립된 공격 스킬이라 다른 스킬에 최대 1.3배 곱해지는 게 게임 메커니즘과 안 맞다"는 지적을 받아 재설계. `damageMappingActive`에서 빼서 `damageMapping`으로 옮기고(count=1) 전사 3직업 `mainSkillMapping`에 독립 선택형 스킬로 추가, 기존 "돌진 레벨" 보너스 슬라이더(히어로 원래 있던 것 포함)와 `rushBonus` 상태·스냅샷 코드 전부 제거. 레벨1→30(72%→130%) 스케일링 정확히 확인(2,534→4,576, 비율 1.806배 = 130/72 일치).
3. **문서화(Low)**: `data.md`에 `scripts/sources/item-name-overrides.json` 설명 추가 — 손수 관리 소스, `build-drop-index.mjs`가 읽음, 새 미매칭 아이템 발견 시 추가하는 방법까지 기록.

**검증**: `npx tsc --noEmit`, `npx eslint src/ scripts/`, `npx vitest run`(30개 통과) 전부 통과.

---

### 한방컷 계산기 — 플래닛 누락 스킬 6종 구현 (돌진/생츄어리/닌자 스톰/드래곤 펄스/스나이핑) + 버서크 오판 정정

어제(2026-07-08) TODO에 "누락"으로 기록해둔 11개 스킬 중, 소환수 6종(엘퀴네스/이프리트/바하뮤트/피닉스/프리져/서포트 옥토퍼스)과 닌자 앰부쉬는 이 계산기의 "1회 사용=N타" 모델과 구조적으로 안 맞아서(지속시간 동안 알아서 도는 소환수·도트뎀 스킬) 사용자 확인 후 이번 범위에서 제외. 나머지 6종만 구현.

**리서치**: 타격 횟수(count) 등 부족한 데이터를 채우려고 서브에이전트로 namu.wiki "RED 이전 스킬" 문서를 재조사(claude-in-chrome, WebFetch는 namu.wiki Cloudflare 403으로 막힘). 결과: 돌진은 히어로/다크나이트 문서와 팔라딘 문서 수치가 서로 달라서(Lv15/30: 100%/130% vs 110%/140%) 사용자에게 확인받아 히어로/다크나이트 값으로 확정. 드래곤 펄스/스나이핑/피닉스/프리져/호밍/서포트 옥토퍼스는 애초에 1~30 레벨 테이블 자체가 존재하지 않는 마스터 전용 고정 효과 스킬로 확인(조사 실패가 아니라 정상).

**중요 발견 — 버서크는 원래 안 빠져 있었음**: 구현 착수 전 코드를 다시 훑어보니 `damageMappingActive["버서크"]`에 레벨 0~30 실데이터가 이미 있고, 다크나이트 패널에 슬라이더까지 이미 정상 동작 중이었음. 어제 조사가 `mainSkillMapping.json`(선택형 주력기 목록)만 grep해서 "버서크"가 안 보이니 누락으로 판단했는데, 실제로는 패시브 보너스 전용의 별도 메커니즘(`damageMappingActive` + 잡별 전용 슬라이더)으로 이미 구현되어 있었던 것 — 이 계산기엔 "선택형 주력 스킬"과 "레벨 입력하면 자동 % 환산되는 패시브 슬라이더" 두 가지 트랙이 있다는 걸 이번에 명확히 파악. 같은 이유로 히어로의 "돌진 레벨" 슬라이더(`rushBonus`)도 이미 존재했었는데, 데이터가 없어서 `1+레벨/100`이라는 조잡한 자리표시자 근사식만 쓰고 있었음.

**구현**:
1. **돌진**: 기존 히어로 전용 `rushBonus` 슬라이더의 계산식을 조잡한 근사치에서 `damageMappingActive["돌진"]` 실데이터 조회로 교체(레벨1 72%~레벨30 130%, 버서크와 동일한 패턴). 팔라딘·다크나이트 패널에도 동일 슬라이더 추가해 3개 전사 계열 공용화(`rushEffect` 훅, `isHeroJob || isPagePaladinJob || isSpearmanJob` 게이트).
2. **생츄어리/닌자 스톰/드래곤 펄스/스나이핑**: `mainSkillMapping`에 선택 가능한 스킬로 추가. 앵커값(Lv1/15/30)만 있는 생츄어리·닌자 스톰은 기존 브랜디쉬 방식(1~10/11~30 구간보간, `v10 = v15 - 5*step2`로 경계값 역산)으로 전체 1~30 테이블 생성. 드래곤 펄스는 마스터 전용 고정 100%라 전 레벨 동일값으로 등록.
3. **스나이핑 특수 메커니즘**: `specialBaseDamage`에 신규 분기 추가 — 스탯 계산을 아예 건너뛰고 min/max를 199999로 고정, `ignoreDefense: true`로 방어력 감산도 스킵. 기존 힐/베놈 등 특수 스킬과 동일한 패턴이라 엔진 자체는 변경 없음.
4. **생츄어리/스나이핑 안내 문구**: 생츄어리는 "대상 HP 1 고정, 즉사 아님"을, 스나이핑은 "스탯/레벨 무관 고정 데미지"를 스킬 패널에 문구로 표시(스나이핑 HP1 고정 룰과 즉사 판정을 실제 계산 로직에 반영하진 않음 — 이 계산기의 "몇 방컷" 개념 자체와 안 맞아서 정보 제공용 안내로만 처리).

**부수 발견 — 데이터 파이프라인 드리프트**: 스킬 데이터를 추가하려고 `data/raw/skills.raw.ts`를 열어보니, 커밋된 `data/skills/{mainSkillMapping,range20,range30,damageMapping}.json` 4개 파일이 시그너스 나이츠 5개 직업(107개 스킬, 소울마스터/플레임위자드/윈드브레이커/나이트워커/스트라이커) 데이터를 담고 있는데 정작 소스 `skills.raw.ts`엔 그 내용이 전혀 없었음 — `data.md`의 "산출물 JSON 직접 편집 금지, 소스 고치고 재빌드" 골든룰이 예전에 깨져 있었던 상태(시그너스 데이터를 산출물에 직접 넣고 소스는 안 고친 것으로 추정). 그대로 `node scripts/normalize-skills.mjs`를 돌렸으면 시그너스 데이터가 통째로 날아갈 뻔했음 — 재생성 전에 커밋된 JSON과 소스를 diff해서 시그너스분만 추출해 `skills.raw.ts`에 역이식한 뒤 재생성. 재생성 후 diff로 데이터 유실 없음 재확인.

**작업 중 실수 하나**: 새 스킬 데이터를 삽입할 때 `damageMapping`의 실제 닫는 위치를 착각해서(중간에 `sharpEyesMapping`/`criticalThrowMapping` 블록이 끼어있는 걸 놓침) 처음엔 `criticalThrowMapping` 안에 잘못 삽입했었음 — brace-depth를 직접 카운트하는 스크립트로 진짜 닫는 위치를 찾아서 발견 및 수정. 이후 `require()`로 실제 파싱 결과를 매번 검증하는 방식으로 전환.

**검증**: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(기존 30개 그대로 통과) 전부 통과. 개발 서버 띄워서 claude-in-chrome으로 직접 확인 — 팔라딘 생츄어리 선택 시 안내 문구 노출, 신궁 스나이핑 선택 시 한방컷 공격력이 스탯 무관 199,999 고정으로 나오는 것, 나이트로드 닌자 스톰이 레벨 1→30에서 717→1,365로 42%/80% 비율 그대로 스케일링되는 것, 히어로 브랜디쉬 기준으로 돌진 레벨 0→30 설정 시 9,504→12,355(정확히 1.3배)로 오르는 것까지 전부 실측 확인.

---

### 큐브 시뮬레이터 — 장비 종류 필터를 실제 롤 시뮬레이터에도 적용

코드 리뷰로 발견: "장비 종류" 선택 필터가 "원하는 옵션 기댓값" 계산기(`estimateOptionOdds`/`estimateCombinedOptionOdds`용 옵션 목록)에만 적용되고, 정작 큐브를 클릭해서 굴리는 실제 롤(`rollCubeResult`)과 그 확률 계산의 등급 내 가중치 합산(`eligiblePoolAtGrade`)은 필터 없이 전체 풀을 그대로 썼음 — 다른 부위 전용 스킬 옵션(예: 신발 선택 중인데 "쓸만한 미스틱 도어"가 뽑힘)이 실제로 결과에 나올 수 있었고, 가중치 총합 계산에도 그 옵션들이 끼어들어 다른 옵션들의 확률이 미세하게 부정확했음.

- "장비 종류" 선택 UI를 롤/기댓값 계산기 양쪽에서 공유하도록 상단 공용 위치로 이동.
- `suspiciousPoolForEquip`/`miraclePoolForEquip`(`isOptionAllowedForEquip`로 미리 필터링한 풀)을 롤(`rollCubeResult`)과 확률 계산 양쪽에 동일하게 전달하도록 수정 — 필터링 기준이 한 곳(컴포넌트)에만 있고 엔진 함수들은 이미 필터링된 풀을 받는 구조 유지.
- 검증: Node로 2만 회 가중치 추출 시뮬레이션을 돌려 신발 선택 시 "쓸만한 컴뱃 오더스"만 나오고 다른 부위 스킬(미스틱 도어/하이퍼 바디/샤프 아이즈/어드밴스드 블레스/윈드 부스터)은 전혀 안 나오는 것 확인. `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run` 통과.

### 큐브 시뮬레이터 — 같은 옵션 중복 선택 시 잘못 계산되던 버그 수정

코드 리뷰로 발견: `estimateCombinedOptionOdds`가 선택된 옵션 id들을 `new Set(targetEntryIds)`로 그냥 중복 제거해서 넘기고 있었음 — 사용자가 같은 옵션을 3슬롯 중 2개에 고르면(예: STR을 두 줄에) "그 옵션이 최소 1줄 나올 확률"로 잘못 계산되고 있었고, 실제로는 "최소 2줄 나올 확률"을 계산해야 정확함.

- `requiredCountById`(`Map<id, 선택된 횟수>`)로 같은 id가 몇 번 선택됐는지 추적하도록 전면 재작성. 줄이 최대 3개·목표도 최대 3개라 계산량이 작으므로, "N줄 이상 동시 만족" 확률을 조합을 직접 열거(brute-force)하는 방식으로 정확하게 계산.
- UI: 선택된 옵션 배지에 "(N줄)" 라벨 추가, 같은 id가 여러 슬롯에 선택돼도 React key 중복이 안 나도록 distinct id 기준으로 렌더링.
- 부수 개선: 캐시 비용 표시에 "(추정 환율 기준)"과 메포 병기 추가 — 원화 단독 표시가 실제 결제 금액처럼 보인다는 지적 반영.
- 신규 vitest 1개 추가(총 30개). `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run` 통과.

### 큐브 시뮬레이터 — 비상식적으로 큰 기댓값 수정, 등급업/옵션재설정 분리, 장비 종류 필터, UI 정리

사용자가 "약 118,311,108,362,038개" 같은 비상식적인 숫자를 지적하며, 실제 다른 사이트(maplanet.net)의 큐브 계산기가
쓰는 방식을 참고해도 좋다고 함. maplanet.net/cube-calc를 직접 열어 UI 구조를 확인:

- **목표 타입: "등급 업" / "옵션 띄우기"** 라디오 버튼으로 분리되어 있음 — "등급 업"은 옵션과 무관하게 순수 등급 상승
  기댓값만, "옵션 띄우기"는 "잠재능력 등급"(현재 등급, 드롭다운엔 레어/에픽/유니크만 있고 일반·레전드리 없음)을
  고정해두고 그 등급 안에서의 옵션 재설정 비용만 계산함. 즉 "등급까지 도달하는 비용"과 "그 등급에서 옵션 맞추는
  비용"을 아예 분리해서 보여줌 — 우리가 기존에 "옵션 기댓값" 하나로 등급 상승 확률까지 다 섞어서 계산하다 보니
  레전드리 등급 다중 옵션 조합에서 극단적으로 큰 숫자(1억 단위 이상)가 나온 이유였음.
- **"장비 종류" 드롭다운**(무기/한벌옷/하의/상의/얼굴장식/눈장식/귀고리/펜던트/벨트/반지/모자/장갑/신발/방패/망토)이
  있고, 안내 문구에 "옵션 목록은 고른 장비에서 실제로 나올 수 있는 옵션만 보여요. 예: 장갑→쓸만한 샤프
  아이즈(쓸샾), 상의·하의·전신 갑옷→쓸만한 하이퍼 바디(쓸바디), 모자→쓸만한 미스틱 도어, 신발→쓸만한 헤이스트"
  라고 명시 — 스킬 재사용("쓸만한 OO 스킬 사용 가능") 옵션이 장비 부위별로 다르다는 걸 확인.

이를 토대로 반영:

1. **`estimateGradeUpExpectedUses(fromGrade, toGrade=4)`** (`cubeSimulator.ts`) 신규 추가 — 옵션과 무관하게
   순수 등급 상승 확률(`GRADE_UP_CHANCE`)만으로 "이미 fromGrade에 도달한 아이템을 레전드리까지 올리는 데 평균
   몇 개 필요한지" 계산(각 단계 확률의 역수 합). "등급업 기댓값" 전용 패널로 분리해서 항상 노출.
2. **`estimateOptionOdds`/`estimateCombinedOptionOdds`에 `fixedOverallGrade?` 파라미터 추가** — 넘기면 등급
   상승 카스케이드 전체를 적분하지 않고 그 등급 하나로 고정해서 계산(`gradeIterations` 헬퍼로 통합). "이미 그
   등급에 도달했다고 가정" 체크박스로 노출 — 껐을 때는 기존처럼 등급업 확률까지 섞인 "처음부터" 비용(레전드리
   3옵션 조합이면 실제로 수천만 개 단위가 나올 수 있고, 이건 버그가 아니라 정직한 계산 결과임을 토글로 명시),
   켰을 때는 옵션 재설정만 남아 훨씬 현실적인 숫자(검증: 모자용 레전드리 스킬 옵션 하나 기준 23,162,081개 →
   토글 On 시 75.0개).
3. **장비 종류 필터** — `cube-data.js`에서 7개 스킬 옵션("쓸만한 헤이스트/미스틱 도어/샤프 아이즈/하이퍼
   바디/컴뱃 오더스/어드밴스드 블레스/윈드 부스터")의 `optionType`을 직접 확인한 결과, 등급만 다르고
   optionType이 같은 짝이 있음을 발견(51=미스틱 도어·어드밴스드 블레스, 53=하이퍼 바디, 54=샤프
   아이즈·윈드 부스터, 55=헤이스트·컴뱃 오더스) — 즉 "같은 부위의, 등급별로 다른 이름의 스킬 옵션"이었음.
   웹 검색으로 51=모자, 53=상의/하의/전신 갑옷, 54=장갑, 55=신발임을 교차 확인(더쿠 검색 결과: 어드밴스드
   블레스=모자, 윈드 부스터=장갑, 컴뱃 오더스=신발, 레전드리 등급·120레벨 이상 한정 — maplanet.net의 4개
   예시와 optionType 매칭 결과가 서로 일치). 이 4개 optionType만 선택한 장비 종류에 맞게 필터링하고, 나머지
   스탯/공격력류 옵션은 부위 무관하게 항상 노출(스탯 옵션이 부위별로 다르다는 근거는 못 찾아서 확대 적용 안 함).
4. **UI 정리** — 등급→옵션 드롭다운 쌍을 슬롯별로 테두리 박스에 묶어서 시각적으로 구분되게 정리, "등급업
   기댓값"과 "원하는 옵션 기댓값"을 별도 카드로 분리.

검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(신규 5개 포함 29 passed) 통과. 브라우저에서 장비
종류를 "모자"로 선택 시 스킬 옵션 목록이 실제로 필터링되는 것, 레벨 120 이상에서만 "쓸만한 어드밴스드 블레스"가
노출되는 것(reqLevel 120), 토글 on/off에 따라 23,162,081개 ↔ 75.0개로 바뀌는 것 확인.

---

## 2026-07-08

### 플래닛 데이터 정확도 재검증

**556종 카탈로그 매칭 몬스터 스탯 차이 로컬 분석**: `scripts/sources/planet/monster-catalog-data.js`(KMS pre-BigBang 데이터마이닝)와 `data/monsters.json`을 직접 diff하는 임시 스크립트로 분석. 556종 중 346종은 EXP가 정확히 4배 배율과 일치(문제없음). 나머지 중 50종은 **레벨 자체가 다름** — 몇 배씩 벌어지는 HP/레벨 차이 발견.

**중요 발견 — 예티/킹슬라임 계열이 포스트 빅뱅 스탯을 쓰고 있었을 가능성**: 예티(mobCode 6300001) 기존 데이터는 Lv117·HP406890인데, namu.wiki "메이플스토리/빅뱅 이전" 문서 검색 결과 실제 빅뱅 이전 예티는 **Lv65·HP11000**(엘나스) — catalog 값과 정확히 일치. 킹슬라임도 동일 패턴(기존 Lv23 vs 빅뱅 이전 정설 Lv40, catalog와 일치). 즉 `data/monsters.json`(메랜 원본, Zone A) 자체가 빅뱅 이후 리메이크된 스탯을 잘못 가져다 쓴 것으로 보이는 버그 — 플래닛뿐 아니라 메랜 본가에도 영향 있을 수 있어 별도 확인 필요(오늘은 플래닛 데이터에만 오버라이드 적용, 메랜 원본은 손대지 않음).

레벨 불일치 50종 전체를 `divergence-overrides.json`의 `monsterOverrides`에 catalog 값으로 일괄 반영(`scripts/build-planet-data.mjs` 재실행 후 `data/planet/monsters.json` 재생성). 예티·킹슬라임 2건만 namu.wiki로 개별 재검증했고 나머지는 동일 소스 신뢰도에 근거한 잠정 적용 — 주석에 명시.

**핑크빈 EXP는 미해결로 남김**: 웹 검색으로 확인한 표준 빅뱅 이전 값(EXP 6,290,000 — 메랜 원본과 정확히 일치)에 4배를 적용하면 25,160,000인데, 기존 오버라이드(조아요 소스) 값은 50,000,000(~7.95배), catalog 값은 200,000,000(~31.8배)로 세 값이 전부 불일치. 추가 검증 없이는 판단 불가라 조아요 값을 잠정 유지하고 주석에 세 후보값을 모두 기록.

**버그 발견 및 수정**: `applyMonsterOverrides`가 오버라이드 객체를 그대로 스프레드(`{...monster, ...override}`)해서 `_source`/`_comment` 같은 내부 주석 필드가 `data/planet/monsters.json` 출력에 그대로 leak되고 있었음(`appendNewMonsters`는 이미 `_`-prefix 필드를 걸러내고 있었는데 이 함수만 누락). `_`-prefix 필드 필터링 추가로 수정.

**시그너스 여제의 축복 스택 여부**: 웹 검색 결과 플래닛 고유 차이라는 근거를 찾지 못함 — 정령의 축복과 중첩 안 되고 더 높은 효과만 적용되는 표준 메이플스토리 규칙과 동일하게 작동하는 것으로 보임(디시 마이너 갤러리 질문글 1건 외 참고자료 없어 확정은 아님).

### 드랍 확률 샘플 검증 — 조아요(chowayo.com) 직접 대조

달팽이(100100)/파란 달팽이(100101) 두 몬스터의 드랍 아이템을 `chowayo.com/monsters/{mobCode}`와 전수 대조.

- **일반 아이템/소비품/광물**: 빨간 포션·초록사과·활전용 화살·석궁전용 화살·청동/가넷/강철/자수정의 원석 등 7~8개 항목 전부 정확히 일치 — 4배 배율 로직 자체는 정확하게 동작 확인.
- **문제 발견 — 주문서(장비 강화 스크롤) 드랍률 불일치**: 망토 마법방어력 주문서 60%·투구 방어력 주문서 10%(달팽이), 망토 체력 주문서 100%(파란 달팽이) 전부 chowayo에서 **동일하게 0.012%**로 표시되는데, 우리 데이터는 몬스터마다 다른 값(메랜 원본 base × 4 — 0.032%/0.024%/0.024%). 주문서는 몬스터별 개별 확률이 아니라 스크롤 자체의 고정/등급별 확률 공식을 따르는 것으로 추정 — 메랜 원본 스크래핑 단계에서부터 부정확했을 가능성. `data/drop-index.json`(메랜 원본, Zone D) 이슈라 플래닛 전용 오버라이드로는 해결 안 됨, 원본 스크래핑 로직 재검토 필요.
- **커버리지 갭 — 몬스터 카드 드랍 자체가 데이터에 없음**: chowayo엔 각 몬스터별 "OO 카드"(0.4%) 드랍이 있는데 `data/drop-index.json` 전체에 몬스터 카드 아이템이 하나도 없음(카드키/메모리카드 등 이름만 비슷한 무관 아이템 2개뿐). 드랍표 스크래핑 소스가 애초에 몬스터 카드 시스템을 다루지 않는 것으로 보임 — 정확도 버그라기보다 기능 커버리지 공백.

### 큐브 시뮬레이터 재정비

**옵션 수치 로직 수정**: `cube-data.js`의 모든 옵션이 정확히 `level["1"]~["20"]` 20개 브라켓을 갖는 걸 재확인. 기존 구현은 이 20단계를 "무작위 균등분포"로 뽑고 있었는데(페이지에도 그렇게 명시), 실제로는 옵션 값이 아이템 착용 레벨에 따라 결정되는 고정 구간(1~10 → 브라켓1, 11~20 → 브라켓2, ... 191~200 → 브라켓20)이라는 게 데이터 구조 자체(레벨 제한 필드 `reqLevel`과의 관계, 값이 브라켓 11 이후 고점(plateau)에서 멈추는 패턴)로 봤을 때 훨씬 타당함. `src/lib/calculators/cubeSimulator.ts`에 `levelBracket(itemLevel)` 함수 추가해 착용 레벨로 결정론적으로 값을 계산하도록 변경. 실제 서버의 정확한 구간 경계값은 확인 못 해서(공식 자료 없음) 페이지 안내 문구에 "약간 다를 수 있음" 명시는 유지.

**부수적으로 발견한 버그**: `scripts/build-planet-data.mjs`의 `applyMonsterOverrides`가 오버라이드 객체를 그대로 스프레드해서 `_source` 등 내부 주석 필드가 `data/planet/monsters.json`에 그대로 leak되고 있었음(위 "플래닛 데이터 정확도 재검증" 항목 참고) — `appendNewMonsters`처럼 `_`-prefix 필터링 추가.

**UI**: 결과 히스토리(`최근 결과` 패널)가 지금까지는 등급 구분 없는 회색 텍스트였는데, 메인 "결과" 패널과 동일한 등급별 색상(`gradeClassName`)을 적용하고 각 항목에 어떤 큐브(수상한/미라클)를 사용했는지 라벨 추가.

**단위 테스트 도입**: 프로젝트에 테스트 러너가 전혀 없었음(`src/lib/calculators/takenDamage/__tests__/sample.ts`는 assertion 없이 console.log만 하는 수동 확인용 스크립트) — `vitest` 설치(`vitest.config.ts`, `npm run test`), `src/lib/calculators/__tests__/cubeSimulator.test.ts`에 `levelBracket`/`rollCubeLine`/`rollCubeLines` 대상 12개 테스트 작성(브라켓 경계값, reqLevel 필터링, 결정론적 값 재현 등). `build-planet-data.mjs`는 내부 함수가 export되어 있지 않아 이번엔 테스트 대상에서 제외.

**검증**: `npx vitest run`(12 passed), `npx tsc --noEmit`, `npx eslint src/` 전부 통과. 개발 서버 띄워서 브라우저로 큐브 사용 → 결과/히스토리 렌더링, 착용 레벨 변경 시 수치 변화까지 직접 확인.

### 큐브 시뮬레이터 UI 전면 재설계 — 실제 아이템 이미지 + 이펙트/사운드

사용자 요청: 최대한 간단한 UI + 실제 큐브 이미지를 클릭하는 방식 + 사용 시 이펙트 + 사운드(가능하면 메이플 UI 느낌).

- **레이아웃 단순화**: 기존 좌(설정)/우(결과) 2단 그리드 → 착용 레벨 입력 → 큐브 2개(수상한/미라클) 아이콘 → 결과 영역이 위에서 아래로 흐르는 1단 레이아웃으로 교체. 큐브 종류 선택용 탭과 "사용하기" 버튼을 없애고, **큐브 아이콘 자체를 클릭하면 그 큐브를 바로 사용**하도록 통합(선택+실행이 한 동작).
- **실제 아이템 아이콘**: maplestory.io 공개 API에서 아이템 검색(`/api/KMS/389/item?searchFor=`)으로 "수상한 큐브"(itemId 2710000)와 "미라클 큐브"(itemId 5062000) 실제 아이콘을 찾아 적용. 기존 `getItemIconCandidateUrls`(여러 리전/버전 폴백) 패턴 재사용, `DropTable.tsx`의 아이콘 폴백 컴포넌트 패턴을 그대로 차용.
- **이펙트**: 클릭 시 큐브가 흔들리는 `cube-shake`(0.45s) → 골드/시안 방사형 플래시(`cube-flash`) → 결과가 살짝 떠오르며 나타나는 `cube-reveal` 순서로 연출. 키프레임은 `globals.css` 말미에 기존 `fadeUp`/`slimeFloat` 패턴과 동일한 방식으로 추가, `prefers-reduced-motion`에서는 전부 비활성화.
- **사운드**: 외부 음원 없이 Web Audio API로 직접 합성(`src/lib/audio/cubeSfx.ts`) — 클릭 즉시 낮은 "퉁" 타격음, 결과 공개 시 상승 아르페지오(도-미-솔-도) 차임. 저작권 있는 실제 게임 음원을 가져오는 대신 자체 합성으로 대체. 헤더에 🔊/🔇 음소거 토글 추가.
- **메이플 UI 느낌**: 기존 사이트의 레트로/글래스 테마(`--retro-border`, `--brand-accent` 등)를 그대로 따르되, 큐브를 장비창 슬롯처럼 보더 박스로 감싸 아이템 사용 UI에 가깝게 표현. 전체 테마를 픽셀아트로 새로 짜지는 않음(기존 디자인 시스템과의 일관성 우선).
- **검증**: 브라우저에서 직접 클릭 → 흔들림 → 플래시 → 결과 노출 확인, 콘솔에 코드 관련 에러 없음(브라우저 확장 프로그램발 hydration 경고 1건은 무관 — React 자체가 "브라우저 확장 프로그램이 DOM을 건드렸을 때 발생"이라고 명시).

### 큐브 시뮬레이터 — 등급 상승 연출 추가 + 가독성 개선

사용자 피드백: 미라클 큐브는 실제로 에픽 → 유니크 → 레전드리처럼 등급이 단계적으로 올라가는 연출이 있는데 그게 없었고, 클릭해도 임팩트가 부족했고, 결과 옵션 텍스트가 잘 안 보임.

- **등급 상승(climb) 연출**: 최종 결과(3줄 중 최고 등급)를 먼저 계산해두고, 큐브별 시작 등급(수상한 큐브=레어, 미라클 큐브=에픽 — 실제 서버 기준)부터 최종 등급까지 한 단계씩(550ms 간격) 큰 배지로 올라가며 보여줌. 각 단계마다 배지가 팝(스케일 바운스, `cube-grade-bump` 키프레임)하고 등급이 높아질수록 음높이가 올라가는 효과음(`playCubeGradeUpSound`) 재생 — 최종 결과의 최고 등급이 시작 등급보다 낮으면(가중치 데이터상 드물게 가능) 억지 연출 없이 바로 그 등급만 보여줌(거짓 서스펜스 방지).
- **가독성 개선**: 기존 3단계 색상 구분(파랑/초록/회색 계열)을 실제 게임 등급 색상 5단계로 교체(일반=회색, 레어=파랑, 에픽=보라, 유니크=골드, 레전드리=에메랄드), 등급 표시를 작은 회색 텍스트 → 진하게 채워진 pill 배지로 변경, 옵션 텍스트를 `text-sm` → `text-base font-bold`로 키움.
- **버그 수정**: `Date.now()`를 비동기 핸들러 안에서 직접 호출하다 `react-hooks/purity` eslint 규칙(impure function 경고)에 걸려 `useRef` 카운터로 교체.
- **검증**: `npx tsc --noEmit`, `npx eslint src/`(0 errors) 통과, 브라우저에서 미라클 큐브 클릭 → 등급 상승 배지 → 결과 노출까지 직접 확인.

### 큐브 시뮬레이터 — 등급 고정 모드 추가

사용자 피드백: 실제 서버는 반복 사용으로 등급이 오르는 "등급업" 과정이 있는데, 그거 말고 원하는 등급을 바로 골라서 그 등급의 옵션만 재설정(리롤)해보고 싶다는 요청. 다른 메이플 큐브 시뮬레이터(츄츄지지 등)를 참고해보니 "시작 등급 고정" 형태로 이미 흔한 기능임을 확인.

- `src/lib/calculators/cubeSimulator.ts`에 `filterPoolByGrade(pool, grade)` 추가 — 등급 고정 모드에서 해당 등급 옵션만 남겨서 뽑음. 유닛 테스트 2개 추가(총 14개).
- 컴포넌트에 "등급 고정" 체크박스 + 등급 5개(일반~레전드리) 선택 칩 추가. 켜져 있으면 등급 상승(climb) 연출 없이 흔들림 → 바로 결과 공개(고른 등급이 이미 확정이라 서스펜스 연출이 의미 없음).
- 안내 문구에 "등급 고정 모드는 실제 서버의 등급업 확률을 재현하지 않는다"는 점 명시 — 확률 시뮬레이션이 아니라 옵션 구성 미리보기 용도임을 분명히 함.
- 검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(14 passed) 전부 통과. 브라우저에서 레전드리 고정 → 미라클 큐브 사용 → 3줄 전부 레전드리로 즉시 공개되는 것 확인.

### 큐브 시뮬레이터 — 실제 등급 배치 로직 적용 (1번째=표기등급, 2·3번째=한단계 낮은 등급)

사용자 피드백: "레전드리는 유니크랑 레전드리가 나오고, 유니크는 에픽이랑 유니크가 나온다" — 실제 서버는 3줄이 전부 독립적으로 같은 등급 풀에서 뽑히는 게 아니라, 1번째 줄은 큐브의 "표기 등급" 그대로, 2·3번째 줄은 기본적으로 한 단계 낮은 등급이되 낮은 확률로 표기 등급 그대로 나오는("이탈") 구조. 기존 구현(등급 고정 모드 포함)은 3줄을 전부 같은 등급 풀에서 독립적으로 뽑고 있어서 이 부분이 틀려 있었음.

**리서치**: 넥슨 공식 공지("큐브의 잠재능력 재설정 로직과 세부 확률 공개", maplestory.nexon.com/News/Notice/Notice/133316)에서 이탈확률 확인 — 레드 큐브 2번째 10%/3번째 1%, 블랙 큐브 2번째 20%/3번째 5%. 메이플플래닛의 수상한 큐브/미라클 큐브에 대한 공식 수치는 못 찾아서, 수상한 큐브=레드 큐브급, 미라클 큐브=블랙 큐브급으로 대응해 근사 적용(잠정치, 코드에 출처 주석 명시).

**구현**: `src/lib/calculators/cubeSimulator.ts`를 3줄 독립 샘플링(`rollCubeLine`/`rollCubeLines`) 방식에서 `rollCubeResult(pool, itemLevel, cubeType, fixedOverallGrade?)` 단일 함수로 교체 —
1. `pickOverallGrade`: 등급별 가중치 합계로 큐브의 "표기 등급"을 먼저 결정(고정 등급 모드에서는 이 단계를 건너뛰고 사용자가 고른 등급을 그대로 사용)
2. 1번째 줄은 표기 등급에서, 2·3번째 줄은 이탈 확률에 따라 표기 등급 또는 한 단계 낮은 등급에서 각각 뽑음
3. `filterPoolByGrade`(직전 세션에서 추가했던 등급 고정용 헬퍼)는 이 변경으로 불필요해져 제거, 등급 고정 모드도 `rollCubeResult`에 `fixedOverallGrade`를 넘기는 방식으로 통합

기존 `rollCubeLine`/`rollCubeLines`/`filterPoolByGrade`와 그 테스트는 전부 제거하고 `rollCubeResult`/`pickOverallGrade` 기준으로 테스트 재작성(14개).

검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(14 passed) 통과. 브라우저에서 레전드리 고정 + 미라클 큐브 사용 → 레전드리/유니크/유니크로 나오는 것 확인.

### 큐브 시뮬레이터 — 등급 확률을 mapleplanet.co.kr 공식 페이지에서 직접 검증

사용자가 "레드/블랙 큐브 근사치 말고 실제 수치를 다른 사이트에서 찾아 적용해달라"고 요청. `mapleplanet.co.kr/Cube`를 WebFetch로 열어봤지만 403(봇 차단)이라, claude-in-chrome으로 실제 브라우저에서 직접 열어 JS로 상태를 조작하며 확인함.

**사이트 자체 버그 발견**: 페이지의 "수상한 큐브"/"미라클 큐브" 탭 버튼을 클릭해도 확률표 텍스트가 항상 "(수상한 큐브)"로 고정 표시되는 버그가 있음(`document.getElementById('miracleCubeLink').click()`으로 내부 상태 `window.activeCube`는 실제로 "miracle"로 바뀌는데 화면 텍스트만 안 바뀜). 수상한 큐브 → 미라클 큐브로 순서대로 두 번 클릭해야 정상적으로 갱신됨(진짜 상태 변경 이벤트가 필요했던 것으로 추정).

**확인된 사실(두 큐브 완전히 동일)**:
- 등급 상승: 레어→에픽 6.0000%, 에픽→유니크 1.8000% (수상한/미라클 동일 — 이전에 미라클을 블랙 큐브급 20%/5%로 다르게 넣었던 게 틀렸음, 삭제)
- 옵션 등급: 1번째 옵션은 표기 등급 100%, 2번째 옵션은 표기 등급 유지 10%(아니면 한 단계 하락), 3번째 옵션은 유지 1%(아니면 하락) — 기존에 넣었던 수상한 큐브 수치(10%/1%)와 정확히 일치, 미라클도 동일하게 통일
- **공식 확률표엔 레전드리 단계가 없음**(유니크까지만 표기). 사이트 전역 JS `window.gradeLabel`도 `{0:노멀,1:레어,2:에픽,3:유니크}` 4개뿐이고 페이지 전체에 "레전드리" 문자열 자체가 없음 — 그런데 `cube-data.js` 원본 풀엔 grade:4 옵션이 실제로 존재(수상한 56개/미라클 44개)

사용자에게 이 모순을 보고하고 처리 방법을 물어봄 → 레전드리 유지하기로 결정(실제 게임에서 레전드리를 봤다는 사용자 경험 신뢰). 처음엔 유니크→레전드리 확률을 앞 두 단계 감소 비율(1.8%/6%=0.3배)로 추정한 0.54%를 임시로 넣었으나, 사용자가 "다른 곳은 확인했냐"고 재차 확인함 — 실제로 다른 사이트/커뮤니티 정리글을 더 찾아봐도 메이플플래닛의 유니크→레전드리 수치는 어디에도 없었음. 다만 넥슨 공식 큐브 확률 가이드를 순서대로 대조하다가 **레어→에픽 6.0%/에픽→유니크 1.8%가 "레드 큐브"의 공식 확률표와 소수점까지 정확히 일치**하는 걸 발견 — 메이플플래닛이 레드 큐브 표를 그대로 가져다 쓴 것으로 보여, 추정치 대신 레드 큐브의 공식 유니크→레전드리 확률(0.3%)로 교체함. `cubeSimulator.ts` 주석에 근거 명시.

**구현**: `pickOverallGrade`(풀 가중치 합산 방식)를 완전히 폐기하고 `rollOverallGrade()`로 교체 — 항상 레어(1)에서 시작해 순차적으로 에픽→유니크→레전드리 상승을 시도하는 카스케이드 방식(공식 확률표 그대로 재현). `DEVIATION_CHANCE`도 두 큐브 동일값(10%/1%)으로 통일. 컴포넌트의 `CLIMB_BASELINE`(큐브별로 다른 시작 등급)도 두 큐브 모두 레어 시작으로 확인되어 `CLIMB_BASELINE_GRADE` 단일 상수로 단순화.

테스트 15개로 갱신(`rollOverallGrade`의 에스컬레이션 단계별 검증 3개 추가, 두 큐브 이탈확률 동일함 검증으로 기존 "미라클이 더 높다" 테스트 대체). `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(15 passed) 통과. 브라우저에서 수상한 큐브 사용 → 레어/일반/일반(가장 흔한 케이스, 이탈 실패)으로 나오는 것 확인.

### 큐브 시뮬레이터 — 연출 속도 개선 (결과를 빨리 보고 싶어함)

사용자 피드백: 등급이 실제로 오르지 않는 대부분의 경우(레어 유지, 확률상 가장 흔함)에도 매번 상승 연출을 보여주는 게 느리게 느껴짐 — 사람들은 상승 연출보다 옵션 결과를 빨리 보고 싶어함. 그리고 큐브 사용 중에 이전 결과 옵션 리스트가 사라지는 것도 원치 않음.

- 실제로 등급 상승이 없었던 경우(표기 등급이 시작 등급인 레어에 그대로 머무름 — 확률상 94%로 가장 흔함) 상승 연출을 완전히 생략하고 흔들림 직후 바로 옵션 공개. 등급 고정 모드도 동일하게 즉시 공개.
- 이전 결과를 `null`로 지우지 않고 그대로 화면에 둔 채로 다음 큐브 사용을 준비 — 흔들리는 동안 화면이 비지 않고 이전 옵션이 계속 보이다가, 새 결과가 준비되면 그 자리에서 바로 교체됨(상승 연출이 필요한 경우에만 그 사이에 등급 배지가 잠깐 뜸).
- 흔들림/상승 단계별 시간도 단축(450ms→350ms, 550ms→400ms), 마지막 홀드 딜레이 제거.
- 검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(15 passed) 통과. 브라우저에서 연속으로 큐브 사용 → 등급 유지 시 즉시 옵션 교체, 다른 큐브를 눌러도 이전 옵션이 화면에 남아있다가 새 결과로 바로 바뀌는 것 확인.

### 큐브 시뮬레이터 — 유니크→레전드리 확률 근거 보강 + "원하는 옵션 기댓값" 계산기 추가

사용자가 등급 고정으로 레전드리를 계속 돌렸는데 2·3번째 줄 이탈(레전드리 유지)이 거의 안 나온다고 문의 — 실제로 1만 회 시뮬레이션 결과 10.3%/1.0%로 공식 확률(10%/1%)과 정확히 일치함을 확인, 의도한 정상 동작임을 답변. 이어서 "공식으로 적용된 게 맞냐"는 재질문에 등급상승/이탈 확률은 공식 확인, 유니크→레전드리(0.54%, 앞 단계 감소비율 추정치)만 미확인이라고 정정.

"다른 곳은 확인 안 했냐"는 지적에 재검색 — 메이플플래닛 전용 정보글은 여전히 유니크에서 끊김. 대신 넥슨 공식 큐브 가이드를 큐브 종류별로 대조하다가 **레어→에픽 6.0%/에픽→유니크 1.8%가 "레드 큐브" 공식 확률표와 소수점까지 정확히 일치**하는 걸 발견 — 메이플플래닛이 레드 큐브의 등급 상승표를 통째로 가져다 쓴 것으로 보여, 추정치(0.54%) 대신 레드 큐브의 공식 유니크→레전드리 확률(0.3%)로 교체(`GRADE_UP_CHANCE.uniqueToLegendary`). 다만 메이플플래닛 자체가 100% 레드 큐브와 동일한지는 확인된 게 아니라 TODO에 잠정치로 기록.

**"원하는 옵션 기댓값" 계산기 추가**: 사용자가 특정 옵션을 고르면 큐브 1회당 확률과 평균 기댓값을 계산해서 보여주는 기능 요청 — UI로 추가하기로 결정. 실제 반복 시뮬레이션 없이 닫힌 형태(closed-form) 수식으로 계산:
- `overallGradeDistribution()`: 등급 상승 카스케이드(레어 94%/에픽 5.892%/유니크 0.1077%/레전드리 0.000324%)를 정확한 확률 분포로 계산
- `estimateOptionOdds(pool, itemLevel, cubeType, targetEntryId)`: 표기 등급별로 1~3번째 줄이 해당 등급·해당 옵션일 확률을 조합해 "큐브 1회당 확률"을 계산, 기댓값은 역수(1/p)
- 컴포넌트에 큐브 종류 탭 + 등급별로 그룹화된 옵션 선택 `<select>` + 확률/기댓값 표시 UI 추가. `resolveOptionText`를 `cubeSimulator.ts`에서 export해서 선택 목록에 착용 레벨 기준 실제 수치 텍스트 표시
- 테스트 4개 추가(레어/레전드리 등급 각각 손계산 검증값과 대조, 가중치 비율에 따른 확률 스케일링 검증, 존재하지 않는 id는 null) — 총 19개
- 검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(19 passed) 통과. 브라우저에서 레전드리 등급 옵션(기댓값 약 2200만 회) vs 일반 등급 옵션(확률 11.47%, 기댓값 8.7회)으로 등급별 스케일 차이 확인.

### 큐브 시뮬레이터 — 코드 리뷰 반영 + 기댓값 계산기 확장(메소 비용/3옵션 동시 선택)

**코드 리뷰(Codex)로 발견된 버그 수정**:
- `estimateOptionOdds`의 `withinGradeProb` 분자가 착용 레벨 미달 옵션에도 `target.weight`를 그대로 써서, 실제로는 절대 안 나오는 옵션(예: 레벨 70에 reqLevel 100 옵션)에도 0이 아닌 확률이 표시되던 버그. `lineHitProbability` 헬퍼로 통합하면서 target이 `eligiblePoolAtGrade` 후보에 실제로 포함되는지 확인하도록 수정, 회귀 테스트 추가.
- 컴포넌트의 옵션 선택 목록도 레벨 미달 옵션을 그대로 보여주고 있던 문제 — `eligiblePoolAtGrade`를 export해서 UI 목록 자체를 착용 레벨 기준으로 필터링(레벨 미달 옵션은 목록에서 아예 제외).
- `sitemap.ts`에서 플래닛 라우트 5개(큐브 시뮬레이터 포함)가 이번 세션 이전부터 이미 빠져 있던 것 발견 — 복원.
- `<img>` eslint 경고 — `DropTable.tsx`와 동일하게 파일 상단에 `/* eslint-disable @next/next/no-img-element */` 추가(외부 동적 아이콘이라 next/image 부적합, 기존 컨벤션과 통일).

**기댓값에 메소 비용 포함**: 큐브 가격을 고정값으로 넣으려 했으나 조사 결과 미라클 큐브는 메소가 아니라 **캐시샵(현금) 아이템**이고, 수상한 큐브는 **거래소 시세가 유동적**(고정 메소가 없음)이라는 걸 발견 — 사용자에게 확인 후 실제로 다른 사이트(maplanet.net, 메이플플래닛 비공식 거래소 시세 사이트)에서 직접 조사: 미라클 큐브는 "캐시샵 110개 묶음=45,000메포" 환율 계산으로 645,386메소(2026-07-08 기준) 확인, 수상한 큐브는 시세 추적 자체가 없어(몬스터 드랍으로도 획득 가능해 거래 가치가 낮은 것으로 추정) 신뢰할 기본값을 못 찾음. 큐브 가격을 직접 입력하는 필드로 추가(미라클 큐브만 기본값 645,386 채움), 입력 시 "예상 총 비용(기댓값 × 가격)" 표시.

**3개 옵션 동시 선택**: 잠재능력 줄이 3개라는 지적에 따라 단일 선택 → 3슬롯 선택으로 확장. `estimateCombinedOptionOdds` 추가 — 등급 상승 카스케이드의 각 분기(표기 등급 x 줄2/3의 유지·하락)마다 포함-배제 원리로 "선택한 옵션들이 전부 동시에 나올 확률"을 계산(줄마다 최대 하나의 옵션만 나올 수 있다는 제약 반영). 각 옵션의 개별 확률도 함께 표시.

테스트 5개 추가(reqLevel 회귀 테스트, 단일/2개/3개 선택 조합 확률 손계산 대조) — 총 24개. 검증: `npx tsc --noEmit`, `npx eslint src/`(0 errors, 0 warnings), `npx vitest run`(24 passed) 통과. 브라우저에서 2옵션(DEX+INT, 결합확률 0.74%)/1옵션+미라클 큐브 가격(예상 비용 4,900,214메소) 확인.

### 큐브 시뮬레이터 — 메소 비용 대신 "몇 개 필요한지" + 미라클 큐브 캐시 비용으로 변경

사용자 피드백: 메소 비용은 필요 없고, 대략 몇 개의 큐브를 써야 하는지(이미 "기댓값"으로 표시 중)와 — 미라클 큐브는 캐시 아이템이니 **캐시로 얼마나 써야 하는지**를 보여주면 좋겠다는 요청.

- 큐브 가격 직접 입력 필드/메소 비용 계산 전부 제거(`cubePriceByType` 상태, `CUBE_PRICE_DEFAULT` 등).
- 미라클 큐브 전용으로 "예상 캐시 비용" 추가 — 공식 공지(mapleplanet.co.kr/board/notice/511, "미라클 큐브 가격 변경 관련 안내")에서 확인한 캐시샵 묶음 판매가(110개=45,000메포) 기준으로 `기댓값 × (45000/110)` 계산, 메포 단위로 표시. 수상한 큐브는 캐시 아이템이 아니라서 캐시 비용 표시 안 함.
- 검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(24 passed, 로직 변경 없어 테스트 그대로 통과) 통과. 브라우저에서 미라클 큐브 레전드리 옵션 선택 → 기댓값 17,823,419개·예상 캐시 비용 약 72억 메포로 정상 계산되는 것 확인(극희귀 옵션이라 나온 큰 숫자, 계산식 자체는 검증됨).

### 큐브 시뮬레이터 — 원화 환산 + 검색 가능한 옵션 선택 UI + 자동 기댓값 + 확률 프레이밍 개선

이어진 사용자 피드백 4가지를 한 번에 반영:

1. **캐시는 현금(원화)으로**: 메포 표시로는 부족하다는 지적 — 메포->원화 공식 고정가를 찾지 못해(플랫폼 결제 페이지 접근 불가, maplanet.net도 메소↔메포 환율만 취급하고 메포↔원화는 다루지 않음), 커뮤니티에 통용되는 메소마켓 환율(1,150~1,250메포 ≈ 1,000원)의 중간값(1,200메포=1,000원)을 근사치로 채택 — `KRW_PER_MEPO_APPROX` 상수로 명시, 잠정치임을 주석에 남김.
2. **옵션 선택 UI 개선**: 기존 3개의 큰 `<select>`(등급별 optgroup만 있고 텍스트 검색 불가)를 커스텀 `OptionPicker` 컴포넌트로 교체 — 검색창 + 등급별 색상 배지 목록의 드롭다운(바깥 클릭/Esc로 닫힘). 선택된 옵션은 버튼 자체가 해당 등급 색으로 물듦.
3. **"원하는 옵션 기댓값" 패널 강조**: 일반 텍스트 헤더 → 🎯 아이콘 + 브랜드 강조색 테두리를 두른 카드로 승격. "평균 몇 개 필요한지"를 가장 크고 굵은 숫자로 표시(`text-xl font-extrabold`), 확률/캐시 비용은 보조 정보로 아래에.
4. **옵션 미선택 시 자동 기댓값**: 옵션을 하나도 고르지 않은 상태에서 큐브를 사용하면, 그 결과(방금 뽑힌 3줄) 자체의 확률/기댓값을 자동으로 계산해서 보여줌("옵션을 고르지 않아서, 방금 사용한 OO 큐브 결과 기준으로 보여드려요"). 이를 위해 `ResolvedCubeLine`에 `id` 필드 추가(기존엔 text/grade/optionType만 있어 결과 줄을 다시 특정 옵션으로 역참조할 수 없었음), `pickLineAtGrade`가 채워주도록 수정.
5. **확률 표기 방식 변경**: "STR : +9%"처럼 희귀한 옵션이 "0.0000%"로 표시돼 체감이 안 된다는 피드백 — `formatProbability`를 새로 만들어 0.001% 미만이면 "1/N" 분수 형태로 표시(예: "1/36,731,195"), 그 이상은 기존처럼 %. 기댓값("평균 몇 개")을 확률보다 먼저·크게 보여주는 방향으로 순서도 변경.

검증: `npx tsc --noEmit`, `npx eslint src/`(React Compiler가 처음에 `useMemo` 안의 `for...of` + 조기 `return` 패턴을 memoization 보존 불가로 에러 처리 — `flatMap`/`find` 조합으로 바꿔서 해결), `npx vitest run`(24 passed) 통과. 브라우저에서 옵션 검색·선택, 등급 색상 배지, 옵션 미선택 상태로 큐브 사용 후 자동 기댓값(수상한 큐브 결과 마법방어력+70/STR+4/DEX+4 조합 → 결합확률 0.0604%·기댓값 1656.6개) 전부 확인.

### 큐브 시뮬레이터 — 옵션 드롭다운을 등급→옵션 2단계로, 자동 기댓값 제거

바로 다음 세션에서 사용자가 방향을 일부 되돌림:

1. **옵션 드롭다운이 너무 길다** — 검색 가능한 커스텀 콤보박스(`OptionPicker`, 등급 그룹 전체를 한 목록에 표시)를 버리고, "등급 선택" 드롭다운 → 그 등급 안의 옵션만 보여주는 두 번째 드롭다운으로 교체(`GradeOptionPicker`). 등급을 안 고르면 옵션 드롭다운은 비활성화(`등급을 먼저 선택` 플레이스홀더). 등급 변경 시 이전 선택 옵션은 초기화. 검색 UI(입력창, 바깥 클릭 감지 등)가 통째로 필요 없어져 컴포넌트가 더 단순해짐.
2. **"옵션을 골랐을 때만" 기댓값 표시** — 직전에 추가했던 "옵션 미선택 시 방금 뽑은 결과 기준 자동 표시" 기능을 제거. 이를 위해 추가했던 `ResolvedCubeLine.id` 필드와 `pickLineAtGrade`의 관련 코드도 함께 되돌림(다른 곳에서 쓰이지 않는 필드라 정리).
3. **"등급 고정" 옵션은 평소에도 보이게** — 큐브 사용 시뮬레이터 쪽의 "등급 고정" 등급 배지 버튼들이 체크박스를 켜야만 보이던 것을, 체크 여부와 무관하게 항상 보이도록 변경(체크박스는 여전히 실제 롤에 고정 등급을 적용할지만 결정).

검증: `npx tsc --noEmit`, `npx eslint src/`, `npx vitest run`(24 passed, 엔진 로직은 안 건드려서 그대로 통과) 통과. 브라우저에서 등급 선택 → 옵션 목록이 그 등급으로 좁혀지는 것, 옵션 미선택 시 안내 문구만 뜨고 선택해야 확률/기댓값이 뜨는 것, 등급 고정 배지가 체크 여부와 무관하게 항상 보이는 것 확인.

---

## 2026-07-07

### 메이플 플래닛(Planet) 서버 지원 추가

**배경**: 메이플랜드 외에 최근 흥행 중인 신규 사설서버 "메이플 플래닛"(넥슨 MapleStory Worlds 플랫폼, 빅뱅 직전 KMS 1.2.95~98 기반 — 메이플랜드와 동일 버전)의 유저층을 흡수하기 위해 드랍테이블/계산기부터 플래닛 버전 제공. 이미 플래닛 전용 커뮤니티 사이트가 여럿(planet-helper.com, 플래닛고, 조아요, mapleplanet.gg) 있어 정면 대결보다 "메랜Hub가 잘하는 기능부터" 전략으로 진행.

**라우트 구조**: 기존 메랜 URL(`/`, `/drop-table`, `/calculator/damage`, `/calculators/onehit`)은 그대로 두고 `/planet` 이하에 미러링 — SEO 자산 보존 + 서버별 URL 분리.
- `/planet` — 랜딩 페이지 (기능 카드 그리드)
- `/planet/drop-table`, `/planet/calculator/damage`, `/planet/calculators/onehit`
- `/planet/cube-simulator` — 플래닛 전용 신규 기능(아래 참고, 메랜엔 큐브/잠재능력 시스템 자체가 없음)

**컴포넌트 재사용 전략**: `DropTable`/`TakenDamageCalculator`/`OneHitCalculatorClient`(구 onehit page.tsx에서 분리)는 메랜과 플래닛이 공유. 몬스터 데이터/서버 구분은 전부 **prop으로 주입**(`monsters`, `server`, `calculatorBasePath`, `itemLinkBase`) — 기존에 있던 `setMonsterProvider`(전역 mutable 싱글턴) 패턴은 동시 요청 환경에서 안전하지 않아 사용하지 않음.

**사이드바/홈페이지**: 서버 전환 pill을 큰 카드형 버튼(🌲메이플랜드/🪐메이플 플래닛)으로 확장, 홈페이지에도 "서버를 선택하세요" 2카드 섹션 추가. 메랜Hub 로고 옆에 항상 MAPLELAND/PLANET 배지 표시 (기존엔 플래닛일 때만 표시했었음 — 대칭으로 수정).

---

### 플래닛 색상 테마 분리 (cyan ↔ amber)

`--brand-accent`, `--brand-accent-2` 등 CSS 커스텀 프로퍼티를 도입, `[data-server="planet"]` 선택자로 앰버/오렌지 톤으로 전환되게 함(메랜은 기존 cyan/emerald 유지). `DropTable`/`TakenDamageCalculator`/`onehit-calculator-client`의 하드코딩된 cyan/sky/emerald 클래스를 CSS 변수 참조로 교체. `PanelHeader.tsx`(드랍테이블류)와 `Panel.tsx`(계산기류)가 서로 다른 톤 시스템을 쓰고 있어서 둘 다 따로 `[data-server="planet"]` 오버라이드 필요했음 — 처음에 `PanelHeader`만 고치고 `Panel`을 놓쳐서 계산기 페이지 창 헤더가 여전히 파란색이던 버그를 나중에 발견해 수정.

**Turbopack 트러블슈팅**: `globals.css` 증분 수정이 dev 서버에 반영 안 되는 현상이 세션 내내 반복됨 — `.next` 캐시 삭제 후 서버 재시작으로만 해결됨. CSS만 고쳤는데 브라우저에 반영이 안 되면 우선 의심할 것.

---

### 플래닛 데이터 파이프라인

Planet은 메랜과 동일 KMS 버전이라 **메랜 원본 데이터를 베이스로 재사용 + 배율/보강 데이터만 얹는 방식**으로 구축(`scripts/build-planet-data.mjs`, 상세는 `data.md` 참고).

- **드랍률 4배** 배율을 `drop-index.json`/`item-detail-by.json`의 `prob`에 직접 반영 (커뮤니티 사이트 패치노트로 배율 로직 검증함: 듀얼 버크의 일비 표창 0.008%→0.032% 정확히 일치)
- **몬스터 속성(불/얼음/전기/독/성 약점·반감·면역)**: 외부 영문 데이터셋을 mobCode로 매칭해 자동 보강 (464종 매칭, 기존 데이터와 충돌 0건 — 항상 상위집합이라 안전하게 덮어씀)
- **신규 몬스터 124종 추가**: 메랜 원본에 없는 몬스터(파풀라투스, 피아누스, 도도, 오베론 등 포함)를 외부 카탈로그 데이터에서 자동 추가. 기존에 있는 mobCode는 스탯 차이가 있어도 검증 없이 덮어쓰지 않음(556종 보류 중)
- **출현 맵(`map` 필드) 보강**: maplestory.io 공개 API 기반 데이터로 451종 몬스터의 구체적 스폰 맵 채움 (기존엔 전 몬스터 공백이었음)
- **데이터 정확도 검증**: 조아요(chowayo.com) 등과 스팟체크 — 자쿰 3페이즈/기본 달팽이는 완전 일치, 빨간 달팽이(레벨/EXP 뒤바뀜)·핑크빈(EXP 큰 차이) 등 불일치 발견해 `divergence-overrides.json`에 수정 반영(단일 출처만 확인한 잠정치)

**소스 파일 위치 정리**: 손수 관리/외부 출처 데이터(`divergence-overrides.json`, `monster-attribute-data.js`, `monster-catalog-data.js`, `map-catalog-data.js`, `cube-data.js`)는 `data/*.json`이 "빌드 산출물 전용, 직접 편집 금지"라는 프로젝트 규칙과 충돌해서 `data/planet/` → **`scripts/sources/planet/`**로 이동. `data/planet/`엔 이제 산출물만 남음.

**중요 발견**: 세션 도중 받은 데이터 파일 중 `app.js`가 실제로는 경쟁 사이트(플래닛고)의 **웹앱 소스코드 + 라이브 Firebase 자격증명**이었음을 발견 — 삭제 처리. 나머지 데이터 파일들은 출처가 명시돼 있어(공식 mapleplanet.co.kr, maplestory.io 공개 API, KMS 데이터마이닝 팩) 사용 가능하다고 판단.

---

### 큐브 시뮬레이터 (신규 기능, `/planet/cube-simulator`)

공식 mapleplanet.co.kr/Cube에 공개된 수상한 큐브(185개)/미라클 큐브(132개) 옵션 확률 풀 기반. 옵션 종류는 공식 가중치(weight)로 뽑고, 옵션 내 수치 등급(1~20단계)은 균등분포로 단순화 — 페이지에 이 한계를 명시. 메랜엔 큐브/잠재능력 시스템 자체가 없어 사이드바에 플래닛 모드에서만 노출.

---

### 계산기 프리셋 서버 분리 + 로컬 상태 버그 수정

`character_presets` 테이블에 `server` 컬럼 추가 마이그레이션(`20260707_add_server_to_character_presets.sql`, unique 제약을 `(user_id, calculator, server, name)`으로 확장) — 운영 Supabase에 적용 완료 확인함(익명 키로 스키마 조회해 컬럼 존재 검증).

외부 코드 리뷰(codex) 피드백으로 발견된 문제 수정:
- 한방컷 계산기의 로컬 프로필 저장 키, 피격뎀 계산기의 QuickSlots 저장 키가 서버 구분 없이 공통이라 메랜↔플래닛 전환 시 몬스터/스탯 상태가 서로 덮이는 문제 — `server`별로 키 분리(단, **기존 메랜 유저 저장분 유실처럼 보이지 않게 메랜은 기존 키 유지, 플래닛만 새 키** 사용)

---

### SEO — 플래닛 검색 노출

`sitemap.ts`에 플래닛 라우트 5개 전부 추가(기존엔 메랜만 있었음), 루트 레이아웃과 플래닛 각 페이지에 "메이플 플래닛/메이플플래닛/플래닛" 등 keywords 메타 추가, 한방컷·큐브 시뮬레이터 페이지엔 JSON-LD 구조화 데이터도 추가(기존 메랜 한방컷 계산기 SEO 커밋 패턴 재사용).

---

## 2026-06-30

### 드롭테이블 신규 아이템 추가 (시그너스 업데이트)

**원인**: xn--o80b01o9mlw3kdzc.com 사이트가 `drop-rate-box` div에 `style` 속성을 추가했으나
기존 정규식 `<div class="drop-rate-box">` 가 속성 있는 경우를 매칭 못 하던 버그 수정.
→ `scripts/fetch-item-detail-by.mjs` 정규식 수정 (`"[^>]*>` 패턴 적용)

**신규 추가 스크립트**: `scripts/fetch-new-items-drop-data.mjs`
- xn--o80b01o9mlw3kdzc.com/itemnote 페이지 전체 스크래핑 (레벨 70+)
- 기존 drop-index.json에 없는 신규 아이템 필터링 후 item_detail 드롭 데이터 수집
- 카테고리: 전사/마법사/궁수/도적/해적/무기(Lv70+) + 소비(Weapon/Armor/Mastery Scroll 전체)

**결과** (`data/drop-index.json` 재빌드):
- 아이템 수: 2515 → 2687 (+172)
- 타임리스/리버스 세트 전 직업 완전 추가 (핑크빈 드롭: 0.125%/0.5%)
- 신규 주문서 203종, 마스터리북 포함

**재빌드 순서**:
1. `node scripts/fetch-new-items-drop-data.mjs` — 신규 item_detail 드롭 데이터 수집
2. `node scripts/build-drop-index.mjs` — drop-index.json 재빌드

---

### 시그너스 기사단 N방컷 계산기 추가

**스킬 데이터 (data/skills/)**
- `mainSkillMapping.json`: 5개 직업 항목 추가 (소울마스터, 플레임위자드, 윈드브레이커, 나이트워커, 스트라이커)
- `damageMapping.json`: 시그너스 공격 스킬 25종 전 레벨 데이터 추가 (총 107개 스킬)
  - 기존 동명 스킬과 충돌 방지: 어벤져(나워), 트리플 스로우(나워), 에너지 버스터(스커), 쇼크웨이브(스커) 등 명칭 구분
- `range20.json`: 20레벨 만렙 시그너스 스킬 20종 추가
- `range30.json`: 30레벨 만렙 시그너스 스킬 6종 추가 (소울 블레이드, 브랜디쉬(소마), 소울 드라이버, 플레임 기어, 파이어 스트라이크, 샤크 웨이브)

**계산기 페이지 (calculators/onehit/page.tsx)**
- `jobGroups`에 "시그너스" 추가 → 드롭다운에서 선택 가능
- `jobOptionsByGroup.시그너스`: 5개 직업 정의
- `jobProfile`: 5개 직업 스탯 프로필 (플레임위자드 = INT/LUK/마법직, 나머지 = 물리직)
- 직업 플래그 추가: isCygnusJob, isSoulMasterJob, isFlameWizardJob, isWindBreakerJob, isNightWalkerJob, isStrikerJob, isArcherOrWindBreaker
- 무기 타입: 소울마스터(검), 플레임위자드(없음), 윈드브레이커(활, 화살선택), 나이트워커/스트라이커(잠금)
- 정확도 공식: 시그너스 직업별 분기 (전사계/마법계/궁수계/도적계/해적계 공식 각각 적용)
- 나이트워커: 도적계 부스탯(STR) 합산, 크리티컬 스로우, 표창 선택 UI 활성화
- 플레임위자드: 마법사와 동일하게 패시브 마스터리 비적용

---

### 파일 구조 정리
- `party/page.tsx`: Coming Soon 스텁 → `redirect('/parties')`로 교체 (실제 기능 페이지로 연결)
- `nav-bar.tsx` 삭제: 정의만 있고 아무 데서도 import되지 않던 미사용 컴포넌트
- 루트 잡파일 삭제: `This`, `You` (빈 파일), `FETCH_HEAD` (git 아티팩트 잘못 위치)
- TypeScript 에러 0개 확인

---


### 문서화 체계 수립
- CLAUDE.md, AGENTS.md, database.md, data.md, project-notes.md, TODO.md, DEVLOG.md 작성
- 프로젝트 문서화 체계 수립 (Pet/pawprint 방식 접목)
- 데이터 파이프라인 현황 정리 (`data.md`)
- DB 스키마 전체 문서화 (`database.md`)
- 레거시/중복 경로 목록 확인 필요 항목 TODO에 추가

### 전체 최적화 (번들 + 버그)

**클라이언트 번들 경량화 (~9.5MB 절감)**
- `DropTable.tsx`: `drop-index.json` (3.8MB) + `item-detail-by.json` (318KB) import 제거
  - 두 파일을 서버 컴포넌트(`drop-table/page.tsx`)에서 import → props로 전달
  - `"use client"` 번들에서 ~4.1MB 제거
- `QuestBoard.tsx`: 6개 JSON import 모두 제거 (quests, monsters, monster-spawns, drop-index, item-detail-by, npc-locations = ~5.4MB)
  - `quests/page.tsx` 서버 컴포넌트에서 import → props로 전달
- `tsconfig.json`: 중첩된 `maplelandhub/` 디렉토리 exclude 추가 (TS 컴파일 충돌 방지)

**코드 품질 수정**
- `sidebar-shell.tsx`: 파티 매칭 링크 `/party`(Coming Soon 스텁) → `/parties`(실제 기능) 수정
- `calculators/onehit/page.tsx`: 프로덕션 `console.log` 제거 (useEffect 전체 삭제)
- `StatTable.tsx`: `highlight` prop 버그 수정 — 두 분기가 동일한 클래스를 사용하던 문제, highlight 시 `--retro-header-yellow` 색상 적용
- `TakenDamageCalculator.tsx`: 538번째 줄 중복 `<p>` 제거

**코드 구조 개선**
- `DropTable.tsx`: `getItemGroup`, `getItemLevel` 함수를 컴포넌트 바디에서 모듈 레벨로 이동
- `QuestBoard.tsx`: 로컬 `formatNumber` 제거 → `src/lib/utils.ts` 공유 함수 사용
- `DropIndexData`, `ItemDetailByData` 타입 export (page에서 재사용)
- `QuestBoardProps` 타입 export

---

### 드랍 테이블 검색 성능 최적화 + 자동완성 선택 버그 수정

**문제**: `/drop-table` 페이지에서 아이템/몬스터 검색 시 타이핑할 때 체감 딜레이 발생.

**원인** (`src/components/DropTable.tsx`):
- `filteredItems`/`filteredMonsters`가 키 입력마다 아이템 2,687개·몬스터 653개 전체를 `sort()`로 정렬
- 정렬 비교 함수(comparator) 안에서 `getMatchScore` → `getSearchKeys`(정규식 다수 + 한글 초성 변환)를 이름마다 매 비교 시점에 재계산
- 정렬 특성상 같은 항목의 검색 키가 비교당 여러 번 중복 계산되어, 키 입력 1번에 검색 키 계산이 수만 번 발생

**수정**:
- `itemSearchKeysById`, `monsterSearchKeysByMobCode`: 아이템/몬스터별 검색 키를 `useMemo`로 1회만 계산해 `Map`에 캐싱 (의존성은 `dropData.items` / 빈 배열 — 마운트 후 재계산 안 됨)
- `getMatchScore(name, keyword)` → `getMatchScoreFromKeys(keys, keyword)`로 변경, 캐싱된 키 배열을 직접 사용
- 정렬 비교 함수에서 점수를 매번 재계산하던 방식 → 키워드가 있을 때 점수를 단일 패스(O(n))로 먼저 계산한 뒤, 가벼운 숫자 비교로만 정렬

**검증**:
- 브라우저 실측(`next dev`, 비압축 빌드 기준): "주문서" 입력 시 키 입력당 처리 시간 4.7ms → 41.4ms(첫 글자, JIT 워밍업) → 12.5ms → 9.9ms로 프레임 예산(16ms) 안쪽 안정화
- `npx tsc --noEmit`, `npx eslint src/` 통과

**추가로 발견한 버그 수정**:
- `suggestionItems` 배열 순서가 `[...items, ...monsters]`(아이템 우선)였는데, 실제 렌더링 순서는 몬스터 → 아이템이라 키보드 `ArrowDown`/`Enter` 선택 시 화면 하이라이트와 실제 선택 항목이 어긋나는 버그 존재
- `[...monsters, ...items]`로 순서를 렌더링과 일치시켜 수정
- 브라우저에서 ArrowDown → 하이라이트된 몬스터 → Enter → 동일 몬스터 선택까지 직접 확인

---

<!-- 새 작업은 여기 위에 날짜 역순으로 추가 -->
