# 메랜Hub TODO

---

## 즉시 확인 필요 (레거시 정리)

- [x] 파티 매칭 기능 완전 제거 — `/party`, `/parties` 라우트, 컴포넌트, 타입 모두 삭제
- [ ] `/calculator/oneshot` vs `/calculator/onehit` — 중복 확인 후 redirect 또는 제거
- [ ] `/services/*` 경로들 — 각 기능 페이지 redirect인지 확인 후 정리
- [ ] `src/components/Panel.tsx` vs `src/components/ui/Panel.tsx` — 중복 제거
- [ ] `src/components/nav-bar.tsx` — 사용처 없음 확인 후 삭제 검토

---

## 데이터 파이프라인

- [ ] `data/release-filters.json` 주기적 검토 — 신규 출시 몬스터 차단 해제
- [ ] 드롭 데이터 최신화 프로세스 문서화 (언제, 어디서 원본 소스 구하는지)
- [x] 대용량 JSON 클라이언트 번들 포함 여부 감사 — DropTable(~4.1MB), QuestBoard(~5.4MB) 서버 props로 이전 완료
- [ ] `src/data/mapledb/` 파일들 `data/*.json`과 동기화 상태 확인

---

## 기능 개선

### 우선순위 높음
- [ ] 퀘스트 추적기 — 비로그인 로컬스토리지 지원 (현재 로그인만)
- [ ] 드롭 테이블 — 아이템 검색 (아이템명으로 드롭하는 몬스터 역검색)

### 우선순위 중간
- [ ] 계산기 프리셋 — 공유 링크 기능
- [ ] 버프 타이머 — 알림 소리/브라우저 알림
- [ ] 어드민 — 피드백 상태 변경 UI 개선

### 우선순위 낮음
- [ ] 다크모드 지원
- [ ] 모바일 최적화 (사이드바 모바일 드로어)
- [ ] 검색 기능 (몬스터/아이템 전역 검색)

---

## 기술 부채

- [x] TypeScript 엄격 모드 (`strict: true`) — 이미 적용되어 있음, 에러 0개 확인
- [x] `maplelandhub/` 중첩 폴더 — `.gitignore`에 추가 완료
- [ ] `src/components/` 정리 — ui/ 폴더로 원자 이동, 피처 컴포넌트 분리
- [ ] API Route 에러 처리 표준화
- [ ] 계산기 로직 단위 테스트 확충 (`src/lib/calculators/takenDamage/__tests__/` 참조)
- [ ] Web Vitals 수집 데이터 활용 계획 수립

---

## 배포 / 인프라

- [ ] Cloudflare Pages 환경변수 관리 문서화 (NEXT_PUBLIC_APP_URL 등)
- [x] `RESEND_API_KEY` + `ADMIN_EMAIL` 로컬 테스트 완료 — Cloudflare Pages(운영)에도 등록 필요
- [ ] Resend 발신 도메인 인증 후 `from` 주소 `onboarding@resend.dev`에서 실제 도메인으로 변경
- [ ] Supabase 백업 주기 확인 및 문서화
- [ ] maplestory.io API 다운 시 fallback 이미지 처리
- [x] `supabase/migrations/20260707_add_server_to_character_presets.sql`을 운영 Supabase에 적용 완료 (2026-07-07)

---

## 디자인 리뉴얼 (내일)

- [ ] 사이드바뿐 아니라 **전체 레이아웃/디자인**을 다시 손보기 — 색깔 위주로 (지금은 메랜 레이아웃에 강조색만 바꾼 수준)

---

## 메이플 플래닛 후속 작업

- [x] `divergence-overrides.json`의 빨간 달팽이 EXP — catalog 데이터로 2차 교차검증 완료(Lv4/EXP8, 3개 출처 일치: 조아요/catalog/내부 정합성)
- [ ] 핑크빈 EXP는 여전히 미해결 — 조아요(50,000,000) vs catalog(200,000,000) vs 표준 4배율(25,160,000) 3개 값이 서로 불일치, 2026-07-08 세션에서 발견만 하고 못 정함. `divergence-overrides.json`의 8820001 주석 참고
- [x] 카탈로그 매칭된 556종 중 레벨 자체가 다른 50종 — catalog 값으로 일괄 반영 완료(`divergence-overrides.json` monsterOverrides). 예티/킹슬라임은 namu.wiki 빅뱅 이전 문서로 개별 재검증(기존 메랜 원본이 포스트 빅뱅 스탯을 잘못 쓰고 있었던 것으로 추정 — Zone A `data/monsters.json` 자체 버그일 가능성, 별도 확인 필요). 나머지 40여 종은 catalog 신뢰도에 근거한 일괄 적용이라 개별 재검증 아직 안 됨
- [ ] 나머지 384종(레벨은 같지만 HP/acc/eva/def 차이)은 미반영 — acc 필드는 메랜 원본 자체가 0으로 비어있는 경우가 다수(206종 중 203종)라 "플래닛 차이"라기보다 메랜 원본 데이터 누락일 가능성이 높음, Planet 오버라이드보다 메랜 원본 데이터 이슈로 별도 트래킹 필요
- [ ] `data/planet/release-filters.json` 수동 검토 — 메랜 걸 그대로 복사한 상태, 플래닛 실제 미출시 기준과 다를 수 있음
- [x] 시그너스 "여제의 축복" 스택 방식 확인 — 웹 검색 결과 표준 메이플스토리 규칙(정령의 축복과 중첩 안 되고 더 높은 효과만 적용)과 동일한 것으로 보임, 플래닛 고유 차이 증거 못 찾음(디시 마이너 갤러리 질문글 1건 외 참고 자료 없음)
- [ ] 핑크빈 서브페이즈(무적 페이즈/소환 몹) mobCode 확보 후 `newMonsters`에 추가 — catalog에도 본체(8820001) 1종만 있어 여전히 확보 못 함
- [ ] **드랍테이블 — 주문서(장비 강화 스크롤) 드랍률이 조아요와 불일치** — chowayo는 스크롤별 몬스터 무관 고정값(0.012% 등)인데 우리는 몬스터별 메랜 원본 base×4 값이라 서로 다름(2026-07-08 발견). 메랜 원본 스크래핑(`scripts/build-drops-parsed-from-dropchance-html.mjs` 등, Zone D) 단계 자체 재검토 필요 — 플래닛 오버라이드로 해결할 문제 아님
- [ ] **드랍테이블 — 몬스터 카드 드랍이 데이터에 아예 없음**(2026-07-08 발견) — chowayo엔 모든 몬스터에 카드 드랍(보통 0.4%)이 있는데 `data/drop-index.json`엔 몬스터 카드 아이템 자체가 없음. 기능 추가할지 여부 결정 필요
- [x] 큐브 시뮬레이터 재정비(2026-07-08) — 옵션 수치(1~20단계)를 균등분포 랜덤 대신 착용 레벨 10구간 기반 고정값으로 계산하도록 수정(`levelBracket`), 결과 히스토리에 등급 색상/큐브 종류 표시 추가, vitest 도입 후 `cubeSimulator.test.ts` 12개 단위 테스트 작성. `build-planet-data.mjs`는 내부 함수 미export라 테스트 대상에서 제외 — 필요시 추후 리팩토링 후 테스트 추가
- [ ] `applyMonsterOverrides`의 `_`-prefix 필드 leak 버그는 수정 완료(2026-07-08), 재발 방지용 `build-planet-data.mjs` 자체 단위 테스트는 여전히 없음
- [x] **큐브 시뮬레이터 — 2·3번째 옵션 이탈확률**(2026-07-08) — mapleplanet.co.kr/Cube 공식 페이지에서 직접 확인 완료. 수상한/미라클 큐브 동일: 등급상승 레어→에픽 6%/에픽→유니크 1.8%, 이탈확률 2번째 10%/3번째 1%. `cubeSimulator.ts` 참고
- [ ] **큐브 시뮬레이터 — 유니크→레전드리 확률은 여전히 메이플플래닛 공식 확인 아님**(2026-07-08) — 공식 확률표엔 레전드리 단계 자체가 없음(사이트 JS `gradeLabel`도 4단계뿐, "레전드리" 문자열이 페이지 어디에도 없음). 다만 앞 두 단계(레어→에픽 6%/에픽→유니크 1.8%)가 넥슨 공식 "레드 큐브" 확률표와 소수점까지 정확히 일치하는 걸 발견해서, 레드 큐브의 공식 유니크→레전드리 확률(0.3%)로 교체함(단순 감소비율 추정치 0.54%보다 근거 있음) — 그래도 메이플플래닛이 실제로 레드 큐브와 100% 동일한 표를 쓰는지는 확인된 게 아니라 `GRADE_UP_CHANCE.uniqueToLegendary` 여전히 잠정치
- [x] **큐브 시뮬레이터 — "원하는 옵션 기댓값" 계산기 추가**(2026-07-08) — `estimateOptionOdds`(닫힌 형태 확률 계산, 실제 반복 시뮬레이션 아님)로 옵션별 큐브 1회당 확률/기댓값 표시. 옵션 선택 목록이 착용 레벨에 따라 실시간으로 텍스트 갱신됨(`resolveOptionText`). 유니크→레전드리 확률이 잠정치이므로 레전드리 등급 옵션의 기댓값도 그만큼 부정확할 수 있음 — 위 항목과 연동
- [x] **큐브 시뮬레이터 — reqLevel 미달 옵션 확률 계산 버그**(2026-07-08, 코드 리뷰로 발견) — `estimateOptionOdds`가 착용 레벨 미달 옵션에도 0이 아닌 확률을 계산하던 버그 수정, UI 옵션 목록도 레벨 미달 항목 제외하도록 수정
- [x] **sitemap.ts — 플래닛 라우트 5개 누락 복원**(2026-07-08, 코드 리뷰로 발견) — 이번 세션 이전부터 이미 빠져 있었음(원인 미상), 큐브 시뮬레이터 포함 5개 라우트 복원
- [x] **큐브 시뮬레이터 — 3옵션 동시 선택 기능 추가**(2026-07-08) — `estimateCombinedOptionOdds`로 최대 3개 옵션이 동시에 나올 확률 계산(포함-배제 원리). 메소 비용 계산은 안 하기로 함(사용자 요청) — 대신 "기댓값"(몇 개 필요한지)과, 미라클 큐브 한정으로 캐시 비용(원화) 표시
- [ ] **큐브 시뮬레이터 — 메포→원화 환율이 근사치**(2026-07-08) — 메포 공식 고정 결제가를 못 찾아서(플랫폼 결제 페이지 미확인) 커뮤니티 통용 메소마켓 환율(1,200메포≈1,000원)의 중간값으로 근사. `CubeSimulator.tsx`의 `KRW_PER_MEPO_APPROX` 참고 — 실제 캐시샵 정가 확인되면 교체 필요
- [x] **큐브 시뮬레이터 — 옵션 선택 UI 개선 + 자동 기댓값 + 확률 프레이밍**(2026-07-08) — 검색 가능한 커스텀 드롭다운(`OptionPicker`)으로 교체, 옵션 미선택 시 방금 뽑은 결과 기준 자동 기댓값 표시(`ResolvedCubeLine`에 `id` 필드 추가 필요했음), 희귀 옵션은 "1/N" 분수로 표시
- [x] **큐브 시뮬레이터 — 등급 먼저 고르는 2단계 드롭다운 + 옵션 선택 시에만 기댓값 표시**(2026-07-08) — 검색형 콤보박스를 버리고 등급→옵션 2단계 네이티브 select로 교체, "옵션 미선택 시 자동 표시"는 제거(옵션을 골라야만 표시), `등급 고정` 배지는 체크박스와 무관하게 항상 노출
- [x] **큐브 시뮬레이터 — 비상식적으로 큰 기댓값 수정 + 등급업/옵션재설정 분리 + 장비 종류 필터**(2026-07-08) — maplanet.net 큐브 계산기를 참고해 "등급업 기댓값"(옵션과 무관하게 등급만 올리는 비용, `estimateGradeUpExpectedUses`)과 "옵션 재설정 기댓값"을 분리된 패널로 나눔. "이미 그 등급에 도달했다고 가정" 토글 추가(`estimateOptionOdds`/`estimateCombinedOptionOdds`에 `fixedOverallGrade` 파라미터 추가) — 꺼져있으면 기존처럼 등급업 확률까지 섞인 값(레전드리 3옵션 조합 시 2천만 개 단위로 커질 수 있음, 이게 정상임을 토글로 명시), 켜져있으면 그 등급에 이미 도달했다고 보고 옵션 재설정 비용만 계산(같은 조합이 75개로 줄어듦). 장비 종류(모자/장갑/신발/상의·하의·전신/기타) 선택 필터도 추가 — cube-data.js 안에서 스킬 옵션(쓸만한 OO)들이 optionType으로 짝지어진 것을 근거로(51=모자, 53=상의 등, 54=장갑, 55=신발) 부위 무관 노출을 방지. 신규 vitest 5개 추가(29개 전체 통과)
- [x] **큐브 시뮬레이터 — 같은 옵션 중복 선택 시 잘못 계산되던 버그 수정**(2026-07-08, 코드 리뷰로 발견) — `estimateCombinedOptionOdds`가 `new Set(targetEntryIds)`로 중복 id를 그냥 제거해서, 같은 옵션을 슬롯 2개에 고르면 "1줄 이상" 확률로 잘못 계산되던 버그. 요구 횟수(같은 id가 몇 번 선택됐는지)를 추적해서 "N줄 이상 일치" 확률을 직접 열거(brute-force, 줄 3개·목표 최대 3개라 저렴함)로 정확히 계산하도록 전면 재작성. UI에도 "(N줄)" 라벨 추가, 중복 id로 인한 React key 중복도 distinct id 기준 렌더링으로 해결. 캐시 비용 표시에도 "(추정 환율 기준)" 라벨 + 메포 병기 추가(원화 단독 표시가 실제 결제 비용처럼 보인다는 지적 반영). vitest 1개 추가(30개 전체 통과)
- [x] **큐브 시뮬레이터 — 장비 종류 필터를 실제 롤 시뮬레이터에도 적용**(2026-07-08) — 기존엔 "장비 종류" 선택이 "원하는 옵션 기댓값" 계산기의 옵션 목록에만 적용되고, 실제 큐브 클릭 롤(`rollCubeResult`)과 확률 계산의 등급 내 가중치 합산(`eligiblePoolAtGrade`)은 필터 없이 전체 풀을 썼음 — 다른 부위 전용 스킬 옵션이 실제로 뽑히거나, 가중치 총합 계산에 끼어들어 다른 옵션들 확률도 미세하게 부정확했던 상태. 장비 종류 선택 UI를 상단 공용 위치로 옮기고, `suspiciousPoolForEquip`/`miraclePoolForEquip`으로 미리 필터링한 풀을 롤과 확률 계산 양쪽에 동일하게 전달하도록 수정. Node로 2만 회 가중치 추출 시뮬레이션 돌려서 신발 선택 시 "쓸만한 컴뱃 오더스"만 나오고 다른 부위 스킬(미스틱 도어/하이퍼 바디/샤프 아이즈/어드밴스드 블레스/윈드 부스터)은 전혀 안 나오는 것 확인
