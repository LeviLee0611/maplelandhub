# 메랜Hub TODO

---

## 즉시 확인 필요 (레거시 정리)

- [x] 파티 매칭 기능 완전 제거 — `/party`, `/parties` 라우트, 컴포넌트, 타입 모두 삭제
- [ ] `/calculator/oneshot` vs `/calculator/onehit` — 중복 확인 후 redirect 또는 제거
- [ ] `/services/*` 경로들 — 각 기능 페이지 redirect인지 확인 후 정리
- [ ] `src/components/Panel.tsx` vs `src/components/ui/Panel.tsx` — 중복 제거
- [ ] `src/components/nav-bar.tsx` — 사용처 없음 확인 후 삭제 검토

---

## 데이터 파이프라인

- [ ] `data/release-filters.json` 주기적 검토 — 신규 출시 몬스터 차단 해제
- [ ] 드롭 데이터 최신화 프로세스 문서화 (언제, 어디서 원본 소스 구하는지)
- [x] **대용량 JSON 클라이언트 전송 문제 재확인 및 실제 수정**(2026-07-09) — 예전에 "DropTable/QuestBoard 서버 props로 이전 완료"라고 기록해뒀던 게 사실 근본 해결이 아니었음(서버 컴포넌트에서 import는 했지만 `"use client"` 컴포넌트에 그대로 props로 넘겨서 RSC 페이로드에 그대로 직렬화되고 있었음 — 실측 `/drop-table`·`/planet/drop-table` 각각 2.4MB). DropTable은 이번에 제대로 고침: 검색용 아이템 목록은 필요한 필드만 남겨 서버에서 슬림화(0.82MB→0.18MB), 드랍 연관관계(dropsByMonsterId/monstersByItemId, 합쳐서 ~1MB)는 아예 초기 props에서 빼고 몬스터/아이템 선택 시에만 `/api/drop-table/{monster,item}/{mapleland,planet}` 라우트로 온디맨드 fetch. 결과: 2.4MB→680KB(raw), gzip 228KB→70KB. 코드리뷰로 API 라우트가 메랜+플래닛 데이터를 한 함수에 같이 번들하던 것도 서버별 라우트 파일로 분리해서 해결(`src/lib/drop-table-lookup.ts`에 순수 로직만 공유). **QuestBoard도 이어서 처리 완료**(2026-07-09) — DropTable과 접근 자체가 다름(단일 선택이 아니라 703개 퀘스트 전체를 동시에 검색/필터링하므로 온디맨드 fetch 방식 대신, 퀘스트 데이터가 실제로 참조하는 아이템/NPC/몬스터 id만 서버에서 미리 골라 훨씬 작은 버전을 만들어 전달하는 방식 사용). `/quests` 3.38MB→1.0MB(raw), gzip 334KB→124KB.
  - **작업 중 발견한 버그**: 처음엔 보상 아이템(`quest.rewards.items`) 기준으로만 골랐는데, 실제로 "드랍 몬스터" 표시 기능(`itemDropMonstersByItemId`)은 완료 시 제출하는 수집 아이템(`quest.requirements.complete.items`, 보상과는 다른 별도 집합, 546종)에 쓰이는 거였음 — 잘못 필터링해서 "어디서 드랍되는지" 정보가 통째로 빠질 뻔했음. 실제 브라우저 검증(스크린샷 대신 DOM에서 "드랍 몬스터" 라벨 개수 세는 방식)으로 발견 → 두 집합(rewards + requirements.complete.items)을 합쳐서 수정. Node 스크립트로 "첫 20개 퀘스트 중 몇 개가 드랍처 정보를 표시해야 하는지" 미리 계산해두고 브라우저 실측이 정확히 일치하는지 대조하는 방식으로 재검증 완료.
  - **코드 리뷰 반영**: 변수명 `rewardItemIds`가 실제로는 보상+수집템을 다 담고 있어서 이름과 역할이 안 맞는다는 지적 → `referencedItemIds`로 개명. 슬림 데이터 조립 로직(50줄+)이 page.tsx에 그대로 있던 것도 `src/lib/quest-board-data.ts`(`buildSlimQuestBoardData`)로 추출해서 DropTable 쪽 `drop-table-lookup.ts`와 같은 패턴으로 통일. 프로덕션 빌드 정적 HTML(`quests.html`, 944KB)에서 "드랍 몬스터" 라벨 8개 확인해서 리팩토링 후에도 동일하게 동작하는 것 재검증.
- [ ] `src/data/mapledb/` 파일들 `data/*.json`과 동기화 상태 확인

---

## 기능 개선

### 우선순위 높음
- [ ] 퀘스트 추적기 — 비로그인 로컬스토리지 지원 (현재 로그인만)
- [ ] 드롭 테이블 — 아이템 검색 (아이템명으로 드롭하는 몬스터 역검색)

### 우선순위 중간
- [ ] 계산기 프리셋 — 공유 링크 기능
- [ ] 버프 타이머 — 알림 소리/브라우저 알림
- [ ] 어드민 — 피드백 상태 변경 UI 개선

### 우선순위 낮음
- [ ] 다크모드 지원
- [ ] 모바일 최적화 (사이드바 모바일 드로어)
- [ ] 검색 기능 (몬스터/아이템 전역 검색)

---

## 기술 부채

- [x] TypeScript 엄격 모드 (`strict: true`) — 이미 적용되어 있음, 에러 0개 확인
- [x] `maplelandhub/` 중첩 폴더 — `.gitignore`에 추가 완료
- [ ] `src/components/` 정리 — ui/ 폴더로 원자 이동, 피처 컴포넌트 분리
- [ ] API Route 에러 처리 표준화
- [ ] 계산기 로직 단위 테스트 확충 (`src/lib/calculators/takenDamage/__tests__/` 참조)
- [ ] Web Vitals 수집 데이터 활용 계획 수립

---

## 배포 / 인프라

- [ ] Cloudflare Pages 환경변수 관리 문서화 (NEXT_PUBLIC_APP_URL 등)
- [x] `RESEND_API_KEY` + `ADMIN_EMAIL` 로컬 테스트 완료 — Cloudflare Pages(운영)에도 등록 필요
- [ ] Resend 발신 도메인 인증 후 `from` 주소 `onboarding@resend.dev`에서 실제 도메인으로 변경
- [ ] Supabase 백업 주기 확인 및 문서화
- [ ] maplestory.io API 다운 시 fallback 이미지 처리
- [x] `supabase/migrations/20260707_add_server_to_character_presets.sql`을 운영 Supabase에 적용 완료 (2026-07-07)

---

## SEO 점검 (2026-07-09)

- [x] 플래닛 페이지 5개(홈/드랍테이블/계산기 2종/큐브 시뮬레이터) 메타데이터 전수 점검 — 전부 `layout.tsx`에 title/description/canonical/OG/키워드 정상 존재 확인 (처음엔 `page.tsx`만 확인해서 "없다"고 잘못 판단했었음, `layout.tsx` 패턴 놓친 것)
- [x] `/admin/*`, `/ui/demo`가 robots.txt에서 크롤링 허용 상태였음 — `disallow` 추가
- [x] `/feedback` 페이지에 메타데이터 없던 것 — `layout.tsx` 추가해서 title/description/canonical 부여
- [x] sitemap.xml 플래닛 5개 라우트 정상 포함 확인(이미 이전 세션에 복구됨), robots.txt 문제없음
- [x] `/calculator/oneshot`, `/calculator/onehit` 레거시 경로 — `/calculators/onehit`로 정상 redirect라 중복 콘텐츠 문제 없음 확인
- [x] `/services/*` 설명 페이지들 — 실제 계산기와 다른 고유 콘텐츠 + 자체 canonical이라 문제없음 확인
- [ ] OG 공유 이미지가 전 페이지 favicon.ico(작은 아이콘) 하나로 통일 — 카톡/디스코드 공유 미리보기가 볼품없을 수 있음, 1200x630 전용 이미지 제작 필요(디자인 작업)

---

## 디자인 리뉴얼

- [x] 사이드바뿐 아니라 **전체 레이아웃/디자인**을 다시 손보기 — 색깔 위주로(2026-07-09) — 메이플랜드 브랜드 강조색을 시안/블루(`#38bdf8`)에서 마젠타/버건디(`#be123c`)로 교체. `--brand-accent` CSS 변수 기반이라 패널 헤더/칩/버튼/스피너 등에 자동 전파, 로그인 버튼·홈페이지 뱃지·사이드바 활성 탭 등 하드코딩돼 있던 시안 색상도 함께 교체. 플래닛 모드(앰버/오렌지)는 기존 `[data-server="planet"]` 분리 구조를 그대로 활용해 그대로 유지 — 두 서버 시각적 구분 유지. 기존에 중복돼 있던 플래닛 전용 오버라이드 CSS 블록은 이제 불필요해져서 정리

---

## 메이플 플래닛 후속 작업

- [x] `divergence-overrides.json`의 빨간 달팽이 EXP — catalog 데이터로 2차 교차검증 완료(Lv4/EXP8, 3개 출처 일치: 조아요/catalog/내부 정합성)
- [ ] 핑크빈 EXP는 여전히 미해결 — 조아요(50,000,000) vs catalog(200,000,000) vs 표준 4배율(25,160,000) 3개 값이 서로 불일치, 2026-07-08 세션에서 발견만 하고 못 정함. `divergence-overrides.json`의 8820001 주석 참고
- [x] 카탈로그 매칭된 556종 중 레벨 자체가 다른 50종 — catalog 값으로 일괄 반영 완료(`divergence-overrides.json` monsterOverrides). 예티/킹슬라임은 namu.wiki 빅뱅 이전 문서로 개별 재검증(기존 메랜 원본이 포스트 빅뱅 스탯을 잘못 쓰고 있었던 것으로 추정 — Zone A `data/monsters.json` 자체 버그일 가능성, 별도 확인 필요). 나머지 40여 종은 catalog 신뢰도에 근거한 일괄 적용이라 개별 재검증 아직 안 됨
- [ ] 나머지 384종(레벨은 같지만 HP/acc/eva/def 차이)은 미반영 — acc 필드는 메랜 원본 자체가 0으로 비어있는 경우가 다수(206종 중 203종)라 "플래닛 차이"라기보다 메랜 원본 데이터 누락일 가능성이 높음, Planet 오버라이드보다 메랜 원본 데이터 이슈로 별도 트래킹 필요
- [ ] `data/planet/release-filters.json` 수동 검토 — 메랜 걸 그대로 복사한 상태, 플래닛 실제 미출시 기준과 다를 수 있음
- [x] 시그너스 "여제의 축복" 스택 방식 확인 — 웹 검색 결과 표준 메이플스토리 규칙(정령의 축복과 중첩 안 되고 더 높은 효과만 적용)과 동일한 것으로 보임, 플래닛 고유 차이 증거 못 찾음(디시 마이너 갤러리 질문글 1건 외 참고 자료 없음)
- [ ] 핑크빈 서브페이즈(무적 페이즈/소환 몹) mobCode 확보 후 `newMonsters`에 추가 — catalog에도 본체(8820001) 1종만 있어 여전히 확보 못 함
- [ ] **드랍테이블 — 주문서(장비 강화 스크롤) 드랍률이 조아요와 불일치** — chowayo는 스크롤별 몬스터 무관 고정값(0.012% 등)인데 우리는 몬스터별 메랜 원본 base×4 값이라 서로 다름(2026-07-08 발견). 메랜 원본 스크래핑(`scripts/build-drops-parsed-from-dropchance-html.mjs` 등, Zone D) 단계 자체 재검토 필요 — 플래닛 오버라이드로 해결할 문제 아님
- [ ] **드랍테이블 — 몬스터 카드 드랍이 데이터에 아예 없음**(2026-07-08 발견) — chowayo엔 모든 몬스터에 카드 드랍(보통 0.4%)이 있는데 `data/drop-index.json`엔 몬스터 카드 아이템 자체가 없음. 기능 추가할지 여부 결정 필요
- [x] 큐브 시뮬레이터 재정비(2026-07-08) — 옵션 수치(1~20단계)를 균등분포 랜덤 대신 착용 레벨 10구간 기반 고정값으로 계산하도록 수정(`levelBracket`), 결과 히스토리에 등급 색상/큐브 종류 표시 추가, vitest 도입 후 `cubeSimulator.test.ts` 12개 단위 테스트 작성. `build-planet-data.mjs`는 내부 함수 미export라 테스트 대상에서 제외 — 필요시 추후 리팩토링 후 테스트 추가
- [ ] `applyMonsterOverrides`의 `_`-prefix 필드 leak 버그는 수정 완료(2026-07-08), 재발 방지용 `build-planet-data.mjs` 자체 단위 테스트는 여전히 없음
- [x] **큐브 시뮬레이터 — 2·3번째 옵션 이탈확률**(2026-07-08) — mapleplanet.co.kr/Cube 공식 페이지에서 직접 확인 완료. 수상한/미라클 큐브 동일: 등급상승 레어→에픽 6%/에픽→유니크 1.8%, 이탈확률 2번째 10%/3번째 1%. `cubeSimulator.ts` 참고
- [ ] **큐브 시뮬레이터 — 유니크→레전드리 확률은 여전히 메이플플래닛 공식 확인 아님**(2026-07-08) — 공식 확률표엔 레전드리 단계 자체가 없음(사이트 JS `gradeLabel`도 4단계뿐, "레전드리" 문자열이 페이지 어디에도 없음). 다만 앞 두 단계(레어→에픽 6%/에픽→유니크 1.8%)가 넥슨 공식 "레드 큐브" 확률표와 소수점까지 정확히 일치하는 걸 발견해서, 레드 큐브의 공식 유니크→레전드리 확률(0.3%)로 교체함(단순 감소비율 추정치 0.54%보다 근거 있음) — 그래도 메이플플래닛이 실제로 레드 큐브와 100% 동일한 표를 쓰는지는 확인된 게 아니라 `GRADE_UP_CHANCE.uniqueToLegendary` 여전히 잠정치
- [x] **큐브 시뮬레이터 — "원하는 옵션 기댓값" 계산기 추가**(2026-07-08) — `estimateOptionOdds`(닫힌 형태 확률 계산, 실제 반복 시뮬레이션 아님)로 옵션별 큐브 1회당 확률/기댓값 표시. 옵션 선택 목록이 착용 레벨에 따라 실시간으로 텍스트 갱신됨(`resolveOptionText`). 유니크→레전드리 확률이 잠정치이므로 레전드리 등급 옵션의 기댓값도 그만큼 부정확할 수 있음 — 위 항목과 연동
- [x] **큐브 시뮬레이터 — reqLevel 미달 옵션 확률 계산 버그**(2026-07-08, 코드 리뷰로 발견) — `estimateOptionOdds`가 착용 레벨 미달 옵션에도 0이 아닌 확률을 계산하던 버그 수정, UI 옵션 목록도 레벨 미달 항목 제외하도록 수정
- [x] **sitemap.ts — 플래닛 라우트 5개 누락 복원**(2026-07-08, 코드 리뷰로 발견) — 이번 세션 이전부터 이미 빠져 있었음(원인 미상), 큐브 시뮬레이터 포함 5개 라우트 복원
- [x] **큐브 시뮬레이터 — 3옵션 동시 선택 기능 추가**(2026-07-08) — `estimateCombinedOptionOdds`로 최대 3개 옵션이 동시에 나올 확률 계산(포함-배제 원리). 메소 비용 계산은 안 하기로 함(사용자 요청) — 대신 "기댓값"(몇 개 필요한지)과, 미라클 큐브 한정으로 캐시 비용(원화) 표시
- [ ] **큐브 시뮬레이터 — 메포→원화 환율이 근사치**(2026-07-08) — 메포 공식 고정 결제가를 못 찾아서(플랫폼 결제 페이지 미확인) 커뮤니티 통용 메소마켓 환율(1,200메포≈1,000원)의 중간값으로 근사. `CubeSimulator.tsx`의 `KRW_PER_MEPO_APPROX` 참고 — 실제 캐시샵 정가 확인되면 교체 필요
- [x] **큐브 시뮬레이터 — 옵션 선택 UI 개선 + 자동 기댓값 + 확률 프레이밍**(2026-07-08) — 검색 가능한 커스텀 드롭다운(`OptionPicker`)으로 교체, 옵션 미선택 시 방금 뽑은 결과 기준 자동 기댓값 표시(`ResolvedCubeLine`에 `id` 필드 추가 필요했음), 희귀 옵션은 "1/N" 분수로 표시
- [x] **큐브 시뮬레이터 — 등급 먼저 고르는 2단계 드롭다운 + 옵션 선택 시에만 기댓값 표시**(2026-07-08) — 검색형 콤보박스를 버리고 등급→옵션 2단계 네이티브 select로 교체, "옵션 미선택 시 자동 표시"는 제거(옵션을 골라야만 표시), `등급 고정` 배지는 체크박스와 무관하게 항상 노출
- [x] **큐브 시뮬레이터 — 비상식적으로 큰 기댓값 수정 + 등급업/옵션재설정 분리 + 장비 종류 필터**(2026-07-08) — maplanet.net 큐브 계산기를 참고해 "등급업 기댓값"(옵션과 무관하게 등급만 올리는 비용, `estimateGradeUpExpectedUses`)과 "옵션 재설정 기댓값"을 분리된 패널로 나눔. "이미 그 등급에 도달했다고 가정" 토글 추가(`estimateOptionOdds`/`estimateCombinedOptionOdds`에 `fixedOverallGrade` 파라미터 추가) — 꺼져있으면 기존처럼 등급업 확률까지 섞인 값(레전드리 3옵션 조합 시 2천만 개 단위로 커질 수 있음, 이게 정상임을 토글로 명시), 켜져있으면 그 등급에 이미 도달했다고 보고 옵션 재설정 비용만 계산(같은 조합이 75개로 줄어듦). 장비 종류(모자/장갑/신발/상의·하의·전신/기타) 선택 필터도 추가 — cube-data.js 안에서 스킬 옵션(쓸만한 OO)들이 optionType으로 짝지어진 것을 근거로(51=모자, 53=상의 등, 54=장갑, 55=신발) 부위 무관 노출을 방지. 신규 vitest 5개 추가(29개 전체 통과)
- [x] **큐브 시뮬레이터 — 같은 옵션 중복 선택 시 잘못 계산되던 버그 수정**(2026-07-08, 코드 리뷰로 발견) — `estimateCombinedOptionOdds`가 `new Set(targetEntryIds)`로 중복 id를 그냥 제거해서, 같은 옵션을 슬롯 2개에 고르면 "1줄 이상" 확률로 잘못 계산되던 버그. 요구 횟수(같은 id가 몇 번 선택됐는지)를 추적해서 "N줄 이상 일치" 확률을 직접 열거(brute-force, 줄 3개·목표 최대 3개라 저렴함)로 정확히 계산하도록 전면 재작성. UI에도 "(N줄)" 라벨 추가, 중복 id로 인한 React key 중복도 distinct id 기준 렌더링으로 해결. 캐시 비용 표시에도 "(추정 환율 기준)" 라벨 + 메포 병기 추가(원화 단독 표시가 실제 결제 비용처럼 보인다는 지적 반영). vitest 1개 추가(30개 전체 통과)
- [x] **큐브 시뮬레이터 — 장비 종류 필터를 실제 롤 시뮬레이터에도 적용**(2026-07-08) — 기존엔 "장비 종류" 선택이 "원하는 옵션 기댓값" 계산기의 옵션 목록에만 적용되고, 실제 큐브 클릭 롤(`rollCubeResult`)과 확률 계산의 등급 내 가중치 합산(`eligiblePoolAtGrade`)은 필터 없이 전체 풀을 썼음 — 다른 부위 전용 스킬 옵션이 실제로 뽑히거나, 가중치 총합 계산에 끼어들어 다른 옵션들 확률도 미세하게 부정확했던 상태. 장비 종류 선택 UI를 상단 공용 위치로 옮기고, `suspiciousPoolForEquip`/`miraclePoolForEquip`으로 미리 필터링한 풀을 롤과 확률 계산 양쪽에 동일하게 전달하도록 수정. Node로 2만 회 가중치 추출 시뮬레이션 돌려서 신발 선택 시 "쓸만한 컴뱃 오더스"만 나오고 다른 부위 스킬(미스틱 도어/하이퍼 바디/샤프 아이즈/어드밴스드 블레스/윈드 부스터)은 전혀 안 나오는 것 확인

### 한방컷 계산기 — 플래닛 누락 스킬 조사 (2026-07-08)

플래닛은 메랜의 모든 직업을 포함하면서 "후반 스킬"까지 더 있다는 사용자 확인 — namu.wiki의 "OO/RED 이전 스킬" 문서(2013 RED 개편 이전 = 메랜/플래닛과 같은 프리빅뱅 시절)로 직업별 4차 스킬을 훑어 `mainSkillMapping.json`과 대조. 방법론 검증: 기존 `damageMapping.json`의 브랜디쉬 데이터를 역산해보니 namu.wiki가 제공하는 레벨 1/15/30 앵커 포인트 세 개를 정확히 지나는 구간별 선형보간(1~10구간, 11~30구간)으로 만들어진 값과 일치 — 이 프로젝트가 원래 이 방식으로 스킬 데이터를 구축해왔음을 확인, 새 스킬도 같은 방식 적용 가능. maple.land 공식 패치노트는 claude-in-chrome 확장에 해당 도메인 권한이 없어 열람 못 함(교차검증용으로 추후 권한 승인 후 확인 필요). 상세 레벨별 수치는 `scratchpad/skill-gap-research.md`(세션 임시 경로라 다음 세션엔 없을 수 있음, 필요시 재조사) 참고.

- [x] **전사 — 돌진 구현 완료**(2026-07-09, 코드 리뷰로 재설계) — 히어로/다크나이트 문서 값(Lv1 72%~Lv30 130%, 전체 구간 선형) 채택. 처음엔 기존 히어로 전용 "돌진 레벨" 보너스 슬라이더(`rushBonus`)를 실데이터로 교체해서 팔라딘/다크나이트까지 확장하는 방식으로 넣었는데, Codex 리뷰로 "돌진은 패시브 버프가 아니라 별도 공격 스킬이라 브랜디쉬/블래스트 등에 최대 1.3배로 곱해지는 게 게임 메커니즘과 안 맞다"는 지적을 받아 전면 재설계 — `damageMappingActive`에서 빼서 `damageMapping`으로 옮기고(count=1) 전사 3직업 `mainSkillMapping`에 독립 선택형 스킬로 추가, 기존 "돌진 레벨" 보너스 슬라이더(히어로 원래 있던 것 포함) 완전 제거
- [x] **팔라딘 — 생츄어리 구현 완료**(2026-07-09) — 앵커값(Lv1 420%/Lv15 700%/Lv30 900%) 기준 1~10/11~30 구간보간으로 전체 테이블 생성해 `mainSkillMapping`/`damageMapping`에 선택 가능한 스킬로 추가. HP1 고정 특수룰은 계산 로직엔 반영 안 하고(개념상 이 계산기가 노리는 "즉사" 판정과 안 맞음) 스킬 선택 시 안내 문구로만 표시
- [x] **다크나이트 — 버서크는 원래부터 이미 구현되어 있었음**(2026-07-09 재확인) — 어제 조사가 `mainSkillMapping.json`만 훑어서 "누락"으로 오판했던 것. 실제로는 `damageMappingActive["버서크"]`에 레벨 0~30 실데이터(Lv1 113%~Lv30 200%, TODO 조사치와 소폭 다르지만 기존 데이터가 더 정교해 보여 그대로 유지)가 있고 다크나이트 패널의 "버서크" 슬라이더가 이미 정상 작동 중이었음 — 별도 작업 불필요
- [ ] **아크메이지(불,독)/(썬,콜) — 엘퀴네스/이프리트 여전히 미구현** — 이번 세션에서 사용자 판단으로 범위 제외(소환수는 "1회 사용=N타" 구조인 이 계산기 모델과 안 맞고, 공격 주기 데이터도 namu.wiki에 없음). **버그 발견**: 기존 코드에 이미 `ifritBonus`(변수명)라는 자리표시자 슬라이더가 있는데 아크메이지 공용 패널에서 라벨이 항상 "엘퀴네스 레벨"로 고정 표시됨(썬콜 유저가 봐도 "엘퀴네스"로 보임) — 실제 스킬 데이터 없이 `1+레벨/100` 근사치만 적용하는 자리표시자이니 소환수 제대로 구현할 때 라벨 버그도 같이 정리 필요
- [ ] **비숍 — 바하뮤트 여전히 미구현** — 위와 동일 사유(소환수). 기존 `bahamutBonus` 슬라이더도 동일하게 `1+레벨/100` 근사치 자리표시자
- [x] **보우마스터/신궁 — 드래곤 펄스 구현 완료**(2026-07-09) — 레벨 무관 고정 100% 마스터 전용 스킬로 확인되어(1/15/30 테이블 자체가 없음) 전 레벨 100 고정값으로 등록, 두 직업 공용으로 추가
- [ ] **보우마스터 — 피닉스 / 신궁 — 프리져 여전히 미구현**(소환수, 이번 범위 제외) — **참고**: `specialBaseDamage`에 "피닉스"/"프리져"를 위한 DEX*2.5+STR 계산식이 이미 존재함(용도 불명 — 예전에 준비만 해두고 안 쓴 것으로 추정). `mainSkillMapping`에 스킬명만 추가하면 바로 선택 가능해질 수 있어 다음에 손볼 때 비교적 저비용
- [x] **신궁 — 스나이핑 구현 완료**(2026-07-09) — "스탯 무관 고정 199999 데미지, 물리무효 무시" 메커니즘을 `specialBaseDamage`에 신규 분기로 추가(민/맥스 199999 고정, `ignoreDefense: true`). 정확한 레벨별 MP/재사용 테이블은 여전히 못 찾음(마스터 전용으로 추정) — 스킬 레벨 슬라이더는 UI상 존재하지만 결과에 영향 없음을 안내 문구로 명시. **버그 발견 및 수정**(Codex 리뷰): min/max는 199999로 고정했지만 `finalDamageMultiplier`(크리티컬샷/샤프아이즈/골든이글 등)가 그대로 곱해져서 199999를 초과할 수 있었음 — 스나이핑 선택 시 `finalDamageMultiplier` 자체를 1로 고정하도록 수정, 크리티컬샷 30 설정해도 199,999 그대로 유지되는 것 확인
- [x] **나이트로드 — 닌자 스톰 구현 완료**(2026-07-09) — 앵커값(Lv1 42%/Lv15 65%/Lv30 80%) 구간보간으로 추가
- [ ] **나이트로드/섀도어 — 닌자 앰부쉬 여전히 미구현**(이번 범위 제외) — 조사 결과 "지속시간 동안 틱딜"하는 도트뎀 스킬로 확인되어(N타 강타 구조가 아님) 현재 계산기의 "1회 사용=N타" 모델과 구조적으로 안 맞음, 별도 설계 필요
- [ ] **건슬링거/발키리/캡틴 — 호밍·서포트 옥토퍼스 여전히 미구현**(이번 범위 제외) — 둘 다 마스터 전용 고정값만 확인됨(1/15/30 테이블 없음), 서포트 옥토퍼스는 소환수라 위 소환수 이슈와 동일
- [x] 인파이터/버커니어/바이퍼는 4차 공격 스킬 전부 이미 커버됨 — 재작업 불필요
- [ ] 시그너스 5종(소울마스터/플레임위자드/윈드브레이커/나이트워커/스트라이커)은 이번엔 깊게 재검증 안 함 — 다른 직업군 대비 이미 완성도 높다고 판단(별도 세션에서 mainSkillMapping+damageMapping+range20+range30 전체를 처음부터 구축한 이력, 107개 스킬), 우선순위 낮음
- [x] **데이터 파이프라인 드리프트 발견 및 복구**(2026-07-09) — `data/skills/mainSkillMapping.json`·`range20.json`·`range30.json`·`damageMapping.json` 4개 파일이 시그너스 나이츠 5개 직업(107개 스킬) 관련 내용을 담고 있었는데 정작 소스 파일 `data/raw/skills.raw.ts`엔 그 내용이 없었음 — 예전 세션에서 시그너스 데이터를 산출물 JSON에 직접 손으로 추가하고 소스는 안 고친 것으로 추정, `data.md`의 "산출물 직접 편집 금지" 골든룰 위반 상태였음. 이번에 스킬 추가 작업 중 발견해서 시그너스 데이터를 `skills.raw.ts`로 역이식 후 `node scripts/normalize-skills.mjs`로 재생성 — 소스가 다시 완전한 진실 공급원이 됨(재생성해도 데이터 유실 없음 확인 완료)
- [x] **드랍테이블 — 아이콘 없는 아이템 다수 수정**(2026-07-09) — 사용자가 드랍 테이블에서 아이콘 없는 아이템이 있다고 제보. 원인: 드랍표 HTML 스크래핑 시 실제 아이템 DB와 이름이 안 맞으면 "가상 아이템"(synthetic, 음수 ID)으로 처리되는데, 마스터리북/카드/주문서류는 대표 아이콘으로 대충 표시되지만 그 외(장비/재료/펫템 등)는 아이콘 자체가 없었음. 전체 110개 중 56개가 진짜 미해결 상태였고, maplestory.io 검색 API로 이름 대조해서 54개는 정확한 실제 아이템 ID 확보(오타 3개 — 촉진게→촉진제, 얼음조각 이름 누락, 포숀→포션 — 도 같이 바로잡음). `scripts/sources/item-name-overrides.json`(수기 관리 소스) 신설해 `build-drop-index.mjs`가 참고하도록 연결, `npm run build:drop-index` 재실행. 무관한 기존 아이템 2600여 개는 전부 그대로인 것 확인(diff 검증 완료). 남은 2개("그륜", "녹견량포")는 스크래핑 원본 이름 자체가 깨져 있어(G.팬텀워치 드랍) 실제 아이템 특정 불가 — 오매칭보다 미표시가 낫다고 판단해 보류
