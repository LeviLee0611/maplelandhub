# 메랜Hub TODO

> 남은 액션만 기록. 완료된 작업의 상세 경위/디버깅 기록은 `DEVLOG.md`(날짜별, 최신이 위)에 있음 — 이 파일에서 항목을 완료 처리할 땐 지우지 말고 DEVLOG.md에 옮겨 적은 뒤 여기서 제거.

---

## 다음 세션 확인 필요 (2026-09-09 세션 말미에 발견, 미해결)

- [ ] **몬스터 이미지 502/404 12종 — 정적 이미지 교체 필요**: 다중 버전 폴백 체인을 붙여도 복구 안 될 가능성 높음(서버 자체 렌더링 실패로 보임). `MOB_ICON_STATIC_OVERRIDES`(`src/lib/maplestory-io.ts`)에 수동으로 정적 이미지 추가하는 게 유일한 해법. 대상: 미믹(8220036), 푼코(6300003), 푸코(6230101), 셰이드(5090000), 자쿰 3페이즈(8800002), 자쿰 1페이즈(8800000), 알리샤르(2600622, 404), 퍼펫골렘(9300024), 스켈독(9000100), 리게이터(9500108), 주니어 페페(9500112), 카오스 자쿰(8888889, 404). 인벤 몬스터 도감 등에서 이미지 구해와야 함(혼테일/카오스자쿰 때 썼던 방식과 동일).
- [ ] **무루무루(아란 리엔 몬스터, mobCode 100133) 드랍 데이터 이상** — 사용자가 드랍테이블에서 발견: 레벨7 튜토리얼 몬스터인데 스틸 풀 헬름/옐로우 메탈 기어/블루 로리카 아머·바지/소피아 바지/스틸 그리브/아이언 볼 등 장비 7종이 극희귀 확률(0.0005~0.0012%, 메랜 기준)로 붙어있음 — 상식적으로 리엔 튜토리얼 몹이 드롭할 아이템이 아님. `data/drop-index.json`에서 확인 결과 이 7종은 진짜로 mobCode 100133에 배정돼있음(다른 이름 중복 몬스터로 오배정된 게 아님, 조사 완료). **의심되는 원인**: 무루/무루파/무루피아/무루무루/무루쿤(mobCode 100130~100134, 2026-07-31 `scripts/add-aran-rien-monsters.mjs`에서 순차 배정)이 실제 KMS 공식 mobCode가 아니라 우리가 임의로 붙인 번호일 가능성 — 100xxx대는 원래 헤네시스 초반 몬스터들이 쓰는 진짜 KMS ID 대역이라, legacy-mapledb 드랍 소스가 이 번호를 "무루무루"가 아니라 원래 그 번호를 쓰던 실제 몬스터로 인식해서 엉뚱한 드랍표를 갖다붙였을 가능성이 높음. mapledb.kr엔 100133 페이지가 없어(폴백만 뜸) 원 소유자를 못 찾음 — 나무위키 등에서 mobCode 100130~100134 또는 인접 번호대의 실제 몬스터가 뭔지 확인 필요. 확인되면 (a) 무루 5종을 실제로 안 쓰이는 mobCode로 재배정하거나 (b) 최소한 이 7종 드랍은 제거. 무루/무루파/무루피아/무루쿤(100130~100132,100134)도 같은 문제가 있는지 같이 확인할 것.

### 2026-09-09 세션 종료 시 사용자 피드백 — 내일 우선 확인할 회귀 후보 4건

오늘 작업(서버별 스킬 분리 `damageMappingPlanetOverrides` 구조, 몬스터 스탯/이미지 정정)의 방향 자체는 사용자가 긍정 평가함(좋은 방향이었다고 확인). 다만 오늘 만든 변경들이 새로 회귀를 만들었을 수 있는 지점 4곳을 사용자가 직접 짚어줌 — 내일 세션 최우선으로 확인:

- [ ] **정령의 축복 상한을 넘는 과거 프리셋 값이 그대로 계산되는 문제** — 오늘 `SPIRIT_BLESSING_BY_SERVER.planet.maxLevel`을 200→22로 정정(`onehit-calculator-client.tsx`). 문제는 200이 최댓값이던 시절에 저장된 캐릭터 프리셋/퀵슬롯(`character_presets` DB 또는 localStorage)에 `spiritBlessingLevel`이 22보다 큰 값(예: 150)으로 저장돼있을 수 있는데, 그 값을 그대로 불러와서 `spiritAtt = spiritBlessingLevel * attPerLevel` 계산에 넣으면 슬라이더 UI의 `max={22}` 제한과 무관하게 실제 계산 결과는 여전히 부풀려진 수치가 나올 위험. 프리셋 로드 시 `Math.min(value, SPIRIT_BLESSING_BY_SERVER[server].maxLevel)`로 클램프하는 보정 로직이 있는지 확인하고, 없으면 추가 필요(다른 필드들의 프리셋 복원 검증 effect와 같은 패턴으로).
- [ ] **몬스터 변경 시 이미지 실패 상태가 남을 가능성** — 오늘 `DropTable.tsx`/`MonsterSelect.tsx`/`MonsterPanel.tsx`의 몬스터 아이콘 폴백을 `handleMapleIoImageError`로 교체하면서 `<img>` DOM 엘리먼트의 `dataset.fallback`/`dataset.maple-retry` 값으로 재시도 상태를 추적하도록 함. React가 같은 `<img>` DOM 노드를 재사용한 채 `src`만 바꾸는 경우(예: 몬스터 A 선택 → 이미지 실패로 재시도 2단계까지 감 → 몬스터 B로 변경), 새 몬스터 B의 `src`가 정상인데도 이전 A의 `dataset.fallback`/`retry` 값이 안 지워진 채 남아있으면 잘못된 단계에서 시작하거나 정상 이미지인데도 불필요한 재시도를 타는 등 꼬일 수 있음. 몬스터 변경 시 해당 dataset을 초기화하거나, `key={monster.mobCode}`를 줘서 React가 새 DOM 노드를 만들도록 하는 방법 검토.
- [ ] **메랜 스킬 수정이 플래닛에도 전파되는 범위 재확인** — 오늘 `damageMapping`(메랜 기준 공유 테이블)을 6/19 패치값으로 다수 수정(돌진/브랜디쉬/블래스트 등). `damageMappingPlanetOverrides`에 없는 스킬은 전부 플래닛도 이 새 메랜값을 그대로 상속받는 구조인데, 그중 실제로는 플래닛이 다른 값을 쓰는 스킬이 섞여있을 수 있음(8/28 패치에서 다뤄진 8개 스킬 외에 아직 대조 안 한 스킬들). **외부 리뷰(REVIEW-2026-09-09.md, 커밋 제외됨)로 구체화됨**: 오늘 메랜에서 수정한 12개 스킬 중 브랜디쉬·블래스트(둘 다 오버라이드 있음) 제외한 **10개(돌진/백스핀 블로우/더블 어퍼/인비지블샷/파이어 버너/쿨링 이펙트/래피드 파이어/에로우 봄/아이언 에로우/윈드 샷)는 플래닛 오버라이드 없이 새 메랜값이 그대로 플래닛에 전파됨** — 이 10개가 실제 플래닛에서도 같은 값을 쓰는지 개별 확인 필요(레벨1/마스터 직전/마스터 지점 위주로 대조 권장).
- [ ] **재빌드 시 오늘 데이터 보정이 유지되는지 확인** — 오늘 `data/monsters.json`에 일회성 스크립트 3개(`add-sharenian-statue-monsters.mjs`, `fix-duplicate-name-placeholder-monsters.mjs`, `fix-duplicate-name-placeholder-monsters-batch2.mjs`)로 직접 patch를 여러 건 넣음. `data.md`에 이미 문서화된 위험("일회성 보스 데이터 보강 스크립트는 `data/monsters.json`을 재생성하는 베이스 파이프라인을 다시 돌리면 유실됨")이 오늘 추가한 63종 몬스터 스탯 보정에도 그대로 적용되는지, 만약 그렇다면 이 3개 스크립트도 기존 목록(발록/카오스자쿰 등)과 같은 "재실행 필요 목록"에 등록해야 함 — `data.md`의 "일회성 보스 데이터 보강 스크립트" 절에 오늘 만든 3개 스크립트 추가 여부 확인.

### 최적화 후보 (사용자 지정 우선순위)

- [ ] **성능 측정 요청 중복 제거** — `WebVitalsReporter`(또는 관련 훅)가 같은 지표를 중복 전송하고 있는지 확인(리렌더/StrictMode 등으로 인한 중복 fire 가능성).
- [ ] **드롭 상세 중복 요청 제거** — `/api/drop-table/{monster,item}/{server}` 온디맨드 fetch가 같은 선택에 대해 중복 호출되고 있는지 확인(예: 같은 몬스터를 다시 클릭하거나, effect 의존성 배열 문제로 재요청).

### 광고 배치 — 사용자 최종 결정

1순위: **결과·설명 영역 아래 독립된 슬롯 1개**로 시작. 계산기류는 입력/버튼이 밀집한 조작 영역이 많아서 그 사이에 광고를 끼우면 사용 흐름이 끊긴다는 판단 — 중간 삽입은 하지 않음.
2순위(후보): 넓은 PC 화면 기준 우측 여백에 광고 칸.

---

## 신규 콘텐츠/패치 확인 필요 (2026-08-20 수동 점검)

자동 패치체크 루틴이 인프라 문제로 지난 6주간 실질적으로 작동 안 해서(아래 "자동 패치체크 루틴 인프라 문제" 항목 참고), 이 세션에서 2026-07-24 이후 패치를 확인함. `maple.land`는 WebFetch로 원문에 직접 접근됨. `mapleplanet.co.kr`는 이 세션에서도 WebFetch가 403으로 막혀 원문 전체는 못 읽었고, WebSearch가 인용한 패치노트 문구(스니펫)로만 확인함 — 플래닛 항목은 원문 대조가 아직 안 된 상태라는 점 감안. 데이터/계산기에 안전하게 반영 가능한 항목은 없었음(전부 수치 미기재 또는 계산기 모델 범위 밖) — 아래는 확인만 하고 반영 보류한 항목. 각 항목의 재검토 상태는 `scripts/state/last-patch-check.json`의 `pendingReview`에도 기록해둠(체크포인트 날짜가 전진해도 이 목록이 안 비면 자동 루틴이 다시 확인하도록 설계, `data.md` 참고).

- [ ] 메이플랜드 2026-08-07 패치 "무릉도장"(무릉도원 38층 도전 던전, 최상층 보스 "소공") — 신규 사냥터 콘텐츠 반영 검토 후보. 패치노트 자체엔 층별 몬스터/드롭 정보가 없고 "층 클리어 시 수련 점수 → 벨트 교환" 구조로 보여 전통적 드롭 사냥터와 다를 수 있음. 일반 메이플스토리 위키의 무릉도장 정보(80층/"킹 오멘" 등)는 넥슨 정식 서버 기준이라 메랜(38층/"소공")과 층수·보스명부터 달라 그대로 참고 불가 — 반영하려면 메랜 자체 원문/커뮤니티 소스로 몬스터 mobCode·드롭 확인 필요.
- [ ] 메이플랜드 2026-08-14 패치 마법사 계열 스킬 밸런스 — 매직 컴포지션(불독/썬콜)/파이어 데몬/아이스 데몬이 "기본 공격력이 향상되었습니다"로만 적혀있고 정확한 %가 원문에 없음(`https://maple.land/board/notices/pzx6wmuz4h4slkbvklaojerw`). 정확한 수치 확보 전엔 `data/raw/skills.raw.ts` 반영 보류.
  - 같은 패치의 패럴라이즈(석화) 타겟 수 1→2마리, 체인 라이트닝 전이 감소 공식 변경(1차 전이부터 30%씩 감소 → 2차 전이부터 감소)은 수치 자체는 확인됨 — 다만 한방컷 계산기가 단일 대상 데미지%만 모델링해(기존 "최대 N마리" 광역기 처리 방침과 동일, 아란 절 참고) 다타겟/타겟 수 변경은 애초에 반영 대상 아님.
- [ ] 메이플플래닛 2026-07-24 패치 "석탄 가루" 드롭처 변경(폐광→엘나스) — `data/drop-index.json`/`data/planet/drop-index.json` 양쪽에 "석탄 가루" 자체가 없음(grep 0건, 확인 완료) — 반영할 기존 데이터가 없어 보류. 향후 폐광/엘나스 드롭 데이터 스크래핑 시 참고.
- [ ] 메이플플래닛 2026-08-18 패치 카오스 자쿰/카오스 혼테일 입장 제한(매일 → 주 2회, 월요일) — `/farming-manager`가 현재 "준비 중" placeholder라 보스 입장 제한을 트래킹하는 기능 자체가 없어 반영 대상 아님. 향후 파밍 매니저 실제 구현 시 참고.
- [x] 메이플랜드 2026-08-21 패치 "길드대항전"(페리온 유적발굴 캠프) 몬스터 스탯 반영 → **2026-09-09 완료.** 원문 재확인 결과 몬스터는 14종(악마 슬라임/머슬스톤/다크 머슬스톤/마스터 머슬스톤/퍼펫골렘/미스트 나이트/블랙 나이트/**주니어 가고일**/가고일/사자석상 A/사자석상 B/기사석상 A/기사석상 B/에레고스) — 기존 TODO엔 "주니어 가고일"이 누락돼있었음. 조사해보니 9종(주니어 가고일 포함, mobCode 9300019~9300028 중 8개+9300026)은 이미 완전한 스탯(acc/eva/needAcc/def/mDef)으로 `data/monsters.json`에 들어있었음(출처 불명, 아마 과거 세션에서 mapledb.kr로 확보해놓고 TODO 갱신을 안 했던 것으로 추정) — 나머지 5종(다크 머슬스톤, 사자석상 A/B, 기사석상 A/B)만 실제로 미확보 상태였음. mobCode가 9300019~9300028 구간에 몰려있고 9300021만 비어있는 패턴을 보고 "다크 머슬스톤=9300021"로 추정 후 mapledb.kr에서 9300021/9300029~9300032를 직접 조회해 5종 전부 확인·반영(`scripts/add-sharenian-statue-monsters.mjs` 신설). 사자석상 A/B·기사석상 A/B는 서로 HP만 다르고 나머지 스탯이 완전히 동일 — namu.wiki 스니펫("A/B는 스킬과 체력만 다름")과 일치해 신뢰도 확인. 8/28 플래닛 패치노트에도 "길드대항전이 추가되며 에레고스/사자석상/기사석상 HP가 원작 대비 조정됨"이라고 명시돼있어 플래닛에도 실제 라이브 콘텐츠임을 재확인(정확한 조정값은 패치노트에 없어 반영 안 함 — 메랜값을 플래닛에도 그대로 상속).
  **주의**: 다크 머슬스톤은 레벨200/HP9,999,999/방어1,999/mDef1,999/회피999처럼 극단적으로 둥근 수치 — mapledb.kr 원문 그대로지만, "신념의 방"(길드대항전 던전 특정 구간)에 배치된 걸 보면 실제로 잡으라고 만든 몬스터가 아니라 진행을 막는 장애물/벽일 가능성이 높음(추정, 확정 아님). acc는 mapledb.kr에 해당 항목 자체가 없어 5종 전부 0 처리(근거 없이 추정 안 함, 이 데이터셋 전반의 기존 acc 공백 패턴과 동일). watk/matk도 페이지에 없어 0.
  `data/release-filters.json`/`data/planet/release-filters.json`의 `allowedMobCodes`에 14종 mobCode(9300019~9300032) 전부 추가해 노출. 예티 사례에서 발견된 "이름 중복 플레이스홀더"(주니어 가고일의 9300033 duplicate)는 의도대로 계속 차단 상태 유지 확인.
  **드롭 데이터는 이번에도 미반영**(TODO 하단 항목 참고) — mapledb.kr 페이지들에 드롭 정보 자체가 없었음, 별도 소스 필요.
  검증: `npx tsc --noEmit`/`npx eslint src/`/`npx vitest run`(100개) 전부 통과, `node scripts/build-planet-data.mjs` 재실행으로 플래닛 동기화 확인, release-filter 로직 직접 시뮬레이션해 14종 전부 노출·9300033만 차단 유지되는 것 확인.
- [ ] 길드대항전 몬스터 14종 드롭 데이터 미반영 — mapledb.kr에 드롭 정보 없음, 별도 소스(공식 패치노트도 드롭 아이템 언급 없음 — 마스터리북 5종은 보상으로만 명시, 몬스터 드롭 아님) 필요. 착수한다면 mapleland 원본 `monster_detail/<mobCode>` 페이지 직접 스크래핑(에델슈타인 때 방식) 먼저 시도.
- [ ] 메이플랜드 2026-09-07 패치 신규 지역 "에델슈타인" — 몬스터 25종(이름/레벨/HP/EXP/속성/스폰맵/def·mDef·watk·matk·needAcc)을 Maple Note 스냅샷으로 확보해 `scripts/import-edelstein-monsters.mjs`로 `data/monsters.json`/`data/monster-spawns.json`에 반영 완료(2026-09-08, 커밋 안 됨). acc/eva만 0(이 데이터셋 전반의 기존 공백, 이번 건 결함 아님).
  **목록 완전성 검증(2026-09-08)**: namu.wiki "메이플스토리/일반 몬스터/에델슈타인" 문서(GMS 공식 데이터 기반)와 대조 — 훈련로봇 시리즈/경비로봇E/날뛰는 인형/비상용 경비로봇L 같은 몬컬 제외·퀘스트·직업 전용 개체를 뺀 "진짜 필드 몬스터"만 세면 에델슈타인 본토 13종 + 레벤 광산 12종 = 정확히 25종으로 저희 목록과 이름 기준 1:1 일치. 본토 13종은 watk/matk까지 완전히 일치. **다만 레벤 광산 12종(경비로봇~광석 이터)은 이름만 일치하고 watk/matk 수치가 namu.wiki 값과 다름** — namu.wiki는 오늘자 라이브 GMS 기준이고 메랜은 프리빅뱅 옛날 버전 재현이라 후반 사냥터만 그간 밸런스패치됐을 가능성 높음(추정, 미확정). 레벤 광산 12종 수치는 별도 메랜/구버전 GMS 소스로 재검증 필요.
  **드롭 데이터(2026-09-08 완료)**: `scripts/fetch-edelstein-drops.mjs` 신설, `monster_detail/<mobCode>` 페이지의 GET 섹션을 직접 파싱해 25종 전부 드랍 연결 완료(39건). AF형/고장난 DF형 안드로이드/광석 이터는 배틀메이지 마스터리북(피니쉬 블로우/다크 제네시스/와일드 발칸)을 드랍 — 오늘 추가한 배틀메이지 콘텐츠와 자연스럽게 연결됨.
  **레벨 정정(2026-09-08)**: 공식 9/7 패치노트 원문 대조 결과 7종이 ±1~3레벨 틀려있었음(순찰로봇 14→15, 이상한 이정표 16→17, 구렁이 18→19, 안전제일 31→30, 경비로봇L 79→78, 라칸 82→83, 고장난 DF형 안드로이드 96→95) — `data/monsters.json`/`data/planet/monsters.json` 정정 완료. hp/exp는 패치노트에 없어 기존 스크래핑값 유지(레벨 변동폭이 작아 근사치로 판단).
- [x] 메이플랜드 배틀메이지(2026-09-07 출시)가 메이플플래닛에도 출시됐는지 → **2026-09-09 사용자 확인: 플래닛엔 아직 배틀메이지 없음.** 같은 기간 플래닛 공식 패치노트(7/30~9/3, 원문 확인함)에도 언급 없었던 것과 일치. `onehit-calculator-client.tsx`의 `server === "planet"`일 때 배틀메이지를 "기본 공격"만 노출하는 기존 게이팅이 정확한 상태이므로 코드 변경 없음. 플래닛에 출시되면 게이팅 제거 + 플래닛 실측 데이터 확보 필요.
- [x] 메이플플래닛 2026-08-28 밸런스 패치 반영 → **2026-09-09 완료.** 공식 원문(`mapleplanet.co.kr/board/update/700`) 전체를 curl로 직접 확보해 damage%가 명시된 8개 스킬(브랜디쉬/블래스트/빅뱅/엔젤레이/스트레이프/어벤져/드래곤 스트라이크/스나이핑)을 반영. 상세 경위·검증 방법·범위 밖으로 제외한 항목은 DEVLOG.md 2026-09-09 "밸런스 패치 반영" 절 참고. **아키텍처 변경**: 이 작업 도중 메랜도 2026-06-19에 독자적으로 대규모 스킬 재조정(KMS 1.2.35→1.2.89 기준)을 했다는 사실이 드러나 `damageMapping` 단일 공유 테이블 구조로는 두 서버를 동시에 정확히 표현할 수 없음이 확인됨 — `data/raw/skills.raw.ts`에 `damageMappingPlanetOverrides`(마스터레벨만) 신설, `onehit-calculator-client.tsx`의 `skillBase`가 `server==="planet"`일 때 이 오버라이드를 우선 조회하도록 변경. 앞으로 서버 간 스킬% 차이가 추가로 발견되면 이 테이블에 추가하면 됨.
  **함께 반영한 메랜 2026-06-19 값(공식 원문 확인, 이번에 처음 반영됨)**: 돌진(→140%), 브랜디쉬(→270%, 플래닛과 별개), 블래스트(→600%, 플래닛과 별개), 백스핀 블로우(→260%), 더블 어퍼(→310%), 인비지블샷(→180%), 파이어 버너(→190%), 쿨링 이펙트(→160%), 래피드 파이어(→200%), 에로우 봄(→150%), 아이언 에로우(→200%), 윈드 샷(→380%). 어썰터(이미 500%)/트리플 파이어(이미 210%)/파이어 샷(이미 150%)은 기존 값이 이미 일치해 변경 없음.
  **반영 안 한 항목(둘 다 buff/mechanic-only라 one-hit 계산기 범위 밖, 또는 소환수라 원래 미구현)**: 정령의 축복/인레이지/위협/하이퍼바디/버서크/홀리실드/집중/스턴마스터리/배틀쉽 내구도/에너지차지·슈퍼트랜스폼 스탠스 — 데미지% 아닌 버프·확률·메커니즘 변경. 파이어 데몬(기본공격력150)/아이스 데몬(130, 둘 다 6/19 패치로 확인됨)은 소환수 스탯이라 애초에 계산기 미구현 범위(TODO "한방컷 계산기 — 스킬 미구현 항목" 참고). 페럴라이즈/스피어버스터/폴암버스터/부메랑스텝/쇼다운/스틸/브랜디쉬의 "최대 N마리" 타겟 수 변경은 이 계산기가 원래 타겟 수를 모델링 안 해서 반영 대상 아님(아란 절과 동일 원칙). 윈드 피어싱은 `mainSkillMapping`에 애초에 없어(윈드브레이커 목록에 미포함) 반영할 데이터 자체가 없음.
  **알려진 데이터 불일치(주의)**: 엔젤레이의 기존 저장값(Lv30=250)이 플래닛 패치노트의 "기존" 값(240)과 10 차이가 남 — 정확한 유래 불명(사소한 오차로 보이나 미해결). 오버라이드 자체(140%×2)는 패치노트 "변경 후" 값을 그대로 썼으므로 이 불일치와 무관하게 정확함.
  **검증**: `npx tsc --noEmit`/`npx eslint src/`/`npx vitest run`(100개)/`npm run build` 전부 통과. Node 스크립트로 `skillBase` 해석 로직을 8개 스킬×2서버 전부 시뮬레이션해 기대값과 일치 확인(예: 블래스트 메랜 600%×1 vs 플래닛 330%×2). 브라우저 클릭 테스트는 이 세션에 도구가 없어 못 함.

- [ ] **네오 도쿄(지팡구) — 미출시 확인됨(2026-09-09), 향후 패치체크 때 계속 모니터링만** — 공식 패치노트 2026-05-29~09-07 전체(15건) 원문 전수 확인 결과 네오도쿄/지팡구 언급 전혀 없음. 사용자가 "7월경 이미 나왔을 수 있다"고 기억했던 부분은 확인 결과 사실 아님. 상세 경위는 DEVLOG.md 2026-09-09 참고.

## 자동 패치체크 루틴 인프라 문제 (우선순위 높음 — 사용자 조치 필요)

- [ ] **루틴 자체가 사라짐(2026-09-08 확인)** — `trig_01Wf6e7JvS3biMsG2Me2q2Cn`(매주 금요일 실행)이 `RemoteTrigger` API 조회에서 404로 나옴. 2026-07-09부터 8주 가까이 돌았다는 이전 기록과 달리 이번엔 트리거 자체가 존재하지 않음(원인 불명 — 아래 (a)(b) 권한 문제와는 별개의 새 증상). 사용자가 claude.ai 쪽에서 루틴이 삭제/만료됐는지 확인하고 필요시 재생성 필요. 재생성 시 아래 (a)(b)도 함께 재확인.
  (a) 과거 확인된 문제: 클라우드 환경의 네트워크 egress 정책이 `maple.land`/`mapleplanet.co.kr`/`arca.live`/`namu.wiki`를 WebFetch/curl에서 차단(403 EGRESS_BLOCKED) — WebSearch 스니펫만 가능해 패치노트 원문 확인 불가. 참고로 이 대화형 세션에서는 `maple.land`는 정상 접근되지만 `mapleplanet.co.kr`는 매번 WebFetch 403(사이트 자체 봇 차단으로 추정) — egress 정책과는 다른 원인일 수 있어 재검증 필요.
  (b) 과거 확인된 문제: 2026-08-07 실행 땐 로컬 커밋까지는 했으나 `git push`/GitHub MCP 쓰기 도구(`create_branch`/`push_files`/`create_or_update_file`) 전부 403 "Resource not accessible by integration" — 저장소 쓰기 권한 자체가 없어 브랜치/PR 생성 불가. GitHub App의 저장소 쓰기 권한(Contents: write, ref 생성) 확인 필요. (이 대화형 세션은 push 제약 없음 — 직접 커밋·push 가능.)

---

## 레거시/중복 정리

---

## 데이터 파이프라인

- [ ] `data/release-filters.json` 주기적 검토 — 신규 출시 몬스터 차단 해제
- [ ] 드롭 데이터 최신화 프로세스 문서화 (원본 소스를 언제/어디서 구하는지)
- [ ] **`npm run build:drop-index`(및 체인되는 `build:planet-data`) 재실행 시 일회성 보스/이벤트 데이터 유실 위험** — 카오스 자쿰/카오스 혼테일/발록 추가 무기/아란 리엔 몬스터 드롭 등은 스크래핑 소스(`drops-parsed.json`)가 아니라 산출물(`data/drop-index.json`)에 일회성 스크립트로 직접 patch돼있어서, 베이스 파이프라인을 재실행하면 처음부터 다시 생성되며 이 데이터들이 전부 사라짐(2026-08-03 실제로 발생 — `git diff`로 발견 후 `git checkout`으로 복구, DEVLOG 참고). 근본 해결책 후보: (a) 일회성 스크립트들을 `build-drop-index.mjs` 실행 후 항상 자동으로 재적용하는 후처리 단계로 승격, (b) 해당 데이터를 `scripts/sources/`류 손수 관리 소스로 옮겨 스크래핑 소스에도 반영. 현재는 "베이스 파이프라인 재실행 금지, 필요하면 결과물에 최소 patch만" 원칙으로 회피 중.

---

## 기능 개선

### 우선순위 중간 (2026-09-09 하향 — 아래 참고)
- [x] ~~메랜 전용 아란 스킬 데미지% 실측치 확보~~ → **2026-09-09: 메랜(mapleland)에 아란이 실제로 출시된 적이 있는지부터 근본적으로 재확인됨.** 공식 패치노트 20건(2026-04-10~09-07) 전문을 태그 제거 후 직접 텍스트 검색했으나 "아란" 언급이 0건(같은 방식으로 확인한 배틀메이지는 9/7 패치에 명확히 있음 — 신직업 출시 시 패치노트에 반드시 언급된다는 대조군 확인). namu.wiki에도 다른 15개 이상 직업엔 있는 "Mapleland/직업/아란" 서브문서가 없고, 네이버 블로그(2026-07-05 작성)엔 "메랜에서도 현재 테스피아(테스트서버)에서 아란 플레이 가능, 조만간 업데이트될 듯"이라는 문구 확인 — 즉 최소 7월 초까지 메랜 본섭엔 아란이 없었던 것으로 보임. 기존에 "메랜에 아란 라이브 중"으로 기록해뒀던 근거(2026-07-31, 2026-08-20)는 둘 다 namu.wiki 검색 스니펫만 보고 판단한 것으로, 원문 검증 없이 내린 결론이었음이 드러남.
  **사용자 결정(2026-09-09)**: 그럼에도 기존 데이터(리엔 몬스터, `release-filters.json`의 `allowedMobCodes: 9300383`, 계산기 3종의 메랜측 아란 노출)는 그대로 유지. 대신 "다른 직업들도 서버 간 스킬셋이 크게 다르지 않다"는 판단 하에, `onehit-calculator-client.tsx`의 `server !== "planet"` 게이팅을 제거해 메랜에서도 플래닛과 동일한 `damageMapping`("아란" 키 공유) 값을 그대로 노출하도록 변경함. 즉 메랜 전용 실측치가 확보된 게 아니라 **플래닛 실측치를 메랜에도 근사치로 적용하기로 한 것** — 정확도보다 기능 제공을 택한 명시적 트레이드오프. 레벨1~마스터 전체 표를 주는 메랜 전용 소스는 여전히 못 찾음(아래 탐색 이력 참고).
  **2026-09-09 탐색 이력(소스 확보는 전부 실패)**: namu.wiki "Mapleland/직업/아란" 서브문서 여전히 없음(WebFetch가 namu.wiki 전역에서 403이라 WebSearch 스니펫으로 카테고리 목록 대조). dcinside `id=mapleland` 갤러리 "아란 스킬/툴팁/가이드" 검색 — 수치 있는 정리글 없음(상세 가이드는 전부 `id=planetmaple`, 플래닛 기준). 스킬 시뮬레이터 2곳(`skill.mapleland.st`, `maple-lab.st/skill`) JS 번들을 직접 다운로드해 "아란"/"aran" grep — 둘 다 매치 0건, 애초에 아란 자체를 지원 안 함(전사/마법사/궁수/도적/해적 5대 직업군만 구현). 네이버 블로그 약 20건 확인 — 전부 스킬 포인트 빌드오더/사냥터 가이드뿐, 레벨별 데미지% 표 0건(대부분 플래닛 기준). 다음에 다시 찾을 필요 있다면 이 5곳은 전부 재시도 대상에서 제외하고 새 경로를 찾을 것 — 인게임 캡처를 직접 올리는 커뮤니티 글 자체가 존재하지 않는 것으로 보임.
  **남은 검토 과제**: 메랜에 아란이 언제(혹은 아직도 안) 출시됐는지 공식적으로 재확인되면 이 판단 자체를 재검토할 것. 리엔 몬스터/9300383 필터 예외/계산기 노출을 계속 유지할지도 그때 같이 판단.

### 우선순위 중간
- [ ] 계산기 프리셋 — 공유 링크 기능
- [ ] 버프 타이머 — 알림 소리/브라우저 알림
- [ ] 어드민 — 피드백 상태 변경 UI 개선

### 우선순위 낮음
- [ ] 다크모드 지원
- [ ] 모바일 최적화 (사이드바 모바일 드로어)
- [ ] 사이트 전역 검색 (몬스터/아이템/퀘스트 통합 검색 — 드롭테이블 내부의 몬스터↔아이템 검색과는 별개, 그건 이미 구현됨)

---

## 기술 부채

- [ ] `src/components/` 정리 — ui/ 폴더로 원자 컴포넌트 이동, 피처 컴포넌트 분리 (범위가 커서 별도 세션/Plan 필요 판단, 이번엔 손대지 않음)
- [ ] API Route 에러 처리 표준화 (범위가 커서 별도 세션/Plan 필요 판단, 이번엔 손대지 않음)
- [ ] Web Vitals 수집 데이터 활용 계획 수립 (범위가 커서 별도 세션/Plan 필요 판단, 이번엔 손대지 않음)

---

## 배포 / 인프라

- [ ] Cloudflare Pages 환경변수 관리 문서화 (NEXT_PUBLIC_APP_URL 등)
- [ ] `RESEND_API_KEY`/`ADMIN_EMAIL`을 Cloudflare Pages(운영)에 등록 — 로컬 테스트는 완료(2026-07-07). 미설정 시 피드백 알림이 "조용히 스킵"되어(`src/app/api/feedback/notify/route.ts`) 운영자가 알림 미작동을 눈치채지 못할 위험 있음 — 등록 전까지는 어드민 피드백 목록을 주기적으로 직접 확인 필요, 향후 어드민 UI에 알림 상태 표시 추가 검토
- [ ] Resend 발신 도메인 인증 후 `from` 주소를 `onboarding@resend.dev`에서 실제 도메인으로 변경
- [ ] Supabase 백업 주기 확인 및 문서화
- [ ] maplestory.io API 다운 시 fallback 이미지 처리
- [ ] OG 공유 이미지 제작 — 현재 전 페이지가 favicon.ico로 통일되어 있음(디자인 작업), 1200x630 전용 이미지 필요

---

## 메이플 플래닛 데이터 정확도

### 정확도 오류 가능성 (우선순위 높음 — 사용자에게 틀린 값 노출 위험)
- [ ] 드랍테이블 — 주문서(장비 강화 스크롤) 드랍률이 조아요(플래닛)와 불일치 — **2026-09-09 조사**: 마뇽/한손검 60% 스크롤은 우리 계산(메랜 원본×4=3.2%)이 조아요 실측(3.2%)과 정확히 일치했지만, 예티/게비알/초록왕도깨비/콜드샤크는 정확히 2배 낮게, 주니어 발록은 정확히 2배 높게, 스텀피/좀비머쉬맘은 1.25~1.3배 차이가 나는 등 몬스터마다 불일치 방향·크기가 들쭉날쭉함.
  **1차 가설(반증됨)**: `build-merged-drops-parsed.mjs`가 legacy-mapledb를 dropchance-html보다 우선하는 게 원인이 아닌지 의심해 드라이런으로 검증했으나(둘 다 스크립트/데이터 건드리지 않음) **오히려 legacy-mapledb 우선이 맞는 것으로 확인됨** — dropchance-html로 되돌리면 6,066개 항목(전체 legacy 소스 9,104개 중)이 바뀌는데 그중 다수가 0%로 사라지거나 1,000배 이상 튀는 등 dropchance-html 쪽 문제로 보이는 값들이라 병합 로직 자체는 만지지 않기로 결론.
  **2차 가설(예티 사례로 발견, 부분 반증됨)**: 원본 블로그(`data/dropchance`, maplekibun.tistory.com/892)는 몬스터를 이름으로만 기록하는데, `monsters.json`에 **같은 이름의 몬스터가 90개나 중복**돼 있음을 발견(전체 745종 중 205개체 영향) — 그중 73개 그룹이 "정상 스탯 몬스터 1개 + acc/eva=0·def/mDef=10인 플레이스홀더 몬스터 1~3개"가 이름을 공유하는 패턴(몬스터북 카드용 더미로 추정, mobCode 대부분 9200xxx/9300xxx/9500xxx대). 예티(6300000 정상 vs 6300001/6300002/9500141 플레이스홀더)가 대표 사례로, 한손검 스크롤 0.14%(원본)가 6300001(플레이스홀더)에 붙어있어 정작 화면에 보이는 진짜 예티(6300000)엔 이 드랍이 없을 것으로 추정. **다만 dropchance-html 매칭 로직(`pickMonsterMatches`) 자체는 재현 테스트 결과 정상으로 확인됨** — 73개 혼합 그룹 전부(73/73) `monsters.json` 배열상 정상 스탯 몬스터가 항상 먼저 나와서 기존 "첫 매치 사용" 로직이 이미 올바르게 동작 중. 그리프의 "전신 갑옷 지력 주문서 60% = 0%"처럼 이상해 보였던 값도 원본 블로그 자체에 "0.00%"로 그렇게 적혀있는 것으로 확인(파싱 버그 아님).
  **남은 결론**: 코드/파이프라인 로직엔 고칠 버그가 없음이 확인됨. 실제 남은 문제는 `src/data/mapledb/drops.js`(legacy-mapledb, 우리가 재생성 안 하는 외부 정적 스크래핑 자산)에 이미 잘못된 mobCode로 박혀있는 개별 배정(예: 예티 스크롤→6300001)뿐 — 이건 로직으로 못 고치고 건별 수기 오버라이드가 유일한 해법. 마뇽/그리프/주니어 발록도 "정상+정상" 중복(리프레 지역 원본 vs "기타" 지역 축소판) 패턴이라 같은 부류의 오배정 위험이 있음.
  **2026-09-09 후속 — "플레이스홀더" 정체가 대부분 실제 몬스터로 밝혀짐**: 사용자가 "예티/페페 보스전을 나도 모르는데 리서치해보자"고 제안 — 73개 "혼합 그룹" 중 실제 아이템이 붙어있던 10개 mobCode(예티 6300001/6300002, 다크 예티 6400001/6400002, 페페 6130103, 다크 페페 6230201, 파이어독 9500138, 마스터 소울테니 9500127, 데스테니 9500135, G.팬텀워치 9500137)를 mapledb.kr에서 직접 조회한 결과 **전부 진짜 존재하는 완전한 몬스터**였음(플레이스홀더/더미 데이터가 아니었음) — 우리 쪽에서 스탯을 못 채워서 acc=0/eva=0/def=mDef=10으로 비어있었을 뿐. 예티/다크예티/페페/다크페페 6종은 "Transformed/Separated Yeti" 등으로 표기돼있어 예티&페페 스토리 관련 퀘스트 몬스터로 추정(변신/분리 상태의 같은 개체, def/mDef/eva/needAcc가 "진짜" 형제 몬스터와 완전히 일치해 신뢰도 높음). 나머지 4종(파이어독=Bain, 마스터소울테니=Master Soul Teddy, 데스테니=Death Teddy, G.팬텀워치=G.Phantom Watch)은 이름만 우연히 같고 완전히 다른 별개 보스/몬스터였음. `scripts/fix-duplicate-name-placeholder-monsters.mjs`로 10종 전부 완전한 스탯 반영 완료, 9500xxx대 4종은 `release-filters.json`(메랜+플래닛)에도 노출 등록(6xxxxxx대 6종은 애초에 9,000,000 미만이라 필터에 안 걸려서 이미 노출 중이었음 — 그동안 부정확한 0/10 스탯인 채로 사이트에 떠 있었을 가능성 있음, 이번 수정으로 해결).
  **2026-09-09 3차 확인 — "둘 다 정상 스탯"인 47개 그룹은 오배정 없음**: 이름은 같지만 양쪽 다 완전한 스탯을 가진 그룹(마뇽/그리프/예티/다크예티/페페/주니어발록/크림슨발록 등 47개)의 드랍 개수를 전부 대조 — 항상 "리프레 본체/필드 버전"이 더 많은 드랍(20~70개), "기타 지역/PQ 버전"이 더 적은 드랍(1~30개)을 갖는 일관된 패턴이라 오배정 징후 없음(서로 다른 진짜 콘텐츠가 각자 드랍표를 갖는 정상 모습). 즉 이름 중복발 드랍 오배정 문제는 1차 배치(예티 6300001 사례)에서 확인된 것 외엔 추가로 발견 안 됨 — **legacy-mapledb 개별 오배정은 예티 사례가 이례적이었을 가능성이 높고, 광범위한 문제는 아닌 것으로 잠정 결론.**
  **2026-09-09 2차 배치 — 84개 플레이스홀더 mobCode 전수 조사 완료**: "아이템이 붙어있는 것만" 보는 대신 73개 혼합 그룹의 플레이스홀더 mobCode **84개 전부**를 mapledb.kr로 기계적 대조(`search.php?q=<mobCode>&t=mob`, "물리 방어력" 라벨 존재 여부로 실존 여부 판별 — 이 사이트는 없는 mobCode도 "인기 검색어" 폴백을 보여주는데 그 폴백에 우연히 "LEVEL" 문자열이 섞여있어 단순 LEVEL 존재 체크는 오탐 남발함, 반드시 "물리 방어력" 같은 몬스터 상세 페이지 전용 라벨로 판별할 것). 결과: **53개는 실제 mapledb.kr 페이지가 있는 진짜 몬스터**(자동 파싱 스크립트로 level/hp/exp/eva/needAcc/def/mDef/ele 추출), **31개는 mapledb.kr에도 없어 진짜 더미로 잠정 결론**(슬라임/돼지/버섯류/와일드보어 등 초반 몬스터의 나머지 사본들). `scripts/fix-duplicate-name-placeholder-monsters-batch2.mjs`로 53종 전부 반영, 그중 51종을 릴리즈 필터에 노출 등록(예외: 9300033은 이미 노출 중인 9300026과 완전히 동일한 스탯이라 중복 카드 방지 위해 숨김 유지, 8190005는 mobCode가 9,000,000 미만이라 원래도 노출 대상이라 필터 등록 불필요). 1차+2차 합쳐 총 63개 mobCode의 스탯을 완성함 — 남은 31개(진짜 더미로 확인됨)는 그대로 방치.
- [ ] 큐브 시뮬레이터 — 유니크→레전드리 확률(0.3%)이 넥슨 공식 "레드 큐브" 확률표에서 차용한 값 — 메이플플래닛이 실제로 레드 큐브와 동일한 표를 쓰는지는 확인된 게 아님(`GRADE_UP_CHANCE.uniqueToLegendary` 여전히 잠정치)
- [ ] 큐브 빌더(2026-07-16 신설, 현재 노출 중단 상태) — 보스뎀%/방어율무시% 배율이 표준 메이플 관례(단순 곱연산)로 구현됨, 메랜/플래닛 공식 검증은 아님(`src/lib/calculators/cubeBuilder.ts`의 `computeCharacterPower`). 크리티컬 최소/최대 데미지 옵션도 구분 없이 단순 합산으로 근사 처리 중. **재노출 전 확인 필요**: 장비 슬롯 추가 버튼의 브라우저 실동작 검증이 미완료 상태(2026-07-09 세션 — 코드 검토 3회/유닛·데이터 감사 테스트 54개는 통과했지만 실제 화면 클릭 확인은 못 함, DEVLOG 참고)

### 데이터 누락
- [ ] 카탈로그 매칭 556종 중 나머지 384종(레벨은 같지만 HP/acc/eva/def 차이)이 미반영 — acc 필드는 메랜 원본 자체가 비어있는 경우가 다수라(206종 중 203종) 플래닛 차이가 아니라 메랜 원본 데이터 누락 이슈일 가능성이 높음, 별도 트래킹 필요
- [ ] 핑크빈 서브페이즈(무적 페이즈/소환 몹) mobCode 확보 후 `newMonsters`에 추가 — **2026-08-20 부분 진전**: 핑크빈 EXP 조사 중 inven.co.kr(KMS 몬스터 DB)에서 mobCode 8820000이 "핑크빈"과 동일한 이름/설명문을 쓰면서도 스탯이 완전히 다른(HP=1, def=0, acc=650, eva=225, 물공6802/마공7126 — 본체 8820001은 HP21억/방어 1700·1930/명중·회피 0) 것을 발견, 무적 페이즈일 가능성이 높음. 다만 (a) 이 mobCode가 실제로 메랜/플래닛 서버에도 존재하는지 확인 안 됨(KMS 원본 DB일 뿐), (b) 나머지 소환 몹 3종의 mobCode는 못 찾음, (c) EXP=0·드롭 없는 몬스터라 추가해도 사용자 체감 효과는 낮음 — 근거 불충분해 이번엔 반영 안 하고 리드만 남김.
- [ ] 드랍테이블 — 몬스터 카드 드랍이 `data/drop-index.json`에 아예 없음(조아요엔 전 몬스터 카드 드랍 존재). **2026-08-20 조사**: 메랜에 실제 몬스터북 시스템 존재 확인(카드 1장당 20분 버프, 5장 완성 시 1시간 추가 EXP 효과 — 단순 수집용이 아니라 실제 게임플레이 영향 있음). 다만 거의 전 몬스터(수백 종)에 카드 아이템을 새로 만들고 드랍 연결을 걸어야 하는 규모라 "누락 데이터 보완"이 아니라 "신규 기능 구축" 급 작업, 정확한 드랍률(%)도 공식 수치가 어디에도 없어(체감 후기만 있음) 전부 "정보 없음"으로 표시될 예정. 사용자 결정: 이번엔 보류. 착수한다면 메랜닷컴(itshim.dev)/메이플노트 클래식 등에서 전질 데이터 존재 여부 먼저 확인 필요.

### UX / 향후 검토
- [ ] `data/planet/release-filters.json` 수동 검토 — 메랜 걸 그대로 복사한 상태, 플래닛 실제 미출시 기준과 다를 수 있음
- [ ] 큐브 시뮬레이터 — 메포→원화 환율이 커뮤니티 소통 시세(1,200메포≈1,000원) 근사치. 공식 결제가 확인되면 `CubeSimulator.tsx`의 `KRW_PER_MEPO_APPROX` 교체 필요
- [ ] 큐브 빌더 — 몬스터 방어력을 절대값으로 입력받음(프리빅뱅 공식 특성), cubeplanner.kr류의 "방어율%" UX와 다름 — 필요하면 보스별 절대 방어력 참고표 추가 검토

---

## 아란(신규 직업) 콘텐츠 — 남은 항목

리엔 지역 몬스터 6종(무루/무루파/무루피아/무루무루/무루쿤/튜토리얼 무루)과 드롭(털뭉치, 확률 미상)은 메랜/플래닛 양쪽에 반영 완료(2026-07-31). 한방컷 계산기는 아란 스킬 데미지%까지 반영 완료(2026-07-31, 아래 절 참고). 피격뎀 계산기/큐브 빌더는 여전히 아란을 직업으로 선택만 가능(스탯 기반 기본 공격만, 개별 스킬 데이터 불필요한 구조라 추가 반영 대상 아님). 남은 항목:

- [ ] 구인모루 퀘스트라인 미반영 — `data/quests.json`에 아란 도입 스토리(구인모루 마을, 13단계: 푸니의 부탁→...→늑대의 진화) 자체가 없음. 순서/이름은 나무위키로 확보했으나 담당 NPC/시작·완료 조건/보상 수치는 확인 못함 — 근거 없이 채우지 않기로 결정(2026-07-31). `scripts/import-quests-from-turbopack.mjs`는 게임 클라이언트 덤프 전용 파서라 위키 텍스트를 못 넣음, 반영하려면 일회성 스크립트로 수기 작성 필요.
- [ ] 아란 전용 장비(폴암/방어구 세트) 드롭 소스 미확인 — 리엔 몬스터 6종은 전부 저레벨 튜토리얼 몹이라 장비보다 퀘스트 아이템 위주로 보임, 실제 장비 획득 경로(상점/퀘스트보상 등) 별도 조사 필요.

---

## 한방컷 계산기 — 스킬 미구현 항목

소환수 계열(엘퀴네스/이프리트/바하뮤트/피닉스/프리져/서포트 옥토퍼스)과 닌자 앰부쉬는 이 계산기의 "1회 사용=N타" 모델과 구조적으로 안 맞아 범위 제외 결정됨(2026-07-09). 재설계 없이는 착수하지 않음.

- [ ] 아크메이지(불/독, 썬/콜) — 엘퀴네스/이프리트 소환수 미구현. **버그 동반**: 자리표시자 `ifritBonus` 슬라이더 라벨이 썬콜 유저에게도 항상 "엘퀴네스 레벨"로 고정 표시됨 — 소환수 구현할 때 같이 정리
- [ ] 비숍 — 바하뮤트 소환수 미구현 (`bahamutBonus` 자리표시자, `1+레벨/100` 근사치만 적용 중)
- [ ] 보우마스터 — 피닉스 / 신궁 — 프리져 미구현 — `specialBaseDamage`에 계산식(DEX*2.5+STR)은 이미 존재해서 `mainSkillMapping`에 스킬명만 추가하면 비교적 저비용으로 가능
- [ ] 나이트로드/섀도어 — 닌자 앰부쉬 미구현 — 지속시간 동안 틱딜하는 도트뎀 구조라 별도 설계 필요
- [ ] 건슬링거/발키리/캡틴 — 호밍·서포트 옥토퍼스 미구현 (마스터 전용 고정값만 확인됨, 서포트 옥토퍼스는 소환수라 위 이슈와 동일)
- [ ] 시그너스 5종(소울마스터/플레임위자드/윈드브레이커/나이트워커/스트라이커) 재검증 — 우선순위 낮음, 별도 세션에서 107개 스킬 전체 구축된 이력 있어 완성도 높다고 판단됨
- [x] 아란 — 스킬 데미지% 반영 완료(2026-07-31). 출처: 디시인사이드 페리온(메이플플래닛) 마이너 갤러리
  "[✧팁정보] 아란 스킬 정리"(작성자 Knight, 2026-07-03 16:03,
  `https://gall.dcinside.com/mgallery/board/view/?id=planetmaple&no=65932`) — 인게임 스킬 툴팁을 그대로 캡처한
  게시물로, 레벨 1~마스터 전체 표를 브라우저로 직접 열람해 옮겨적음(과거 세션에서 시도했던 나무위키/StrategyWiki/
  AyumiLove/넥슨 아카이브/matip.kr/인벤 등은 표가 없거나 값이 상충해 전부 실패했던 출처였음, 이번 건은 그 문제를
  해결). `maplelandzzul.gg` 스킬트리의 마스터값과 교차검증한 결과 10개 중 8개 일치(파이널 차지만 서버 간 수치
  차이 가능성 있음, 아래 참고) — 신뢰도 높다고 판단해 반영.

  **반영한 9개 스킬** (`data/raw/skills.raw.ts`의 `mainSkillMapping["아란"]`/`damageMapping`, 레벨 1~마스터 전체
  표 그대로 입력, `range20`/`range30`에도 등록):
  - 더블 스윙(마스터20): Lv1 102% → Lv20 140%, 2단 타격 → `count:2`
  - 트리플 스윙(마스터20): Lv1 193% → Lv20 250%, 3단 타격 → `count:3`
  - 콤보 스매쉬(마스터20, 콤보30↑ 소비): Lv1 320% → Lv20 700%(최대 10마리, 계산기 미반영 — 아래 설명)
  - 파이널 차지(마스터20, 트리플스윙 직후 전용): Lv1 50% → Lv20 70%(최대 9마리, 미반영). 레벨별 증가폭이
    Lv19→20 구간만 +2%로 불규칙(나머지는 전부 +1%) — 원문 그대로 반영, 오타 아님.
  - 롤링 스핀(마스터20): Lv1 44% → Lv20 120%(최대 12마리, 넉백 확률 38→95%는 미반영)
  - 콤보 펜릴(마스터30, 콤보100↑ 소비): Lv1 510% → Lv30 800%, 원문 "2번 공격한다" → `count:2`(최대 10마리는 미반영)
  - 파이널 토스(마스터30, 트리플스윙 직후 전용): Lv1 52% → Lv30 110%. 원문에 별도로 있는 "띄운 상태 공격시
    추가데미지 42→100%"는 파이널 토스 자신이 아니라 그 이후 다른 공격이 얻는 보너스라 미반영.
  - 파이널 블로우(마스터30, 트리플스윙 직후 전용): Lv1 410% → Lv30 700%, 최대 마리 수 표기 없음(count:1)
  - 바디 프레셔(마스터20): Lv1 5% → Lv20 100%. 아란 전용 "바디 어택"(몸통 부딪히기) 활성화 스킬 — 계산기의
    "스킬 하나 선택=그 데미지%로 1타" 모델을 그대로 적용해 일반 스킬처럼 반영.

  **"최대 N마리" 광역기 처리 방식**: 콤보 스매쉬/파이널 차지/롤링 스핀/콤보 펜릴은 실측 원문상 "여러 대상에게
  각 1(또는 2)회씩" 타격하는 광역기이지 "한 대상을 여러 번" 때리는 게 아님. 이 계산기는 몬스터 1마리 기준
  원킬 여부만 계산하므로(기존 슬래시 블래스트 등 광역 패턴도 동일하게 최대 마리 수는 무시하고 단일 대상
  데미지%만 반영해온 선례를 그대로 따름) 최대 마리 수는 모델링하지 않았고, `count`는 "동일 대상에게 몇 번
  맞는가"만 표현(콤보 펜릴만 원문상 대상당 2타라 `count:2`, 나머지 광역기는 대상당 1타라 기본값 1).

  **제외한 항목**:
  - 콤보 템페스트(마스터30, 콤보200↑ 소비): 보스 몬스터에게만 데미지%가 존재(Lv1 1210%→Lv30 1500%, 3연속
    타격, 얼음화 10→40초). 일반 몬스터에게는 %가 아니라 "가벼운 타격에도 죽는 얼음 상태"로 만드는 CC라
    한방컷 계산기의 데미지% 모델과 맞지 않음. 계산기에 보스/일반 몬스터 구분 자체가 없어 이번엔 제외 —
    보스 전용 처리(별도 토글/안내 문구)를 추가하려면 Zone B 설계가 더 필요.
  - 풀 스윙(마스터20): "더블 어택과 트리플 어택의 데미지를 높여준다"는 설명과 달리 실측 표의 값이 오히려
    더블 스윙(Lv1 72%×2타)/트리플 스윙(Lv1 274%) 자체 마스터값보다 낮게 나와 해석이 불명확(더블/트리플
    스윙과 별도로 레벨업하는 대체 스킬인지, 상호작용 방식이 다른지 불확실). 근거 없이 추정 반영하지 않음.
  - 콤보 배리어(마스터30): "받는 데미지 %만큼 흡수"하는 버프(Lv1 8.4% → Lv30 20%). 온헛 계산기 범위 밖 —
    `TakenDamageCalculator.tsx`(피격뎀 계산기) 반영 후보로 남겨둠, 이번 세션에서는 미구현.
  - 나머지 패시브/버프/이동기(콤보 어빌리티, 컴뱃 스텝, 폴암 부스터, 폴암 마스터리, 콤보 드레인, 스노우 차지,
    스마트 넉백, 콤보 크리티컬, 하이 마스터리, 프리즈 스탠딩, 하이 디펜스)는 기존 판단대로 계속 제외.

  **서버 간 수치 차이 가능성**: 파이널 차지는 플래닛 실측(70%, 최대9마리, 마스터20)과 `maplelandzzul.gg`
  메랜 값(90%, 최대12마리, 마스터30로 추정되던 값)이 달랐음 — 다만 애초에 메랜에 아란이 출시됐다는 근거 자체가
  불확실해짐(위 "우선순위 중간" 절 2026-09-09 항목 참고), `maplelandzzul.gg` 값도 테스피아(테스트서버) 기준일
  가능성 있음. Codex 리뷰(2026-07-31)로 플래닛 실측치가 공용 `damageMapping`을 통해 메랜 계산기에도 그대로
  노출되던 걸 발견해 한때 `server !== "planet"`일 땐 "기본 공격"만 노출하도록 막았으나(2026-07-31), **2026-09-09
  사용자 결정으로 이 게이팅을 다시 제거** — "다른 직업들도 서버 간 스킬셋이 크게 다르지 않을 것"이라는 판단
  하에 플래닛 실측치를 메랜에도 근사치로 노출하기로 함(`onehit-calculator-client.tsx`의 해당 분기 삭제). 메랜
  전용 검증 출처가 확보되면 정확한 값으로 교체 검토(아래 항목).

  - [ ] 메랜 전용 아란 스킬 데미지% 출처 확보 — 확보되면 `onehit-calculator-client.tsx`의 `damageMapping["아란"]`을
    서버별 오버라이드 구조로 분리해 정확한 값으로 교체(현재는 플래닛 값을 메랜에도 그대로 노출 중, 위 참고).
